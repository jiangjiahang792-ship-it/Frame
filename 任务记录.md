# 任务记录

## 2026-09-14：全部外部信号改为到达直接发送

- 修改前提交当前源码、测试和文档，恢复点为 `b9dbc8e`；历史测试输出及DLL备份未纳入提交。
- PLC写入、串口发送、相机IO移除有序提交接口，不再等待前序工件或有序端点租约；流程编译器不再把外发节点登记为有序信号。保留底层设备已有同步，不新增发送锁。
- 相机IO按用户要求保留“是否异步”，两种模式均直接开始脉冲；同步等待保持及复位，异步继续后续节点并观察最终结果，复用在途生命周期登记防止运行会话提前结束，不用于发送排序。
- 旧AI信号发送移除未观察的后台任务，直接调用并等待发送及可选复位；PLC失败响应不再忽略。TCP客户端等待实际发送及已配置的响应接收，不提前报告节点成功。
- 相机帧缓存、单流程FIFO、流程组调度及业务配置的保持/复位不改。并发写同地址或同相机线路可能交错，直接发送不保证跨工件顺序。
- 验证通过：x64 Debug独立目录 `bin/ExternalSignalDirectValidation` 编译、外部信号直接执行专项（真实PLC节点与假设备、相机同步/异步及复位异常、前序未结束/故障不阻塞、串口驱动错误、本机TCP延迟响应、旧AI写入及复位）、流程编译器、Modbus直接写入、结果发送、有序协调器兼容测试、通信失败分类专项；`git diff --check` 无格式错误。没有连接现场PLC、相机或物理串口。

## 2026-09-14：结果发送节点恢复到达即发

- 结果发送节点的 PLC、Modbus 两种设备路径均改为整批预检后直接逐行发送，移除工件有序提交接口和流程编译器有序节点登记，不新增发送锁。
- 保留本轮上游结果校验、参数和值快照、全部行预检、数据先于数量、失败行及成功条数、发送前取消和等待实际写入完成；失败不重试、不回放。
- 仅保证本次调用内部行顺序，不再保证跨工件顺序或整批设备独占；独立 PLC 写入、相机 IO 的有序机制不变，底层通信库同步不变。
- 验证通过：x64 Debug 独立目录 `bin/ResultSendDirectValidation` 编译、结果发送专项、流程编译器、普通 Modbus 直接写入、独立 PLC 有序写入、有序协调器、通信失败分类专项；`git diff --check` 无差异格式错误。仅使用本机模拟服务，未连接现场设备。

## 2026-09-12：Modbus写入节点恢复到达即写

- 按用户最终确认，仅修改普通“Modbus写入”节点：到达节点后直接调用设备Write，按配置写入On、Off或其他数据；不等待前序工件，不新增锁、不提交发送队列、不自动复位或重试。
- 保留写入前参数快照与类型校验、取消检查、通信结果检查、人工写入门禁及设备既有自动重连；不改通信库内部同步。
- 从流程编译器有序外发目录移除Modbus写入，允许该节点并行路径运行，不影响相机IO、PLC、串口及“结果发送”节点的原有有序逻辑。
- 边界：普通Modbus写入不再保证工件先后顺序，也不参与结果发送节点的整批端点互斥；混用同一地址需要方案或PLC自行协调。失败仍按原流程异常处理，不自动补发。
- 验证：Debug|x64独立目录 `bin/ModbusDirectValidation` 编译成功；直接写入专项覆盖真实Run入口、On保持及显式Off、无工件上下文、前序未完成及顺序域故障不阻塞、发送前取消、断线拒绝、失败不重试、RTU参数和本机Modbus协议。流程编译器、完整顺序协调器及结果发送专项回归通过；未连接现场设备。

## 2026-09-12：Modbus与PLC后台重连及发送前失败分类

- 修改前确认当前代码已提交至 `4fee33d`，工作区无新的已跟踪文件修改。
- 为HSL网口和串口主站增加共享通信锁的独立设备恢复机制，区分真实连接状态与持久化连接意图；用户主动断开停止重试，后台重连不停止方案、不写业务地址进行探测。
- 修正结果发送前未连接的错误分类，避免明确未发送时误冻结整台设备；真实写入异常及部分写入仍保留Unknown保护，不自动补发或重置工件顺序。
- 测试发现并修复RTU及松下串口超时设置会重新初始化串口、丢失配置端口号的问题；改为更新当前SerialPort实例，RTU关闭时直接关闭实际绑定管线。
- 界面新增“断开并停止重连”右键菜单，控件位于Designer文件；后台状态通知切回UI线程，离线时也可撤销连接请求。TCP使用系统保活，不发送业务探测指令。
- 安全边界：重连只恢复传输连接，不自动补发历史结果、不解除失败工件顺序域；明确未发送按Rejected归还物理端点，部分写入或响应丢失仍按Unknown冻结保护。Modbus TCP从站作为监听服务，不执行主站主动重连。
- 验证：Debug|x64独立构建 `bin/CommunicationReconnectValidation` 成功；真实适配器的Modbus TCP、三菱MC、基恩士Nano、松下MC首连失败恢复/空闲断线恢复/通信锁互斥/连接意图往返/主动关闭通过；RTU及松下不存在串口失败、端口号保留、取消重试通过；结果发送专项、Modbus/PLC有序写入集成、完整顺序协调器回归通过。未做现场设备断电拔线及USB串口拔插验证。

## 2026-09-12：相机掉线自动重连不再主动停止方案

- 删除重连守护中的全方案停止请求，改为隔离断线相机的旧票据和缓存帧，本轮取图明确失败，其他流程保持运行。
- 新取图异步等待当前相机完成重连、方案参数恢复和取流确认，用户停止仍可取消等待；参数或取流恢复失败会继续重试。
- 保留物理停流、回调排空及流代数确认，重连期间旧SDK触发返回异常不再升级为全方案停止；其他生产身份故障的保护不变。
- 原分支同组等待规则不变，掉线期间未采集的工件无法自动补拍，现场仍需处理漏检工件。
- 验证：Debug|x64编译到 `bin/CameraReconnectValidation` 成功；相机工件管线运行测试、海康重连结构回归、回调隔离回归通过，覆盖单相机隔离、旧帧及旧连接异常拒绝、其他相机继续收帧、重连代数确认与预算回收。未连接现场相机，断电重连仍需实机验证。

## 2026-09-12：原分支流程组扩展至40组

- 按用户最新要求切回 `codex/keyence-nano-overtcp`，恢复原有同组调度；独立流程运行改动保存在名称含 `codex/independent-process-runtime: feature WIP` 的Git stash中，未合并到原分支。
- 原工作区相机重连、ROI及结果发送等未提交改动保留；仅扩展流程组枚举及组别设置界面。
- 新增到Group40，显式保持既有Group1至Group7的数值不变；运行时从枚举生成全部选项，保存与回显共用同一组别数组。
- Designer中同步提供1组至40组选项，保留已有语言显示及同组触发、优先级和信号排序语义。
- 验证：Debug|x64编译到 `bin/ProcessGroup40Validation` 成功；真实程序集窗体逐组验证1至40组的选择、保存、重新打开回显、JSON往返、既有枚举编号兼容、语言刷新保持选择及空选择不覆盖原值，全部通过。未连接现场设备。

## 2026-09-10：RL12端子数量文本接入ROI绘制

- `RL12Parse` 在保留“端子数量”公共数量判定配置语义的同时，额外写入 `DetectItem.TerminalCount` 检测结果和绘制文本。
- “端子数量”不绑定独立模型类别框，使用除“飞丝”外检测项的最大匹配数量作为当前值，供ROI结果绘制按检测项订阅输出文本。

## 2026-09-09：RL12解析接入端子数量公共判定

- `RL12Parse` 使用检测项配置里的“端子数量”作为公共数量标准，不再为每个模型类别新增独立数量检测项。
- 解析每个非“飞丝”的检测项时，在原有尺寸、面积或存在性上下限判断外，同时检查该检测项本轮检出数量是否落在“端子数量”的上下限内；数量不一致时该检测项输出 NG。
- “飞丝”保持原有独立判定语义，不参与端子数量公共校验；未配置或未启用“端子数量”时保持旧方案行为。

## 2026-09-01：多端子解析接入端子数量公共判定

- `MultiTerminalParse` 使用检测项配置里的“端子数量”作为公共数量标准，不再为每个模型类别新增独立数量检测项。
- 解析每个非“飞丝”的检测项时，除原有尺寸/面积上下限判断外，同时检查该检测项本轮检出数量是否落在“端子数量”的上下限内；数量不一致时对应检测项直接输出 NG。
- “飞丝”保持原有独立判定语义，不参与端子数量公共校验；未配置或未启用“端子数量”时保持旧方案行为。
- 验证：语言资源 JSON 解析通过；`Debug|Any CPU` 完整解决方案构建通过，0 错误、154 个既有警告；`Release|x64` 完整解决方案构建通过，0 错误、155 个既有警告。

## 2026-08-29：无监督训练 -2 与默认分数兜底修复

- 根据现场日志和失败训练残留复现确认：只有 1 张 NG 时，anomalib 将 NG 按 50% 拆分为验证集和测试集会触发除零异常，原生 DLL 将子进程异常统一返回为 `-2`；托管训练层现在为单张 NG 生成仅供 native 拆分的内部副本，客户样本统计仍保持真实的 1 张。
- native 返回 `-2` 时释放当前检测器、清理不完整模型输出并使用全新句柄自动重试一次；错误信息补充返回码含义，运行前增加训练脚本、Python 和本地预训练资源完整性检查。
- 无监督和大模型的自动分数校准统一改为非阻断增强步骤；样本不足、模型加载失败、推理失败、OK/NG 分数无法区分或校验不通过时，均写入界面当前默认阈值并继续打包模型。
- 新增无监督训练韧性测试，覆盖单 NG 拆分副本、重复执行幂等、`-2` 重试结构、环境预检和默认分数降级策略。
- 使用日志中的原始失败图片完成两次 Release x64 端到端 GPU 回归：`1 OK + 1 NG` 已生成 ONNX 和模板并采用默认阈值；有预留校准图但 `NG 分数 <= OK 分数` 时已记录原因、采用默认阈值并成功生成模板。相关自动化测试与 Release x64 全量编译均通过。

## 2026-08-29：AI 训练少样本阈值默认策略

- 大模型训练样本下限调整为至少 1 张 OK 图片，NG 图片不再作为训练前置硬条件；无监督训练样本下限调整为至少 1 张 OK 图片和 1 张 NG 图片。
- 自动识别分数校准改为有足够预留样本时才执行；样本不足以额外预留 OK/NG 校准图时不再报警阻断训练，模板阈值使用训练界面当前默认值写入。
- 数据集拆分只在不影响最低训练样本的情况下预留校准图，避免少样本场景被校准逻辑扣走后 native 训练无图可用。

## 2026-08-24：图像源方案参数权威性与离线恢复修复

- 现场日志中的“启动失败”实际不是许可证注册失败：两台相机启动时尚未枚举成功，图像源参数窗体在恢复方案期间读取SDK当前触发延迟、曝光和增益，离线异常导致节点恢复整体中断。
- 图像源恢复期间不再读取相机当前曝光、增益和触发延迟；曝光、增益、触发源、极性、延迟和超时均只从方案参数恢复，相机离线不会阻断节点创建。
- 方案保存的`CameraName`作为运行时重新绑定键；方案恢复、相机回调启动和图像源节点运行前均解析当前设备对象，避免相机重新枚举或重新添加后继续持有旧对象。
- 相机在线恢复、用户重新连接相机以及正式运行启动前都会把方案曝光和增益写入硬件。例如方案曝光5000us、相机原值2000us时，以5000us为准；写入失败记录明确警告，不反向覆盖方案。
- 运行调参窗口改为显示方案曝光和增益，不再调用SDK读取当前值；图像源数值控件遇到超范围方案值时动态扩展范围，曝光和增益均保留三位小数，禁止打开窗体后再保存造成静默截断。
- 在线相机应用方案前若正在取流，则先停流、写入完整参数并恢复取流；每轮采图固定捕获同一个相机对象，订阅、触发、预算和解绑不再受运行中对象换代影响。
- 整套运行和单流程运行均在触碰硬件前检查全方案启用图像源；同一相机只有参数完全一致才允许共享，曝光、增益、触发源、硬触发极性、延迟或超时不同会明确拒绝运行。
- 新增`ImageSourceSchemeParameterAuthority.Tests.ps1`，除源码契约外反射执行编译后的共享相机参数比较；`Auto/SOFT`等价、曝光不同和硬触发极性不同的行为均通过。
- Debug完整构建与Release x64核心编译均为0错误，分别保留154和155个既有警告；方案权威性、数值保真、重连、触发安全、统一回调、工件相机管线、50ms触发架构、回调隔离和回调快路径共9组回归通过。独立审查提出的共享相机冲突、取流中写参数、控件截断和对象换代解绑4项P1均已逐项修复并复审。

## 2026-08-24：图像源软硬触发统一接入与停止会话隔离

- 生产图像源按实际触发源分流：`Auto/SOFT`建立软件票据后只调用一次`GrabOne`；`LINE0～LINE4`在单相机临界区内原子完成硬件票据登记和武装，只等待下一张物理触发帧，绝不补发软件触发命令。
- 硬触发图像源仍要求没有活动上游节点，防止流程先运行后才等待一个无法回溯确认的外部触发沿；软触发图像源允许位于Modbus读取、PLC读取、逻辑判定等可编辑节点之后，由流程运行到图像源时再发软触发。
- 旧配置中的`Auto`统一按`SOFT`处理，并在方案启动设置和节点每次设置两条路径中都向相机SDK写入明确的软触发源；极性只对`LINE0～LINE4`线路触发设置。
- 正常停止或相机生产故障提交后，旧活动会话退出前拒绝新运行入口；下一次启动同时换代运行令牌、工件会话纪元、有序信号协调器、相机票据注册表和图像预算，旧票据故障或旧令牌迟到取消不能误伤新会话。
- 首会话资源换代全部成功后才增加活动运行计数；旧相机票据表在2000ms内无法排空时启动明确失败，活动计数保持0、`IsRunning=false`，释放阻塞后可以重试，不留下虚假运行状态。
- Debug和Release x64编译均为0错误；软硬触发、相机回调、帧等待、内存预算、海康隔离/重连、工件会话、流程编译、有序信号及四类外发节点回归全部通过。测试包含真实阻塞票据的初始化超时回滚、正常停止取消前门闩、零活动换代后的旧令牌迟到取消隔离、旧故障延迟取消隔离，以及编译后执行`Auto/SOFT/LINE0～LINE4`分类逻辑。独立复审最终`P0=0、P1=0、P2=0`，审查智能体复跑三组核心专项全部通过。

## 2026-08-24：30秒有序信号顺序缺口主动报警

- `OrderedSignalCoordinator`新增默认30000ms、允许1000～600000ms的缺口计时参数；只在后续意图已经排队且当前队首的连续前序工件/信号不存在时计时，设备正常执行和没有后续压力时不启动报警。
- 每个顺序域使用唯一可取消异步计时和代数校验；前序补齐、调度开始、工件推进、域阻断及协调器停止都会淘汰旧计时，超时回调必须在状态锁内重新确认同一缺口，不能凭旧快照误报。
- 到期后禁止越过缺口：域内未开始意图全部明确拒绝，但必须先把尚未发布的工件终态升级为`Stopped`，再唤醒等待任务；新增内部终态升级入口，覆盖流程已先请求成功封口但仍等待缺失信号的情况，避免自动意图回收竞态发布`Faulted`。
- 新增结构化缺口事件，包含顺序域、会话、期望工件/信号、首个等待节点、积压数量和超时证据。`Solution`仅接受当前协调器事件，并在统一锁顺序下先提交故障停机门闩再取消对应旧令牌；活动会话退出前拒绝新入口，零活动时下次会话原子换代令牌，旧会话迟到事件和旧令牌迟到取消均不会误停新会话。
- 协调器行为测试覆盖跨工件缺口、同工件信号缺口、公开入口自动回收、终态升级、阈值内补齐，以及长设备动作期间已有后一工件排队仍零误报；Solution桥接行为测试覆盖旧来源零提交、零活动换代隔离和活动会话故障门闩。保留10000信号倒序压力。
- Debug x64与跳过既有构建后复制命令的Release x64核心工程编译均为0错误；完整8组回归通过，协调器专项额外连续复跑3轮失败0。独立复审最终`P0=0、P1=0、P2=0`，审查智能体再次运行两个专项通过。

## 2026-08-24：Modbus写入节点接入有序协调器

- `NodeModbusWrite`实现统一有序外发接口；设备、TCP/RTU物理端点、地址、寄存器类型和已经解析的独立值数组在节点到达时一次性冻结，真实写入只由生产会话协调器调用。
- 同一TCP的IP端口或同一RTU串口使用唯一端点锁，覆盖整个物理连接而不是寄存器地址；两个不同设备对象只要指向相同物理连接也必须互斥，不同端点保持并行。
- 订阅值在进入队列前读取且不再写回共享节点参数；取得发送权后设备已断开则零写入拒绝，协议调用开始后的异常按`Unknown`冻结端点，正常HSL响应记录为带端点、地址和类型凭据的`DeviceAcknowledged`。
- `IModbus`新增默认5000ms、允许100～60000ms的有限操作超时；TCP应用连接和接收超时，RTU应用管线接收及底层串口读写超时，运行时调整即时更新。
- 参数页保存失败不再继续发送；自定义值在保存时严格校验，支持负数、科学计数法和中英文分隔符，拒绝非法布尔、NaN、无穷大及数组空项。人工运行改为异步等待，并通过方案运行生命周期门禁禁止生产或重载期间插队。
- 新增真实假Modbus专项，覆盖TCP和RTU同端点跨对象互斥、参数替换竞态、在途封口等待、确认凭据、异常停机、发送前取消零写入和数值边界；专项连续复跑10次失败0，最终Modbus专项及七组既有外发/协调/工件/流程回归全部通过。
- 独立审查发现TCP连接失败返回被误报成功的P1；现已在成功事件和`IsConnect=true`前检查HSL结果，并用编译程序集行为测试覆盖空结果与`IsSuccess=false`。复审最终`P0=0、P1=0`。
- Debug x64和跳过既有构建后复制命令的Release x64核心项目编译0错误；完整解决方案Release再次停在原有构建后复制命令，编译器未报错，已主动终止且没有遗留会话。
- 四类业务外发节点均已接入；下一批实现30秒顺序缺口主动报警。

## 2026-08-24：PLC写入节点接入有序协调器

- `NodePlcWrite`实现统一有序外发接口，PLC对象、物理连接键、地址、类型和解析后的值在节点到达时冻结；真实写入只由协调器调用。
- PLC端点按品牌、通信方式、IP端口或COM口归并，覆盖整条物理连接而不是单个寄存器；相同物理连接跨对象、跨生产线互斥，不同连接保持并行。
- HSL成功响应记录为带凭据的`DeviceAcknowledged`；失败、空响应或写入异常均按`Unknown`冻结端点，发送权取得后已知断开则在零写入前明确拒绝。
- `IPlc`新增默认5000ms有限操作超时并应用到三菱、松下网口连接/接收，以及松下串口接收和底层写入超时；运行时调整同步更新。修复三菱PLC参数构造函数错误创建松下协议客户端的问题。
- 参数保存前严格验证设备、地址、类型和值；数字数组改用逗号分隔，支持负数与科学计数法，拒绝非法布尔、NaN和无穷大，界面提示已在设计器文件同步更新。
- 新增PLC写入真实假设备专项，覆盖相同物理端点跨对象互斥、参数替换竞态、在途封口等待、确认凭据、失败响应停机、发送前取消零写入和数值边界；Debug与Release x64编译0错误，完整回归通过，独立复审最终`P0=0、P1=0`。Modbus节点尚未接入。

## 2026-08-24：相机IO节点接入有序协调器

- `NodeCameraIO`实现统一有序外发接口，运行时取得当前工件和生产会话协调器；相机、稳定相机身份、线路、模式、保持时间及异步开关均在节点到达时冻结。
- 同一物理相机使用唯一`CAMERA-IO`端点锁，保证共享`LineSelector`状态以及完整“低、高、保持、低”脉冲不会被其他流程或其他线路节点插入。
- 拉高后发生异常会再次尝试把原线路恢复为低电平；协调器把不确定结果按`Unknown`冻结端点，禁止自动重发造成重复信号。
- 生产信号节点限制为输出线路，设计器移除输入选项；保持时间限制为0～1000000微秒，较长脉冲使用异步粗等待加末段自旋，避免全程占用CPU核心。
- 旧异步模式移除游离`Task.Run`，节点可提前返回但信号意图仍由工件上下文排空后再发布终态；同步模式等待完整脉冲。
- 参数页线路能力枚举通过方案运行生命周期门禁，生产或重置期间不能修改相机共享线路选择器。
- 新增相机IO有序接入专项检查，并用可控假相机真实执行编译节点，覆盖跨生产线互斥、参数替换竞态、在途封口等待、拉高异常回低、未知结果停机和发送前取消零写入；Debug x64编译0错误，相机IO专项、流程生命周期和10000信号倒序测试通过。PLC和Modbus节点尚未接入。

## 2026-08-24：串口发送节点接入有序协调器

- `NodeComSend`实现统一有序外发接口，运行时取得当前工件和会话协调器；串口对象、命令、编码及物理端口键在节点到达时冻结，真实`Send`只由协调器调用一次。
- 端点键使用`COM:物理端口名`，不使用界面别名；端口名为空明确拒绝。同一物理串口跨流程互斥，不同端点仍可并行。
- 串口API正常返回记为`LocalCallCompleted`，不冒充下位机确认；写入异常按`Unknown`冻结端点，发送权取得后端口已关闭则明确拒绝。
- 修复`ComDevice.Close()`关闭后仍把`IsOpen`设为`true`的旧错误。
- 串口写入新增默认2000ms有限超时，允许范围100～60000ms；空命令由节点和设备API双重拒绝，不能无发送却判成功。
- 参数页测试发送改为后台执行，并通过方案运行生命周期门禁与生产启动原子互斥；运行或重置期间禁止手动命令插队。
- 新增串口有序接入专项检查；Debug x64编译0错误，串口专项、流程生命周期和10000信号倒序测试通过。相机IO、PLC和Modbus节点尚未接入。

## 2026-08-24：有序协调器接入生产会话生命周期

- `Solution`新增当前生产会话唯一有序协调器；生产会话纪元重建和方案重置时同步替换协调器，旧会话冻结端点、阻断状态和序号不会带入下一轮。
- 工件上下文创建后立即登记协调器，方案流程组根工件和单流程自有工件均通过协调器封口；继承流程只提交分支终态，不重复封口父工件。
- 修复方案流程组根入口仍直接调用`TryComplete`的遗漏，防止协调器队首永远无法推进。
- 扩展流程接入专项测试，验证协调器替换、创建即登记、统一封口及会话隔离；Debug x64编译0错误，有序信号10000次倒序专项继续通过。
- 本提交只完成会话生命周期，不修改串口、相机IO、PLC和Modbus节点，四类节点按独立提交继续接入。

## 2026-08-24：完成实际有序信号协调核心

- 新增`OrderedSignalCoordinator`，按生产顺序域、会话纪元、工件序号和工件内信号序号执行严格FIFO；后一工件先到达时只登记，必须等前一工件封口后才能发送。
- 新增`SignalEndpointSerializer`，同一物理端点跨生产线互斥，不同物理端点保持并行；设备返回`Unknown`时冻结端点到协调器关闭，禁止盲重试和后续误写。
- 关闭流程新增独立取消源，并与外部副作用提交点原子竞争；关闭前尚未开始的端点等待明确返回`CancelledBeforeStart`，不会在释放冻结端点后迟到调用设备。
- 活动工件内重复`IntentId`返回首次结果，工件封口后移除完整意图记录；旧工件重复提交明确拒绝，解决长时间生产持续持有发送委托和上下文的问题。
- 新增有序信号专项测试，覆盖跨工件/工件内倒序、共享端点互斥、独立端点并行、取消、未知冻结、关闭竞态和重复提交；10000个信号倒序提交全部完成，设备动作准确执行10000次。
- Debug x64编译0错误；工件契约、100000次工件FIFO、流程入口和流程编译专项检查通过。四类外发节点及30秒顺序缺口定时报警仍在下一批逐个接入，本轮未改业务节点。

## 2026-08-24：图像源软触发接入生产票据

- 生产图像源改为先登记`WorkpieceIdentity + CameraKey`票据和图像字节预算，再执行一次真实软触发；海康转换线程返回的Mat直接转为带预算生命周期的`OutputImage`。
- 每台相机按当前宽高估算BGR源图与灰度缓存的最坏持有量，并与历史平均或8MB下限取最大值；实际记账覆盖源Mat和灰度Mat，最后一个图像租约释放时归零。
- 节点`TimeOut`同时限制SDK触发命令和帧到达；新增“帧已完成但触发任务卡住”回归，确认超时失败且Mat预算归零。
- 预览软触发成功后保留待返回帧标记，延迟预览帧被转换线程消费前禁止生产登记；生产票据存在时也禁止预览软触发，关闭双向错帧窗口。
- 节点删除和释放增加不可用状态复核，运行线程不能在取消事件之后重新安装生产等待；同相机并发SDK命令明确拒绝，且只有FIFO队首票据能触发，前序取得帧或失败出队后才能触发下一票据。
- 硬触发生产暂时明确拒绝，待外部触发就绪握手能证明物理触发沿后再接入；这是稳定性门禁，不把旧回调风险包装成已完成。
- Debug与Release x64编译均为0错误；相机票据、统一回调、快速回调、海康隔离/重连、工件上下文、流程编译和图像生命周期专项检查通过；独立复审最终结论`P0=0、P1=0`，全程后台运行，未启动软件主界面。

## 2026-08-24：海康转换线程接入生产票据

- SDK回调第一步在任何锁之前冻结单调时钟，随后仍只执行原始帧复制和有界入队；Mat转换、票据匹配、日志和停机全部留在每相机顺序转换线程。
- 转换后的Mat优先交给同相机生产票据，未登记票据时继续走旧预览事件；生产接管成功后不再克隆或重复分发该Mat。
- 票据新增触发单调时钟，触发前已经进入回调但延迟转换的旧帧会被明确拒绝，避免工件与图像错配。
- 原始缓冲耗尽和入口异常冻结首个故障帧号与发生时刻，延迟报告不会错误归属后一工件；转换异常同样按当前帧回调时刻处理。
- 相机停流重启使用故障代数两阶段确认，新流启动期间发生的新故障不能被确认动作清除，确认失败会回滚停流。
- Debug与Release x64编译0错误；相机票据、海康回调隔离和触发语义三组后台专项测试通过。图像源节点尚未登记真实票据，下一提交继续接入。

## 2026-08-24：Solution接管相机生产管线生命周期

- `Solution`新增全方案生产帧实际字节预算和多相机票据注册表；两者随方案设备生命周期成对重建，不再允许旧方案票据、帧号或预算状态污染新方案。
- 生产故障使用注册表独立FIFO线程通知，控制层记录相机键、工件号和故障原因后取消方案；SDK回调及相机转换线程不执行设备关闭。
- 相机稳定键在对象第一次使用时冻结：已有序列号则使用品牌与序列号，否则分配进程内不可变唯一身份；后续SDK补齐属性或界面改名都不会改变键。
- 重置顺序固定为流程排空、节点释放、设备物理释放、票据注册表重建；设备释放失败时不销毁旧注册表。
- 旧注册表关闭超时会保留引用和订阅供下次重试；故障回调在来源校验时抓取当代取消令牌，旧方案迟到故障不会停止新方案。
- Debug与Release x64编译0错误，相机工件管线专项测试通过；真实海康帧分发和图像源触发仍在下一提交接入。

## 2026-08-21：LAB颜色识别接入位置修正

- 状态：已完成，`LAB颜色识别`参数界面新增`位置修正`分组，支持勾选`启用修正`并订阅`位置修正`节点输出的修正信息列表。
- 运行逻辑：启用修正且模板配置了搜索 ROI 时，会把模板基准 ROI 顶点按每个目标的位置修正信息映射到当前图像，再取当前包围矩形执行 LAB 颜色识别；未启用修正或未配置 ROI 时保持原整图/静态 ROI 行为。
- 多目标处理：与卡尺工具一致读取`List<PositionCorrectionInfo>`，对每个目标分别执行颜色检测并汇总输出矩形、文本和 OK/NG；排序校验按单个目标独立校验后汇总整体结果。
- 界面保存：位置修正控件已加入`.Designer.cs`设计器文件，保存参数时会记录`UsePositionCorrection`、`CorrectionText1`和`CorrectionText2`，启用修正但未选择订阅时会提示。

## 2026-08-18：新增相机曝光增益节点

- 状态：已完成，新增`相机曝光增益`节点并放入`图像采集`大类，工具箱入口使用现有`相机拍照`图标。
- 功能说明：节点参数界面提供`选择相机`下拉框、`曝光(us)`数值框和`增益`数值框；选择相机后自动读取当前相机支持范围和当前值，保存时按方案中的相机对象绑定。
- 运行逻辑：流程运行到该节点时直接调用相机接口写入`SetExposureTime`和`SetGain`，不执行取图，降低额外耗时。
- 兼容说明：参数保存相机自定义名称，方案重新打开后会按名称恢复相机对象；新增节点类型、节点工厂、语言键和工程编译项均已接入。
- 拖拽修复：补齐普通流程画布`ProcessEditPanel.CreateNode`中的`CameraExposureGain`创建分支，解决工具箱可见但拖入画布提示“创建节点失败”的问题；同步补齐向导工具箱缩写和画布节点颜色分组。

## 2026-08-07：新增Blob分析工具节点

- 状态：已完成，新增`Blob分析工具`工具箱入口，放入`图像处理`大类，并接入节点创建工厂和流程编辑面板。
- 功能说明：节点订阅输入图像后按最小/最大灰度值做区间二值化，区间内像素作为感兴趣输出区域；可选启用检测区域，并使用`位置修正`输出的当前目标框作为提取范围。
- 输出结果：输出`面积`、`二值化处理后的灰度图`和`算法结果`，便于后续结果显示、绘制和条件判断继续订阅。
- 界面调整：按卡尺工具风格重做参数窗体，提供左侧参数、右侧图像预览，以及`绘制ROI`、`确认ROI`、`刷新图像`、`执行`、`确定`操作；检测区域改为保存绘制的基准ROI，启用位置修正时运行阶段自动映射到当前图像。
- 交互增强：灰度范围新增滑块并与数值框联动，拖动时实时刷新右侧二值化效果；输出区域新增`所有区域`、`最大区域`、`最小区域`三种筛选模式。

## 2026-08-07：新增定位工具分组

- 状态：已完成，新增`定位工具`大类，并将`模板匹配`、`NCC模板匹配`、`位置修正`移入该分组。
- 图标调整：`定位工具`分组及其三个节点改为青绿色图标色，避免继续使用深度学习的紫色。
- 兼容说明：仅调整工具箱分类和显示，不删除节点类型和运行逻辑，旧方案已有节点仍可正常加载。

## 2026-08-07：工具箱重新分组

- 状态：已完成，按要求将`检测识别`改为`深度学习`，并将`AI检测`显示名改为`目标分类`。
- 分类调整：新增`颜色检测`大类，将`颜色识别`移入并显示为`LAB颜色识别`；新增`条码识别`大类，将`二维码识别`移入；将`模板匹配`和`NCC模板匹配`移入`测量工具`。
- 入口精简：从工具箱中移除`电池极耳检测`入口；仅隐藏入口，不删除旧节点类型和运行逻辑，旧方案已有节点仍可正常加载。

## 2026-08-07：检测识别工具箱入口精简

- 状态：已完成，按要求从工具箱`检测识别`分组中移除`直线检测`、`圆检测`和`二值化分析`入口。
- 兼容说明：仅隐藏工具箱入口，未删除`LineFind`、`CircleFind`、`BinarizationAnalysis`节点类型和运行逻辑，旧方案中的已有节点仍可正常加载。

## 2026-08-07：工具箱结果绘制入口精简与中文显示修复

- 状态：已完成，按要求从工具箱中移除`AI结果绘制`和`ROI结果绘制2`，保留`ROI结果绘制`作为当前可见绘制入口，并将其图标键改为通用`结果处理`。
- 中文显示：补齐`3D图像源`、`NCC模板匹配`、`ROI结果绘制2`的流程工具箱语言键，避免工具箱按节点类型刷新语言时显示为英文枚举名。
- 兼容说明：仅隐藏工具箱入口，未删除旧节点类型和创建逻辑，旧方案中的已有节点仍可正常加载。

## 2026-08-06：检测识别工具箱加入NCC模板匹配节点

- 状态：已修正，保留原`模版匹配`节点不改名、不删除，同时在检测识别工具箱中新增独立`NCC模板匹配`节点。
- 原节点恢复：`MatchTemplate`入口、节点工厂注册、参数窗体默认标题和结果输出文案恢复为原模板匹配语义；原节点默认会话继续保留既有OpenCvSharp CPU后备路径。
- 新增节点：新增`NccMatchTemplate`节点类型和`NodeNccMatchTemplate`节点类，参数界面复用原模板匹配运行参数，包含最小分数、最大个数、角度范围、角度步长、最大重叠率、极速粗匹配、搜索区域和多目标排序。
- 算法原则：`NCC模板匹配`节点强制使用`Native\MatchTool.dll`中的demo一致 native 接口，包括`LearnPatternMem`和`MatchMem`，不新增、不替换、不改写NCC匹配核心算法。
- 输出同步：已将更新后的`ToolTreeView.xml`同步到`bin\Debug`、`bin\x64\Debug`和`bin\x64\Release`，避免从旧输出目录启动时仍缺少新增节点。
- 验证：更新并通过模板匹配CPU兼容专项检查，保护原`MatchTemplate`和新增`NccMatchTemplate`两节点并存，以及NCC节点强制demo native算法路径；回归模板匹配托管封装、崩溃诊断、native导出、多目标排序输出和订阅端口目录检查；解决方案`Debug|x64`编译通过，结果为0个错误、175个既有警告。
- 拖入修复：补齐普通流程画布`ProcessEditPanel.CreateNode`中的`NccMatchTemplate`创建分支，解决工具箱可见但拖入画布提示“当前节点类型创建失败”的问题；同步补齐工具箱图标缩写和画布分类颜色分支。
- DLL同步：按要求将框架`Native\MatchTool.dll`替换为demo目录`CSharpDemo\MatchTool.dll`同一二进制，并同步到`bin\Debug\Native`、`bin\x64\Debug\Native`和`bin\x64\Release\Native`；替换前旧DLL备份为`Native\MatchTool.dll.codex-bak-before-demo-dll-20260806`。
- 取图加速：确认卡尺工具运行时直接从订阅`OutputImage`读取只读`Mat`，不会调用`UpdataImage`或整图转`Bitmap`；已将`NCC模板匹配`节点单独改为同类运行方式，跳过旧模板匹配的`UpdataImage`、整图`ToBitmap`、源图`To24Bpp`和结果`OutputBitmap.ToMat`路径，直接把订阅`Mat`的内存指针、宽高、步长、通道传给demo一致`MatchMem`；原`模版匹配`节点保持旧逻辑不变。
- 取图验证：新增专项检查保护`NCC模板匹配`必须走`MatchTemplateFromSubscribedMat`和`FastMatchEngineHandle.MatchMat`，禁止运行时回到`UpdataImage`；复验模板匹配CPU兼容、native导出、托管封装和多目标排序输出检查均通过；解决方案`Debug|x64`编译通过，结果为0个错误、175个既有警告。
- DLL零拷贝优化：确认`MatchTool.dll`的`MatchMem`原生接口本身支持8位1/3/4通道；NCC节点改为优先传入上游已经缓存的灰度`Mat`，C#层不再为1通道或4通道输入转成24位BGR，模板已经是24位时也不再每帧复制。同步修改demo同源DLL的单通道入口，使其直接引用调用期间有效的灰度内存，不再`clone`整图；NCC核心匹配、参数和结果处理逻辑均未改动。
- DLL指令集：demo构建脚本新增可配置`MATCHTOOL_ENABLE_AVX2`开关，算法源码默认构建行为仍保留AVX2；本次框架发布DLL按现有`MatchTool.runtime.txt`使用`x64-generic`构建，避免低配电脑误入AVX2指令，同时保持demo和框架使用同一DLL。
- DLL同步验证：最终`MatchTool.dll`已同步到demo、框架`Native`及Debug/Release运行目录，SHA-256统一为`139B557A126E8589DE07B7EE0E517C3BDC459D52D5460B2C0128A21907AB8AA9`；原DLL已分别备份。8个native导出接口检查、native创建释放烟雾测试、NCC专项检查、托管封装和动态结果检查均通过，`Debug|x64`编译为0错误、175个既有警告。
- 等价性验证：使用同一灰度模板和同一像素内容，分别按1通道零拷贝输入与demo式3通道BGR输入调用`MatchMem`，两边均返回1个结果，中心`(69.000,57.000)`、分数`1.000000`完全一致，确认仅移除内存复制和通道往返，未改变匹配效果。

## 2026-08-05：修复手动调参保存刷新图像源数值越界

- 状态：已完成，针对手动调参点击“保存设置”后弹出“参数保存失败”的问题进行修复。
- 根因：保存运行参数后会触发节点参数界面刷新，图像源参数窗体直接把节点里的曝光和增益写入`NumericUpDown.Value`；当运行参数过滤未显示或未修改相机曝光/增益时，参数值可能仍为0，而真实相机曝光最小值可能大于0，导致WinForms抛出数值越界异常。
- 修复：新增图像源数值控件安全回填方法，刷新曝光和增益时先按控件当前`Minimum/Maximum`夹紧，再写入`Value`，避免刷新界面阶段中断保存流程。
- 验证：新增`Tests\ManualTuningImageSourceNumericClamp.Tests.ps1`检查手动调参刷新曝光、增益均走范围保护。

## 2026-07-30：修复顺序连线运行器多入线节点提前执行

- 问题现象：`152.多条件判断` 在 `164.四则运算` 本轮产出前执行，导致读取 `164.V1` 为空并把后续 `144.ROI结果绘制` 显示结果带成 NG。
- 根因分析：顺序连线运行器只等待待执行队列中已有的订阅源，没有检查当前节点所有入线来源是否已经完成；当 `138->152` 或 `142->152` 先把 `152` 放入队列时，`164` 仍在 `160/163->164` 路径后方，尚未入队执行。
- 修复方式：在顺序连线运行器中，节点执行前先检查未完成的入线来源；如果该上游节点已在待执行队列中，或可从待执行队列中的节点继续沿连线到达，则当前节点放回队尾等待，避免多入线节点提前执行。
- 自动检查：新增 `Tests\ConnectedGraphSequentialInputWait.Tests.ps1`，保护“入线等待必须发生在节点标记已访问前”的调度规则。

## 2026-07-16：相机添加与实时调试界面 HTML 原型

- 状态：已完成第六版界面原型，暂未改动正式 WinForms 窗体代码。
- 需求背景：现有相机添加界面只能选择品牌、设备和自定义名称，缺少添加前实时画面预览与曝光、增益、触发、IO、3D 图像模式、点云显示等直接调试能力。
- 参考范围：已查看 `Forms\CameraAdd` 现有窗体结构、`Device\Camera\ICamera.cs`、`Device\3D\I3DCamera.cs`、`Device\3D\Camera3DModels.cs` 以及用户提供的 SDK CHM 手册中相机枚举、打开、取流、取帧、参数读写相关内容。
- 原型文件：新增 `artifacts\camera-add-workbench-prototype.html`，采用“左侧设备添加与列表、中间实时画面、右侧实时参数”的工作台式布局。
- 原型能力：包含 2D/3D 设备切换、搜索筛选、自定义名称、实时预览、曝光/增益滑块、连续/软触发/硬触发切换、触发延迟、缓存节点、3D 深度图/点云模式、原生点云缓存、IO 线路与频闪开关等界面效果。
- 交互说明：HTML 内置模拟设备和画面动画，仅用于确认布局、交互密度和功能入口；后续落地 WinForms 时再把控件加入对应 `.Designer.cs` 文件，保证可在设计器中查看。
- 二版调整：根据用户提供的 SDK 属性页截图，将右侧参数区重排为深色“属性树 + 属性表”形式，收敛为“基本属性”“触发 / IO 输入”“触发 / IO 输出”“高级属性 / ROI”四个实际页面，不再使用卡片堆叠式参数面板。
- 二版字段：基本属性覆盖帧率、自动曝光、曝光时间、自动增益、增益、宽度、高度、像素格式、测试模式；触发输入覆盖触发器选择器、触发模式、触发源、触发极性、触发延迟；触发输出覆盖线路选择器、线路模式、线路防抖时间；ROI 覆盖区域选择器、像素格式、绘制 ROI、偏移、宽高和恢复最大画幅。
- 三版调整：根据用户提供的设备树截图，将左侧从“添加表单”改为 SDK 风格设备树，包含 GigE、USB、PCIe、Serial Port、GenTL 分组、网卡层级、相机节点和底部设备信息。
- 三版交互：实时预览默认显示“无图像”，选中设备不会自动采集；必须点击预览工具栏的运行按钮后才开始绘制模拟实时画面，停止后恢复未采集状态。
- 四版调整：根据用户反馈“左侧不应该搞这么复杂”，将左侧从完整 SDK 设备树收敛为业务软件更易用的简洁设备选择区，仅保留搜索、设备卡片、自定义名称、添加到方案和当前设备摘要。
- 四版交互：左侧点击设备只切换当前对象，点击“添加”只加入方案并记录状态，不自动打开相机；中间预览区仍由运行按钮控制采集开始和停止。
- 五版调整：根据当前软件截图，明确保留左侧现有“相机列表 + 添加按钮 + 相机开关”结构，不再把左侧设计成设备搜索或设备树。
- 五版范围：仅将右侧“实时图像预览”区域改为深色预览工作区，内部包含运行/单帧/显示模式工具栏、默认“无图像”状态、底部关键指标条，以及右侧属性调试面板。
- 六版调整：根据用户反馈删除实时预览底部关键指标条，并将顶部工具栏收敛为“开始采集”和“保持图像”两个核心操作，减少预览区视觉噪声。
- 六版交互：点击“开始采集”后才绘制模拟实时画面，按钮切换为“停止采集”；“保持图像”仅在采集中生效，用于冻结当前帧，停止采集时自动取消保持状态。
- 七版调整：根据用户反馈隐藏 HTML 原型右侧属性面板顶部工具条，仅保留属性树和属性内容区。
- 代码落地：新增 `Forms\CameraAdd\CameraLiveDebugControl.cs` 与 `Forms\CameraAdd\CameraLiveDebugControl.Designer.cs`，将实时预览、开始采集、保持图像、基本属性、触发输入、IO 输出和 ROI 调试区做成可在设计器查看的 WinForms 控件。
- 接入方式：`FrmCameraListView` 选中 2D 或 3D 相机时改为加载 `CameraLiveDebugControl`，左侧相机列表、添加按钮和相机开关保持原有结构。
- 采集交互：2D 相机使用 `OnMatReceived` 回调转 Bitmap 刷新预览，刷新节流约 30 FPS；3D 相机接入 `FrameArrived` 并优先调用 SDK 原生点云显示；停止或释放控件时会注销回调并停止预览采集。
- 参数交互：2D 相机已接入曝光、增益、触发模式、触发源、触发极性、触发延迟、线路选择、线路模式、频闪使能和线路反转的即时写入；3D 相机当前接入采集、点云显示、触发模式、触发源和触发延迟配置。
- 编译检查：使用 Visual Studio 2022 MSBuild 编译 `TDJS-Vision.csproj`，结果为 0 个错误、180 个既有警告。
- 审查修复：补充右侧预览控件替换时的显式释放；记录预览控件是否实际启动取流，停止时只停止自身启动的取流；修复异步图像投递失败时的 Bitmap 释放；3D 帧信息先拷贝再投递到 UI；ROI 页面暂设为预留入口，避免未接入 SDK 写入时误改相机。
- 窗口调整：根据现场截图反馈，将 `FrmCameraListView` 默认打开状态改为最大化，并设置最小尺寸 `1360×700`，避免实时预览和右侧属性区互相遮挡。
- 布局修正：根据用户反馈正式 WinForms 与 HTML 原型不一致，将左侧相机列表改为固定 `252px`，右侧属性区改为固定 `526px`，中间预览区自适应；右侧属性树改为“属性树 / 常用属性 / 触发 / 高级属性”的分组结构，更接近 HTML 原型比例。
- 预览修正：根据现场反馈“开始采集后没有图像”，在 2D 预览中保留 SDK Mat 回调的同时新增后台主动取帧兜底；开始采集且触发模式关闭时会切到连续采集，避免硬触发源未触发导致画面一直为空。
- 字体修正：右侧调试区字体从粗宋体调整为更接近 HTML 原型的“微软雅黑”，属性树分组节点加粗、子项正常字重，减少视觉压迫感。
- 资源修正：属性树分组字体纳入控件释放流程，避免相机管理窗口反复打开关闭时累积字体句柄。
- 编译复验：使用 Visual Studio 2022 MSBuild 编译 `TDJS-Vision.csproj`，结果为 0 个错误、180 个既有警告。
- SDK 细节优化：新增相机触发源和触发极性 SDK 可选枚举读取接口，右侧调试区的触发源、触发极性、线路选择和线路模式优先使用 SDK `SupportEnumEntries` 动态生成，不再固定写死所有线路。
- 触发联动：触发模式为“关闭”时隐藏触发源、触发极性和触发延迟；触发模式为“打开”时显示触发源和触发延迟，仅硬件线路触发时显示触发极性，避免无效参数干扰操作。
- IO 联动：线路模式改为按界面中文值写入 SDK，线路模式为“输出”时才显示频闪使能；补齐线路 3、线路 4 的线路选择写入分支，并修正线路模式读取错误码判断。
- 触发源修复：修正 `CameraHik.GetTriggerSource()` 的反向判断，只有触发模式关闭时才返回连续采集，触发打开时按 SDK 当前触发源返回软件触发或线路触发。
- 自动检查：新增 `Tests\CameraLiveDebugControlDynamicSdkOptions.Tests.ps1`，覆盖 SDK 动态选项、触发行显隐、IO 联动和触发源读取逻辑；专项检查通过，`TDJS-Vision.csproj` 编译结果为 0 个错误、180 个既有警告。
- 软触发修正：触发模式为“打开”且触发源为“软件触发”时新增“软触发一次”按钮，主动取帧轮询在软件触发源下不再自动调用 `GetOneFrameImage()`，避免把软触发误变成连续实时触发；开始采集时会按界面当前触发模式和触发源重新写入 SDK。
- 线路模式修正：读取线路模式前先把 SDK `LineSelector` 切到界面当前线路，再读取该线路对应的 `LineMode` 可选项；切换线路后会重新刷新线路模式列表，避免线路 1 仍显示上一个线路的模式能力。
- 自动复验：扩展 `Tests\CameraLiveDebugControlDynamicSdkOptions.Tests.ps1` 覆盖手动软触发按钮、软件触发轮询跳过、开始采集前触发配置写入、当前线路模式刷新；专项检查通过，`TDJS-Vision.csproj` 编译结果为 0 个错误、180 个既有警告。
- 调试状态保护：相机管理窗口进入实时调试时先保存 2D 相机原始触发模式、触发源、触发延迟和触发极性；默认预览临时切为连续采集，停止采集、切换相机或关闭窗口时恢复原始流程相机状态，避免覆盖图像源节点的 `On + Line0` 等配置。
- 默认预览优化：窗口显示后自动恢复上一次点击的 2D/3D 相机，没有历史选择时自动显示第一台相机，并自动启动临时连续采集预览；左侧列表同步高亮当前相机。
- 关闭逻辑修正：移除 `FrmCameraListView_FormClosing` 中强制把所有相机改成 `TriggerMode.On + TriggerSource.LINE0` 的旧逻辑，关闭窗口只释放实时调试控件，由控件负责恢复调试前状态。
- 自动检查：新增 `Tests\CameraLiveDebugStateRestore.Tests.ps1`，覆盖默认连续预览、最近相机恢复、调试前状态快照、停止释放恢复和关闭窗口禁止硬编码 LINE0；并复验 SDK 动态选项专项检查通过。
- 编译复验：使用 Visual Studio 2022 MSBuild 编译 `TDJS-Vision.csproj`，结果为 0 个错误、179 个既有警告。
- 成像判定：集成 `D:\MyCode\PublicWook\自动对焦清晰度算法\相机清晰亮度判定` demo 中的 Tenengrad 清晰度和平均亮度算法，新增独立的相机预览图像质量分析模块，亮度判定收敛为“暗 / 正常 / 亮”，清晰度判定收敛为“模糊 / 清晰”。
- 异步分析：实时预览先显示原图，质量分析使用后台任务处理位图副本，约 200ms 节流一次，并用并发门保证同一时刻只分析一帧；分析忙时直接丢弃后续分析请求，避免堵塞实时画面。
- 预览优先：实时图像先投递到预览区域，再启动后台亮度 / 清晰度分析，避免分析启动前的位图复制影响画面刷新响应。
- 亮度优先提示：右侧“基本属性”新增“成像判定”显示区，包含亮度判定、清晰度判定和调试提示；提示逻辑优先要求先调亮度，亮度正常后才提示调整焦距提升清晰度。
- ROI 判定区域：ROI 页从“预留”改为可用，默认按 demo 创建图像中间半高 ROI；实时预览会绘制蓝色 ROI 框，右侧 ROI 数值修改后亮度 / 清晰度分析只统计 ROI 内区域，并自动限制在当前图像范围内。
- ROI 编辑：ROI 页新增“编辑ROI / 完成”操作，进入编辑后可在实时预览图上拖动 ROI 或拖拽边缘 / 角点缩放 ROI，完成后成像判定持续使用当前 ROI。
- ROI 保持：按相机唯一键缓存用户编辑后的 ROI，重新进入相机管理界面或重新创建实时调试控件时优先恢复上次 ROI；数字输入、拖拽编辑、恢复默认和控件释放时都会保存快照。
- 图像并发修复：修复预览图像先交给 PictureBox 后再创建分析副本导致的 GDI+“对象当前正在其他地方使用”异常；现在在 UI 接管图像前先分离分析副本，后台分析只处理独立副本。
- 自动检查：新增 `Tests\CameraLiveDebugQualityAsync.Tests.ps1`，覆盖质量分析模块注册、后台异步分析、并发门、位图副本、ROI 判定区域、ROI 编辑控件、亮度优先提示和设计器中文控件；同步复验相机状态恢复与 SDK 动态选项专项检查通过。
- 编译复验：使用 Visual Studio 2022 MSBuild 编译 `TDJS-Vision.csproj`，结果为 0 个错误、179 个既有警告。

## 2026-07-14：制作 TDJS-Vision 视觉检测能力说明 PPT

- 状态：已完成。
- 交付文件：`outputs\TDJS-Vision视觉检测能力说明.pptx`。
- 页面数量：15 页，16:9 宽屏，可编辑 PowerPoint。
- 内容范围：软件当前检测能力总览、测量、定位、缺陷检测、传统视觉、YOLO、无监督检测、大模型、算法选型和运行参数上下限调整。
- 事实依据：当前项目 `README.md`、`ToolTreeView.xml`、测量/检测/运行参数相关代码，以及 `docs\manual-assets` 中的真实软件界面截图。
- 真实性原则：未填写未经现场验证的精度、节拍、收益或模型指标。
- 视觉检查：已导出 15 页 PNG 和整套联系表逐页检查，修正第 3 页输出说明排版、第 8 页标点换行和第 15 页标题换行。
- 自动检查：`slides_test.py` 返回 `Test passed. No overflow detected.`；PPTX 压缩包内校验到 15 个幻灯片 XML，最终文件大小 2,080,655 字节。
- Logo 修订：用户确认 `ig6s1-ksbbd-001.ico` 为软件正式 Logo；已提取 ICO 中的 32×32 透明图标并替换封面旧 Logo。
- 修订交付：保留原版，新增 `outputs\TDJS-Vision视觉检测能力说明-正式Logo版.pptx`。
- 修订检查：正式 Logo 版共 15 页，`slides_test.py` 返回 `Test passed. No overflow detected.`，最终文件大小 2,077,405 字节。

## 2026-07-14：根据操作流程视频制作 Word 文档

- 状态：已完成
- 输入目录：`C:\Users\34652\Desktop\框架操作流程视频`
- 图标：`D:\MyCode\PublicWook\TDJS-Vision\ig6s1-ksbbd-001.ico`
- 处理原则：每个实际存在的视频对应一份 Word 操作流程文档；文档使用简体中文，包含关键画面、操作步骤、注意事项和完成检查。
- 当前清点结果：目录内有 1 个 MP4 视频和 3 个 JPG 封面图片；JPG 不作为视频内容编写依据。
- 辅助工具：新增 `tools\文档生成\VideoFrameExtractor.cs`，用于读取视频元数据并按时间间隔导出操作画面。
- 辅助工具：新增 `tools\文档生成\ImageOcrExtractor.cs`，用于辅助识别关键画面中的界面文字。
- 文档生成：新增 `tools\文档生成\build_video_documents.py`，用于统一生成封面、页眉页脚、步骤正文、视频截图和最终检查清单。
- 生成结果：`artifacts\操作流程文档\搭建AI检测端子线芯操作流程.docx`，共 15 页，包含 12 个操作步骤和 13 张关键视频截图。
- 视觉检查：使用 Microsoft Word 导出校验 PDF，并将全部 15 页渲染为图片逐页检查；未发现空白页、内容裁切、文字遮挡、图片溢出或错误分页。
- 交付说明：输入目录仅有 `搭建AI检测端子线芯视频.mp4` 这一份实际视频，因此本次交付 1 份对应 Word 文档；另外 3 个 JPG 文件是封面图片，没有据此虚构视频内容。

## 2026-07-14：补充生成新增 3 个操作流程视频的 Word 文档

- 状态：已完成
- 新增视频：`框架基本功能介绍.mp4`、`模型加密与获取视频.mp4`、`科瑞IO卡前期连接工作.mp4`。
- 辅助工具：新增 `tools\文档生成\build_additional_video_documents.py`，复用既有 Word 版式、页眉页脚、编号、提示框和截图组件，对新增视频进行批量文档生成。
- 抽帧结果：按 5 秒间隔分别抽取新增视频画面，并生成联系表辅助核对关键步骤。
- 文档生成：新增 `artifacts\操作流程文档\框架基本功能介绍操作流程.docx`，共 15 页，覆盖软件入口、方案、日志、流程编辑、检测项、通信、画布、系统信号、AI训练和语言切换。
- 文档生成：新增 `artifacts\操作流程文档\模型加密与获取操作流程.docx`，共 10 页，覆盖模型目录选择、CPU/GPU 模型区分、ONNX 转 Engine、模型加密和交付文件整理。
- 文档生成：新增 `artifacts\操作流程文档\科瑞IO卡前期连接操作流程.docx`，共 9 页，覆盖默认地址 ping、网卡选择、IPv4 静态 IP 配置、通信验证和软件启动提示。
- 视觉检查：本机缺少 LibreOffice 渲染器，改用 Microsoft Word 导出校验 PDF，再使用 Python PDF 渲染库输出页面图片；三份文档全部逐页检查，未发现空白页、内容裁切、文字遮挡、图片溢出或错误分页。
- 交付位置：三份新增文档已复制到 `C:\Users\34652\Desktop\框架操作流程视频\操作流程文档`。

## 2026-07-14：增加软件启动动画和真实加载进度

- 状态：已完成。
- 启动方式：采用方案 1，启动动画运行在独立 STA 界面线程，主线程负责关键初始化和最终主页面切换。
- 界面效果：新增轻盈科技风格无边框启动页、独立红蓝单眼机器人、上下浮动、眨眼、天线呼吸灯、状态文字、对象计数和真实进度条。
- 加载范围：方案文件解析、旧数据清理、光源、相机、PLC、Modbus、TCP、串口、检测流程及流程节点均会上报当前阶段、对象名称及“当前/总数”。
- 切换规则：启动页至少显示 2 秒；关键数据成功加载后才显示主页面；脚本引擎预热和自动运行任务不阻塞主页面显示。
- 失败处理：关键初始化失败时停留在启动页，以简体中文显示错误，并提供“重试”和“退出软件”；加载器原有静默异常已补充日志和失败汇总。
- 扩展设计：通过 `IStartupProgressReporter`、`StartupProgressContext`、`StartupSplashController` 和 `StartupApplicationContext` 分离进度上报、线程管理与启动协调，便于后续替换启动页或增加加载阶段。
- 设计器要求：启动页全部控件均定义在 `Forms\Startup\FormStartup.Designer.cs`，可以在 Visual Studio 窗体设计器中查看和调整布局。
- 资源文件：新增 `Resources\StartupMascot.png`，并注册到项目资源 `Properties\Resources.resx`。
- 自动检查：`Tests\StartupSplash.Tests.ps1`、`Tests\ProgramSingleInstance.Tests.ps1`、`Tests\FormMainAuthorizationMenu.Tests.ps1`、`Tests\FormMainUnsupervisedMenu.Tests.ps1` 全部通过。
- 编译检查：使用 Visual Studio 2022 MSBuild 完整编译 `TDJS-Vision.sln`，结果为 0 个错误、182 个既有警告。
- 前台显示修复：调整启动完成时的窗口切换顺序，先显示主窗体，再关闭置顶启动页，最后调用主窗体激活和置前，避免用户还需单击任务栏才能看到主页面。

## 2026-07-14：工作区独占显示与手动打开方案加载动画

- 状态：已实现并通过自动检查。
- 流程编辑：打开流程编辑窗口前自动隐藏主页面；关闭流程编辑窗口后自动显示、激活并置前主页面，避免任务栏同时出现两个业务窗口。
- AI 训练：无监督训练和大模型训练窗口采用同一套独占显示规则；训练窗口正常关闭或显示期间发生异常时都会恢复主页面。
- 扩展设计：新增 `IExclusiveWorkspacePresenter` 与 `ExclusiveWorkspacePresenter`，统一管理独占工作区的预定位、主窗体隐藏和异常安全恢复，便于后续接入其他全屏业务窗口。
- 手动打开方案：文件菜单和工具栏“打开方案”统一复用机器人加载动画，加载时显示当前阶段、当前数据名称、对象计数和真实进度，完成后直接返回主页面。
- 线程安全：方案反序列化及设备、流程控件恢复继续在主 UI 线程执行；动画运行在独立 STA 界面线程，避免跨线程访问 WinForms 控件。
- 失败处理：方案文件不存在、读取失败、解析失败或设备恢复失败时，都会先关闭动画并恢复主页面，再显示简体中文错误提示并记录完整异常日志。
- 重复使用修复：WinForms 全局视觉样式和文本呈现设置只在 `Program.Main` 执行一次，手动打开方案时创建第二个动画线程不会再触发“必须在创建首个窗口前调用”的异常。
- 异常安全：动画创建超时会请求线程退出，线程仍存活时不会提前释放同步对象；动画关闭、主窗体显示或激活异常均单独记录，不会覆盖原始方案加载异常。
- 自动检查：新增 `Tests\ExclusiveWorkspace.Tests.ps1`，并同步更新无监督训练、大模型训练和窗体最大化测试的旧显示方式断言；专项测试、启动动画、程序单实例、授权菜单、无监督训练菜单、窗体最大化、大模型集成和训练对话框所有权检查全部通过。
- 测试编码：本轮涉及中文内容的 PowerShell 测试统一保存为 UTF-8 BOM，并使用 Windows PowerShell 5.1 原生 `-File` 方式复验通过。
- 代码审查：独立复审确认首次发现的关键问题和重要异常路径均已修复，未发现新的关键或重要问题。
- 编译检查：使用 Visual Studio 2022 MSBuild 完整编译 `TDJS-Vision.sln`，结果为 0 个错误、182 个既有警告。

## 2026-07-15：方案数据加载异常改为非阻断

- 状态：已完成。
- 启动规则：自动加载方案和命令行方案发生文件不存在、配置读取、路径解析、JSON 解析、设备恢复、流程恢复或节点恢复异常时，不再停留在“重试/退出软件”页面，统一记录日志后继续进入主页面。
- 安全规则：方案数据没有完整恢复时自动关闭“启动后自动运行”，避免直接运行不完整方案。
- 手动打开：手动打开方案失败后只记录完整异常日志，加载动画正常关闭并直接恢复主页面，不再显示错误弹窗。
- 新建方案：加载 `空方案.Sol` 时建立无界面进度作用域，设备或节点恢复异常只记录日志；`Solution.Load` 返回完整加载结果，不再自行显示“反序列化异常”弹窗。
- 节点恢复：移除 `ProcessEditPanel` 在非启动作用域下的节点恢复错误弹窗，节点异常始终写入运行日志并继续恢复后续节点。
- 失败隔离：新增 `StartupProgressContext.DrainFailures()`，统一读取并清空设备、流程和节点恢复失败，避免外层启动协调器再次抛出同一错误。
- 自动检查：新增 `Tests\NonBlockingSolutionLoad.Tests.ps1`，覆盖启动加载、手动打开、新建方案、失败集合清空、自动运行关闭和流程节点无弹窗规则；同步复验启动动画、独占工作区、方案资源释放、单实例、授权菜单、无监督菜单、窗体最大化、大模型集成和训练对话框所有权检查。
- 编译检查：使用 Visual Studio 2022 MSBuild 完整编译 `TDJS-Vision.sln`，结果为 0 个错误、182 个既有警告。

## 2026-07-16：大模型训练每次强制重建模型链

- 状态：已完成。
- GPU 训练：每次固定重新导出所选分辨率 ONNX，再使用当前电脑显卡及所选精度重新生成 TensorRT Engine，最后重新训练 memory bank，不再复用同名旧模型。
- CPU 训练：每次固定重新导出 ONNX 后重新训练 memory bank，不生成 TensorRT Engine。
- 临时产物：ONNX 和 Engine 均写入同目录 `<模型名>_<GUID>.building.<扩展名>` 唯一路径，避免并行调用互删固定临时文件。
- 有效性保护：发布前检查产物至少 64 字节，并通过实际 `LargeModelDinov2Detector.LoadModel` native 路径验证 ONNX 或 Engine 可加载；验证失败保留旧正式模型且不启动 Bank。
- 取消保护：导出和构建进程启动前、最终退出后、正式模型替换前及 Bank 启动前均检查取消；终止进程后最多等待 5 秒再清理临时文件。
- native 生命周期：DINOv2 Init 返回句柄先保存在局部变量中，仅在检测器未释放且令牌未取消时发布；阻塞 Init 期间取消会在返回后释放该句柄，Dispose 后不会复活且 native Train 不会启动。
- 串行保护：在 `LargeModelDll` 中持有 `FileShare.None` 锁文件，跨线程、跨进程保护“数据集准备 → 模型生成 → Bank → 模板打包”完整链，等待锁期间支持取消。
- 路径边界：模板工作目录的固定可信根为 `Model\_LargeModelTraining`；模板名 `.`、`..` 在任何递归清理前被拒绝，规范化后的工作目录及清理目标必须严格位于可信根下。
- 模板发布：`.tdlarge` 写入同目录 `<模板名>_<GUID>.building.tdlarge`，复制模型和 Bank 期间支持取消；ZIP 关闭后验证清单、本次模型和 Bank 条目可读，再原子替换正式模板。
- 模板失败安全：打包取消、源流读取失败或 ZIP 验证失败均保留旧正式模板并尽力清理临时文件；正式模板提交后不再以令牌检查把成功改报为取消。
- 清理规则：失败或取消后尽力清理本次 GUID 临时文件；清理失败会记录包含路径和原因的简体中文日志，不会覆盖原始异常。
- 日志线程安全：stdout 和 stderr 使用线程安全缓存合并，避免异步输出事件并发写入 `StringBuilder`。
- Python 环境隔离：ONNX 导出子进程显式把 `PYTHONHOME` 指向 `LargeModelDll\third_party\python_native_env` 并清空继承的 `PYTHONPATH`，避免无监督模块初始化后遗留的全局 Python 环境污染大模型导出。
- 环境回归：新增 `PythonIsolation` 生产行为用例，模拟 `PYTHONHOME` 指向 `UnsupervisedDll\python_env` 的污染场景，确认导出前会恢复到大模型独立环境。
- 自动检查：组件行为测试覆盖连续两次训练、CPU/GPU 顺序、模型发布、取消、无效产物、跨进程锁、失败时 Bank 未启动和并发日志；新增生产行为测试动态编译真实服务、生产阶段、检测器和模板发布器，直接执行 `Train`，覆盖阻塞 Init 取消、`..` 哨兵路径、打包取消/失败/成功及 ONNX → Engine → Bank → Package 实际路径传递。
- 编译检查：Visual Studio 2022 MSBuild 的 x64 Debug `/p:RestorePackages=false` 构建结果为 0 个错误；180 个项目既有警告中没有本次新增文件告警。

## 2026-07-17：大模型节点改为保存时预加载和预热

- 状态：已完成。
- 模板选择：参数窗口只读取 `.tdlarge` 中的 `manifest.json`，不再把 Engine、ONNX 和 Bank 完整解包到 `_LargeModelPreview`，避免选择模板时产生大文件复制和重复缓存。
- 保存流程：点击“保存”后在后台线程完成模板解包、native 模型初始化和一次空图推理预热；界面显示“模型加载中...”，完成前禁止重复选择、刷新或保存。
- 运行流程：保存成功后生产流程直接复用已经加载并预热的检测器，只执行输入图像准备和 native 推理；旧方案未经过保存窗口时仍保留首次运行兜底加载。
- 失败保护：新模板加载或预热失败时不替换旧检测器，也不提交新节点参数，参数窗口保持打开并显示简体中文原因。
- 并发一致性：模型预加载提交、节点参数提交、流程参数读取和推理共用同一个配置门，连续运行期间保存新模板时不会出现“新检测器配旧参数”而重新加载旧模型的竞态。
- 性能修正：图像阈值、面积阈值和最大框数仅作为单次推理参数，不再参与 native 模型缓存匹配，调整阈值不会重新加载模型。
- 运行快速路径：保存后的常规推理只使用内存中的模板匹配状态，不再每帧读取模板文件时间戳。
- 自动检查：扩展 `Tests\LargeModelIntegration.Tests.ps1`，覆盖清单轻量读取、保存等待预加载、预热后原子替换和阈值不触发重载；新增 `Tests\LargeModelSavePreloadConcurrency.Tests.ps1`，动态编译生产配置门并执行并发提交、失败不提交行为检查，专项检查全部通过。
- 编译检查：Visual Studio 2022 MSBuild 按 `Debug|x64` 编译 `TDJS-Vision.csproj`，结果为 0 个错误。

## 2026-07-17：补全大模型节点中英文名称

- 状态：已完成。
- 问题原因：流程工具箱通过 `ProcessNew.NodeType.LargeModelDetection` 查找节点名称，翻译文件缺少该键时语言管理器直接返回键名，界面因此显示为被截断的 `ProcessNew.N...`。
- 修复内容：简体中文增加“大模型调用”，英文增加“Large Model Detection”。
- 自动检查：扩展 `Tests\LargeModelIntegration.Tests.ps1`，同时核对中英文翻译键和值，防止后续回归。

## 2026-07-17：同步 Debug 与 Release 运行环境

- 状态：已完成。
- Release 编译：使用当前源码重新编译 `Release|x64`，结果为 0 个错误、180 个项目既有警告。
- 环境同步：将 Debug 的语言资源、大模型、无监督、YOLO GPU、Native、二维码模型、模板和公共依赖目录镜像到 Release；Release 主程序继续保留 Release 编译产物，没有使用 Debug EXE 或 PDB 覆盖。
- 残留清理：镜像过程删除 Release 大模型目录中 Debug 不存在的旧 Bank、备份 Engine、Python 缓存、开发库和调试符号。
- 数据保护：没有覆盖 `Logs`、`Model`、`ProjConfigs` 和设备运行日志等业务数据目录。
- 一致性检查：21 个公共运行目录使用只读镜像复查，返回码全部为 0，不存在待复制、待删除或大小不同的文件。
- 环境实测：Release 的无监督 Python 和大模型独立 Python 均成功导入 `_ctypes`、`ctypes` 和 `torch`；大模型集成检查与无监督节点检查均通过。

## 2026-07-17：修复大模型 GPU 训练 native 闪退

- 状态：已完成。
- 根因确认：客户机日志在 ONNX 导出完成后、Engine 生成前中断；本机 Windows 事件记录确认 `Dinov2AD.dll!init` 抛出 `0xc0000005`。问题来自模型发布阶段新增的进程内 native 预验证，部分机器会直接终止主程序。
- ONNX 校验：移除发布阶段对 `Dinov2AD.dll!init` 的调用，改为使用大模型独立 Python 子进程执行 `onnx.checker.check_model`。
- Engine 校验：改为独立 `trtexec --loadEngine=<临时Engine> --skipInference` 子进程反序列化验证；即使 native 或驱动在校验期间崩溃，也不会带崩主程序。
- 训练顺序：继续保持每次训练 `ONNX → Engine → Bank → 模板`，Bank 训练阶段只执行一次正式 native 初始化，不再预验证一次后重复初始化。
- 取消与失败：两个校验子进程均支持取消、终止等待、输出收集和简体中文错误信息；失败时继续由发布器清理本次 GUID 临时文件并保留旧正式模型。
- 自动检查：大模型新产物、协调器行为、生产入口、节点集成和保存预加载并发共 5 组专项检查全部通过。
- 编译检查：`Debug|x64` 与 `Release|x64` 均为 0 个错误、180 个项目既有警告。

- 2026-07-17：相机管理调试界面新增“同步亮度到流程”按钮，点击后会把当前曝光时间和增益同步到当前方案中使用同一 2D 相机的图像源节点，并标记方案已修改；新增结构测试覆盖按钮、事件绑定和流程参数写回。

## 2026-07-17：无监督节点改为保存时预加载和预热

- 状态：已完成。
- 模板选择：参数窗口只读取 `.tdunsup` 中的 `manifest.json`，不再把 `model.onnx` 完整解包到 `_UnsupervisedPreview`，避免选择和刷新模板时复制大文件。
- 保存流程：点击“保存”后在后台完成运行环境校验、模板解包、native 模型加载和一次模板尺寸空图预热；界面显示“模型加载中...”，完成前禁止重复选择、刷新或保存。
- 运行流程：保存成功后流程直接复用已经加载并预热的检测器；旧方案未重新保存参数时仍保留首次运行兜底加载。
- 失败保护：候选模型只有加载和预热全部成功后才替换旧检测器并提交新参数；失败时释放候选检测器，保留旧检测器和旧参数，参数窗口保持打开并显示简体中文原因。
- 并发一致性：模型预加载、参数提交、流程参数读取和推理共用无监督配置门，避免连续运行期间保存新模板时出现跨版本模型与参数组合。
- 运行性能：保存后的正常推理使用内存中的模板路径和加载期参数匹配，不再每帧读取模板文件时间戳。
- 自动检查：新增 `Tests\UnsupervisedSavePreload.Tests.ps1`，覆盖轻量清单读取、异步保存、空图预热、候选模型发布、失败不提交及并发原子性；无监督保存预加载、节点和模板包共 3 组专项检查全部通过。
- 编译检查：Visual Studio 2022 MSBuild 的 `Debug|x64` 和 `Release|x64` 编译均为 0 个错误；现有警告分别为 179 个和 180 个。

## 2026-07-17：AI 推理实际后端与分段耗时诊断

- 状态：已完成，等待客户机日志复测。
- 无监督后端：训练模板记录的普通 `GPU` 在推理时明确映射为 DLL Demo 相同的 `GPU_ORT`，普通 `AUTO` 映射为 `AUTO_ORT`；显式的 ORT 和 TensorRT token 保持不变。
- 失败策略：用户明确选择无监督 GPU 或大模型 GPU 时，GPU/Engine 初始化失败直接报告错误，不再静默改用 CPU；只有 AUTO 模式允许 CPU 回退。
- 加载日志：无监督和大模型加载成功后记录实际设备及实际模型完整路径，能够直接确认客户机是否真正使用 GPU、TensorRT Engine 或 CPU ONNX。
- 推理日志：节点成功日志新增输入图像尺寸、实际设备、图像准备耗时、native `Infer` 耗时和结果处理耗时；原节点总耗时继续保留。
- 计时边界：ROI 裁剪、BGR 转换和托管图像复制计入图像准备；P/Invoke `Infer` 单独计入 native 推理；返回数组转换、坐标映射和结果构建计入结果处理。
- 优化边界：本轮不删除现有图像复制，等待客户机分段日志确认真实瓶颈后再进行单变量性能优化。
- 自动检查：新增 `Tests\InferenceBackendDiagnostics.Tests.ps1`，覆盖 GPU_ORT 映射、显式 GPU 禁止 CPU 回退、实际设备/模型状态和分段耗时日志；无监督保存预加载、无监督节点、模板包、大模型集成及保存并发检查全部通过。
- 编译检查：Visual Studio 2022 MSBuild 的 `Debug|x64` 和 `Release|x64` 编译均为 0 个错误；项目既有警告分别为 179 个和 180 个。

## 2026-07-17：打开方案时顺序预加载 AI 模型

- 状态：已完成。
- 覆盖入口：在 `ConfigHelper.SolLoad` 完成设备、流程、节点和参数恢复后统一执行，因此同时覆盖软件启动自动打开方案和用户手动打开方案。
- 预加载范围：只处理已启用流程中已启用且已有保存参数的大模型、无监督节点；未配置模型或禁用的节点不占用 GPU 和内存。
- 执行顺序：按照方案中的流程和节点顺序逐个加载并预热，前一个完成后才处理下一个，避免多个 GPU 模型并行初始化造成显存峰值和 native 依赖竞争。
- 扩展方式：新增 `INodeRuntimePreloader` 统一接口，方案加载层不依赖大模型或无监督具体类型，后续 AI 节点可通过实现接口接入。
- 失败处理：单个节点预加载失败会记录流程、节点和原始异常，继续处理其他节点，并交由现有方案启动失败汇总机制统一报告。
- 进度日志：方案加载动画在 95% 到 99% 显示当前预加载的流程和节点；完成后记录总数、成功数和失败数。
- 自动检查：新增 `Tests\SolutionAiRuntimePreload.Tests.ps1`，覆盖统一接口、启用状态筛选、顺序等待、禁止并行、失败隔离、进度上报、两类节点接入和方案恢复调用顺序。
- 回归检查：方案数据加载异常非阻断、启动动画、方案资源释放、AI 推理后端、无监督保存预加载、无监督节点、模板包、大模型集成和大模型保存并发检查全部通过。
- 编译检查：Visual Studio 2022 MSBuild 的 `Debug|x64` 和 `Release|x64` 编译均为 0 个错误；项目既有警告分别为 179 个和 180 个。

## 2026-07-20：上传框架到 GitHub 远程仓库

- 状态：已完成。
- 目标仓库：`git@github.com:jiangjiahang792-ship-it/Frame.git`。
- 分支映射：将当前框架分支发布为远程 `main` 分支。
- 远程保留：原有 TDJS 远程地址保留为备用远程，GitHub 仓库设置为新的 `origin`。
- 忽略检查：确认 `.gitignore` 已忽略所有层级的 `bin` 与 `obj` 文件夹，且 Git 索引中不存在这两类构建产物。
- 上传状态：用户已明确确认外部上传风险；干净发布快照已成功推送到远程 `main`，`bin` 与 `obj` 文件夹未上传。由于本机未配置 GitHub SSH 密钥，本次实际传输使用 Windows 中已有的 GitHub HTTPS 凭据，项目 `origin` 仍保持为指定的 SSH 地址。

## 2026-07-20：固化 Release 运行库模板与空方案

- 状态：已完成。
- 基准来源：以当前确认可用的 `bin\x64\Release` 顶层 DLL 环境为基准，并通过隔离 Release 生成识别项目引用和 NuGet 无法自动恢复的文件。
- 运行库模板：新增项目根目录 `RuntimeDll`，固化 35 个非自动生成的顶层 DLL，共约 249.62 MB；普通动态加载或原生依赖 DLL 后续统一放入该目录。
- 自动复制：`TDJS-Vision.csproj` 新增统一的 `CopyRuntimeDependencies` 生成目标，AnyCPU/x64 的 Debug、Release 生成完成后都把 `RuntimeDll\*.dll` 复制到当前程序输出目录根部；未变化文件自动跳过，兼顾一致性和生成效率。
- 空方案修复：恢复根目录唯一 `空方案.Sol`，并设置每次生成都复制到程序输出目录，避免“新建方案”因输出目录缺少模板而记录文件不存在异常。
- 隔离环境：`LargeModelDll`、`UnsupervisedDll` 按要求继续作为 Debug 本地环境；约 5 GB 的 `YoloGPUDll` 也保持现状，不纳入每次生成复制。三个目录均未绑定到项目内容。
- 添加规则：仅运行时使用的普通 DLL 放入 `RuntimeDll`；C# 代码直接引用类型的托管 DLL 放入项目根目录 `dll` 并在 Visual Studio 中添加项目引用。
- 自动检查：新增 `Tests\RuntimeDependencyTemplate.Tests.ps1`，覆盖空方案 JSON、35 个基准 DLL、统一复制目标、跳过未变化文件以及三个大型隔离目录的排除规则。
- 一致性检查：35 个模板 DLL 与 `bin\x64\Debug`、`bin\x64\Release` 中同名文件逐个执行 SHA-256 校验，空方案模板与两个输出副本同步校验，结果全部一致。
- 编译检查：Visual Studio 2022 MSBuild 的 `Debug|x64` 和 `Release|x64` 均为 0 个错误；项目既有警告分别为 179 个和 180 个。

## 2026-07-22：修复手动调参禁用项、动态输出刷新与调试权限

- 状态：已完成，未执行 Git 提交、切换分支或推送操作。
- 多条件开关：为每个条件新增独立的 `Enabled` 检测状态，原有 `EnableRunParamAdjust` 只负责决定是否显示在手动调参表；关闭检测后该行继续保留，可随时重新启用。
- 多条件判定：禁用项不读取上游结果、不参与比较、不回写源节点判定状态，也不建立运行等待依赖；全部条件禁用时按工艺约定判定为 `OK/True`，运行明细保留“已禁用”诊断以维持条件索引稳定。
- 动态变量来源：节点实现配置变量提供接口时，订阅候选项只采用当前参数定义，不再把上一次运行结果合并回来；同步修正四则运算、组合输入、组合输出和组合模块的旧结果残留逻辑。
- 即时刷新：节点参数窗口返回后统一发布输出定义变化事件，普通订阅控件、多条件、四则运算、组合输出和组合模块立即刷新候选变量，不再依赖单次运行；尚未运行的新变量按声明类型参与筛选。
- 权限控制：新增统一用户权限上下文；图像窗口“一键学习”和“手动调参”按钮保持可见，但只有最高权限可用，登录和退出后即时更新；主工具栏运行参数入口及事件入口同步增加最高权限限制，避免中低权限绕过。
- 自动检查：新增 `MultiConditionManualDisable.Tests.ps1`、`DynamicOutputImmediateRefresh.Tests.ps1` 和 `ManualTuningPermission.Tests.ps1`，均先复现失败后完成修复并通过。
- 编译检查：Visual Studio 2022 MSBuild 的 `Debug|x64` 编译结果为 0 个错误、179 个项目既有警告。

## 2026-07-22：统一订阅结果的数据类型与可见性契约

- 状态：已完成，按用户要求未执行 Git 提交、切换分支或推送操作。
- 类型原则：订阅兼容性以实际数据类型为准，不再按来源节点做业务硬限制；`bool` 明确归为布尔，整数和浮点数统一归为数值，图像、点、点集、轮廓、直线、圆、椭圆、矩形、区域、位姿、位置修正和算法结果等复杂对象分别归类。
- 数值兼容：建立统一数值转换规则；例如 Modbus 读取的 `short` 可以直接被接受数值的节点订阅，并按输入目标类型执行安全扩宽或受检转换；数值不会被当作布尔，溢出或不兼容类型给出明确中文错误。
- 统一目录：新增订阅端口声明、类型兼容服务和带缓存的订阅结果目录；静态结果、参数化动态变量及调用方输入契约从同一入口筛选和读取，避免各窗体重复反射并产生不同规则。
- 输出整理：当前 230 个带 `DisplayName` 的静态结果全部增加订阅分类；默认界面保留常用结果，高级结果通过订阅控件右键菜单“显示高级结果”查看，46 个诊断、耗时、内部字典和重复字段不再提供新建订阅。
- 旧方案兼容：旧方案已选择的隐藏结果继续显示为“兼容订阅”并保持可读取；属性已经不存在时显示“结果不存在”，不再悄悄回退到第一个候选结果。
- 输入约束：所有可见订阅控件均声明输入契约；图像工具只显示图像，位置修正和测量工具只显示对应复杂类型，逻辑与通信工具按布尔、数值或文本需求显示，确实需要任意结构的节点显式声明为通用输入。
- 动态选择器：四则运算、多条件、组合模块和组合输出改用统一结果目录；动态变量始终采用当前参数声明的真实类型，因此变量删除或改型后无需先单次运行即可刷新。
- Modbus 写入：写入订阅不再固定读取 `bool`；根据线圈/寄存器和所选数据类型切换布尔或数值输入契约，并支持 `short` 等数值直接格式化写入。
- 自动检查：订阅类型兼容、统一目录、230 项输出分类、调用方契约、Modbus 数值订阅、动态变量即时刷新、位置修正兼容、多条件禁用、调试权限、保存图像多条件订阅及测量串行采样共 11 组检查通过。
- 编译检查：Visual Studio 2022 MSBuild 的解决方案 `Debug|x64` 完整编译结果为 0 个错误、179 个项目既有警告。

## 2026-07-22：简化ROI结果绘制2并统一自动几何绘制

- 状态：已完成，按用户要求未执行 Git 提交、切换分支或推送操作。
- 设计文档：新增 docs\superpowers\specs\2026-07-22-result-overlay-two-type-simplification-design.md 和对应实施计划，明确“文本、ROI”两类界面、单布尔颜色判定、源颜色保留、旧方案兼容和扩展接口。
- 参数界面：ROI结果绘制2只保留“添加文本”“添加ROI”“删除”；基础区统一配置输入图像、可选布尔判定和OK/NG颜色，文本低频参数放入默认折叠的高级设置；所有控件均在 .Designer.cs 中，文本位置显示为“左上角、右上角、左下角、右下角”等简体中文。
- 自动ROI：新增 IOverlayGeometryAdapter、绘制上下文和带类型缓存的适配器注册表；一个ROI订阅可按实际类型绘制点、点集、线、圆、圆弧、椭圆、矩形、旋转矩形、轮廓、区域、测量结果和完整算法结果。
- 完整结果：订阅 AlgorithmResult 或多目标测量结果时复制矩形、NG矩形映射、线、圆、圆弧、椭圆和轮廓，明确不复制文本；多个目标的全部几何按原有顺序进入统一显示结果。
- 颜色规则：新版只接受一个严格的 bool 判定；配置后统一使用OK/NG颜色，未配置时保留来源几何颜色，来源颜色为空时使用OK色兜底；已有方案的多条颜色规则和旧线/矩形/区域枚举继续保留兼容运行。
- 空结果处理：算法或测量节点正常运行但未检出几何时不再误绘“未找到”；无法识别的普通订阅值仍显示原有缺失提示。
- 性能处理：每个ROI项每轮只读取一次订阅值；实际类型分派和算法结果属性读取器使用并发字典缓存；无状态算法结果适配器使用共享实例，避免多目标每帧重复反射和重复创建适配器。
- 依赖范围：手动调参过滤加入新版单布尔判定来源；未配置新判定时继续收集旧颜色规则来源，保证旧方案打开和手动调参不丢依赖。
- 自动检查：ROI结果绘制简化、几何适配行为、订阅调用方契约、类型兼容、端口目录、输出分类、动态变量即时刷新、位置修正订阅兼容、组合测量串行采样和多目标测量运行器共10组检查通过；专项行为覆盖源颜色保留、颜色覆盖、点集合十字标记、直接矩形、文本排除及新旧参数序列化往返。
- 界面检查：使用最新 Debug|x64 程序在屏幕外实际打开窗体，分别检查空白状态和新增ROI状态，未发现控件遮挡或英文枚举残留。
- 编译检查：Visual Studio 2022 MSBuild 的解决方案 Debug|x64 完整编译结果为0个错误、179个项目既有警告。

## 2026-07-23：修复自动ROI适配器未参与项目编译

- 状态：已完成，未执行 Git 提交、切换分支或推送操作。
- 根因：项目仍使用旧式显式编译清单，新增的 OverlayGeometryAdapterRegistry.cs 虽然已经存在，但 TDJS-Vision.csproj 缺少对应 Compile Include，因此 NodeResultOverlayDraw2 无法解析适配器注册表和绘制上下文。
- 修复：把自动ROI几何适配器文件加入项目编译项，没有修改算法行为或其它节点逻辑。
- 红灯验证：修复前 Debug|x64 稳定出现3个编译错误，几何适配专项明确失败并提示“项目文件必须编译ROI几何适配器”。
- 绿灯验证：修复后 Debug|x64 完整编译为0个错误、179个既有警告；ROI几何适配、ROI结果绘制简化和订阅调用方契约3组检查通过。

## 2026-07-23：把两类型简化迁移到实际ROI结果绘制节点

- 状态：已完成，未执行 Git 提交、切换分支或推送操作。
- 现场根因：用户方案打开的是 NodeType.ResultOverlayDraw，窗体标题为“ROI结果绘制”；此前简化功能落在独立的 NodeType.ResultOverlayDraw2，因此实际节点界面没有变化。
- 迁移策略：保留 ResultOverlayDraw 的节点类型、参数类型、结果类型和输出名称，原位增加自动ROI与单布尔判定；没有把旧节点强制映射成ResultOverlayDraw2，避免现有.Sol方案参数和下游订阅变化。
- 参数兼容：新增 JudgeText1、JudgeText2、全局OK/NG颜色和Roi枚举；旧Text、Line、Rectangle、Region、每项固定颜色及每项判定颜色字段全部保留并完成序列化往返。
- 运行逻辑：新Roi项每轮只读取一次订阅值并复用共享 OverlayGeometryAdapterRegistry；无判定时保留来源几何颜色，配置严格bool判定后用全局OK/NG颜色覆盖；旧线、矩形和区域继续走原专用绘制逻辑。
- 实际界面：NodeParamFormResultOverlayDraw只保留“添加文本”“添加ROI”“删除”，新增单布尔判定和全局OK/NG颜色；文本位置使用简体中文，旧几何项统一显示为“ROI（旧配置）”；全部控件位于对应Designer文件。
- 依赖范围：实际ROI结果绘制的运行等待依赖和手动调参过滤范围均加入新版JudgeText1来源。
- 红绿验证：专项测试先在旧参数缺少JudgeText1时失败，随后依次在旧界面缺少添加ROI、旧手动调参范围缺少判定来源处失败，完成对应最小实现后全部转为通过。
- 界面检查：使用最新Debug|x64程序实际打开标题为“ROI结果绘制”的窗体并自动点击“添加ROI”，确认界面显示文本、ROI、删除三个按钮，ROI设置只包含订阅和线宽，未发现遮挡或旧按钮残留。

## 2026-07-23：增加手动调参保存失败诊断日志

- 状态：已完成，未执行 Git 提交、切换分支或推送操作。
- 反馈现象：主页面进入手动调参后，点击“保存设置”会提示参数保存失败，但表格中的上下限已经在当前内存中更新，原提示无法判断真实失败位置。
- 诊断增强：保存链路增加“读取表格、保存相机参数、保存ROI、保存AI参数、写入AI配置文件、保存多条件、刷新界面、保存方案文件”等阶段标记；异常时只记录一条完整诊断日志。
- 日志内容：包含失败阶段、流程名称、AI节点、AI配置路径、方案路径、异常类型、内部异常和完整调用堆栈，日志关键词为“手动调参保存异常”。
- 安全处理：诊断日志写入异常会被隔离，不会覆盖原始保存异常，也不改变现有参数保存顺序和错误弹窗行为。
- 自动检查：新增 `Tests\ManualTuningSaveDiagnostics.Tests.ps1`，覆盖关键阶段、上下文和完整异常输出。
- 回归检查：手动调参保存诊断日志检查和多条件手动禁用行为检查均通过。
- 编译检查：Visual Studio 2022 MSBuild 的解决方案 `Debug|x64` 完整编译结果为 0 个错误、179 个项目既有警告。

## 2026-07-23：ROI结果绘制改为来源结果自动判定颜色

- 状态：已完成，未执行 Git 提交、切换分支或推送操作。
- 界面简化：实际“ROI结果绘制”删除独立的“颜色判定”订阅，只保留输入图像、OK颜色和NG颜色；控件删除和基础区重新排版均在 `NodeParamFormResultOverlayDraw.Designer.cs` 中完成。
- 参数简化：删除 `JudgeText1`、`JudgeText2` 和 `HasNewJudgeSubscription`，不再为颜色额外保存或建立运行等待依赖；旧方案JSON中可能存在的同名字段由反序列化兼容机制自动忽略。
- 自动判定：新版文本和自动ROI优先读取来源节点完整结果的 `IJudgmentResult.JudgeOk`，兼容 `JudgmentOk`、`DisplayOk`、`ConditionResult`、`IsAllOk`、`IsOk` 以及结果对象内部的 `AlgorithmResult.IsAllOk`。
- 判定优先级：多条件回写后的节点级 `JudgeOk` 优先于当前订阅属性；没有明确布尔判定字段时默认OK，普通数字0和文本不再被隐式转换成NG。
- 性能处理：自动判定读取器按来源结果实际类型缓存，连续运行时不重复扫描公开属性；判定开销统一计入对应文本或ROI绘制项耗时。
- 颜色行为：自动判定为OK时统一使用节点OK颜色，为NG时统一使用节点NG颜色；多个绘制项的总体状态按与逻辑累积。旧线、旧矩形、旧区域及其逐项颜色字段继续保留兼容运行。
- 红灯验证：修改后的专项检查先在旧参数仍含 `JudgeText1` 处按预期失败，确认测试能够捕获本次界面和参数冗余。
- 自动检查：实际ROI两类型简化、ROI几何适配、ROI结果绘制2兼容、订阅调用方契约、订阅输出分类、多条件手动禁用及位置修正订阅兼容共7组检查通过；运行态额外覆盖 `JudgeOk` 真/假、无判定默认OK、普通数字0默认OK和 `AlgorithmResult.IsAllOk=false`。
- 编译检查：Visual Studio 2022 MSBuild 的解决方案 `Debug|x64` 完整编译结果为0个错误、179个项目既有警告。

## 2026-07-23：ROI结果绘制文本自动展开多目标测量值

- 状态：已完成，未执行 Git 提交、切换分支或推送操作。
- 现场现象：模板匹配得到3个目标，卡尺找线节点也生成3个 `Items`，但ROI结果绘制订阅“长度”时只显示一个数值。
- 根因：卡尺结果中的 `Length`、`Angle` 等基础输出是为旧方案保留的第一目标摘要；原文本绘制逻辑直接读取摘要，没有继续关联多目标 `Items` 中的同名字段。显示控件本身支持多行，不是界面渲染丢失。
- 通用修复：文本项订阅多目标节点的摘要字段时，自动识别带 `MultiTarget` 声明的 `Items`，按模板目标顺序读取单目标结果中的同名属性并合并为一行方括号数组，例如 `[128.737,0,126.512]`；用户仍选择原来的“长度、半径、直径、角度、距离、坐标”等结果，无需改成高级结构订阅。
- 失败占位：测量工具为失败目标生成的数值0会保留在数组原位置，不会删除该值或导致后续目标顺序前移。
- 兼容规则：来源不是多目标结果、所选字段在单目标结果中不存在或Items为空时继续采用原有单值文本逻辑；手动文本和算法结果文本不受影响。
- 性能处理：多目标集合属性和单目标同名值属性按“来源结果类型+订阅结果名称”缓存，连续运行时不重复反射扫描。
- 红绿验证：第一轮专项运行态测试先因缺少 `BuildTextValueLines` 稳定失败；第二轮格式测试先因仍返回3行“目标N”文本稳定失败，修改后验证3个卡尺目标合并成单行 `[128.737,0,126.512]`，失败目标值保持为0。
- 回归检查：实际ROI结果绘制、共享几何适配器、ROI结果绘制2兼容、多目标卡尺、多目标几何测量、多目标找点、多目标位置修正、多目标测量运行器、多目标仿射变换及位置修正订阅兼容共10组检查通过。
- 编译检查：Visual Studio 2022 MSBuild 的解决方案 `Debug|x64` 完整编译结果为0个错误、179个项目既有警告。

## 2026-07-23：设备许可证启动自动同步

- 唯一来源：主程序每次取得单实例运行权后，使用程序运行目录根部的 `device.license` 作为唯一许可证来源；根目录文件继续直接供 CPU YOLO 使用。
- 自动目标：同步到已部署的 `LargeModelDll`、`UnsupervisedDll`、`YoloGPUDll`、`YoloGPUDll\1050tidll` 和 `YoloGPUDll\750dll`，不创建缺少的运行库目录，也不把三个大型运行库目录绑定到项目生成内容。
- 性能与安全：目标许可证缺失或内容不同时自动同步；长度和逐字节内容完全一致时跳过磁盘写入。更新时先写同目录唯一临时文件，再通过 `File.Replace` 或 `File.Move` 原子切换，失败时清理临时文件并保留旧许可证。
- 异常策略：根目录许可证缺失、目标目录未部署、单目标复制失败或同步器未预期异常均只写简体中文日志，不弹窗、不退出程序、不影响其它目标及后续主程序启动。
- 接口设计：新增 `IDeviceLicenseSynchronizer`、结构化同步结果和默认 `DeviceLicenseSynchronizer` 文件实现，`Program` 只负责启动调用和日志分级，便于后续扩展新的 AI 运行目录。
- 红绿验证：专项测试先因缺少同步器源码失败，再因启动链和项目编译项未接入失败；代码审查后新增原子替换红灯，确认旧实现会被测试拦截。最终覆盖新增、原子覆盖、覆盖失败保留旧文件、同内容跳过、目录缺失跳过、单目标失败隔离、源许可证缺失和启动顺序，全部通过。
- 真实目录验证：编译后的同步器在 `bin\x64\Debug` 中确认大模型、无监督、YOLO GPU 根目录、1050Ti目录和750目录的许可证与根目录 SHA-256 完全一致。
- 回归与编译：设备许可证专项测试、程序单实例启动测试和运行库模板测试全部通过；Visual Studio 2022 MSBuild 的解决方案 `Debug|x64` 完整编译结果为0个错误、179个项目既有警告。

## 2026-07-24：点到线距离按目标编号配对多目标订阅

- 状态：已完成，按用户要求未执行 Git 提交、切换分支或推送操作。
- 需求：三个找点结果和三条找线结果均来自同一个位置修正链路时，点到线距离必须按 `TargetIndex` 一一对应，并输出三个距离结果。
- 失败规则：任一目标的点或直线失败、缺失时保留该目标位置，距离及相关数值输出0，该项和节点总体状态为NG，后续目标不得前移。
- 兼容与性能：单结果订阅保持原行为；多目标只读取一次上游结果并串行计算，不增加并发调度。
- 设计文档：`docs\superpowers\specs\2026-07-24-point-line-multi-target-pairing-design.md`。
- 实施计划：`docs\superpowers\plans\2026-07-24-point-line-multi-target-pairing.md`。
- 根因：点、线节点虽然都输出带 `TargetIndex` 的多目标 `Items`，但点到线距离订阅模式仍通过旧摘要读取器只取第一个点和第一条线，并固定创建一个恒等目标，多目标身份在消费端丢失。
- 通用配对：新增 `MultiTargetMeasurementPairer`，按目标编号升序配对，拒绝重复或无效编号；两侧编号集合不一致时保留编号并为缺失侧创建NG占位。
- 订阅执行：点到线距离一次读取点、线节点完整结果；存在多目标明细时逐项读取几何值并串行计算，不再为每个目标重复读取上游节点；普通单结果节点继续按目标1摘要兼容运行。
- 状态规则：目标点或直线失败、缺失、无有效几何时，该目标距离和坐标保持0、状态NG；其它目标继续执行，节点总体状态按全部目标与逻辑汇总。
- 红绿验证：专项测试先因缺少通用配对器失败，加入配对器后又因点到线节点未读取多目标明细失败；完成接入后覆盖乱序输入、三组一一配对、中间目标失败、编号缺失和重复编号拒绝，测试通过。
- 回归与编译：点到线多目标专项以及多目标几何、找点、卡尺、公共执行器、位置修正、仿射变换和订阅兼容共8组检查通过；完整解决方案 `Debug|x64` 编译为0个错误、179个项目既有警告。

## 2026-07-27：点到线距离支持公共单结果广播

- 状态：已完成，未执行Git提交、切换分支或推送操作。
- 现场复现：流程4模板匹配和找点输出8个目标，卡尺找线未启用位置修正并输出1条公共直线；原严格编号配对只让目标1成功，目标2～8因缺少同编号直线输出0/NG。
- 修正规则：`N+1` 或 `1+N` 时把唯一结果及其成功/失败状态广播到另一侧全部目标；`N+N` 继续按 `TargetIndex` 配对；双方数量都大于1且编号缺失时继续输出0/NG。
- 通用实现：广播规则下沉到 `MultiTargetMeasurementPairer`，点到线距离节点不增加流程4专用判断；广播后的目标编号取多目标一侧，几何值、错误信息和位置修正上下文继续沿用唯一结果。
- 状态规则：公共单结果成功时逐目标参与测量；公共单结果失败时所有对应目标保留原错误并判定NG，避免失败结果被误当成有效公共几何。
- 红绿验证：新增8个点加1条线、1个点加8条线和失败公共线三组行为测试；修复前专项测试稳定在“公共线必须广播到目标8”处失败，修复后通过。
- 回归与编译：点到线多目标专项以及多目标几何、找点、卡尺、公共执行器、位置修正、仿射变换和订阅兼容共8组检查通过；完整解决方案 `Debug|x64` 编译为0个错误、179个项目既有警告。
- 完成前复验：8组检查再次全部返回退出码0；随后重新执行解决方案 `Debug|x64` 构建，增量构建成功并报告0个错误、0个警告。
- 测试脚本兼容：为本批新增及受影响的中文PowerShell回归脚本补充UTF-8 BOM，确保Windows PowerShell 5.1按UTF-8解析中文断言并保留`$PSScriptRoot`文件执行语义。

## 2026-07-27：复合测量工具统一多目标订阅配对

- 状态：已完成，未执行Git提交、切换分支或推送操作。
- 统一范围：线到线夹角、点到点距离、点到区域距离的订阅模式已统一为点到线距离现有方式；绘制ROI与位置修正路径保持不变，未修改任何WinForms控件或Designer文件。
- 配对规则：双方多目标时按`TargetIndex`一一对应；`N+1`和`1+N`时由`MultiTargetMeasurementPairer`广播公共单结果；双方多目标但编号缺失、单项NG或几何读取失败时保留对应编号并输出数值0/NG，后续目标不前移。
- 线到线：一次读取两侧全部目标直线及各自边缘点，逐目标计算夹角和线距统计；单结果上游继续读取摘要并作为公共基准兼容运行。
- 点到点：一次读取两侧全部目标点，逐目标计算距离；点角色选择仍分别沿用点1、点2原参数。
- 点到区域：一次读取全部目标点和区域轮廓，逐目标计算最小/最大距离及区域内判定；单个公共区域可广播到全部目标点。
- 目标身份：三个工具优先沿用当前编号上游结果携带的位置修正上下文；广播来源上下文编号不一致时改用多目标一侧上下文，均缺失时创建同编号恒等修正，保证输出`Items`的目标编号稳定。
- 公共读取：`MeasurementResultReader`新增从节点完整结果或单个多目标明细读取直线边缘点、区域轮廓的`object`入口，原单结果入口继续转发兼容。
- 设计文档：`docs\superpowers\specs\2026-07-27-composite-measurement-multi-target-subscription-design.md`。
- 实施计划：`docs\superpowers\plans\2026-07-27-composite-measurement-multi-target-subscription.md`。
- 红绿验证：新专项检查先在公共读取器缺少单目标明细入口处稳定失败；完成接入后，三个工具的多目标读取、统一配对和订阅执行检查通过。
- 回归检查：复合测量多目标订阅、点到线多目标订阅、复合测量采样与串行、多目标几何测量、公共测量执行器、多目标找点、多目标卡尺、位置修正订阅兼容及多目标仿射变换共9组检查全部通过。
- 编译检查：Visual Studio 2022 MSBuild完整编译解决方案`Debug|x64`，结果为0个错误、179个项目既有警告。

## 2026-07-30：模板匹配替换为新版 MatchTool 算法

- 状态：进行中，已按要求在修改前创建备份分支`codex/backup-before-template-match-new-algorithm-20260730-095839`；未切换当前工作分支，避免影响现有未提交测量节点改动。
- native替换：`Native\MatchTool.dll`已由`Fastest_Image_Pattern_Matching\build-tests\Release\MatchTool.dll`替换，新DLL导出`CreateMatchEngine`、`ReleaseMatchEngine`、`LearnPatternMem`、`MatchMem`、`GetMatchApiVersion`、`GetMatchStatusMessage`、`SavePatternFile`和`LoadPatternFile`共8个核心接口。
- 托管封装：`FastTemplateMatcher`启动时校验模板匹配native API版本不低于`0x00010001`；`LearnPatternMem`和`MatchMem`返回负数错误码时不再转成0个结果，而是通过`GetMatchStatusMessage`生成明确异常，方便低配置电脑现场定位卡死或native失败原因。
- 扩展接口：C#侧补齐`SavePatternFile`和`LoadPatternFile`封装，为后续把已学习模板金字塔与统计量落盘、减少冷启动学习耗时预留入口。
- 参数界面：运行参数页删除旧的“匹配极性”和“尺度模式”控件；保留并开放新版算法实际生效的最小匹配分数、最大匹配个数、角度范围、角度步长、最大重叠率和极速粗匹配；角度范围输入控件从`textBoxScale`更名为`textBoxToleranceAngle`，避免与后续尺度搜索语义混淆。
- 红绿验证：新增`Tests\MatchTemplateNativeApi.Tests.ps1`先在旧DLL缺少`GetMatchApiVersion`时稳定失败，替换新DLL后通过；新增`Tests\MatchTemplateManagedWrapper.Tests.ps1`保护C#封装和Designer参数收口，当前通过。

## 2026-07-30：模板匹配多目标排序输出供四则运算订阅

- 状态：已完成，按用户要求仅修改框架代码和测试，未提交临时检查目录。
- 需求：模板匹配多个目标时只提供两种排序输出方式，列排序输出按中心X从小到大，行排序输出按中心Y从小到大；四则运算工具可直接订阅排序后的目标中心坐标参与计算。
- 参数界面：模板匹配运行参数页新增`多目标排序方式`下拉框，选项为`列排序输出`和`行排序输出`；控件已写入`NodeParamFormMatchTemplate.Designer.cs`，可在WinForms设计器中看到。
- 输出规则：模板匹配构建结果前先按所选方式排序，排序后的顺序重新生成`TargetIndex`；`匹配位姿列表`、`匹配点X/Y`第一目标摘要、结果文字和绘制矩形均使用同一套排序顺序。
- 动态变量：模板匹配节点实现动态结果变量，四则运算配置阶段按`最大匹配个数`预先看到`变量.目标1中心X`、`变量.目标1中心Y`等坐标；运行阶段按当前排序后的`Poses`读取实际值。
- 兼容规则：旧方案未保存排序方式时默认使用列排序输出；目标不存在或目标无效时对应动态变量读取失败，不把空目标当成0参与计算。
- 红绿验证：新增`Tests\MatchTemplateSortedDynamicOutputs.Tests.ps1`，先因缺少排序参数和动态输出稳定失败；完成实现后通过，并回归模板匹配封装、native导出、动态输出刷新和订阅端口目录检查。

## 2026-07-31：模板匹配执行闪退专用诊断日志

- 状态：已完成，按用户要求先加日志用于客户现场逐步排除，未提交临时检查目录。
- 现场现象：客户电脑可以创建模板，但点击执行模板匹配后软件直接闪退，普通异常弹窗和异步UI日志可能来不及落盘。
- 诊断文件：新增模板匹配专用硬日志，直接写入程序目录`Logs\yyyyMMdd_模板匹配诊断.log`；每行包含时间、进程ID、线程ID和当前执行阶段，避免native崩溃时丢失最后定位点。
- 窗体链路：手动执行按钮、同步/异步匹配入口、参数读取、模板/源图确认、搜索区域裁剪、上下文创建、native会话返回、非整图偏移、预览图生成和结果显示均写入阶段日志。
- native链路：`FastMatchNative`初始化、`GetMatchApiVersion`、`CreateMatchEngine`、`LearnPatternMem`、`MatchMem`调用前后均写入日志，并记录源图/模板尺寸、像素格式、stride、匹配阈值、角度、重叠率、粗匹配和最大匹配个数。
- 排查方式：客户复现闪退后只需提供当天`Logs\yyyyMMdd_模板匹配诊断.log`末尾内容；如果最后停在`准备调用MatchMem`后无返回，基本可定位为native DLL匹配过程崩溃或目标电脑native运行环境问题。
- 验证：新增`Tests\MatchTemplateCrashDiagnostics.Tests.ps1`保护关键诊断点；模板匹配诊断、排序输出和托管封装检查通过；解决方案`Debug|x64`编译通过，结果为0个错误、179个既有警告。

## 2026-07-31：模板匹配AVX2低配电脑闪退保护

- 状态：已完成，已准备提交本次改动记录；第三方算法源码和临时构建目录不纳入提交。
- 现场定位：客户日志最后停在`FastMatchEngineHandle.Learn：准备调用LearnPatternMem`，没有`LearnPatternMem返回`；结合算法工程`CMakeLists.txt`和构建元数据确认当前`MatchTool.dll`由`/arch:AVX2`编译，低配电脑CPU不支持AVX2时会在native内部触发不可捕获的进程级闪退。
- 构建尝试：已在项目临时目录复制第三方算法源码并移除`/arch:AVX2`尝试重新生成通用CPU版，但本机缺少原构建依赖`D:\SoftWare\opencv\build`中的OpenCV开发配置和C++头文件，当前环境无法直接产出通用DLL；临时目录仅用于验证，不提交。
- 框架保护：`FastMatchNative`初始化阶段新增CPU指令集检测，当前随包runtime标记为AVX2且电脑不支持AVX2时，在调用`GetMatchApiVersion`、`CreateMatchEngine`、`LearnPatternMem`前抛出明确异常并写诊断日志，避免点击执行后整个软件被native非法指令打掉。
- 发布标记：新增`Native\MatchTool.runtime.txt`说明当前DLL的`instruction_set=AVX2`和`requires_avx2=true`，并加入项目生成内容随`Native\MatchTool.dll`一起复制；后续拿到通用CPU版DLL时，将该文件改为`instruction_set=x64-generic`或`requires_avx2=false`即可取消AVX2拦截。
- 验证：新增`Tests\MatchTemplateCpuCompatibility.Tests.ps1`保护CPU检测、诊断字段、runtime标记和项目复制规则；同时回归模板匹配诊断、多目标排序输出、托管封装和native导出检查；解决方案`Debug|x64`完整编译通过，结果为0个错误、179个既有警告。

## 2026-07-31：模板匹配native初始化异常显示修复

- 状态：已完成，针对点击执行后只显示`FastMatchNative`类型初始化异常的问题做界面提示修复。
- 根因：模板匹配执行异常被异步执行、同步包装和`.NET`类型初始化异常连续包了多层，原界面只取一层`InnerException`，导致真正的AVX2兼容提示或native加载失败原因被隐藏。
- 修复：`GetDisplayExceptionMessage`改为循环展开到最内层异常，手动执行和连续执行都显示真实原因，方便客户现场按提示替换通用CPU版DLL或检查运行库。
- 验证：模板匹配诊断测试新增嵌套异常展开检查。

## 2026-07-31：模板匹配非AVX2电脑自动CPU后备

- 状态：已完成，针对客户电脑提示“请更新通用CPU版MatchTool.dll”的问题改为软件自动后备，不再要求客户手动换DLL。
- 根因：当前随包`MatchTool.dll`确认为AVX2编译版本，本机又缺少原生工程依赖的OpenCV C++开发包，暂时无法直接产出通用CPU版DLL；只拦截并提示会影响客户交付体验。
- 修复：模板匹配执行前读取`MatchTool.runtime.txt`并检测CPU特性；当DLL要求AVX2且本机不支持AVX2时，不初始化`FastMatchNative`，自动切换到OpenCvSharp CPU后备模板匹配。
- 后备算法：使用灰度图、旋转模板、`Cv2.MatchTemplate`、响应峰值抑制和结果重叠率去重生成同结构`FastTemplateMatchResult`，继续支持最大结果数、最小分数、角度范围、角度步长、粗匹配步长、多目标输出和轮廓绘制。
- 性能说明：支持AVX2的电脑仍走高速native；非AVX2电脑走CPU后备，速度会低于native AVX2，但不会闪退，也不需要客户手动替换DLL。
- 验证：CPU兼容测试新增自动后备、OpenCvSharp匹配和runtime说明检查。

## 2026-07-31：模板匹配通用CPU版native DLL

- 状态：已完成，已按用户要求补齐OpenCV C++开发包并重新编译通用CPU版`MatchTool.dll`。
- 依赖补齐：下载并解压OpenCV 4.10.0 Windows C++开发包到项目临时依赖目录`_codex_deps`，仅用于本机编译，不纳入提交。
- 编译方式：复制第三方算法源码到临时目录，移除`/arch:AVX2`，保留`/O2`、`/fp:fast`和OpenMP，使用`BUILD_TESTING=OFF`避开第三方测试工程校验，只编译Release版`MatchTool`目标。
- native替换：`Native\MatchTool.dll`已替换为`x64-generic`通用CPU版本，同时同步官方OpenCV 4.10.0运行库`Native\opencv_world4100.dll`，保证运行时和import lib来源一致。
- runtime标记：`Native\MatchTool.runtime.txt`改为`instruction_set=x64-generic`、`requires_avx2=false`；低配电脑将重新走native高速匹配，不再进入慢速OpenCvSharp后备路径。
- 预期效果：低配电脑不再闪退，也不应再出现14秒后备耗时；速度会低于AVX2专用DLL，但仍使用新版native金字塔、粗匹配、OpenMP和模板学习流程。
- 验证：native导出、CPU兼容、托管封装、崩溃诊断专项测试通过；解决方案`Debug|x64`完整编译通过，结果为0个错误、0个警告；`bin\x64\Debug\Native`中的`opencv_world4100.dll`与`Native`源目录SHA-256一致。

## 2026-08-04：2222方案流程画布数据适配

- 状态：已完成，已将桌面`2222.Sol`转换为当前流程画布框架可直接识别的图结构数据。
- 数据适配：为每个流程节点补齐`HasCanvasLayout`、画布坐标、节点尺寸、起始节点和`OutputLog`配置，并将每个流程标记为`HasCanvasGraph=true`。
- 竖向连线：按`123.Sol`的竖向风格生成`ConnectionInfos`，所有连线锚点均为`Bottom -> Top`，避免旧方案加载时使用`Legacy -> Left`横向连线。
- 条件分支：保留`If/Else/EndIf`旧逻辑节点，生成显式`True`和`False`分支后在`EndIf`汇合，兼顾画布展示和流程运行语义。
- 文件处理：已在桌面生成原文件备份`2222.Sol.codex-bak-20260804`，并用适配后的数据覆盖`2222.Sol`。
- 验证：重新读取桌面`2222.Sol`确认6个流程均为图结构，节点布局异常为0，连线锚点异常为0。

## 2026-08-04：方案判断节点改为多条件判定

- 状态：已完成，针对桌面当前方案`3333.Sol`重新整理流程图结构。
- 节点替换：移除流程中的`If`、`Else`、`EndIf`旧判断节点，将原`If`节点改为`MultiCondition`多条件判断节点，条件统一引用对应`AI检测`节点的`AlgorithmResult.IsAllOk`。
- ROI绘制：将原`DrawAIResult`节点转换为当前通用的`ResultOverlayDraw`，节点名称改为`ROI结果绘制`，绘制项订阅对应AI输出结果。
- 通用左侧逻辑：多条件判断节点左侧默认出口连接`ROI结果绘制 -> 图像显示 -> 保存图片`，保存图片不存在的流程保持现有节点集合不额外新增。
- OK/NG分支：按原流程中的Modbus写入地址识别OK/NG发送节点，分别连接到多条件判断节点的`True`和`False`分支；原有延迟复位Modbus节点接在OK/NG发送后。
- 文件处理：已在桌面生成备份`3333.Sol.codex-bak-multicond-20260804`，并覆盖更新`3333.Sol`。
- 验证：重新读取`3333.Sol`确认6个流程旧判断类型数量为0，每个流程均有1个多条件判断、1个ROI结果绘制、1条左侧默认逻辑、1条True分支和1条False分支。

## 2026-08-04：ROI结果绘制追加AI结果文本订阅

- 状态：已完成，针对桌面方案`3333.Sol`补齐ROI结果绘制的文本显示项。
- 参数调整：每个`ROI结果绘制`节点保留原有`Roi`绘制项，并追加一个`Text`绘制项，统一订阅对应`AI检测 / AI输出结果`。
- 文件处理：已在桌面生成备份`3333.Sol.codex-bak-roi-text-20260804`，并覆盖更新`3333.Sol`。
- 验证：重新读取`3333.Sol`确认6个流程的`ROI结果绘制`节点均包含1个ROI项和1个AI结果文本订阅项。

## 2026-08-04：多条件节点逐目标判断多目标测量结果

- 状态：已完成，按用户要求修复多条件节点订阅多目标测量结果时只读摘要值的问题。
- 判断规则：多条件节点读取到上游测量结果的`Items`多目标明细后，优先按当前条件字段逐个目标读取并判断；全部目标都通过时该条件才通过，任一目标NG或字段不满足条件则整体不通过。
- 兼容回退：如果条件字段不存在于多目标明细项中，继续按原有单值属性或动态变量逻辑读取，兼容旧方案和非多目标节点。
- 诊断信息：多目标判断会输出“目标N=实际值(通过/不通过)”摘要，并记录失败目标原因，便于运行后直接定位是哪一个目标NG。
- 验证：新增`Tests\MultiConditionMultiTargetEvaluation.Tests.ps1`保护逐目标判断入口、全部目标通过规则和失败目标拦截；专项测试和多条件禁用回归测试通过；解决方案编译通过，结果为0个错误、179个既有警告。

## 2026-08-04：ROI结果绘制文本项下拉框显示AI输出结果

- 状态：已完成，针对`ROI结果绘制`文本项选择`AI检测`节点后，`节点结果`下拉框不显示`AI输出结果`的问题进行修复。
- 根因：`AI输出结果`属于`AlgorithmResult`高级结果；ROI项只接受复杂视觉结果时会自动显示高级结果，但文本项同时接受布尔、数字、文本和`AlgorithmResult`，旧筛选逻辑因为包含基础类型而没有自动展开`AlgorithmResult`。
- 修复：订阅端口目录改为当输入契约明确接受某个复杂类别时，自动显示该类别的高级输出；通用全类型订阅仍不自动铺开全部高级结果。
- 验证：已扩展`Tests\SubscriptionPortCatalog.Tests.ps1`，检查ROI结果绘制文本项这种混合契约也能看到AI输出结果。

## 2026-08-04：ROI结果绘制保留AI文本自身颜色

- 状态：已完成，针对ROI结果绘制订阅`AI检测 / AI输出结果`后文本全部变红的问题进行修复。
- 根因：文本绘制分支把来源节点整体OK/NG作为所有文本行的颜色来源，导致AI结果整体NG时，原本每个检测项自带的红绿判定颜色被统一覆盖成NG颜色。
- 修复：文本绘制项读取到`AlgorithmResult`时，优先复制AI结果中每条`ColorText`的原始颜色；没有文本时按`DetectResults`中每个检测项自己的`IsOk`生成红绿文本，不再让多条件判定影响单条AI文本颜色。
- 验证：新增`Tests\ResultOverlayDrawAiTextColor.Tests.ps1`保护AI文本颜色保留逻辑，避免后续回退到整体OK/NG统一上色。

## 2026-08-04：模板匹配无结果时走NG分支

- 状态：已完成，针对`5月28初次方案.Sol`中模板匹配未匹配到结果后没有按现场预期发送`223`分支的问题进行修复。
- 日志定位：模板匹配节点输出`IsOk=false`但节点本身运行成功；后续`位置修正`因为模板位姿列表为空抛异常，使侧面子流程标记失败，且主流程`229.多条件判断=False`时原方案连到了`226`分支。
- 代码修复：位置修正读取到空模板位姿列表时改为输出空的NG结果，发布`TargetCount=0`、`IsOk=false`、`JudgeOk=false`，不再把正常的“未匹配到模板”当异常中断流程。
- 方案修复：已备份桌面方案为`5月28初次方案.Sol.codex-bak-modbus-branch-20260804`，并将`229.多条件判断`的`False`分支改接`223.Modbus写入_复制_2`，`True`分支改接`226.Modbus写入_复制_复制`。
- 验证：新增`Tests\PositionCorrectionEmptyTemplateIsNg.Tests.ps1`保护空模板位姿输出NG不中断；同时回归多目标位置修正和多条件多目标判断检查。

## 2026-08-04：5月28初次方案数据适配

- 状态：已完成，已将桌面`5月28初次方案.Sol`中的旧ROI几何绘制项转换为当前程序的自动ROI数据。
- 数据检查：方案包含4个流程、96个节点、105条连线，已具备`HasCanvasGraph`和画布布局，未发现`If/Else/EndIf/DrawAIResult`旧节点类型。
- 参数适配：将`ROI结果绘制`节点中的4个旧`Line/Rectangle`绘制项改为`Roi`项，保留订阅源、线宽和原OK/NG颜色语义，并为ROI节点显式补齐`OkColorArgb/NgColorArgb`。
- 文件处理：已在桌面生成备份`5月28初次方案.Sol.codex-bak-latest-20260804`，并覆盖更新原方案文件。
- 验证：重新读取桌面方案确认旧ROI几何项数量为0，缺失ROI颜色字段数量为0，旧节点类型数量为0，4个流程均为图结构。

## 2026-08-05：AI解析未检出检测项按0参与判定

- 状态：已完成，按用户反馈修复端子AI解析中Yolo未检出对应检测项时不显示0、且0到0不能判OK的问题。
- 公共判定：`ParseCommon`新增检测项实测值上下限判断方法，统一支持实测值0按配置下限/上限判断，避免各解析文件硬编码OK/NG。
- 解析修复：`RL12Parse`、`RL12angondingParse`、`XMSGParse`和`ClosingTerminals_encrypted_Parse`的数量型检测项改为把检出数量直接显示到AI文本中，未检出时显示0，设置值`0~0`时判OK。
- 缺失兜底：非数量型检测项完全未检出时写入当前值0、显示0和设定值，并按上下限判断颜色与总结果；`XMSGParse`同步修复。
- 兼容修正：`RL12angondingParse`中“少于2个结果缺失”的提前跳过逻辑不再吞掉完全未检出的0值兜底；加密端子解析中咬胶长度缺依赖框不再提前退出整个解析。

## 2026-08-06：多条件遇到失败上游强制NG

- 状态：已完成，针对现场日志中`卡尺找线`失败后`多条件判断=True`且共享变量继续写入OK的问题进行代码修复。
- 根因：多条件节点属于流程容错节点，前面测量节点失败后仍会继续执行；若多条件配置只订阅AI结果，失败的测量节点不会参与最终布尔判断，导致共享变量订阅到True。
- 修复：多条件执行前扫描当前图上游节点，本轮存在启用且状态为失败的上游节点时，直接输出`False`，并在条件明细和诊断信息中记录失败节点。
- 验证：新增`Tests\MultiConditionFailedUpstream.Tests.ps1`保护失败上游强制NG逻辑；多条件失败上游、手动禁用、多目标判断3组检查通过，解决方案`Debug|x64`编译通过，结果为0个错误、175个既有警告。

## 2026-08-06：5月28初次方案损坏数据恢复

- 状态：已完成，针对桌面`5月28初次方案.Sol`关闭软件后数据丢失的问题进行方案文件恢复。
- 损坏定位：当前方案从1MB边界开始后续内容全部变为空字节，断点位于第4个流程的模板图像数据字符串内部，导致原文件不是合法JSON，软件无法完整加载后半段配置。
- 恢复策略：保留当前文件中可完整解析的第1、2、3个流程数据，包含最新运行计数和第3个流程新增的1个节点、2条连线；第4个流程使用`5月28初次方案_备份.Sol`中的完整数据补齐。
- 文件处理：已将损坏原件备份为桌面`5月28初次方案.Sol.codex-corrupt-20260806`，并将恢复后的数据写回桌面`5月28初次方案.Sol`。
- 验证：恢复后的方案可重新解析为合法JSON，非法控制字节数量为0；方案包含4个流程、97个节点、107条连线，节点参数空值数量为0，`NodeCount`保留为244避免后续新增节点ID倒退。

## 2026-08-06：5月28初次方案第4流程断层修复

- 状态：已完成，针对恢复后第4流程使用旧备份导致测量节点链路断层的问题进行二次修复。
- 影响定位：`5月28初次方案_备份.Sol`中的第4流程仍是旧结构，只有`162.卡尺找线_复制 -> 184.四则运算_2 -> 167.多条件判断`链路，且多条件实际订阅`161.模版匹配_2 / 角度`，没有订阅四则运算结果；损坏原件只完整保存到第4流程的`157.图像裁剪_2`，后续现场新增节点无法从损坏文件中恢复。
- 完整来源：桌面`8_6方案.Sol`可完整解析，且第4流程已包含`239.卡尺找线`和`240.线到线夹角`，`167.多条件判断`第一条条件订阅`240.线到线夹角 / 夹角`。
- 文件处理：已将修复前方案备份为桌面`5月28初次方案.Sol.codex-before-p4-merge-20260806`，并把`8_6方案.Sol`中的完整第4流程合并回桌面`5月28初次方案.Sol`，保留当前第1、2、3流程数据和`NodeCount=244`。
- 验证：合并后方案可重新解析为合法JSON，非法控制字节数量为0；方案包含4个流程、98个节点、109条连线，节点参数空值数量为0，第4流程包含15个节点、16条连线，链路为`163 -> 162`、`163 -> 239`、`162/239 -> 240 -> 167`。

## 2026-08-07：方案文件安全保存保护

- 状态：已完成，按要求修复方案保存直接覆盖正式`.Sol`文件的问题，降低模板图像等大字段保存中断导致原方案损坏的风险。
- 保存流程：`SolSave`和关闭时的`SolNumberSave`统一改为先写同目录临时文件，写入时强制刷新到磁盘；临时文件能够完整读取、反序列化并通过流程连线端点校验后，才替换正式方案。
- 替换保护：已有正式方案时先把旧方案备份到程序运行的`bin`目录，备份文件名仍使用`.bak-时间戳`；写入新备份前会删除同一方案旧备份，确保只保留最新一个，不再持续递增堆积；正式方案随后由已校验临时文件通过`File.Replace`替换，首次保存时从临时文件移动为正式方案，不再对正式方案执行`File.WriteAllText`直接覆盖。
- 异常日志：临时写入、空字节检测、JSON反序列化、流程断线校验或替换失败时，都会写入`MsgLevel.Exception`日志，日志中包含正式方案路径、临时文件路径和失败原因；同一次异常已做去重，避免重复刷日志。
- 并发保护：新增方案保存锁，避免手动保存、自动保存和关闭保存同时替换同一个方案文件。
- 验证：新增并更新`Tests\SafeSolutionSave.Tests.ps1`保护安全保存通道、空字节检测、`bin`目录单份备份和失败日志；专项测试通过，解决方案`Debug|x64`编译通过，结果为0个错误、175个既有警告。

## 2026-08-07：模板匹配输出基准角度偏差

- 状态：已完成，按要求将模板匹配和NCC模板匹配的首目标角度输出明确为“角度偏差”。
- 输出含义：结果字段仍复用原有`Angle`属性，避免破坏旧方案订阅；界面显示名改为`角度偏差`，值表示当前匹配目标相对学习模板的旋转偏差。
- 规范化：角度偏差统一归一到`-180°~180°`，例如`270°`会输出为`-90°`，更适合直接做公差判断或写入PLC。
- 展示修正：结果叠加文本同步显示`角度偏差xx°`，避免和绝对角度混淆。

## 2026-08-07：翘曲度检测 Demo

- 状态：已完成，按要求制作独立本地网页 Demo，并准备放置到`D:\MyCode\PublicWook\翘曲度检测`。
- 检测流程：先创建端子模板，再按模板区域跟随定位，随后布置横向卡尺 ROI 阵列提取左右边缘。
- 计算方式：由左右边缘中心点生成实际中心线，使用上下稳定段拟合基准线，输出最大偏移量、检测长度和翘曲度百分比。
- 展示效果：图像上用红色绘制卡尺 ROI、中心线、基准线和最大偏移，便于现场判断检测结果是否准确。

## 2026-08-07：翘曲度检测 C# Demo

- 状态：已完成，按要求将翘曲度检测 Demo 改为 C# WinForms 项目。
- 界面：控件放置在`MainForm.Designer.cs`中，包含加载图片、创建模板、跟随检测、卡尺参数滑条和结果显示。
- 算法：使用模板区域跟随端子位置，横向卡尺 ROI 提取左右边缘，中心点拟合实际中心线，上下稳定段拟合基准线。
- 输出：在图像上用红色绘制卡尺 ROI、中心线、基准线、最大偏移，并输出翘曲量、检测长度、翘曲度和有效卡尺数量。

## 2026-08-07：Blob分析输出宽高并改为矩形检测区域

- 状态：已完成，按要求为Blob分析结果新增`宽度`、`高度`和`检测区域`订阅输出。
- 宽高结果：Blob运行后会基于最终二值输出区域计算整体外接矩形宽高，并在运行日志和叠加文本中同步显示面积、宽、高。
- 绘制区域：Blob检测区域不再通过四个轮廓点输出到显示结果，改为`ColorRotatedRect`旋转矩形；下游订阅算法结果或检测区域时会按矩形框显示。
- 兼容修复：旧版和新版ROI结果绘制的`区域`绘制项在订阅到只有矩形的算法结果时，会自动走矩形绘制通道，避免显示成四个点。
- 验证：解决方案`Debug|Any CPU`编译通过，结果为0个错误、175个既有警告。

## 2026-08-10：卡尺找线输出翘曲度差值

- 状态：已完成，按要求为卡尺找线新增`翘曲度差值`订阅输出。
- 计算方式：对成功拟合后的有效边缘点逐个计算到拟合线的垂直距离，取最大距离减最小距离，结果统一按测量输出规则保留三位小数。
- 输出范围：单目标明细和首目标汇总结果都保存翘曲度差值，预览叠加文本同步显示该数值，后续多条件判断、四则运算和通信节点可直接订阅。
- 验证：新增`Tests\CaliperLineWarpageDifference.Tests.ps1`保护字段、计算方式和预览文本。

## 2026-08-12：无监督 GPU_ORT 加载失败回退

- 状态：已完成，针对加载无监督模板时报`GPU_ORT`返回码`-2`的问题进行修复。
- 根因：模板记录的推理设备为`GPU_ORT`，native 在当前运行环境初始化 GPU ONNX 后端失败后返回`-2`；原运行器只允许`AUTO_*`失败时 CPU 回退，显式`GPU_ORT`失败会直接弹出“模型加载失败”。
- 修复：无监督运行器的 CPU 回退条件扩展为`GPU_*`和`AUTO_*`，GPU 后端加载失败时自动尝试 CPU 加载；若 CPU 也失败，弹窗会同时带出首选设备返回码和 CPU 回退返回码。
- 验证：更新`Tests\InferenceBackendDiagnostics.Tests.ps1`保护`GPU_ORT`加载失败可 CPU 回退的策略。

## 2026-08-13：启动期 AI native 环境加载顺序保护

- 状态：已完成，针对 YOLO/TDAI 与无监督、大模型 native 运行环境同进程加载顺序可能互相影响的问题做启动期隔离。
- 备份：改动前已创建 Git 备份分支`codex/backup-before-ai-runtime-isolation-20260812-2358`，如需回退可直接切换该分支。
- 根因：方案恢复节点参数时 TDAI 会在`SetParam2Form`中立即异步加载模型，而无监督和大模型的统一预加载在流程节点恢复完成之后才执行；当 TDAI 先加载同名 native 依赖时，无监督 GPU ONNX 后端可能初始化失败并回退到 CPU。
- 修复：新增`StartupAiRuntimeLoadGate`，在方案恢复期间暂存 TDAI 自动加载请求；`ConfigHelper.SolLoad`先恢复节点，再顺序预加载无监督和大模型，最后统一放行 TDAI 加载，避免 TDAI 抢先占用 DLL 搜索环境。
- 范围：仅延后方案打开时的 TDAI 自动加载；手动保存 TDAI 参数后的模型加载仍保持即时执行。
- 验证：更新`Tests\SolutionAiRuntimePreload.Tests.ps1`保护 TDAI 延迟加载闸门、工程编译项和“节点恢复 -> AI预加载 -> TDAI放行”的顺序。

## 2026-08-13：YOLO/TDAI native 进程级隔离

- 状态：已完成，按要求从根本隔离 YOLO/TDAI 与无监督、大模型的 native 依赖环境。
- 触发问题：无监督按顺序优先加载后已稳定使用`GPU_ORT`，但 YOLO 的`td_det.dll`随后加载失败，错误为`HRESULT:0x8007007F`；原因是`td_det.dll`依赖的`onnxruntime.dll`、`openvino.dll`与无监督目录存在同名不同版本，单进程无法同时保证两套 native 依赖各自命中正确版本。
- 修复：新增 YOLO 隔离 worker 模式，主程序启动同一个 exe 的隐藏子进程执行 YOLO 初始化和推理；worker 进程独立加载 YOLO 的`td_det.dll`、`yolo_openvino_tensorrt.dll`及其依赖，主程序继续保留无监督`GPU_ORT`和大模型`GPU_TENSORRT`运行环境。
- 接口：`Yolo8Det`、`Yolo8Obb`、`Yolo8Seg`、`Yolo8Pose`在主程序进程中统一走`YoloIsolatedWorkerClient`，worker 进程内才使用原本的本地 native 会话，避免递归创建worker。
- 通信：主程序通过标准输入/输出向worker发送结构化 JSON 请求，图像按连续 Mat 原始字节传输，推理结果再还原为原有`DetResult`、`ObbResult`、`SegResult`、`PoseResult`结构，不改上层解析逻辑。
- 验证：新增`Tests\YoloRuntimeIsolation.Tests.ps1`保护worker入口、工程编译项、四类YOLO模型隔离入口和结构化通信协议。

## 2026-08-13：YOLO隔离worker启动崩溃修复与实进程验证

- 状态：已完成，使用`D:\Resource\大模型 Yolo 无监督演示图像\1.Sol`复现并修复YOLO模型加载失败。
- 根因：Windows事件日志确认隔离worker在模型加载前因`Console.InputEncoding = Encoding.UTF8`抛出`System.IO.IOException`；主程序属于WinForms的WinExe子系统，重定向标准输入输出时没有可设置编码的控制台。
- 修复：worker改为直接用无BOM UTF-8的`StreamReader`和`StreamWriter`包装父进程传入的标准句柄，增加不依赖模型的`ping`握手；主进程在worker退出时补充退出码和标准错误，避免真实异常再次丢失。
- 验证：新增`Tests\YoloWorkerProcess.Integration.Tests.ps1`，实际启动正式WinExe worker完成`ping`往返，并使用方案中的`best.onnx.encrypted`和端子图片完成真实初始化与单帧推理，返回5个检测结果。
- 方案实测：正式`bin\x64\Debug`加载`1.Sol`后，无监督使用`GPU_ORT`、大模型使用`GPU_TENSORRT`、YOLO在隔离worker中加载完成；Windows应用程序日志没有新增worker崩溃事件。专项静态测试和`Debug|x64`编译均通过，编译结果0个错误。

## 2026-08-13：AI检测结果支持多条件判定订阅

- 状态：已完成，修复多条件判定节点看不到 AI检测结果的问题。
- 根因：多条件判定的来源树只接受单值的布尔、数值和文本端口；AI检测节点之前只公开完整`AlgorithmResult`复杂对象，界面会按订阅契约过滤掉，所以表现为无法订阅目标检测结果。
- 修复：AI检测结果新增`判定OK`、`检测项数量`、`检测明细数量`、`检测结果文本`等基础订阅输出；`AI输出结果`显式归类为算法结果，继续供绘制、汇总和通信发送节点使用。
- 判定回写：AI检测结果实现多条件判定回写接口，每次 AI 节点运行前重置回写状态，避免上一轮多条件结果影响下一轮检测。
- 验证：`Tests\SubscriptionConsumerContracts.Tests.ps1`、`Tests\SubscriptionOutputClassification.Tests.ps1`、`Tests\SubscriptionPortCatalog.Tests.ps1`均通过；解决方案`Debug|Any CPU`编译通过，结果为0个错误、175个既有警告。

## 2026-08-13：多端子解析分支改为中文

- 状态：已完成，按要求将`MultiTerminalParse.cs`中`switch (itemName)`下面的检测项判断改为中文名称，便于现场阅读和维护。
- 兼容方式：新增“中文检测项名 -> 内部键”映射，分支判断使用中文，结果字典、自动学习数据和检测项当前值仍使用内部键，避免破坏旧方案和语言切换兼容。
- 语言补齐：补充`DetectItem.FrontRivetCoreHeight`的中英文语言项，避免“前铆脚露线芯高度”显示时回退成内部键。
- 验证：新增`Tests\MultiTerminalParseChineseSwitch.Tests.ps1`保护多端子解析分支不再使用`DetectItem.*`英文内部键；语言JSON解析、订阅契约测试和解决方案`Debug|Any CPU`编译均通过，编译结果0个错误、175个既有警告。

## 2026-08-14：卡尺找线稳定性与拟合质量判定

- 状态：已完成，保持线到线夹角公式不变，修复夹角上游卡尺边缘点漂移、错误点集被接受和缺少质量判定的问题。
- 采样稳定：快速采样由单点最近邻读取改为卡尺宽度上三个代表点的双线性平均，抗干扰采样继续使用全宽度多点双线性平均；前者兼顾现有方案速度，后者适用于高反光现场。
- 鲁棒拟合：用确定性两点共识搜索替代会被异常点拉大的自适应中位残差门限，按内点数、覆盖跨度和平均残差选择最佳点集后再精修直线。
- 质量门控：新增可替换的`ICaliperLineQualityEvaluator`策略，默认检查有效点比例、平均/最大拟合残差、有效点覆盖率和拟合方向偏差；不达标时直接输出NG和简体中文原因，不再把错误直线交给夹角节点。
- 界面与订阅：在卡尺找线设计器中新增“拟合质量判定”参数组，控件全部位于`.Designer.cs`；节点输出增加候选点数、有效点比例、平均/最大残差、覆盖率、方向偏差和失败原因，NG时仍保留实际质量统计便于现场诊断。
- 原图回归：使用`1.Sol`第6流程的模板匹配、位置修正和左右两组卡尺实测两张BMP。右侧快速采样夹角为`0.448510°`与`0.548823°`，差值`0.100313°`，较修改前`0.152616°`降低约34%；完整抗干扰采样差值约`0.0900°`。左侧弯曲图以`12/15`内点通过，直图错误集仅`9/15`内点、有效点比例`60%`，已正确判为NG。
- 验证：新增`Tests\CaliperLineFitQuality.Tests.ps1`并更新采样模式、翘曲度、多目标卡尺和订阅输出契约测试，五项专项测试均通过；解决方案`Debug|Any CPU`编译通过，结果为0个错误、175个既有警告。

## 2026-08-14：卡尺找线新增亚像素与像素模式

- 状态：已完成，为卡尺找线增加“测量精度”下拉选项，提供“亚像素”和“像素”两种模式，默认选择“亚像素”。
- 算法行为：亚像素模式保持原有梯度峰值抛物线插值；像素模式关闭该插值，直接采用梯度峰值所在的整步采样位置，选项实际作用于边缘定位而不是只改变结果显示位数。
- 参数链路：`MeasureMode`已贯通节点参数、方案保存与加载、位置修正后的运行参数以及公共卡尺找线算法；旧方案未保存该字段时自动使用亚像素模式。
- 界面：新增的“测量精度”标签和下拉框均放置在`NodeParamFormCaliperLine.Designer.cs`中，可在WinForms设计器中查看和调整布局。
- 验证：新增`Tests\CaliperMeasureMode.Tests.ps1`，与采样模式、拟合质量、翘曲度和多目标卡尺共5项专项检查全部通过；运行时峰值测试得到亚像素位置`2.166667`、像素位置`2.000000`；解决方案`Debug|Any CPU`和现场实际使用的`Debug|x64`均编译通过，结果为0个错误、175个既有警告；x64成品已确认包含`comboBoxMeasureMode`下拉控件。

## 2026-08-16：桌面旧版方案数据新版化

- 状态：已完成，已将桌面`C:\Users\34652\Desktop\1.Sol`补齐为当前新版方案JSON数据。
- 备份：转换前原文件已备份为`C:\Users\34652\Desktop\1.Sol.codex-before-new-format-20260816075106`，便于需要时回退。
- 迁移：更新方案内`SolName`为当前桌面路径；为ROI结果绘制节点补齐顶层`OkColorArgb`、`NgColorArgb`；为多条件判断条件行补齐`Enabled=true`；保留原设备、流程、节点、模型路径、检测项配置和现场参数。
- 验证：新版方案JSON可重新解析；包含4个流程、32个节点、28条连线，断开连线数量为0；结果绘制顶层颜色缺失数量为0，多条件启用字段缺失数量为0；`NodeCount=38`保留高于最大节点ID 36，避免后续新增节点ID倒退。

## 2026-08-17：AI检测节点新增内部多ROI检测区域

- 状态：已完成，按要求在`ParamFormTDAI`中新增`启用ROI`开关和`绘制检测区域`链接。
- 界面：新增`ParamFormTDAIRoiEditor`绘制窗口，使用`ShowImageControl`显示当前订阅图像；用户可在图像上拖拽绘制多个矩形 ROI，点击目标框选中后按`Del`删除对应 ROI，也可一键清空全部 ROI。
- 图像刷新：点击`绘制检测区域`进入窗口前会先刷新上游并获取当前图像；绘制窗口新增`获取当前图像`按钮，可在不关闭窗口的情况下手动重新获取当前订阅图像，已绘制 ROI 保留在控件上。
- 参数：`NodeParamTDAI`新增`RoiEnable`和`RoiRegions`，保存多个 ROI 的中心点、宽高、角度和名称，旧方案未配置时默认不启用。
- 运行：AI 节点启用 ROI 后会先把每个 ROI 裁剪为独立图像再推理；DET、SEG 分支逐张 ROI 图像检测，并把 ROI 偏移叠加到检测结果坐标中，上游本身已有裁剪偏移时也会继续叠加。
- 验证：新增`Tests\TDAIRoiDetection.Tests.ps1`保护参数持久化、WinForms 设计器控件、绘制窗口提示、`ShowImageControl`拖拽/Del 删除和 AI 运行时多 ROI 裁剪链路。

## 2026-08-19：建立性能资源管理改造计划与Git基线

- 状态：计划书已建立，性能资源管理代码尚未开始修改。
- Git基线：已将当前正式源码、设计器、配置、测试和任务记录提交为`73462eb`，提交信息为“备份: 性能资源管理改造前当前源码基线”。
- 恢复分支：已建立`codex/backup-before-resource-management-20260819`，用于快速恢复改造前版本。
- 计划文件：新增根目录`改造性能计划书.md`，在最上方集中记录可配置关键参数、自动计算公式、允许范围、当前电脑预期值和未来是否开放到系统设置界面。
- 实施要求：后续每一步修改前先提交当前有效改动；每一步完成后更新计划书和任务记录，并执行编译、专项测试和四流程压力测试。
- 清理边界：临时独立工程、编译产物和`.codex-bak`二进制备份保持原样，未纳入源码基线，也未删除。

## 2026-08-19：完成性能改造前本机基准检查

- 状态：步骤1本机可执行基准已完成，尚未修改性能资源管理代码。
- 编译基准：解决方案`Debug|x64`编译通过，0个错误、175个既有警告，MSBuild报告耗时5.25秒。
- 回归基准：日志等级、Debug诊断等级、诊断惰性求值、相机回调快速链路、相机帧等待、统一相机回调、保存图像订阅、日志UI节流和方案资源释放共9项检查通过。
- 环境说明：日志UI测试在项目对应的Windows PowerShell/.NET Framework环境中通过；当前开发电脑未连接客户现场四相机和PLC，真实现场节拍将在硬件环境中复测，模拟数据不会冒充现场结果。

## 2026-08-19：性能改造步骤2第一轮公共诊断骨架

- 状态：第一轮已完成，步骤2继续进行相机、UI和保存后台链路关联。
- 有界诊断：`LogHelper`新增Debug性能诊断待写水位保护，容量4096条；拥堵时不阻塞检测线程，只累计被放弃的诊断数量。
- 流程关联：新增`ProcessPerformanceTrace`，仅在系统设置Debug开启时创建；每轮生成唯一`TraceId`并记录流程开始、节点开始、节点结果、节点异常和流程结束。
- 入口覆盖：统一挂接到`BeginProcessRun`、公共节点运行状态和`NodeBase.SetRunResult`，覆盖画布连接、顺序兼容、相机回调后续流程和调参图像刷新入口。
- 结束保护：正常、失败、取消和提前完成均关闭同一追踪；重复结束不会重复记录，流程结束会报告未闭合节点、待写日志和已丢诊断数量。
- 验证：新增`Tests\PerformanceRunTrace.Tests.ps1`；连同日志等级、诊断惰性求值、相机快速路径和连接图等待共6项专项检查通过；解决方案`Debug|x64`编译通过，0错误、175个既有警告。

## 2026-08-19：性能改造步骤2第二轮相机帧与流程关联

- 状态：第二轮已完成，步骤2下一轮继续处理UI投递、保存后台任务和旧诊断点的Debug开关管控。
- 帧关联：每台海康相机使用`Interlocked`生成单调递增`FrameId`，相机SDK回调、像素转换、内存复制、颜色转换和Mat分发共用同一帧号。
- 流程关联：图像源节点在回调帧交付、迟到帧丢弃、等待器就绪、等待恢复和输出构建阶段同时记录`FrameId`、`TraceId`与`RunId`，可以区分等待相机、图像转换和流程恢复分别消耗的时间。
- 资源边界：新增`CameraFrameTraceRegistry`弱引用关联表，不持有、不克隆、不释放Mat；多订阅者克隆在调用订阅者前分别挂接同一份只读元数据，原有图像所有权规则不变。
- 正式运行开销：原相机回调中的逐帧Info计时改为Debug惰性诊断；Debug关闭时不创建帧元数据、不启动Stopwatch对象、不拼接完整耗时文本。
- 自动检查：新增相机帧关联结构检查和弱引用行为压力测试；相机帧关联、统一回调、等待器、惰性诊断、日志等级、流程追踪与相机快速路径共8项专项检查全部通过。
- 压力检查：弱引用关联表连续执行50000次挂接和读取用时26ms，强制GC后Mat键可以回收。
- 编译检查：解决方案`Debug|x64`完整编译通过，MSBuild报告用时3.22秒，0个错误、175个项目既有警告。
- 环境限制：当前电脑未连接客户现场相机，本轮验证覆盖源码结构、等待器行为、关联表行为、压力和完整编译；真实四相机接近同时回调的耗时数据保留到现场复测。

## 2026-08-19：性能改造步骤2第三轮UI与保存后台诊断关联

- 状态：第三轮已完成，步骤2下一轮继续清理剩余旧式诊断计时并检查通信阶段关联。
- 跨线程上下文：新增只读`PerformanceTraceContext`，仅在系统Debug开启时创建，固定保存图像来源流程、`TraceId`、`RunId`和节点，避免后台任务执行时误读到下一轮流程编号。
- UI关联：图像显示事件把诊断上下文传到`FrmSingleImage`，分别记录显示节点投递、WinForms消息排队、`ShowImageControl.SetImage`和UI刷新完成耗时。
- 保存关联：保存任务携带创建时的诊断上下文，后台取图、Mat转Bitmap、标注绘制、路径准备、压缩写盘和Bitmap释放日志均可还原到原流程批次。
- Debug开销管控：新增按日志等级创建Stopwatch的统一入口；图像显示、显示控件、图像旋转、ROI结果绘制、保存节点和保存参数预览在Debug关闭时不分配诊断计时器。
- 内存现场管控：保存线程只有在Debug开启且任务带流程上下文时才采集进程内存、GC、GDI和句柄快照；正式运行不再无条件构建Mat、Bitmap和OutputImage摘要。
- 日志等级修正：图像旋转慢诊断由Info改为Debug惰性日志；UI与保存历史检索关键词继续保留，新增日志同时包含完整耗时关联信息。
- 自动检查：新增`Tests\PerformanceDiagnosticsUiSave.Tests.ps1`并扩展诊断等级检查；UI、保存、ROI、资源释放、相机与完整流程诊断共12项相关回归通过。
- 编译检查：解决方案`Debug|x64`最终完整编译通过，MSBuild报告用时3.25秒，0个错误、175个项目既有警告。
- 环境说明：涉及OpenCv和System.Drawing运行程序集的2项ROI测试使用项目对应的Windows PowerShell/.NET Framework环境通过，其余检查使用隔离的PowerShell进程执行。

## 2026-08-19：性能改造步骤2第四轮剩余Debug诊断开销清理

- 状态：第四轮已完成，步骤2下一轮只剩通信节点阶段关联检查。
- 流程快速路径：相机回调快速路径和跳过节点结果准备改为仅在Debug开启时创建诊断计时器；并行图运行时用于计算流程`RunTime`的业务总计时保持原样。
- 绘制节点：`ImageDraw`与`ResultOverlayDraw2`性能日志由Info直写改为Debug惰性日志，附带流程名、`TraceId`和`RunId`；Debug关闭时不创建构建明细对象和逐绘制项Stopwatch。
- 找点边界：节点总耗时、订阅读取、灰度准备、运行参数组装和结果发布等分段诊断只在Debug开启时采集；找点算法写入结果的`AlgorithmMs`业务计时继续保留，防止结果字段失真。
- 防漏检查：新增`Tests\RemainingPerformanceDiagnostics.Tests.ps1`并扩展诊断日志等级检查，明确保护业务计时与诊断计时边界。
- 自动验证：流程、相机、UI与保存、找点、绘制2参数兼容、实际ROI绘制、几何适配和资源释放共13项相关回归通过。
- 压力检查：通过编译产物关闭Debug后连续调用诊断计时入口100000次，全部返回空计时器，用时513ms；反射测试宿主强制GC后托管内存差值121904字节，该数值包含反射调用开销，不作为软件现场节拍基准。
- 编译检查：解决方案`Debug|x64`最终完整编译通过，MSBuild报告用时2.56秒，0个错误、175个项目既有警告。
- 环境限制：当前电脑未连接客户现场PLC、相机和生产方案，本轮确认的是代码边界、编译、兼容回归与关闭Debug快速路径；真实四流程节拍仍需现场硬件复测。

## 2026-08-19：性能改造步骤2第五轮通信阶段关联

- 状态：第五轮已完成，步骤2完整耗时诊断正式收口；下一步进入步骤3硬件读取与自动资源档案。
- 统一入口：通信诊断复用所有节点共有的`MarkNodeRunning`和`SetRunResult`追踪入口，没有在16个通信节点中复制计时代码，后续统一维护。
- 自动分类：按`NodeType`覆盖光源、PLC读写与触发、Modbus读写与触发、TCP客户端与服务端、串口、相机IO、相机参数、ERUI IO、结果信号和内部信号等待等17类通信节点。
- 完整耗时：Debug开启时输出“通信节点开始/结束/异常”，同一日志带流程`TraceId`、`RunId`、通信类型、实际墙钟耗时、节点报告耗时、开始线程、结束线程和是否发生异步换线程。
- 慢耗时现场：通信实际耗时达到统一`CommonSlowThresholdMs`后，追加线程池可用量、进程内存、GC、句柄和GDI现场，便于判断8秒停顿来自设备等待还是软件资源拥堵。
- 可编辑流程边界：分类逻辑不访问上一节点、下一节点或旧流程索引，不固定“AI推理 -> PLC写回”；PLC可以出现在流程任意合法位置和分支中。
- 运行验证：编译产物中的17类通信节点逐项识别为通信，`AITD`、`ImageSave`、`MultiCondition`、`ResultOverlayDraw`四类节点逐项确认不误判。
- 自动检查：新增`Tests\CommunicationPerformanceTrace.Tests.ps1`并扩展完整流程追踪测试；通信、流程、UI与保存、资源释放、相机帧、相机等待和诊断共13项相关回归全部通过。
- 压力检查：编译产物连续执行100000轮PLC写入与AI节点分类，共200000次判断，用时772ms且结果稳定；反射测试宿主强制GC后托管内存差值170752字节，该数值包含反射调用开销，不作为生产节拍。
- 编译检查：解决方案`Debug|x64`完整编译通过，MSBuild报告用时2.63秒，0个错误、175个项目既有警告。
- 环境限制：本机没有连接客户PLC、串口和现场TCP设备，本轮证明追踪覆盖、类型分类、构建产物和压力稳定性；真实设备超时仍需用现场Debug日志确认具体通信节点和协议调用。

## 2026-08-19：性能改造步骤3硬件读取与自动资源档案

- 状态：步骤3已完成；本轮只计算并保存当前建议档案，不提前修改线程池、CPU亲和性、节点顺序或AI并发，下一步进入图像资源生命周期。
- 接口设计：新增`IHardwareResourceProbe`、`IResourceProfileCalculator`、`IRuntimeResourceProfileProvider`，将硬件读取、纯公式和运行缓存分离，便于测试、替换和未来系统设置手动覆盖。
- Windows探针：读取WMI CPU名称与物理核心、系统逻辑线程、进程亲和性可用线程、总内存、可用内存、GPU名称和程序程序集所在磁盘空间；局部读取失败写入警告并采用保守值，不阻止方案启动。
- 自动公式：实现计划书顶部CPU保留核心、CPU重任务并发、图像转换并发、保存线程、流程图像预算、保存队列预算、内存低/严重水位和保存队列容量公式；所有结果都有至少1线程或明确容量边界。
- 亲和性边界：发现仅按物理核心计算会在进程被限制到少量逻辑处理器时给出过高建议，已增加`min(物理核心, 进程可用处理器)`约束，并用16核但仅允许2线程的模拟输入验证降级到CPU并发2、转换并发1、保存线程1。
- 方案接入：`Solution.Run`在相机启动前统计启用流程和二维/三维相机，刷新`CurrentResourceProfile`；Debug开启时记录硬件与建议全文，探针降级使用Warn日志。
- AI边界：资源档案固定输出`FollowEditableFlow`，GPU名称只进入诊断，不新增全局AI并发限制。
- 复查修正：首次编译发现项目流程类`Process`遮蔽系统进程类型，已改用`System.Diagnostics.Process`完整类型名；测试宿主最初把PowerShell目录误当程序磁盘，已改为按资源管理程序集自身路径识别，最终正确得到`D:\`。
- 本机实测：Intel Core i7-14650HX，16物理核心、24逻辑线程、进程可用24线程、总内存16087MB，GPU包含NVIDIA GeForce RTX 4060 Laptop GPU，`D:\`剩余约32129MB；连续复测可用内存在1168～1546MB间波动。
- 四流程四相机建议：CPU保留核心4、CPU重任务并发4、图像转换并发4、保存线程2、内存低水位2414MB、严重水位1287MB；流程图像与保存预算随可用内存动态变化，末次为386MB与154MB；未取得真实帧采样时保存队列容量为8。
- 自动验证：新增`Tests\HardwareResourceProfile.Tests.ps1`，覆盖16核标准、双核低内存、零流程/相机、亲和性受限和本机真实WMI读取；连同通信、流程、UI与保存、资源释放、相机等待和诊断共14项相关回归通过。
- 压力检查：固定16核四流程四相机场景连续计算50000轮，CPU并发4、保存容量42和AI策略始终一致，用时624ms；强制GC后反射宿主托管内存差值190616字节。
- 编译检查：第一次编译因`Process`类型名冲突出现3个错误，修正后最终`Debug|x64`完整编译通过，MSBuild报告用时4.73秒，0个错误、175个项目既有警告。
- 环境说明：WMI在受限沙箱内会返回Access denied并触发预期降级；使用应用正常Windows权限复测后全部真实字段读取成功。当前可用内存会随系统负载变化，档案记录的是方案启动时值，不是固定机器常量。

## 2026-08-19：开始性能改造步骤4第一轮图像生命周期基础

- 状态：第一轮已完成，步骤4整体尚未完成，下一轮继续迁移剩余图像生产节点。
- 改动前版本：`bd177e9`，工作区除三个既有未跟踪备份或临时目录外保持干净。
- 本轮范围：建立`OutputImage`显式所有权、引用租约和幂等释放；统一释放被替换或被删除节点的旧`Result`；先迁移所有权明确的采集与预处理生产节点，并让保存后台持有独立租约。
- 盘点发现：当前`OutputImage`不实现释放，默认创建空`Mat`；`NodeBase.Result`直接替换但不回收旧结果；图像源和3D源会原地替换结果内图像；保存队列异步借用上游`Mat`却没有生命周期保护。
- 安全边界：借用共享变量或上游图像时不取得释放权；自产的相机帧、磁盘读取图、转换图、裁剪图、分割图、旋转图和预处理图才登记为拥有；同一`Mat`同时出现在`SrcImg`、`GrayImg`和`Bitmaps`时只释放一次。
- 不在本轮修改：不改变可编辑流程顺序，不增加AI限流，不调整CPU线程数，不提前实施UI最新帧槽和保存有界队列策略。
- 验证要求：新增生命周期运行测试，覆盖共享`Mat`去重、租约延迟释放、重复释放、自产/借用边界、结果替换、保存完成与停止排空；随后执行相关回归、压力测试和`Debug|x64`完整编译。
- 基础实现：新增`INodeResultResourceOwner`、`IImageResourceLease`和`NodeResultResourceManager`；`OutputImage`实现显式自产登记、借用父级租约、活动租约计数及幂等最终释放，反射属性扫描按结果类型缓存，只在Result替换或节点关闭时执行。
- 借用链修正：复查发现只保护保存节点当前包装仍可能漏掉真正的上游拥有者，已补成父级租约传播；裁切未启用、颜色识别和共享`OutputImage`等借用结果会持续保护上游，直到下游保存任务结束。
- 结果回收：`NodeBase.Result`改为原子替换并释放旧结果；节点删除、控件释放、流程删除和方案重置都释放最后一次Result，租约仍在时只提出释放请求，不提前销毁Mat。
- 生产者迁移：二维图像源、共享变量图像、3D深度预览、裁切、分割、旋转、图像预处理、颜色识别借用输出和Blob二值图已区分自产与借用；裁切、分割、3D预览及图像列表转换增加中途异常清理。
- 保存生命周期：后台保存任务入队前取得图像租约；入队失败、正常保存、保存异常和停止队列排空都会释放任务租约；未提前改动步骤6的队列容量与满队列策略。
- 专项压力：`Tests\ImageResourceLifecycle.Tests.ps1`使用可计数Mat覆盖自产彩图、单通道同Mat去重、纯借用、借用时自产灰度、父子租约链、特殊Result接口和反射结果释放；50000轮用时36ms，每轮恰好释放一次。
- 回归结果：生命周期、方案释放、相机等待与回调、帧诊断、UI与保存诊断、完整流程诊断、通信诊断、保存订阅、订阅分类、AI ROI及四项结果绘制等19项相关回归通过；几何适配测试按项目对应的Windows PowerShell环境通过。
- 硬件复测：本机仍正确读取16物理核心、24逻辑线程、16087MB总内存、RTX 4060 Laptop GPU和`D:\`程序磁盘；四流程四相机建议仍为CPU重任务4、图像转换4、保存线程2、AI策略`FollowEditableFlow`。
- 编译结果：两次`Debug|x64`完整编译均通过，最终耗时3.00秒，0个错误、175个项目既有警告。
- 防漏审计：剩余直接写入图像结果的节点已明确列为步骤4第二轮，包括电池耳、二值化、找线、找圆、模板匹配、图像绘制、两套结果绘制和线合并；本轮不把步骤4提前标记完成。

## 2026-08-19：开始性能改造步骤4第二轮剩余生产节点迁移

- 改动前版本：`40f8314`，第一轮已形成独立恢复点；工作区除三个既有未跟踪备份或临时目录外保持干净。
- 本轮范围：迁移电池耳、二值化、找线、找圆、模板匹配、图像绘制、两套结果绘制和线合并的自产/借用边界。
- 补充发现：`TDAI`启用多ROI时会创建仅供本轮推理使用的临时`OutputImage`与多张裁剪Mat；Blob在上游没有灰度缓存时会临时调用`BuildGrayImage`，两类对象都需要独立于Result替换的释放路径。
- 关键决定：自产图登记所有权，借用图持有父级租约，仅在方法内部使用的Mat采用明确的`using/finally`；不能通过“全部Dispose”碰运气，也不能让未发布结果等到下一轮才释放。
- 验证要求：扩展生命周期契约测试逐项覆盖所有生产节点，增加异常和临时图释放检查；本轮完成后重新执行图像算法、订阅、UI保存、流程诊断、完整编译和压力测试。

## 2026-08-19：性能改造步骤4第二轮第一小批节点迁移

- 状态：第一小批已完成并形成独立验证结果；下一小批处理节点内部临时ROI/Bitmap、模板匹配、图像绘制和结果叠加。
- 改动前版本：`ba4e437`，该提交固定了第二轮范围和关键参数，可直接作为本小批恢复点。
- 电池耳：算法生成的结果Mat由新`OutputImage`显式接管；Modbus、结果构建或状态更新发生异常时，尚未发布的结果图在`finally`释放。
- 二值分析：`RunAnalysisAsync`返回的临时Bitmap在所有路径释放，转出的Mat登记为自产资源；每轮建立新的结果对象，不再把新Mat写进上轮Result。
- 找线与找圆：结果底图继续只读复用上游Mat，通过父级租约保证上游有效；删除每轮一次整图Clone，检测方法返回的临时Bitmap在节点退出时释放。
- 发布边界：四个节点均先在局部变量中构建完整结果，`SetRunResult`成功后再通过`NodeBase.Result`一次性替换；旧Result由统一资源管理器释放。
- 防漏检查：扩展`Tests\ImageResourceLifecycle.Tests.ps1`，保护四个节点的自产登记、父级租约、临时Bitmap释放、新Result发布以及禁止找线/找圆恢复整图Clone。
- 自动验证：生命周期50000轮压力测试通过，用时50ms；方案资源释放、订阅消费契约和隔离进程订阅类型兼容回归通过。
- 编译检查：第一次完整编译定位到电池耳新增类型缺少命名空间引用并当场修正；最终`Debug|x64`完整编译4.14秒，0个错误、175个项目既有警告。

## 2026-08-19：性能改造步骤4第二轮第二小批临时图释放

- 状态：第二小批已完成并验证；下一小批迁移模板匹配、图像绘制、两套结果叠加和线组合拟合。
- 改动前版本：`3f746db`，第一小批节点所有权迁移已形成独立恢复点。
- 电池耳临时资源：释放ROI控件每轮生成的全部Mat、模板匹配ROI视图与结果Mat、治具像素测量的灰度/二值/形态学Mat和源图副本；参数预览退出时释放算法结果Mat。
- 找线与找圆临时资源：ROI列表、GaussianBlur、Canny、全部结果绘制Mat按作用域释放；三个PictureBox替换图像时同步释放旧图，防止重复调参累积GDI句柄。
- 无效转换删除：调用点审计确认检测方法返回的Bitmap内容从未被使用，已改为“检测值+成功标志”，删除每轮一次ROI Clone和Mat转Bitmap；主节点仍从订阅输出借用原底图并持有租约。
- 二值分析预览：执行按钮和添加区域后的后台分析都等待完成并释放返回Bitmap，原来未等待的任务调用一并消除。
- AI临时ROI：每轮ROI输出接管全部裁剪Mat及首张灰度图，持有上游租约，推理正常、取消或异常退出后统一释放；裁剪中途异常清理已生成子图，OBB单图Clone在单次Detect后立即释放。
- Blob临时灰度：`GetGrayImage`通过`ownsImage`明确调用方释放责任；上游灰度和单通道源图只借用，彩色临时转灰度由本轮释放；参数预览生成的未发布Result通过统一资源管理器回收。
- 防漏检查：生命周期测试新增参数窗体ROI/预览释放、AI临时ROI父级租约、OBB Clone、Blob临时灰度责任和找线/找圆禁止恢复未使用Bitmap转换等契约。
- 复查修正：一次完整编译发现OpenCv的`LineSegmentPoint`和`CircleSegment`是不可空值类型，失败返回已修为显式`bool`标志；复编后通过，不再依赖空值判断。
- 自动验证：生命周期、AI ROI、UI与保存诊断、方案资源释放和订阅消费契约共5组隔离回归通过；生命周期50000轮压力用时47ms。
- 编译检查：最终`Debug|x64`完整编译2.55秒，0个错误、172个项目既有警告；相较本批开始减少3个无效异步或未使用变量警告。

## 2026-08-19：性能改造步骤4第二轮第三小批输出所有权迁移

- 状态：第三小批已完成并验证；所有直接写入`OutputImage`的生产节点已完成自产/借用边界迁移，下一小批继续审计参数窗体、算法辅助类和显示控件中的临时Mat/Bitmap。
- 改动前版本：`9886b46`，第二小批临时图释放已经形成独立恢复点。
- 图像绘制：取上游Mat时同时取得真实`OutputImage`拥有者并持有计算阶段短租约；Bitmap转出的结果Mat由待发布输出显式接管，异常或取消会回收未发布结果。
- 两套结果绘制：输出继续复用上游干净Mat，不增加整图复制；新输出持有上游父级租约，每轮构建新的完整Result后原子发布，构建或发布失败时释放待发布依赖。
- 模板匹配：普通路径的输出Bitmap转Mat后登记自产所有权，正式节点退出时释放原Bitmap；NCC快路径在native计算期间持有短租约，结果容器再接管父级依赖，完成接管后释放短租约。
- 模板预览：预览结果发布增加待发布回收，Result设置失败时不会遗留已经转换的Mat；界面显示继续按原控件所有权规则使用预览Bitmap，不改变模板算法和绘制内容。
- 线组合拟合：正式运行`needPreviewImage=false`时返回`null`，不再每轮分配空Mat；预览输出通过`finally`释放，结果构建先接管输出，后续计算或发布异常均统一回收。
- 防漏检查：生命周期契约测试新增图像绘制短租约、两套结果绘制父级依赖、线组合空Mat消除与异常释放、普通模板自产Mat以及NCC短租约转父级依赖等保护。
- 全仓输出审计：检索所有`new OutputImage`、`OutputImage.Bitmaps/SrcImg/GrayImg`直接赋值、`TakeOwnership`和`TakeDependency`位置，确认现有直接输出生产点均已有明确所有权；没有把算法内部临时图误算成已完成，保留到下一小批继续处理。
- 自动验证：模板匹配CPU兼容、崩溃诊断、托管封装、native API和排序动态输出5项通过；结果绘制简化、几何适配、AI文字颜色和多目标几何4项通过；性能诊断、方案资源释放、订阅契约与生命周期检查通过。
- 压力检查：图像生命周期连续50000轮，用时54ms；自产Mat每轮恰好释放一次，父子租约最后使用者退出后完成回收。
- 编译检查：补齐异常释放后`Debug|x64`完整编译4.66秒，0个错误、172个项目既有警告；第三小批所有源码和测试修改均通过`git diff --check`。

## 2026-08-19：性能改造步骤4第二轮第四小批公共显示控件GDI释放

- 状态：第四小批已完成并验证；本批只修复公共显示控件自有资源和重绘GDI对象，尚未实施步骤5的最新帧容量1与刷新限速策略。
- 改动前版本：`3deb32c`，第三小批图像输出所有权迁移已形成独立恢复点。
- 控件销毁：`ShowImageControl.Designer.cs`的标准Dispose路径调用统一资源释放入口，同步释放当前显示Bitmap、可释放ROI、文本集合和不属于components容器的交互定时器；原子状态保证重复Dispose安全。
- Mask叠加：`RoiMaskOverlay`实现`IDisposable`并释放内部32位叠加Bitmap；静态ROI更新、全部清空、静态清空、动态清空、删除选中ROI和丢弃过小ROI统一经过可释放ROI回收入口。
- 重绘资源：棋盘背景每次绘制只创建一份暗色画刷，状态栏和标签画刷使用`using`，旋转手柄画笔确定释放，卡尺箭头复用已有作用域画刷；删除所有直接传入绘图API却无人Dispose的GDI对象。
- 行为边界：不改变四个流程对应四个窗口的关系，不改变图像刷新频率、线程投递和算法叠加内容；`DisposeOldImageAsync`及UI最新帧槽保留给步骤5统一改造。
- 新增验证：`Tests\ShowImageControlResourceLifecycle.Tests.ps1`同时保护源码释放契约和实际GDI行为；Windows STA环境连续250次`DrawToBitmap`与100次Mask叠加创建/清空后，GDI对象增长为0。
- 回归结果：图像生命周期、UI与保存诊断、诊断惰性求值、AI ROI、两套结果绘制及几何适配全部通过；生命周期50000轮用时44ms，每轮释放次数准确。
- 编译检查：`Debug|x64`完整编译4.33秒，0个错误、172个项目既有警告。

## 2026-08-19：性能改造步骤4第二轮第五小批颜色识别资源释放

- 状态：第五小批已完成并验证；修复颜色识别正式节点、参数窗体和模板创建窗体的异步图像生命周期，下一小批继续审计其他参数窗体的直接PictureBox与文件载图路径。
- 改动前版本：`2c6d234`，公共显示控件GDI释放已形成独立恢复点。
- 关键缺陷：复查发现颜色识别节点先发布未完成Result，构建图像后又发布同一个Result；在`NodeBase.Result`已经负责释放旧结果后，第二次发布同一对象会释放刚创建的图像依赖，属于必须立即修正的生命周期错误。
- 正式节点：删除运行开始时的空Result发布和构建中途发布；结果、矩形及叠加内容全部完成后只发布一次。读取上游图后立即取得短租约，覆盖Modbus等待与颜色检测，输出构建再持有父级依赖，最后释放短租约。
- 参数窗体：订阅图刷新采用候选Mat/Bitmap成功后交换，旧Mat立即释放，异常时释放候选并清空显示；窗体Designer Dispose释放最后的`srcMat`，颜色模板对话框改为`using`，不再由静态字段长期持有。
- 模板窗体：文件加载直接接管`Cv2.ImRead`返回Mat，删除`ImRead().ToBitmap()`导致原Mat丢失的链式转换；从订阅载图也直接Clone Mat，显示Bitmap由公共控件接管，窗体关闭释放最后Mat并解除鼠标取色事件。
- 异步预览：全量检测和当前颜色检测在进入后台前Clone独立源图，后台只使用快照；刷新图像或关闭窗口不再影响正在运行的OpenCV计算。正常显示后Bitmap交给控件，关闭/异常/未发布路径在finally释放。
- GDI与临时Mat：预览文字Font、半透明背景Brush、自动取色Mean/StdDev Mat全部使用作用域释放；删除两个无人调用的Bitmap转Mat辅助方法和颜色通道重复Dispose。
- 防漏检查：新增`Tests\ColorDiscernResourceLifecycle.Tests.ps1`，保护单次Result发布、输入短租约、父级依赖、待发布回收、窗体Dispose、异步快照和文件读取Mat所有权。
- 自动验证：颜色资源、图像生命周期、订阅消费契约、结果绘制AI文字颜色和UI保存诊断均通过；生命周期50000轮用时40ms。公共显示控件Windows STA压力复测250次重绘与100次Mask循环，GDI增长0。
- 编译检查：`Debug|x64`完整编译2.52秒，0个错误、172个项目既有警告。

## 2026-08-19：性能改造步骤4第二轮第六小批公共ROI与二维码资源释放

- 状态：第六小批已完成并验证；公共旧版ROI显示控件形成统一Bitmap所有权规则，二维码参数窗体补齐异步模型和每轮OpenCV临时资源释放，下一小批继续审计图像分割、旋转和电池耳模板窗体的直接PictureBox路径。
- 改动前版本：`39e71ee`，第五小批颜色识别资源释放已形成独立恢复点。
- 公共ROI控件：`ImageROIEditControl.SetImage`接管传入Bitmap，在新图替换时释放旧图；`ImageROIEditControl.Designer.cs`的标准Dispose路径释放最后一张图，避免各参数窗体长期调参时积累GDI资源。
- 调用点复查：逐项核对裁切、找线、找圆、二维码、电池耳、模板创建和AI训练窗体调用。大模型与无监督训练窗体的`_previewBitmap`只作为当前图读引用，替换时不再重复Dispose已由ROI控件释放的旧Bitmap；关闭时仍统一通过`SetPreviewImage(null)`回收当前图。
- 二维码模型：后台加载改为单一`Task<WeChatQRCode>`，每次检测等待同一模型加载完成；窗体销毁后在任务完成时释放模型，加载失败时显式观察任务异常，避免未观察后台异常。
- 二维码临时图：检测结束在`finally`释放ROI控件生成的全部Mat及模糊Mat；`DetectAndDecode`返回的全部bbox Mat也逐一释放；CLAHE使用作用域释放，删除未使用的`claheEq` Mat。
- UI与异步边界：边缘预览统一通过替换方法释放旧PictureBox图；高斯模糊、CLAHE开关和参数在进入后台前从控件取值，后台任务不再读取WinForms控件。
- 新增验证：`Tests\QrAndRoiImageResourceLifecycle.Tests.ps1`保护所有权源码契约，并在Windows STA环境实例化真实ROI控件；连续替换500张`320×240`位图，旧图底层句柄确认失效，GDI对象增长0。
- 自动验证：公共显示控件250次重绘与100次Mask循环GDI增长0；图像生命周期50000轮用时47ms；AI ROI、训练行为和订阅消费契约通过。无监督窗体既有测试要求按钮文本为“选中设OK/NG”，当前未改动基线Designer为“设OK/NG”，判定为与本轮无关的既有契约差异并如实保留。
- 编译检查：`Debug|x64`完整编译2.59秒，0个错误、172个项目既有警告。

## 2026-08-19：性能改造步骤4第二轮第七小批参数预览与电池耳模板缓存

- 状态：第七小批已完成并验证；图像分割、图像旋转和电池耳模板管理的Bitmap/Mat生命周期已补齐，电池耳正式匹配移除每ROI重复读盘和隐藏窗体整图转换，下一小批继续全仓扫描剩余直接PictureBox、`ToBitmap`和`Cv2.ImRead`路径。
- 改动前版本：`e0fc2a8`，第六小批公共ROI与二维码资源释放已形成独立恢复点。
- 图像分割：窗体明确区分原始Bitmap和带分割线临时Bitmap；重复预览只释放上一张临时图，不误释放仍供再次绘线的原图；刷新时同时释放旧原图和旧叠加图，Designer Dispose按引用去重释放最后资源。
- 图像旋转：原有预览替换已经释放旧图，本批补齐Designer Dispose释放最后一张预览图，并增加同一Bitmap重复设置保护。
- 模板读盘修正：`GetJiErTemplate/GetMarkTemplate`不再调用`LoadTemplateImages`，正式检测首次或版本变化时才加载磁盘模板；正常每轮从缓存Clone独立快照，不访问PictureBox，不在每个ROI重复执行`Cv2.ImRead`和Mat转Bitmap。
- 单轮复用：电池耳每轮只获取一次极耳模板快照和一次Mark模板快照，所有左右ROI复用这两组快照，整轮结束在finally逐张Dispose；命中结果只带出模板尺寸，不再让共享或临时Mat逃出匹配方法。
- 多节点一致性：静态模板Form改为每节点独占Form并由参数窗体Designer Dispose；模板新增或删除递增轻量全局版本，其他节点下一轮发现版本变化后只重新读盘一次，既保持全局模板文件语义，也避免正常轮询磁盘。
- 模板编辑资源：ROI数量为0、1或多张的所有路径都释放ROI Mat；新模板原子替换旧缓存并释放旧Mat；六个PictureBox使用独立预览Bitmap并释放旧图；Designer共享资源图在构造时只解除引用，不误Dispose资源管理器对象。
- 文件与异常边界：文件Bitmap通过二次复制脱离关闭的FileStream；模板窗体释放缓存Mat、六张预览图、ROI底图和占位图；原图保存移入结果Mat的受保护try范围，磁盘异常不会遗留刚Clone的结果Mat。
- 正式链路减负：`UpdataImage`不再每轮为隐藏模板窗体额外生成整图Bitmap；只有用户打开模板管理时才创建并移交该预览图。删除无用空`OutputImage`和长期持有的借用输出字段，编译警告减少1个。
- 新增验证：`Tests\ImageParameterAndBatteryTemplateResourceLifecycle.Tests.ps1`覆盖分割/旋转/模板源码契约，并创建6张真实BMP模板；Windows STA实测100轮六预览重载和250轮两组模板快照获取/释放，GDI对象增长0。
- 自动验证：图像生命周期50000轮用时50ms；公共ROI替换500轮GDI增长0；公共显示250次重绘与100次Mask循环GDI增长0；方案释放和订阅消费契约通过。
- 编译检查：最终`Debug|x64`完整编译2.64秒，0个错误、171个项目既有警告；所有修改通过`git diff --check`后再提交。

## 2026-08-19：性能改造步骤5第一小批UI容量1最新图像槽

- 状态：第一小批已完成并验证；当前完成每窗口容量1、单一UI投递、Mat转换移出UI和公共图片控件释放，尚未实施`UiMaxRefreshFps`，也没有把模拟压力数据当作现场工艺节拍。
- 改动前版本：`791495e`，步骤4第二轮第七小批参数预览与电池耳模板缓存已形成独立恢复点。
- 每窗口独立：每个`FrmSingleImage`实例拥有自己的`PendingViewerFrame`和调度标记，四个流程对应四个窗口时互不排队；同一窗口的新帧原子替换未显示旧帧，Bitmap、叠加结果和Debug追踪上下文始终作为同一组传递。
- UI消息有界：每个窗口最多向WinForms消息队列保留一个刷新任务；UI取出时只消费当下最新帧，刷新期间到达的新帧在本次完成后继续调度，不积压历史图片闭包。
- 所有权回收：被替换帧、句柄未创建、BeginInvoke失败、窗口释放和待帧排空路径都会释放Bitmap；成功显示后所有权移交`ShowImageControl`，窗口销毁同时解除五类静态或全局事件。
- 公共查看器：`YTPictrueBox`在新图替换、清空和Designer Dispose时释放旧图或最后一张图，同一Bitmap重复赋值不会误释放；`MatViewer`在调用线程完成Mat转Bitmap，UI线程只接管Bitmap，模态窗口关闭后确定Dispose。
- 新增压力测试：`Tests\ImageViewerResourceLifecycle.Tests.ps1`使用真实编译程序集，在UI线程暂不处理消息时由后台连续提交500张`320x240`Bitmap，断言待帧峰值为1、UI任务峰值为1，恢复消息处理后待帧归零；额外保留500帧突发后的最新待帧并直接销毁窗口，排空耗时0ms，待帧Bitmap句柄确认失效。
- 多轮耗时：公共控件每轮替换500张图为83～250ms，后台500帧提交为61～176ms，UI只消费最终帧为2～8ms；最终9.16秒构建产物复测分别为183ms、91ms和3ms，销毁待帧窗口排空为0ms；全部轮次公共控件和流程窗口GDI增长均为0/0。
- 最终代码复核：修正并发Debug日志快照后连续三轮，公共控件500次替换分别为83ms、89ms、85ms，后台500帧提交分别为64ms、65ms、69ms，UI只消费最终帧均为3ms；三轮待帧/UI任务峰值仍为1/1，GDI增长仍为0/0。
- 回归结果：UI与保存诊断、公共显示控件、图像生命周期、方案释放和订阅消费契约通过；公共显示控件250次绘制与100次Mask循环GDI增长0，结果资源生命周期50000轮多次实测56～107ms，提交前复核为90ms。
- 编译检查：`Debug|x64`多次完整重建均为0错误，分别记录到4.80秒/171警告、4.93秒/171警告、7.37秒/172警告和9.16秒/171警告；构建时点及工作区状态不同，不将差异视为运行性能改善，最新主线为9.16秒、171个项目既有警告，与上一批数量一致。
- 证据边界：当前数据证明受控500帧突发不会形成500个UI任务、旧Bitmap会及时释放、资源管理器开销很低；尚未测量长期托管堆、私有工作集、Mat原生内存，也没有高分辨率实图、四窗口同时触发及同工况改造前后业务基线，因此不能证明客户现场相机采集到流程结束缩短多少。后续必须采集Debug完整链路的相机等待、节点、UI和通信P50/P95/P99作改造前后对照。

## 2026-08-19：性能改造步骤5第二小批UI刷新限速与发布所有权

- 状态：第二小批功能、自动压力验证、独立智能体复核和最终完整重建均已完成。
- 改动前版本：`ebeb1f5`，步骤5第一小批的最终耗时和证据边界已形成独立恢复点。
- 关键参数：`UiMaxRefreshFps`默认30FPS、允许1～60FPS；每个窗口使用独立状态和独立锁，后续可绑定系统设置控件，不设置进程级总帧数额度。
- 关键实现：新增可替换`IUiFrameRateLimiter`和`PerWindowUiFrameRateLimiter`；自动硬件档案输出最终UI帧率，二维与三维图像显示节点都在`Mat.ToBitmap()`前判断许可，减少本来就不需要显示的Bitmap分配和CPU转换。
- 流程边界：刷新限速只跳过显示转换和UI投递，显示节点仍成功并返回`ContinueRun`；没有修改AI并发，没有插入固定PLC写回，也没有改变用户编辑的节点顺序。
- 发布所有权：`ImageShowPamra`隐藏可直接读取的Bitmap字段，目标窗口先通过`TryTakeBitmap`原子预认领，成功交给容量1槽后再调用`ConfirmBitmapOwnershipTransfer`确认；订阅者预认领后抛异常时，发布器发现未确认转交并立即回收。子显示控件通过`TrySetImage`明确返回是否接管，句柄未建立或已释放时由容量1帧槽立即回收。
- 遗漏补查：旧连续采图发布入口即使未来重新启用，也在`finally`释放无人接管Bitmap；窗口销毁解除事件、排空待帧并重置本窗口门控状态。
- 四窗口确定性验证：四个窗口在相同时间戳都能独立通过第一帧，同一窗口紧邻第二帧均被限速；30FPS在1000Hz测试时钟下必须到第34个时间戳才再次放行；重置窗口1不影响窗口2。
- 四线程压力数据：四个工作线程使用同步起跑信号，每个线程固定压测一个窗口；连续四轮合计各调用100000次，分别用时3ms、2ms、3ms、3ms，每轮四窗口通过数均为`1,1,1,1`。测试增加2秒耗时上限，能同时识别并发独立性和明显性能退化。
- UI压力数据：最终程序集多次复测公共控件替换500帧72～147ms，单个流程窗口后台提交500帧60～90ms，UI最新帧清空2～4ms，带待帧窗口销毁排空0ms；待帧和UI投递峰值始终1/1，GDI增长0/0。
- 生命周期数据：公共显示控件250次绘制与100次Mask循环后GDI增长0；结果图资源生命周期50000轮51～73ms且释放计数准确；无人订阅、订阅者预认领后异常、单次确认转交、子控件拒收和成功显示后窗体销毁路径均验证Bitmap最终失效。
- 硬件档案：本机识别Intel Core i7-14650HX、16物理核心、24逻辑线程、16087MB总内存和RTX 4060 Laptop GPU；最终复核运行启动可用内存1231MB，已低于自动严重水位1287MB，四流程建议CPU重任务并发4、图像转换并发4、保存线程2、UI每窗口30FPS，AI策略`FollowEditableFlow`。
- 智能体复核：只读审查指出“订阅者认领后抛异常”以及“原10万次测试为单线程轮询”两项缺口；主线已分别增加确认转交协议、故障订阅者回收测试和四线程同步起跑测试。智能体同时确认2D/3D门控均位于转Bitmap前，限速不改变AI、PLC或可编辑流程拓扑。
- 回归结果：UI刷新与所有权、硬件档案、图像窗口、UI与保存诊断、公共显示控件、方案释放、订阅消费、日志等级和图像生命周期共9组检查通过；旧生命周期脚本显式传入C#可选参数的`null`，消除PowerShell 7反射绑定差异，不改变产品行为。
- 编译结果：补强确认转交协议和旧连续采图发布入口后，`Debug|x64`最终完整重建6.10秒，0错误、171个项目既有警告；警告数与上一批一致。
- 证据边界：自动压力数据证明四线程门控和所有权路径稳定，但UI压力仍是单窗口受控小图，尚未覆盖四张高分辨率图同时转Bitmap和四个真实窗口同时绘制；也没有四台真实相机、真实PLC和现场算法负载，不能据此把客户现场8秒等待归因或宣称已经消除。后续按Debug完整耗时记录对比相机等待、转换、算法、UI、通信和整轮P50/P95/P99。

## 2026-08-19：性能改造步骤5第三小批四窗口真实转图与UI压力

- 状态：四窗口发布、接管与强制刷新压力、两轮独立智能体复核、相关回归和完整重建均已完成，准备提交。
- 改动前版本：`1ac3a3b`，步骤5第二小批每窗口限速和确认转交协议已形成独立恢复点。
- 最新框架原则：按用户确认，只面向最新方案和配置数据，不再为旧版数据结构增加兼容层；后续改造以稳定性和运行效率同时达标为准，版本恢复依靠Git提交而非运行时双格式兼容。
- 测试真实性：新增`Tests\FourWindowImageDisplayStress.Tests.ps1`，加载最终Debug程序集及真实`OpenCvSharp`/native运行库，创建四个带句柄和子显示控件的`FrmSingleImage`；四个后台线程经同步信号同时起跑，主线程持续处理WinForms消息。
- 输入与节拍：每个线程持有独立`1024x768 CV_8UC3 Mat`，每轮按40ms间隔尝试30帧，逐帧调用生产`TryAcquireWindowRefresh`、`BitmapConverter.ToBitmap`和`PublishImageShowChanged`；主线程约每16ms强制刷新四个窗体，并每25ms采样进程资源。
- 完整性结果：全部异常边界补强和完整链预热后的3个独立进程共9轮、1080次真实转换与投递；每轮四窗口尝试、允许和接管数均为`30,30,30,30`，没有全局限流、窗口饥饿或已转换Bitmap无人接管；每进程执行121～122次四窗强制刷新周期。
- 耗时结果：每轮带40ms主动节拍的墙钟耗时1245～1259ms，最终UI排空0～5ms；各窗口单帧转换平均0.74～1.50ms，30样本P95为0.95～2.69ms，最大1.09～6.58ms。墙钟耗时包含约1200ms节拍等待，不作为纯处理耗时。
- 资源结果：运行期间每25ms采样并在每轮GC前补采尾段，私有内存采样峰值增量28.07～28.32MB、关闭窗口并强制回收后净增0.81～1.08MB；托管内存采样峰值增量11.62～11.92MB、最终净增0.65～0.66MB；GDI采样峰值增长12～15、最终增长均为0；系统最低可用内存1158.02～1322.19MB。
- 基线修正：首轮在任何窗体创建前取GDI基线，结束固定多16个对象；最终改为预热同尺寸`Mat→Bitmap→生产发布→窗口接管→Refresh`完整链后再取基线，最终GDI增长稳定为0，没有通过放宽阈值掩盖持续泄漏。
- 智能体第一轮复核：指出Mat构造失败可能卡住就绪等待、超时后未取消Join、结束采样不是真峰值、发布异常可能重复释放，以及12样本P95等于最大值。主线已逐项修正，并将墙钟明确命名为“带节拍墙钟”。
- 智能体最终复核：确认异常边界补强后无阻断问题、当前版本可以提交；最后一个`Dispose`取消30秒仍失败时静默返回的低风险点也已统一改为终止隔离测试进程，避免后台线程继续访问窗口。
- 测试夹具安全：Mat构造在`finally`释放就绪门闩；起跑等待上限10秒并检查全部线程完成后Join，单轮运行上限15秒，超时使用`CancellationTokenSource`取消并最多等待30秒Join；显式超时或会话Dispose仍无法退出时都终止隔离测试进程，避免销毁仍被发布的窗口。同步事件、窗口、Mat、Bitmap、程序集解析器和最终显示图均有确定清理路径。
- 内存安全：起跑前物理可用内存至少512MB，运行中低于256MB立即取消；Bitmap从创建到交权使用局部`finally`保护，生产发布调用前再将局部责任置空，既不遗漏低内存异常，也不重复释放已经交给窗口的图。
- 验收阈值：单帧转换小于3000ms、每轮小于10000ms、最终私有内存净增小于256MB、托管内存净增小于64MB、GDI最终增长不超过10；当前实测远低于阈值。
- 回归与编译：四窗口高样本、四线程门控与所有权、容量1窗口、公共显示控件、UI与保存诊断、方案释放、订阅消费、日志等级、硬件档案和图像生命周期共10组检查通过；生命周期50000轮52ms。`Debug|x64`完整重建6.05秒、0错误、171个既有警告。
- 证据边界：窗体位于屏幕外，强制`Refresh`能执行WinForms刷新路径但不能证明桌面合成显示耗时，也没有逐帧绘制计数；仍未覆盖客户相机SDK、最大现场分辨率、复杂ROI/文字叠加、AI负载、磁盘保存和PLC通信，不能单独解释或宣称消除现场8秒等待。

## 2026-08-19：性能改造步骤6第一小批共享有界保存工作池

- 状态：第一小批代码、独立复查、专项压力、资源回归和完整编译已完成；步骤6整体仍在进行，本批尚未执行固定方案新旧版本各5000轮同比。
- 改动前版本：`b43b922`，步骤5第三小批四窗口真实转图压力已形成独立恢复点。
- 关键参数：保存线程按硬件档案自动计算，本机为2；主队列初始容量8、任务字节预算按启动可用内存自动计算，本次硬件复测为108MB；淘汰回收容量和字节预算默认等于主队列；入队等待0ms；图像资源滚动样本20帧；停止排空5000ms；空闲线程退出保护1000ms。
- 架构变更：所有保存节点不再各自创建保存线程，统一接入`Solution`唯一共享工作池；固定消费者使用`LongRunning`任务，不占用通用线程池常驻线程，不改变AI节点并发、PLC节点位置或用户编辑的流程顺序。
- 队列规则：新增通用`BoundedPriorityWorkQueue<T>`，同时限制任务数和图像租约字节；普通任务满时立即拒绝，NG任务只淘汰最早普通任务，全NG队列满时拒绝新NG；优先级、所有者和估算字节在入队瞬间冻结。
- 图像所有权：`OutputImage.EstimateRetainedImageBytes`按`Rows×Step`去重统计自产Mat、多图、灰度图和父级依赖链；保存节点在读取源图与估算前取得候选租约，成功接收时转移给工作池，拒绝和异常时立即释放，关闭估算到租约之间的竞态窗口。
- 后台回收：被NG淘汰的普通任务进入独立固定回收线程；回收通道也同时限制任务数和字节，使用主队列冻结的字节值，通道满时明确告警并同步兜底释放；工作池空闲判断包含等待、活动和淘汰回收三类任务。
- 活动计数：消费者在任务离开队列锁之前登记活动状态，关闭“刚出队但活动计数尚未增加”导致的瞬时假空闲；空闲资源重配使用1000ms线程退出保护，活动任务仍严格使用配置的停止超时。
- 写盘与异常：`Mat→Bitmap`、叠加绘制、JPEG编码和磁盘写入全部移到后台消费者；先写唯一`.saving`文件再原子移动到最终名称；多目录任一路径失败会回滚此前已发布文件，临时文件或回滚文件删除失败均写告警；单个Bitmap或租约释放异常不会终止后续消费者。
- 生命周期：方案运行、公开流程运行、手动运行、脚本运行和参数预览统一登记运行会话；重置先禁止新运行并等待全部会话，再停止保存工作池，最后释放节点、设备、AI句柄和图像样本；方案加载从读文件到恢复完成持有方案级互斥作用域，并发`SolReset`也由同一互斥锁串行化。
- 删除与回滚：节点删除保证参数窗体异常也会继续执行节点`Dispose`；流程编辑粘贴失败沿正式删除路径回滚，保存节点退出时取消自己仍在共享队列等待的任务。
- 队列压力：四生产者、两消费者共100000任务，接收772、拒绝99228、淘汰普通364、实际消费408；队列峰值8/8、字节峰值8192/8192、消费者并发2/2、总耗时19ms，释放100000、重复释放0。接收数量随并发调度变化，不作为性能结论；完整性和硬边界为验收项。
- 真实保存压力：预热后连续三轮，每轮四流程并发尝试60张`1024x768 CV_8UC3 Mat`，执行真实`Mat→Bitmap→JPEG→原子同名写盘`。三轮接收18/17/18、淘汰5/5/5、完成13/12/13、失败均0；队列峰值8/8、字节峰值18MB/128MB、并发2/2、同步淘汰释放0、租约释放60/60、重复释放0。
- 耗时结果：三轮入队P95为0.192/0.023/0.030ms，P99与最大值为2.147/0.084/0.086ms，停止排空49/49/55ms；没有出现秒级或8秒级投递停顿。
- 资源结果：完整链预热后，三轮压力前后私有内存92.49→92.74MB，变化`+0.25MB`；托管内存9.20→9.16MB，变化`-0.04MB`；进程句柄880→880，变化`0`。图像生命周期50000轮48ms，滚动采样四线程100000次记录后窗口固定20，QR/ROI替换500轮GDI增长0。
- 异常与停止测试：多目录第二路径非法时首路径文件回滚、`.saving`残留0；首个任务租约释放抛异常后消费者继续完成第二任务；0ms停止进入超时排空，全部租约最终释放，第二次停止可以完全收敛。
- 硬件复测：Intel i7-14650HX，16物理核心、24逻辑线程、16087MB总内存、启动可用1087MB、`D:`可用31982MB；四流程四相机建议CPU重任务4、图像转换4、保存线程2、保存预算108MB、初始容量8、UI 30FPS，AI策略`FollowEditableFlow`。
- 回归与编译：有界队列、保存架构、真实工作池、资源采样、图像生命周期、保存多条件、方案释放、硬件档案、UI与保存诊断、剩余诊断、惰性日志、日志等级、QR/ROI生命周期共13组检查通过；`Debug|x64`完整编译生成成功，0错误、171个项目既有警告。
- 独立复查处理：此前复查指出并发重置、加载全周期、旧池重叠、淘汰字节重读、估算租约竞态、删除失败静默、等待句柄释放和静态测试盲点；本批已补方案互斥、候选租约、冻结字节回调、回收积压空闲判定、删除告警、重复停止收敛及动态队列/真实写盘压力。提交前复查又指出方案恢复期间可能开放运行入口、第二条会话可能早于首会话档案刷新；已将门禁覆盖读文件至恢复结束，并在统一生命周期锁内完成首会话刷新。最终只读复查确认两项闭环，未发现新增P0/P1问题。
- 证据边界：本批数据只证明共享有界队列、真实转图写盘、异常回滚、停止排空和资源生命周期的专项稳定性；尚未覆盖低磁盘空间策略、可控慢磁盘、生产`Solution`完整运行集成、固定方案和本计划规定的新旧版本各5000轮同比，不能宣称步骤6或整套业务性能已经完成优化。
- 下一步：步骤6后续小批实现低磁盘空间和慢盘策略，补生产方案完整链压力；步骤6全部改造收敛后，严格执行固定方案新旧版本各5000轮同比，再决定是否进入步骤7。

## 2026-08-19：固定方案5000轮同比测试工具与旧版校准

- 状态：固定方案自动测试工具已经建立，旧版完成1轮预热和10轮校准；校准只用于验证采集口径，不替代计划书要求的新旧版本各5000轮正式结果。
- 改动前版本：`342878d`，步骤6第一小批共享有界保存工作池已经形成独立恢复点；本批不修改业务源码、固定方案或未改造版程序。
- 固定输入：未改造版使用`D:\MyCode\PublicWook\框架版本\框架8_18号版本\x64\x64\Release\机器视觉AI检测系统V1.0.exe`，固定方案使用`C:\Users\34652\Desktop\8_6方案.Sol`，本机虚拟Modbus服务器`127.0.0.1:502`实际读寄存器成功。
- 精确轮次：新增`Tests\FullSolutionComparison.Tests.ps1`，通过主界面“单次运行”按钮串行调用方案；每次等待按钮从禁用恢复启用后才进入下一轮，确保Group1和Group2都完成，并使正面、同心度、侧面左、侧面右四个流程每轮各执行一次，避免两个循环组独立计数造成轮次偏差。
- 采集范围：逐轮记录整套方案墙钟耗时，统计总耗时、吞吐量、P50/P95/P99和最大值；同时采集CPU、主进程及两个AI工作进程的私有内存和托管堆、GDI、USER对象及句柄起始值/采样峰值/结束值，并从本轮新增日志区间核对四流程状态、异常、Modbus和保存队列告警。
- Debug口径：校准与后续正式同比均不通过自动化修改程序设置，使用两个版本共同的当前用户设置；当前没有生成过用户级覆盖配置，因此按程序默认值保持Debug关闭，外部逐轮计时不依赖新版Debug诊断能力。
- 旧版校准结果：10轮中四个流程均为`10/10`成功，整轮成功10、失败0、异常日志0；Modbus完成日志40、失败0。整轮总执行5437.821ms，吞吐量1.839轮/秒，P50为548.883ms、P95为566.087ms、P99为570.374ms、最大571.446ms。
- 校准资源信号：旧版进程树包含主程序和2个AI工作进程；10轮采样中进程树私有内存991.984→2145.617MB、峰值2145.617MB，托管堆285.163→845.644MB、峰值950.043MB，GDI 805→819、峰值820，句柄2584→2841、峰值2841。短样本明显上涨只能作为长测观察信号，不能先判定持续泄漏，正式5000轮需要继续观察回落、平台期、崩溃或线性增长。
- 校准耗时边界：10轮校准结果位于`Tests\Results\FullSolutionComparison`，只证明自动化轮次、日志核对和资源采集可用；最终结论必须来自未改造版和改造版各5000轮正式结果及同口径差异表。
- 退出自动化复核：旧版主窗体关闭会显示“确认关闭程序”自定义WinForms按钮，首次正式启动检查因此发现校准实例仍在并触发单实例提示；该次尚未进入预热和正式轮次，已作废。测试工具现通过按钮窗口句柄发送标准`BM_CLICK`完成正常确认，并验证主程序和两个AI工作进程全部退出，避免污染后续版本。
- 首次正式长测作废：未改造版按规则预热1轮后完成4424个正式轮次，第4425轮从`2026-08-19 20:58:17.484`开始，到`21:03:10.108`结束；流程最终仍为成功，但整轮耗时292609ms，其中配置50ms的`延迟执行_复制_2`实际记录289548ms，随后Modbus写入记录1987ms。测试脚本120秒轮次上限因此终止后续轮次，本次不能作为5000轮正式结果。
- 外部中断判定：同一时间主智能体30秒监控读取也延迟到315秒，Windows事件在21:03记录独立的`BluetoothAdvertising.exe`运行时崩溃；当前证据说明整机在该时段发生调度暂停、休眠或同等级外部中断，不能把292609ms直接归因于旧版业务算法，也不能把本次4424轮与新版做同比。
- 测试工具补强：通过`SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED)`让正式长测期间系统保持唤醒，轮次观测上限提高到600秒以保留真实长尾；每25轮将CSV和检查点JSON落盘，异常时不再丢失已完成轮次；关闭失败只告警，不覆盖首个测试异常；托管堆没有匹配到性能计数器实例时记录无效值而不是伪0。
- 唤醒保护冒烟修正：首次1轮工具冒烟在启动视觉程序前即因PowerShell将十六进制高位常量解释为负整数而停止，没有产生业务轮次；常量已改为显式`UInt32`十进制值`2147483649/2147483648`，避免参数转换失败。
- 固定方案恢复：冒烟结果发现程序正常退出时会自动写回`C:\Users\34652\Desktop\8_6方案.Sol`，文件由最初1314298字节变化到1314302字节，SHA256由`CAC8C2A8...69F4D`变化为`50361C7F...B8124`。已从`2026-08-19 19:01:41`的C盘卷影副本恢复原件，原件修改时间`17:08:14.452`、长度1314298、SHA256=`CAC8C2A8F3E1CF1489AE0A8AB1A663CBD5291F11F3B01D3496A1270CAE369F4D`，与首次校准记录完全一致；自动写回版本另存到测试结果目录作为证据。
- 方案不可变输入：测试工具新增`ImmutableSchemeSourcePath`，每个版本启动前把同一原始母本恢复到计划书固定路径并核对SHA256；程序正常退出后再次恢复并校验，防止旧版自动写回影响新版输入。若程序尚未退出则拒绝覆盖正在使用的方案并明确告警。

## 2026-08-19：长测后台共存模式

- 状态：按用户要求停止当前长测，不再启动新版；未改造版重跑检查点停在2550/5000轮，检查点P95为683.233ms，最后一轮704.431ms，进程树私有内存1588.613MB、托管堆581.706MB、GDI 843、句柄2976。该数据不是5000轮正式结果，也不能用于新旧版本结论。
- 停止清理：主程序和AI工作进程均已退出；中断导致退出后自动恢复逻辑未执行，主智能体已立即从不可变母本恢复桌面方案，SHA256重新核对为`CAC8C2A8F3E1CF1489AE0A8AB1A663CBD5291F11F3B01D3496A1270CAE369F4D`。
- 后台窗口：测试工具新增`WindowMode`，默认`Minimized`；启动时使用最小化窗口样式，主界面就绪后再调用Windows最小化命令，并在每个进度检查点重新确认，避免测试窗口抢占用户桌面。
- CPU让行：新增`ProcessPriority`，默认`BelowNormal`；主程序和同路径AI工作进程统一设为低于正常优先级，进度采样时重复应用以覆盖AI工作进程重启，让正常办公程序优先获得CPU调度。
- 证据边界：最小化不停止AI、GPU、磁盘和内存负载，低优先级也不能保证办公完全无感；此模式必须标记为“后台共存稳定性测试”。需要比较生产正常性能时应改用`WindowMode=Normal`和`ProcessPriority=Normal`，并在无人办公时运行，不能把两种口径混合比较。
- 500轮临时口径：按用户最新确认，本次只要求不抢桌面焦点，不要求CPU让行；计划运行500轮，`WindowMode=Minimized`、`ProcessPriority=Normal`，不限制CPU核心、AI或GPU，测试工具每50ms轮询一次“单次运行”按钮完成状态。新增`UiPollMilliseconds`参数并记录到结果，50ms只影响外部完成状态检查精度，不修改方案内节点顺序或`RunInterval`。
- 最小化启动冒烟：首次改造版500轮启动时，WinForms在持续最小化状态下未向UI Automation暴露“单次运行”按钮，90秒控件等待超时；正式轮次0、预热0，主程序正常退出，桌面方案SHA256仍为原始`CAC8C2A8...69F4D`。启动定位现改为主窗体就绪后使用`SW_SHOWNA`短暂建立可访问控件树，该API显示但不激活窗口、不抢键盘焦点；找到按钮后立即重新最小化。

## 2026-08-19：旧跑法前500轮临时对比

- 状态：按用户最新要求停止5000轮长测，只比较未改造版已有重跑数据的前500轮与改造版新跑的500轮；本记录是临时诊断，不替代计划书规定的新旧版本各5000轮正式验收。
- 同口径条件：两版均使用原始方案母本，SHA256=`CAC8C2A8F3E1CF1489AE0A8AB1A663CBD5291F11F3B01D3496A1270CAE369F4D`；窗口模式`Normal`、进程优先级`Normal`、UI完成轮询5ms，预热1轮不计入正式数据。
- 数据来源：未改造版取`Tests\Results\FullSolutionComparison\20260819-221646-未改造版正式重跑-5000轮.csv`的前500行；改造版使用`20260819-225156-改造版正常前500轮-500轮.csv/json`的完整500轮。
- 原始整轮耗时：未改造版总耗时281066.462ms、吞吐1.778939轮/秒、P50 548.726ms、P95 664.994ms、P99 704.586ms、最大762.740ms；改造版总耗时174065.014ms、吞吐2.872490轮/秒、P50 348.302ms、P95 388.534ms、P99 406.256ms、最大416.158ms。
- 表面差值：总耗时-38.07%、吞吐+61.47%、P50 -36.53%、P95 -41.57%、P99 -42.34%、最大值-45.44%；这些差值不能认定为框架优化收益，因为两版实际完成的业务负载不一致。
- 功能一致性核对：未改造版前500轮的同心度、正面、侧面左、侧面右均为500成功、0失败；改造版同心度和正面均为500成功、0失败，但侧面左和侧面右均为0成功、500失败，因此完整四流程严格成功轮数为0/500。
- 失败根因：改造版程序根目录`bin\x64\Debug`缺少`device.license`，启动日志明确记录许可证同步跳过，节点133和181均因CPU模型License初始化失败而提前结束。未改造版根目录存在731字节许可证，并成功同步到AI子目录，两个侧面AI节点正常运行。
- 资源数据：未改造版进程树为主程序加2个AI工作进程，采样进程数始终3、私有内存峰值2502.301MB、CPU平均33.993%；改造版因AI模型未启动，采样进程数始终1、私有内存峰值1411.453MB、CPU平均42.835%。进程数和工作负载不同，内存下降43.59%同样不能作为资源优化结论。
- 通信结果：改造版仍记录2000次Modbus成功、0次失败；这只能证明本轮通信节点执行，不能抵消两个侧面流程全部失败。
- 退出与输入恢复：500轮结果落盘后主窗口已经消失，但主进程未自行退出且没有可见顶层窗口，最终执行强制清理；桌面方案已恢复并复核为原始SHA256，当前不再继续启动测试。
- 结论：本轮发现的是运行依赖缺失和退出残留两个稳定性问题，不是有效的改造前后性能结论。下一次具备可比性的500轮测试必须先保证改造版使用与未改造版相同且有效的`device.license`，并以四流程每轮全部完成作为硬性有效条件。

## 2026-08-19：旧界面与有效许可证500轮纠正重测

- 状态：已按用户纠正要求重新执行500轮；上一轮缺少许可证且左右侧面流程全部失败的数据继续保留为失败证据，但不再参与性能结论。本轮仍是用户指定的前500轮临时对比，不替代新旧版本各5000轮正式验收。
- 许可证校准：把未改造版根目录的`device.license`原样复制到改造版`bin\x64\Debug`根目录，复制前后SHA256均为`19C1E84F1C23E3D9BDD9611EEC271135459208EC2B982A1D674436CEE8AB67F2`；启动后主程序成功同步许可证并建立2个AI工作进程，进程树全程为3个进程。
- 界面校准：把未改造版`DockPanel.config`和`DockPanelImageWindows.config`原样复制到改造版运行目录，SHA256分别一致为`8B2B92DA48F21F0AE0845A1540E98318B2AFC5EC67F7BBE37923BC4570EE25E0`和`21EFE7BFD0551FDBE1B8A3ACCF21B7929DE1E81141A74450F3CC435C2B159312`。主显示区只使用图像视图，检测结果与Logo隐藏，日志保持旧版自动折叠状态；16个图像窗体中前6个显示、其余10个隐藏。
- 同口径条件：两版使用同一原始方案SHA256=`CAC8C2A8F3E1CF1489AE0A8AB1A663CBD5291F11F3B01D3496A1270CAE369F4D`，窗口模式`Normal`、进程优先级`Normal`、UI完成轮询5ms，预热1轮排除；本机虚拟Modbus运行于`127.0.0.1:502`。
- 功能结果：改造版500个完整轮次全部成功、失败0、异常日志0；正面、同心度、侧面左、侧面右四个流程均为500成功、0失败；节点133和181的AI推理均正常执行；Modbus成功日志2000、失败0。
- 有效整轮对比：未改造版前500轮总耗时281066.462ms、吞吐1.778939轮/秒、P50/P95/P99/最大值为548.726/664.994/704.586/762.740ms；改造版为260946.546ms、1.916101轮/秒、513.016/609.870/655.885/702.962ms。改造版总耗时下降7.16%、吞吐提升7.71%、P50下降6.51%、P95下降8.29%、P99下降6.91%、最大值下降7.84%。
- 分流程对比：正面P50/P95由531/646ms降至495/588ms；同心度由125/162ms降至114/131ms；侧面左由369/456ms降至340/414ms；侧面右由337/415ms降至310/386.05ms。四个流程的P50和P95均改善，没有通过跳过AI负载换取耗时下降。
- CPU与内存：CPU平均值由33.993%降至33.416%，下降0.577个百分点；两版进程树均为3个进程。未改造版前500轮私有内存采样峰值2502.301MB、末值1437.656MB，改造版峰值2042.633MB、末值1304.832MB，分别下降18.37%和9.24%；改造版托管堆峰值979.082MB、末值443.696MB。
- 图形与句柄：未改造版GDI采样峰值831、句柄峰值2976；改造版GDI峰值832、句柄峰值2857。GDI峰值仅增加1个且结束值831，没有线性爬升；句柄峰值下降4.00%。
- 结果文件：`Tests\Results\FullSolutionComparison\20260819-232729-改造版旧界面许可证有效500轮-500轮.csv/json`；原始逐轮数据、检查点和四流程日志均保留。
- 停止清理：500轮完成后主程序和2个AI工作进程均正常退出，虚拟Modbus已停止，桌面方案恢复并复核为原始SHA256；许可证和两份布局配置在测试完成后仍与未改造版哈希一致。
- 结论：在本机、同方案、同许可证、同6窗口显示、同通信和四流程全部成功的500轮条件下，当前改造版同时取得约6.5%～8.3%的整轮分位耗时改善、7.71%的吞吐提升和更低的私有内存峰值。该结论对本次前500轮有效，但样本规模仍不足以替代计划书的5000轮长尾与长期稳定性验收。

## 2026-08-20：性能改造步骤6第二小批保存磁盘保护

- 状态：低空间、严重低空间、慢盘和保存队列诊断代码、专项故障注入、常规压力及完整编译均已完成；500次固定50ms真实流程触发尚未执行，步骤6整体仍在进行。
- 改动前版本：`ca64af6`，旧界面、有效许可证和四流程全部成功的500轮纠正重测已经形成独立恢复点。
- 关键参数：`SaveDiskLowSpaceMb=2048`、`SaveDiskCriticalSpaceMb=512`、`SaveSlowThresholdMs=500`、`SaveDiskProbeIntervalMs=1000`；参数进入自动资源档案，后续可绑定系统设置控件。
- 保存前准入：共享保存消费者在`Mat→Bitmap`前检查任务全部目标磁盘；低于低水位时普通图明确跳过并释放租约、NG图继续保存；低于严重水位时全部图片停止保存；空间探测失败只告警并继续，避免监控故障误停产。
- 慢写诊断：按单个目标目录统计准备、JPEG编码和原子发布总耗时；达到阈值后低频记录队列现场。诊断快照同时公开队列任务和字节边界、活动消费者、拒绝、淘汰、低空间降级、探测失败、慢写次数及最慢写入；工作池停止写一条不刷新日志窗口的汇总。
- 资源档案接入：`Solution`创建真实共享保存池以及空闲重配时都应用并比较四项磁盘参数；继续保持固定保存消费者，不修改全局线程池、CPU亲和性、AI并发、PLC位置或用户可编辑流程顺序。
- 磁盘策略专项：生产`CachedImageSaveStorageGuard`的正常放行、同盘缓存、低空间普通图跳过、低空间NG保留、严重空间停止NG、探测失败放行和慢写阈值检查全部通过。
- 可控慢盘压力：固定写入前延迟100ms、慢写阈值50ms、队列容量4、消费者1、连续尝试20张；队列峰值不超过4、并发不超过1并出现明确拒绝，所有完成任务均记录慢写且最慢不少于100ms，写盘失败0，20份Mat租约全部释放、重复释放0，工作线程全部退出。
- 常规保存压力：三轮各由四流程并发尝试60张`1024x768`图片，接收17/18/19、拒绝43/42/41、淘汰普通均5、完成12/13/14、失败均0；队列峰值8/8、字节峰值18MB/128MB、并发峰值2/2、入队P99为0.125/0.091/0.072ms、停止排空32/37/39ms，全部180份租约释放且重复释放0。
- 资源结果：常规压力前后私有内存92.50→92.56MB，变化`+0.07MB`；托管内存9.47→9.40MB，变化`-0.07MB`；句柄872→872，变化0。
- 硬件档案：本机16物理核心、24逻辑线程、16087MB总内存、测试时可用2122MB；四流程四相机建议CPU重任务4、图像转换4、保存线程2、流程图像预算530MB、保存队列预算212MB、容量8，AI策略仍为`FollowEditableFlow`。
- 回归与编译：保存架构、生产磁盘保护、硬件资源档案及真实保存工作池压力通过；`Debug|x64`完整编译成功，0错误、171个项目既有警告。
- 证据边界：本批使用确定性空间探针和100ms慢写注入，证明策略、资源边界与回收正确，不代表所有真实磁盘故障形态；完整方案固定50ms触发和计划书5000轮正式同比仍未完成。
- 下一步：以500次触发请求、固定50ms间隔运行真实方案压力，分别报告触发请求、实际准入、忙碌拒绝或合并、完成数、四流程结果、耗时分位和资源峰值，禁止把50ms误写成界面轮询周期。

## 2026-08-20：性能改造步骤6第三小批固定50ms触发工具

- 状态：单方案触发准入、Debug窗口消息入口、绝对50ms节拍测试器、旧版运行依赖硬预检、动态测试和完整编译已完成；真实冒烟与500次正式压力尚未执行。
- 改动前版本：`9045696`，低空间、严重低空间、慢盘和保存队列诊断已经形成独立恢复点。
- 关键参数：`FixedTriggerAttempts=500`、`FixedTriggerIntervalMs=50`、`SolutionRunAdmissionCapacity=1`、`SolutionRunBusyPolicy=RejectAndCount`、`TriggerMessageTimeoutMs=5000`、`FixedTriggerWarmupRounds=1`。
- 关键口径：50ms表示测试器每50ms真实发出一次完整方案触发请求，不是等待按钮完成的轮询周期；500表示请求500次，不伪造为必然完成500轮。
- 准入策略：同一批可编辑流程对象同时只允许1轮完整方案运行；忙碌请求立即拒绝并累计，不阻塞触发线程、不建立等待队列、不合并工件触发，也不改变流程中的AI推理、通信和PLC节点顺序。
- 运行租约：成功、异常和重复完成路径都幂等归还运行权；固定容量保留最近完整方案墙钟耗时，汇总请求、准入、忙碌拒绝、完成、异常、P50/P95/P99和最大值；活动轮次期间禁止重置，预热完成且空闲后才清零。
- 测试入口：仅`Debug`构建并同时设置`TDJS_VISION_FIXED_TRIGGER_STRESS=1`时处理窗口消息，发布版本不开放；每个消息使用5000ms超时，消息超时不计作忙碌拒绝而是直接判定整轮无效。
- 固定节拍：测试器用绝对计划时刻发送，逐次记录计划时间、实际投递时间、节拍漂移、窗口消息耗时和准入结果，避免简单`Sleep(50)`把执行开销逐轮累加成漂移。
- 运行硬预检：固定模式必须指定旧版运行目录，自动复制并核对许可证、主Dock布局和图像窗口布局SHA256；校验图像视图显示、检测结果与Logo隐藏、日志右侧自动折叠、6个图像窗体显示，并在预热后要求主程序和2个AI进程共3个进程。
- 业务验收：最终程序准入计数必须与外部消息结果一致，全部准入轮次必须退出且异常0；四个流程必须分别成功`准入数`次，Modbus成功必须等于`准入数×4`、失败0，任一不一致都拒绝生成有效结论。
- 回归结果：固定触发架构、准入器动态测试、方案资源释放、保存队列架构、保存磁盘保护和PowerShell语法共6项检查通过。准入器已验证忙碌立即拒绝、请求闭合、租约重复完成只计一次、Dispose按异常归还、耗时样本容量3不突破以及空闲清零。
- 编译结果：`Debug|x64`最终编译成功，0错误；本批没有修改WinForms设计器或增加界面控件。
- 当前运行阻塞：发现Visual Studio正在调试另一套路径`D:\MyCode\PublicWook\四芯线软件\五芯线软件\WindowsFormsApp1\WindowsFormsApp1`下的视觉程序，父进程为`devenv.exe`且VS标题显示“正在运行”。该进程不是本项目测试遗留，未擅自结束；并行运行会占用同名单实例锁并污染CPU/内存数据。
- 下一步：等另一套调试程序自然退出后，先运行少量固定50ms真实冒烟；冒烟通过后按相同旧版许可证、旧版Dock布局、6窗口、原始方案和虚拟Modbus条件发送500次正式触发。

## 2026-08-20：性能改造步骤6固定50ms正式500次压力

- 状态：另一套由Visual Studio调试的视觉程序在测试前已自行退出；没有结束Visual Studio。随后完成20次冒烟、100次节拍校准和500次正式压力，正式结果通过测试工具全部硬校验。
- 正式条件：`Debug|x64`提交`32c5407`产物，正常窗口、正常进程优先级、预热1轮、请求500次、绝对计划间隔50ms；使用原始方案SHA256=`CAC8C2A8F3E1CF1489AE0A8AB1A663CBD5291F11F3B01D3496A1270CAE369F4D`、旧版许可证、旧版Dock布局、6个图像窗口和本机`127.0.0.1:502`虚拟Modbus。
- 触发结果：500次请求全部投递，准入47、忙碌即时拒绝453、窗口消息超时0、准入率9.4%；47次准入全部完成，运行异常0，没有方案重入、等待排队或积压。
- 节拍结果：首末实际投递跨度24957.699ms；499个相邻间隔平均50.015ms、P50 49.350ms、P95 61.278ms、P99 120.428ms、最小0.389ms、最大248.280ms，其中366个间隔位于40～60ms。绝对调度漂移P50/P95/P99/最大值8.648/20.723/79.572/200.448ms，消息投递P95/最大值1.557/13.701ms。
- 业务结果：正面、同心度、侧面左、侧面右均为47成功、0失败；Modbus成功188、失败0；异常日志0。完整方案墙钟P50/P95/P99/最大值为519/627/640/640ms；正面、同心度、侧面左、侧面右P50分别为510/114/341/308ms。
- 资源结果：本机24逻辑线程，CPU平均30.652%、采样峰值53.559%；三进程私有内存起始/采样峰值/结束1183.941/1587.465/1269.219MB，托管堆384.999/598.413/488.107MB，GDI 811/820/820，句柄2534/2778/2778。
- 结果文件：`Tests\Results\FullSolutionComparison\20260820-004538-改造版固定50ms正式500次-500次固定触发.csv/json`及对应检查点文件；逐次计划时刻、实际投递、漂移、消息耗时、准入结果和资源快照均保留。
- 清理复核：视觉主程序和2个AI进程正常退出；本轮临时Modbus服务已停止，只保留用户原先运行的HSL模拟器。桌面方案以及运行目录许可证、主Dock布局、图像窗口布局SHA256均与测试前一致。
- 结论：框架在20次/秒输入压力下通过容量1准入保持稳定，未出现超时、异常或资源无界堆积；但当前完整方案P50为519ms，约只能承接1.9次/秒，因此不能把500个50ms工件触发全部执行。453次是明确的过载拒绝，不是流程超时。若现场必须逐件承接50ms节拍，需要继续降低节点耗时或增加真正独立的处理通道，不能通过放大等待队列掩盖处理能力差距。

## 2026-08-20：新旧版本500轮进程I/O同比工具

- 状态：已完成；跨版本进程I/O工具、专项验证、完整编译、旧版500轮、当前版500轮、结果同比和清理复核均已完成。
- 改动前版本：`775877e`，固定50ms正式500次结果和结论已经形成独立恢复点。
- 固定参数：新旧版本各预热1轮、正式串行500轮、正常窗口、正常进程优先级、UI完成轮询5ms、同一原始方案、同一有效许可证、同一旧版Dock布局、6个图像窗口和同一本机虚拟Modbus。
- 跨版本指标：测试器使用Windows `GetProcessIoCounters`从外部采集主程序和两个AI进程的读写操作、读写传输字节、每轮增量分位、每轮平均量及MB/s；要求三个进程计数器全部可读且正式轮次内进程ID不变。
- 口径边界：进程I/O可以排除其他程序的流量，但包含文件、设备和管道等I/O，不直接等于物理盘独占读写；新版保存队列诊断单独列出，不与旧版无采集能力的内部指标做虚假百分比。
- 工具验证：动态创建并完整读回4MB测试文件，读写操作数和读写传输字节均按预期增加；测试器与专项脚本语法通过。进程I/O、固定50ms架构、方案准入、保存队列架构、磁盘保护和方案释放共6组检查通过；`Debug|x64`完整编译1.65秒、0警告、0错误。
- 正式有效性：旧版结果`20260820-010928-未改造版进程IO正式同比-500轮.json/csv`，当前版结果`20260820-011538-当前新版进程IO正式同比-500轮.json/csv`；两版均预热1轮后正式500轮，四流程各500/500成功、Modbus 2000/2000成功、异常0，三个进程全程PID稳定且I/O计数器覆盖完整。
- 进程I/O同比：旧版累计读取9802.834MB、写入9827.866MB，每轮读取20076.203KB、写入20127.470KB；当前版累计读取9802.701MB、写入9827.840MB，每轮读取20075.932KB、写入20127.416KB。总量差异小于0.002%，判定持平；当前版每轮写入P95/P99/最大值下降0.82%/3.27%/3.82%，写入长尾略稳定。
- CPU与内存同比：CPU平均34.699%降至33.189%，下降1.510个百分点；三进程私有内存峰值2471.508MB降至2051.371MB，下降17.00%，结束值下降4.76%；主进程私有内存峰值下降28.87%、结束值下降11.78%。托管堆峰值增加1.94%、结束快照增加18.58%，受GC时点影响不能直接判定泄漏，但必须在长稳测试继续观察。
- 图形资源同比：GDI峰值837降至823、结束823降至822；句柄峰值2896降至2814、结束2886降至2786；没有看到新版GDI或句柄回退。
- 耗时背景：旧版整轮P50/P95/P99/最大值551.622/637.389/674.139/765.688ms，当前版534.162/615.439/642.834/665.613ms，下降3.17%/3.44%/4.64%/13.07%。用户已确认约500ms属于方案本身正常检测时间，本记录只用于同负载资源同比，不将其列为框架故障。
- 磁盘结论：当前版没有增加总体进程I/O量，写入长尾略有收敛；但Windows进程I/O主要包含AI通信等流量，两版测试日志又都没有保存队列活动，不能据此宣称物理图片保存更快。真实图片落盘同比需要使用明确包含保存节点、固定图片数量和固定输出目录的独立方案。
- 清理复核：新旧主程序和AI进程均正常退出；桌面方案SHA256恢复为`CAC8C2A8F3E1CF1489AE0A8AB1A663CBD5291F11F3B01D3496A1270CAE369F4D`，当前版许可证和两份旧版布局哈希均保持一致，用户原有HSL虚拟Modbus未停止。

## 2026-08-20：性能改造步骤6真实图片落盘收尾

- 状态：已完成；新旧同工作量真实落盘、当前版生产容量突发、同进程长稳回收、专项回归、完整编译和结果分析全部完成。
- 改动前版本：`58e98cf`，新旧版本各500轮进程I/O及资源同比已经形成独立恢复点。
- 关键参数：确定性噪声图`1024x768 CV_8UC3`、随机种子20260820、JPEG质量90、预热20张、正式500张、保存线程4、当前版同工作量容量512、硬超时120秒；当前版生产边界另用容量8、保存线程2、500次突发。
- 同比设计：隔离宿主直接调用对应版本生产`ImageQueueProcessor`。旧版是每节点4线程无界队列；当前版同工作量测试临时给足容量和字节预算，保证两版都实际保存500张，再单独恢复生产小容量验证拒绝、淘汰和释放，避免把安全边界与保存编码速度混为一谈。
- 文件验收：逐个JPEG解码并核对`1024x768`尺寸，全部文件计算SHA256；同时校验文件数、空文件、`.saving`残留、写入总字节、进程I/O、保存墙钟、文件吞吐、队列峰值、工作线程峰值、租约释放、私有/托管内存、GDI和句柄。
- 证据边界：生产`Image.Save`完成表示文件已经交给操作系统缓存，不代表调用了强制刷盘或达到断电安全；结果统一描述为“文件保存链路吞吐”，不冒充物理介质持久化时延。
- 工具冒烟：旧版和当前版各20张均全部接收、全部生成有效JPEG，文件大小和首图SHA256完全一致；当前版队列峰值20、工作线程峰值4、20个租约全部释放且无重复释放。
- 回归验证：`Debug|x64`整库编译0警告0错误；真实落盘架构、保存队列架构、磁盘保护、方案资源释放和保存工作池压力共5组检查通过。工作池三轮共180次资源租约全部释放，私有内存变化`+0.08MB`、托管内存变化`-0.07MB`、句柄变化`+1`。
- CPU补测：正式500张执行前补齐10毫秒CPU采样，按`Environment.ProcessorCount`逻辑线程数归一化，结果同时输出平均CPU、采样峰值CPU和归一化逻辑线程数；新旧各20张冒烟均在24个逻辑线程口径下成功出数。
- 长稳设计：现有保存工作池压力工具增加可调正式轮数、每轮尝试数和JSON结果路径；正式使用同一宿主进程执行10轮、每轮500次四流程并发投递，累计5000次，逐轮强制GC后记录私有内存、托管内存和句柄，并汇总CPU、写盘字节、队列峰值、线程峰值、入队长尾和租约释放。
- 长稳冒烟：同一进程2轮、每轮20次通过；40次租约全部释放，队列峰值8、工作线程峰值2、失败0，强制回收后私有内存`+0.059MB`、托管内存`-0.027MB`、句柄0增长。
- 耗时口径复核：首轮500张结果发现旧版`StopProcessing`等待被合并进原保存墙钟，会夸大图片编码/写盘差距。正式结论改为分别记录“全部JPEG完成墙钟”“工作池停止墙钟”和两者总耗时，文件吞吐只按JPEG完成墙钟计算，首轮数据仅作工具校准不进入最终同比。
- 最终同工作量：旧版和当前版均完成500/500张有效JPEG，总字节均为337,698,500，SHA256一致，空文件、无效图和临时文件均为0。文件完成耗时6517→1559ms，下降76.08%；吞吐49.418→206.578MB/s，提升318.02%；平均CPU 17.234%→15.736%，下降1.498个百分点；私有内存峰值117.809→108.555MB，下降7.86%。
- 最终瞬时资源：托管内存峰值21.891→25.912MB，增加4.021MB；句柄峰值898→926，增加28个。强制GC后托管内存只增加0.090MB、私有内存降低4.504MB、结束句柄减少9个、GDI峰值及结束值不变，必须结合长稳结果判断而不能单看峰值。
- 最终生产边界：2线程、容量8、500次突发中接收10、拒绝490、完成10、失败0，队列峰值8、字节峰值18MB、线程峰值2；500个租约全部释放，重复释放0。
- 最终长稳：同一进程10轮×500次共5000次，接收516、拒绝4484、淘汰237、完成279、失败0；5000个租约全部释放，队列/字节/线程峰值8/18MB/2，入队P99最差0.0693ms、最大0.1732ms，平均CPU 16.057%。强制GC后私有内存`-0.031MB`、托管内存`-0.009MB`、句柄`-1`，逐轮无累积上升。
- 完成结论：步骤6按用户确认的500轮阶段同比口径完成；原计划完整方案5000轮已由用户明确停止，保留到步骤7～9全部结束后的最终验收，不阻塞进入步骤7。
- 最终验收：`Debug|x64`整库编译0警告0错误；真实落盘架构、保存队列架构、磁盘保护、方案资源释放和保存工作池压力5组回归全部通过。原始JSON重新解析并交叉校验，500张同工作量哈希、容量8生产边界和5000次长稳均合格。

## 2026-08-20：性能改造步骤7第一小批CPU调度基础设施

- 状态：已完成；当前小批只建立调度器、动态策略、诊断快照和专项模拟测试，不接入业务节点，不改变流程顺序、线程池、CPU亲和性或AI执行方式。
- 改动前版本：`36fe494`，步骤6真实落盘和长稳验收已经形成独立恢复点。
- 关键参数：自动最大并发读取`CpuHeavyMaxConcurrency`，图像转换上限读取`ImageConvertMaxConcurrency`，最小并发1；高/恢复水位85%/65%，持续5秒/10秒，评估周期5秒，冷却30秒，调整步长1，等待耗时样本容量2048。
- 结构决定：全方案只有一份CPU重任务额度，图像转换是该额度内的分类上限，不与传统算法或图片编码额度相加；统一等待队列允许跳过暂时受分类上限阻塞的任务，避免队首阻塞。
- 生命周期：等待支持取消；调低额度只阻止新任务开始，不中断正在运行节点；调度器停止时取消全部等待，已经取得许可的任务可以正常释放；许可重复释放必须幂等。
- AI边界：`CpuWorkloadKind`只包含传统视觉算法、图像转换和图片编码，明确不定义AI推理类型；AI节点继续严格按照可编辑流程执行。
- 硬件复测：Intel i7-14650HX，16物理核心、24逻辑线程、16087MB总内存、启动可用2370MB；四流程四相机自动建议CPU重任务4、图像转换4、保存线程2，动态参数85%/65%、5秒/10秒、评估5秒、冷却30秒、步长1全部进入档案。
- 确定性压力：1000/1000个混合任务完成并释放，全局活动峰值4/4、测试图像转换分类峰值2/2；分类队首受限时后续图片编码正常前进，取消等待正常收敛；高CPU模拟并发4→3→2，恢复条件满足后回到3。
- 耗时与资源：1000任务许可等待P50/P95/P99/最大值363.366/697.587/727.487/732.153ms，该值来自996个任务瞬时排队并各持有2ms的故意饱和测试，不是生产节点耗时；运行时预热后私有内存净变化-0.531MB、托管内存+0.005MB、句柄+8，最终活动0、等待0。
- 编译与回归：`Debug|x64`完整编译5.29秒、0错误、171个既有警告；CPU调度器、硬件档案、方案触发准入和保存队列架构检查全部通过。
- 下一步：形成独立Git恢复点后进入第二小批，把调度器接入真实流程执行链和明确的CPU重任务；AI、UI、相机回调及通信节点保持不进入CPU许可池，再做四流程和高CPU保护测试。

## 2026-08-20：性能改造步骤7第二小批生产执行链接入

- 状态：已完成；生产节点统一执行入口、传统CPU节点分类、显示转换许可、全机CPU低频采样和动态额度保持均已接入并通过专项测试，保存编码尚未进入本小批。
- 改动前版本：`c81ba8b`，步骤7第一小批调度基础设施和1000任务压力已经形成独立恢复点。
- 关键参数：全机CPU采样周期读取自动档案，本机5秒；流程CPU重任务最大4、最小1；图像转换最大4；高/恢复水位85%/65%，持续5秒/10秒，冷却30秒，步长1；UI仍按窗口独立30FPS。
- 执行链：连接图顺序、连接图并行、静态流程运行、实例流程运行、相机回调流程和调参更新流程共六条节点路径统一调用`RunNodeWithCpuResourcePolicy`，资源许可只控制节点开始时间，不更改用户编辑的节点顺序和分支语义。
- 分类结果：图像预处理、裁剪/旋转/分割、传统检测、模板匹配、测量、图像绘制等28类节点进入传统CPU算法许可；TDAI、无监督、大模型、PLC/Modbus/TCP、相机、显示节点外壳、保存入队和组合模块等16类明确直通。组合模块不持有外层许可，内部流程按真实节点分别受控。
- 显示隔离：二维和三维图像显示均先执行窗口帧率门控，只有获准显示的帧才取得图像转换许可；许可只覆盖`Mat→Bitmap`，发布到UI事件前已经释放，因此UI排队或绘制不会占住CPU重任务额度。
- 动态监控：后台`System.Threading.Timer`使用Windows`GetSystemTimes`读取全机CPU累计时间差，不占用UI消息循环，不调用WMI和性能计数器；专项实测有效CPU样本17.78%。相同自动档案重复刷新时动态并发保持3→3，没有被重置回4。
- 压力与回归：调度器1000/1000任务完成，全局峰值4、转换峰值2，私有内存-1.340MB、托管内存+0.005MB、句柄+7；分类28/28、排除16/16；连接图顺序等待、相机回调快速路径和UI帧率/Bitmap所有权通过。UI四线程同步突发100000次耗时8ms，各窗口接收1/1/1/1。
- 编译结果：`Debug|x64`完整编译4.46秒，0错误、170个既有警告。
- 下一步：提交本小批恢复点后，把保存工作池的`Mat→Bitmap`和JPEG编码纳入共享CPU额度；编码与磁盘写入分段，禁止慢盘等待长期占住CPU许可，再执行四流程和高CPU保护压力。

## 2026-08-20：性能改造步骤7第三小批保存编码分段

- 状态：已完成；保存取图转换、JPEG编码、磁盘路径准备和文件写入已按资源类型拆开，专项压力、慢盘隔离、资源释放、完整编译和关联回归全部通过。
- 改动前版本：`1ef416c`，步骤7第二小批生产执行链接入已经形成独立恢复点。
- 关键参数：本机全局CPU重任务上限4、图像转换上限4、保存线程2、保存队列容量8、保存租约预算128MB；测试JPEG质量90，正式压力使用`1024x768`图片执行3轮×60次四流程并发投递，慢盘隔离使用单张`128x128`图片、双目录、每个目录固定100ms路径准备。
- 保存分段：`CreateBitmapForSave`只在图像转换许可内执行，`EncodeBitmapToJpeg`只在图片编码许可内执行；`PrepareWrite`、目录创建、临时文件写入和原子提交全部位于CPU许可之外，磁盘阻塞不能占住传统视觉节点需要的全局CPU额度。
- 编码复用：一张图片只编码一次，随后把同一段JPEG字节写入全部目标目录；每个固定保存工作线程独占并复用一个`MemoryStream`编码缓冲区，避免每轮重新创建大对象缓冲区，缓冲区数量始终受保存线程数限制。
- 慢盘隔离证据：单图双目录只取得1次图像转换许可和1次图片编码许可；两个100ms路径准备点记录到的CPU活动数均为0。两个输出文件均可解码为`128x128`，文件字节逐字节一致，临时文件没有残留。
- 保存压力结果：共尝试180次，接收54、拒绝126、淘汰普通13、完成41、失败0、有效JPEG 41；队列峰值8/8、字节峰值18MB/128MB、工作线程峰值2/2。180个Mat租约全部释放，重复释放0；入队P99最大1.531ms，压力墙钟345ms，平均CPU 6.963%。
- 资源结果：强制回收后私有内存91.945→92.066MB，变化`+0.121MB`；托管内存10.644→9.954MB，变化`-0.690MB`；句柄872→872，变化0。三轮资源快照没有持续爬升。
- CPU调度回归：1000/1000个混合任务完成，全局峰值4/4、转换峰值2/2，最终活动0、等待0；传统CPU节点28/28、明确直通16/16，全机CPU采样有效且相同配置重配保持动态额度3→3。
- 编译与关联回归：`Debug|x64`完整编译3.86秒、0错误、170个既有警告；保存队列结构、保存工作池压力、CPU调度、CPU生产接入、磁盘保护和方案资源释放全部通过。
- 结论：保存链的CPU工作和磁盘等待已经有明确资源边界，慢盘不会长期占用CPU许可，多目录保存不会重复编码；本批不改变AI并发、流程节点顺序、相机回调和通信方式。
- 下一步：提交本小批恢复点后进入步骤7第四小批，运行四流程并发与可控高CPU压力，验证动态降额/恢复期间UI、相机回调、PLC/Modbus通信和节点顺序不被饿死，并分别报告业务耗时、许可等待、CPU、内存、句柄和队列峰值。

## 2026-08-20：性能改造步骤7第四小批第一轮测试基础与VX审查

- 状态：测试基础已完成，VX审查发现1项高风险停止路径问题，第四小批尚未完成；必须先修复并补回归，再运行真实方案500轮。
- 改动前版本：`cd9a663`，步骤7第三小批保存编码与磁盘等待分段已经形成独立恢复点。
- 测试参数：四条流程、每个动态额度阶段每流程50个CPU节点，节点资源类型循环覆盖传统视觉、图像转换和JPEG编码；本机生产口径最大并发4、最小1、图像转换4，高/恢复水位85%/65%，持续5秒/10秒，冷却30秒，步长1。
- 动态结果：额度轨迹`4→3→2→3→4`，降额时已经开始的4个节点没有被取消；额度3/2/3/4阶段活动峰值精确为3/2/3/4，每阶段四流程共200个节点全部完成且各流程节点顺序严格递增。累计许可/归还804/804，结束活动0、等待0，等待P50/P95/P99/最大值0.034/1.011/1.029/1.140ms。
- 控制通道结果：与CPU压力同步运行的UI发布、相机回调、PLC/Modbus通信探针最大间隔18.156/18.156/18.158ms，均持续收到回调；压力墙钟354ms，私有内存`+0.184MB`、托管内存`+0.002MB`、句柄`+2`。
- 四窗口复核：旧脚本原先在PowerShell 7加载.NET Framework程序集时缺少正确运行时入口，已统一转交Windows PowerShell并补基础程序集引用。修复后3轮×4窗口×30帧共360帧全部获准并转交窗口，`1024x768`转换P95为1.45～1.99ms、最大2.32ms；最终私有内存`+0.56MB`、托管内存`+0.66MB`、GDI 0增长。
- 关联回归：相机回调快速路径、通信节点完整耗时关联均通过；当前确定性压力只模拟CPU采样值，不通过烧满24个逻辑线程制造85%系统负载，避免影响用户桌面工作。
- VX审查结论：动态降额与恢复、显示Bitmap所有权、JPEG缓冲复用、多目录失败回滚均未发现新问题，也未发现AI、相机、UI或通信节点被误限流；但保存工作线程在任务出队并持有Mat租约后，使用`CancellationToken.None`等待CPU许可，CPU额度长期占满时可能导致`StopProcessing`和方案重置超时。
- 修复要求：给保存工作池建立独立停止令牌并传入转换、编码许可等待；新增“先占满全部CPU许可，再投递保存任务并停止工作池”的回归，硬校验等待被取消、停止成功、CPU许可无泄漏、已出队Mat租约释放且没有后台线程残留。
- 下一步：先提交本轮测试基础恢复点，再修复VX发现并完成专项回归；修复通过后才运行最小化、低优先级、6图像窗口的真实四流程500轮。

## 2026-08-20：性能改造步骤7第四小批VX审查问题修复

- 状态：已完成；VX发现的保存CPU许可不可取消问题已经修复，饱和停止、正常保存、四流程CPU保护、调度器压力、方案资源释放和完整编译全部通过。
- 改动前版本：`6eccb0b`，第四小批四流程确定性压力、四窗口复核和VX只读审查已经形成独立恢复点。
- 停止策略：保存工作池新增独立`CancellationTokenSource`，转换和编码许可均使用该令牌；正常停止先保留绝大部分预算排空现有保存，只有工作线程未能退出时才在最后最多500ms取消CPU等待并释放尚未开始任务，避免把正常关闭误变成立即丢弃。
- 诊断语义：CPU等待被停止取消的任务单独累计`CanceledTaskCount`并显示为“停止取消”，不计入保存完成或保存失败；已开始磁盘写入的任务不被强行中断，仍受原停止总预算和慢写诊断约束。
- 饱和回归设计：生产`CpuWorkScheduler`最大并发1，外部传统CPU任务先占满唯一许可；随后投递1张`128x128`图片，硬等待到保存消费者已出队、活动数1且CPU等待数1，再调用1000ms预算的`StopProcessing`。
- 饱和回归结果：停止耗时499ms；保存完成/失败/取消0/0/1，CPU许可/归还/等待取消1/1/1，Mat租约释放1、重复释放0，输出文件0；最终保存队列0、CPU活动0、CPU等待0、保存和淘汰回收线程全部退出。
- 正常保存复核：3轮×60次共180次四流程并发投递，接收55、拒绝125、淘汰普通15、完成40、失败0；队列/字节/线程峰值8/18MB/2，180个Mat租约全部释放。入队P99最大1.217ms，墙钟250ms，平均CPU 7.539%；私有/托管内存变化`+0.113/-0.002MB`，句柄0增长。
- 其他回归：四流程动态额度继续按`4→3→2→3→4`变化，804/804许可归还，UI/相机/通信最大间隔18.461/18.464/18.462ms；CPU调度1000/1000任务完成，保存队列结构和方案资源释放通过。
- 编译结果：`Debug|x64`完整编译3.83秒，0错误、170个既有警告。
- 下一步：提交本修复恢复点后，按旧版许可证、旧版Dock布局、6图像窗口、原始方案和本机Modbus，使用最小化、不抢焦点、低优先级方式运行当前版真实四流程500轮，报告耗时、CPU、内存、句柄、GDI、I/O和通信结果。

## 2026-08-20：性能改造步骤7第四小批VX压测工具审查修复

- 状态：VX只读复审确认压测关闭和串行进程树校验存在2项P1；原生确认框识别、关闭失败硬门槛、三进程逐轮硬校验及成功/失败路径实测均已完成。
- 改动前版本：`459d934`，该版本保留了按UI Automation扫描进程控件树的校准方案；实测证明自定义关闭确认框没有进入该控件树，因此不能可靠找到“确定”按钮。
- 关闭定位：改为通过Win32原生窗口枚举按进程ID、顶层标题“提示”、正文“确认关闭程序？”和按钮文字“确定”四重锁定；使用带1000ms超时的窗口消息点击，避免误点其他同名按钮或UI线程无响应时测试器无限阻塞。
- 关闭硬门槛：关闭请求每1秒允许重试，主程序退出后继续等待同路径AI工作进程；主程序或任一工作进程残留时，测试脚本必须返回失败，不能把已经生成的JSON误当成有效同比。
- 串行进程硬门槛：正式开始、每一轮资源采样、正式结束I/O快照和最终资源快照均要求进程树恰好为配置值，本次口径为主程序加2个AI进程共3个；同时保留进程ID稳定和I/O计数器完整校验。
- 成功路径实测：最小化、低优先级、6图像窗口、原始方案执行1轮，四流程各1次成功、Modbus 4次成功、异常0；脚本退出码0，主程序和AI残留0，桌面方案恢复到原始SHA256=`CAC8C2A8F3E1CF1489AE0A8AB1A663CBD5291F11F3B01D3496A1270CAE369F4D`。
- 失败路径实测：故意把期望进程数设置为4，脚本明确拒绝“期望4、实际3”的无效结果；失败后仍正常关闭全部3个进程、残留0，并恢复原始方案哈希。
- 关联验证：完整方案脚本PowerShell语法、进程I/O专项和固定50ms触发架构检查通过；本批只修改测试工具，不修改生产流程、AI并发、PLC顺序或界面。
- 下一步：形成独立Git恢复点后处理VX的P2：保存队列满时必须在确认接纳后才取得Mat租约，并补生产入口满队列租约计数回归。

## 2026-08-20：性能改造步骤7第四小批真实500轮与保存接纳后租约

- 状态：已完成；真实新旧版本500轮同比、VX三轮只读审查、全部P0～P3发现修复、完整编译和资源压力均已收口。
- 改动前版本：`6ae35d7`，压测工具的正常关闭、进程树硬校验及成功/失败路径已经形成独立恢复点。
- 500轮口径：当前版结果`20260820-111114-步骤7第四小批真实四流程500轮-500轮.json`，旧版结果`20260820-112107-步骤7第四小批旧版同口径500轮-500轮.json`；均为最小化窗口、低于正常优先级、旧版有效许可证和Dock布局、6个图像窗口、原始方案、本机Modbus、预热1轮后串行500轮。
- 功能有效性：两版四流程均各500/500成功，Modbus均2000/2000成功，异常0；主程序加2个AI进程全程保持3个，测试后正常关闭且残留0，桌面方案恢复原始SHA256。
- 耗时诚实结论：旧版到当前版总耗时`268525.931→270321.533ms`，增加0.669%；吞吐`1.862018→1.849649轮/秒`，下降0.664%；P50/P95/P99/最大值`531.954/622.652/650.477/758.661ms→539.780/661.415/804.519/949.375ms`，分别回退1.471%/6.225%/23.681%/25.138%。本轮完整方案耗时没有改善，不能用资源下降掩盖长尾回退。
- 长尾定位：当前版慢轮集中在第427～443轮，共6轮不低于800ms；同期CPU只有约17%～26%，每轮I/O量稳定，流程日志显示AI节点133/181推理阶段集中变慢。AI节点保持按可编辑流程执行且不进入本轮CPU调度，因此判断更接近AI运行波动，不是CPU许可硬控；该判断属于时间对齐推断，最终长稳仍需复核。
- CPU和内存：CPU平均`35.305%→35.844%`，增加0.539个百分点；三进程私有内存峰值`2455.078→1992.906MB`下降18.825%，结束`1495.066→1386.180MB`下降7.283%；托管堆峰值`1039.548→944.150MB`下降9.177%，结束`591.711→487.038MB`下降17.691%；GDI峰值下降11，句柄峰值下降100。
- I/O结果：累计读取`9802.979→9859.747MB`，增加0.579%，其中当前版存在一次约78MB的首轮读取，常规每轮读P99约20MB保持一致；累计写入`9827.894→9827.888MB`基本持平，写入P99`20800.690→20134.982KB`下降3.20%。本轮没有证据表明物理磁盘写盘速度退化。
- VX首次P2：保存节点原先在队列确认接收前取得候选Mat租约，容量已满时虽然拒绝快，但候选租约归还可能在检测线程同步触发整条Mat依赖释放。现改为任务预判只读取构建阶段缓存字节，只有有界队列确认可接收后才调用原子快照工厂。
- VX终审P2：公开`Bitmaps`可原地替换且Mat尺寸可变化，两次都读取同一缓存会低估队列内存；`TakeDependency`未拒绝循环依赖，错误的A依赖B、B依赖A会永久互持租约，原父子缓存读取还存在反向加锁条件。
- 原子快照修复：`OutputImage`在资源锁内同时取得`Bitmaps[0]`和后台租约，离锁后在该租约保护下沿完整依赖链重新按`Rows×Step`精确计量；有界队列在同一接纳锁内使用实际值重新检查单项预算、累计预算及NG淘汰集合。实际超限时正常返回`RejectedFull`或`RejectedItemExceedsBudget`并累计拒绝数，不再抛成保存节点运行失败；调用方随后释放未发布任务的快照租约。
- 依赖图修复：依赖边的检查和登记由全局依赖图锁串行；新增直接/传递路径检查，形成环前拒绝第二条边。刷新子级缓存时通过`Interlocked.Read`读取父级不可变估算，不再持有子级资源锁获取父级资源锁；异常路径会移除未完成登记并归还临时父级租约。
- 接纳边界实测：容量1且消费者未启动时第一项接收、第二项拒绝，快照工厂调用1次，已接收/被拒输出活动租约分别1/0，停止后归零；预检后先Dispose和原地换入大Mat导致缓存偏小两种情况均拒绝发布，队列0、租约0。
- 压力结果：最终真实OpenCV保存3轮×60次共180次，接收52、拒绝128、淘汰普通12、完成40、失败0，180份Mat租约全部释放、重复释放0；队列/字节/线程峰值8/18MB/2，入队P99最大0.1055ms，墙钟357ms，CPU平均8.551%，私有/托管内存变化`+0.277/-0.013MB`，句柄900→900。
- 并发与回归：图像生命周期50000轮释放准确；1000轮`AcquireSaveSnapshot`与`Dispose`同时争锁允许快照先成功或Dispose先拒绝两种合法顺序，每轮最终活动租约0且Mat只释放1次；三节点`A→B→C`后`C→A`稳定拒绝，另有1000轮双线程同时建立`A→B/B→A`，每轮恰好接收一条、拒绝一条，5秒死锁门槛未触发且双方资源归零。有界队列100000次释放100000、重复释放0；CPU调度1000/1000、传统CPU节点28/28、明确直通16/16、方案资源释放和保存架构均通过。
- VX最终结论：最终语义修复复核无P0、P1、P2；两个非阻断P3也已处理。队列最终条目只替换实际字节，继续沿用回调前冻结的所有者和优先级；NG实际字节增大后精确淘汰、减小后不多淘汰、可淘汰空间不足时不部分淘汰并明确拒绝，三组测试均通过。
- 编译结果：`Debug|x64`完整编译通过，0错误、170个既有警告；保存磁盘保护、多条件订阅、真实落盘同比架构、方案资源释放、CPU调度1000/1000、传统节点28/28和明确直通16/16均通过。本批不修改AI并发、可编辑流程顺序、PLC/Modbus写回位置或界面布局。
- 下一步：提交本批独立恢复点后，步骤7收口并进入步骤8系统设置参数界面；完整方案P99长尾回退保留到最终长稳复核，不以资源下降替代耗时结论。

## 2026-08-20：性能改造步骤8第一小批机器级配置核心

- 状态：已完成；本批只建立机器配置、自动/手动/最终档案合并和自适应总开关，不改系统设置界面布局。
- 改动前版本：`7ebdc35`，步骤7真实四流程500轮证据已经形成独立恢复点。
- 关键参数：`AdaptiveResourceManagementEnabled`；CPU保留核心、CPU重任务并发、图像转换并发、保存线程；CPU高/恢复水位、持续时间、评估周期、冷却和步长；流程/保存内存预算比例、内存水位、图像采样、停止排空、UI帧率、保存容量和磁盘保护参数。
- 配置边界：机器手动值保存在`LocalApplicationData\TDJS-Vision\performance-resource-settings.json`，不写入`.Sol`方案；设置提供者按快照读取，JSON保存使用同目录临时文件并原子替换，读取损坏文件时回退自动值并保留告警。
- 合并语义：运行时同时保留自动档案和最终档案。手动保留核心数会重新计算未手动指定的CPU并发；手动CPU并发仍受可用物理核心限制；图像转换并发不超过CPU重任务并发；CPU、内存和磁盘成对水位会收敛到安全间隔。
- 自适应开关：关闭后CPU调度器保留当前并发并清空高负载/恢复计时，不再因CPU采样自动升降；用户手动指定的最终并发仍作为固定上限。
- VX首轮审查：发现保存时持锁执行磁盘I/O会让每轮档案读取等待、只改CPU总并发或高水位时子参数未同步收敛、无手动值时重算硬编码并发上限3项P1；另指出覆盖保存路径、快照隔离和缺少产物不得跳过等测试缺口。
- 审查修复：运行线程通过原子引用读取已加载快照，不再竞争磁盘保存锁；保存成功后一次性发布新快照。关联参数即使没有自身手动值也按父参数最终范围收敛；无手动覆盖直接复制自动档案，自定义CPU自动上限保持不变。`IRuntimeResourceProfileProvider`同步公开自动/最终档案结果，界面无需依赖具体实现。
- VX第二轮审查：继续发现首次加载/并发保存可能反向发布旧内存快照，以及1流程4相机时自动转换并发4会被调度器静默压为1两项P1；现将磁盘操作和对应快照发布放入同一持久化顺序，并让自动计算阶段直接限制转换并发不超过共享CPU额度。最终复审无P0、P1、P2，仅保留可控延迟假存储并发交错压力为非阻断P3补强项。
- 验证结果：`PerformanceResourceSettings.Tests.ps1`通过，覆盖自动/手动/最终值分离、单独降低CPU并发、单独降低CPU高水位、保留核心联动、纯自动一致、关闭动态调节、JSON首次保存和重复覆盖、快照隔离、无残留临时/备份文件及损坏降级；没有编译产物时测试会直接失败。CPU调度压力1000/1000通过，峰值并发4/4、转换2/2；本机硬件档案回归通过，识别16核24线程并给出CPU/转换/保存并发4/4/2；`Debug|x64`完整编译0错误、170个既有警告。
- 诚实边界：流程图像预算和内存水位目前仍主要用于档案计算与日志，下一批界面必须标为规划值或不开放，不能宣称已经执行内存硬管控；保存线程/容量在工作池繁忙时不能强制热切换。
- 下一步：提交本批恢复点；在`FrmSystemSetting.Designer.cs`增加“性能与资源”页，显示硬件、自动值、手动值、最终值和生效状态，并实现恢复自动、重新读取硬件、保存应用及事件退订。

## 2026-08-20：性能改造步骤8第二小批系统设置界面与安全应用

- 状态：已完成；系统设置参数界面、草稿预览、机器保存、分区应用状态、事件释放、VX最终复审和专项压力均已收口。
- 改动前版本：`a3a0538`，步骤8第一小批机器配置核心已经形成独立恢复点。
- 界面参数：在`FrmSystemSetting.Designer.cs`加入“常规设置/性能与资源”双页、26项参数8列表格；列为类别、参数、自动值、使用手动、手动值、最终值、推荐范围和生效状态。顶部显示硬件、方案规模和本次预览，操作为“启用CPU动态并发调节”“重新读取硬件”“恢复全部自动”“保存并应用”。
- 启动与生命周期：主窗体不再启动时创建系统设置，改为菜单点击时`using`创建，避免启动阶段提前探测硬件和保留过期方案预览；打开窗体不会重复保存常规设置或改写开机启动项。方案事件在OnLoad句柄就绪后订阅，并由OnFormClosed和Dispose幂等退订。
- 草稿与持久化：未保存编辑通过可替换设置提供器生成自动/最终预览，不写文件、不修改运行组件；保存按钮完整校验数字和本机核心上限后原子保存。数字解析只允许符号和小数点，不接受千位分隔符，避免不同区域设置把`1.5`误读为`15`。
- 安全应用：CPU调度、CPU采样和UI档案允许安全热更新；图像采样在无活动流程时替换；保存工作池只在未创建或安全空闲时使用新边界，运行中返回待下一轮。方案重置期间不修改任何运行组件。停止排空超时同时进入运行停止和保存池配置匹配，界面可显示“运行停止已应用；保存池待下一轮”。
- 诚实边界：流程图像预算和内存水位仍标为“规划值，尚未启用内存硬管控”；顶部统一称“本次预览”，不把未保存草稿描述成当前运行值。AI仍跟随用户可编辑流程，不增加全局AI并发限制；不改变PLC/Modbus节点顺序。
- VX审查：首轮发现3项P1、3项P2、2项P3，修复启动期构造、首次保留核心上限、排空超时配置匹配、重置热更新、Dispose退订、旧测试断言、草稿文案和区域数字解析。复审剩余排空超时混合状态1项P2，新增`RuntimeAndImageSave`状态后最终复审无P0、P1、P2。
- UI专项：使用独立临时机器配置和离屏不激活窗口验证，合法保留核心首次预览成功；构造、OnLoad和草稿编辑前后配置SHA256不变；真实调用保存按钮后配置发生变化；重置和活动流程返回Pending；直接Dispose完成事件退订。26行、8列、两页签、布局边界和离屏位图均通过。
- 资源回归：保存工作池3轮×60次共180次，接收54、拒绝126、淘汰普通14、完成40、失败0、文件40；180份Mat租约全部释放、重复释放0，队列/字节/线程峰值8/18MB/2，入队P99最大0.1132ms，墙钟440ms，私有内存`+0.336MB`、托管内存`-0.012MB`、句柄0增长。CPU调度1000/1000、硬件档案、保存架构、日志等级和启动动画回归均通过。
- 编译结果：`Debug|x64`完整编译通过，0错误、170个既有警告；无焦点测试完成后没有后台主程序或测试窗体残留。
- 下一步：提交本批独立恢复点，步骤8正式完成；进入步骤9前先固定最终回归口径，再执行Debug关闭/开启、长尾、内存、GDI、队列峰值和丢弃统计验收。

## 2026-08-20：开始性能改造步骤9第一小批后台验收工具

- 改动前版本：`ec91e62`，步骤8系统设置资源参数界面已形成独立恢复点。
- 办公约束：步骤9工具默认最小化、不激活窗口、进程优先级低于正常；禁止`SendKeys`、`AppActivate`、`SetForegroundWindow`以及键盘鼠标模拟，不占用用户Windows输入控制。
- Debug隔离：完整方案工具新增`Current/Off/On`模式；Off和On只通过子进程环境变量生效，工具启动后立即恢复父进程环境，且不写入用户设置。
- 证据分级：共享办公环境标记为`SharedDesktopBackground`，只用于功能、资源边界和长稳趋势；只有`Dedicated`专用空闲环境的数据允许进入最终性能结论。
- 分阶段策略：先执行编译与低负载专项回归，再用少量完整方案冒烟确认窗口、许可证、进程树和资源采样，随后分别进入Debug关/开1000轮；10000轮和2小时长稳必须在前级无异常后执行。
- 新增检查：`Tests\Step9PerformanceAcceptance.Tests.ps1`保护窗口模式、进程优先级、输入禁令、Debug覆盖不落盘、环境变量恢复和性能结论资格标记。
- VX首轮审查：发现既有进程会被测试清理1项P0，以及启动焦点、重复实例提示、关闭残留、四窗口直接Show等P1；本批先关闭完整界面长测，修正完整方案工具为启动前拒绝同名程序、后台窗体不激活、关闭不弹确认、只清理自有进程，并增加前台焦点检测。
- 统计口径修正：固定节拍补进程PID稳定性校验，未采样CPU写空值并标记`CpuSampled=false`；串行CPU使用正式墙钟结束快照，吞吐量改按墙钟计算；真实落盘I/O在JPEG验证读取前结束采样；保存压力目录按测试实例使用GUID隔离。
- 首次共享办公冒烟：修正前已完成Debug关闭预热1轮+正式3轮，四流程各3/3、Modbus 12/12、异常0，P50/P95/P99/最大值810.133/917.410/926.946/929.330ms，CPU平均/峰值24.185%/28.013%。该数据明确标记`PerformanceConclusionEligible=false`，只证明冒烟功能，不作为性能结论。
- 办公资源门禁：本机硬件档案复测读取16物理核心、24逻辑线程、16087MB总内存，但测试时可用内存仅1617MB，低于自动低水位2414MB；因此暂停完整方案和保存压力，并为共享办公完整方案测试增加4096MB启动保护线。
- 启动页补强：后台验收启动页关闭置顶和任务栏显示，强制最小化并设为透明；即使启动初始化持续数秒，也不会覆盖用户正在操作的办公窗口。
- 工具兼容修正：默认结果目录改为进入脚本正文后基于`PSScriptRoot`解析，避免部分Windows PowerShell调用方式在参数绑定阶段得到空路径。
- 最终编译：`Debug|x64`完整编译及后续单进程Compile复核均为0错误、170个既有警告；本批新增后台显示策略已进入正式程序集。
- 无窗口回归：步骤9安全检查、完整方案进程I/O、真实落盘架构、启动动画、日志等级、UI与保存诊断共6项通过；共享办公内存门禁动态验证在启动视觉程序前正确拒绝，视觉进程残留0。
- 短时压力：CPU调度1000/1000通过；四流程CPU保护完成804/804许可；保存工作池1轮20次接收10、拒绝10、完成10、失败0，租约20/20释放、重复释放0、队列/字节/线程峰值8/18MB/2、入队P99 0.1014ms，私有/托管内存变化`+0.164/-0.894MB`、句柄0增长、平均CPU 6.889%。
- 最终安全补强：后台显示策略改为性能验收与后台请求两个进程开关同时开启；完整方案工具始终给自启测试子进程设置验收标记，`Current`模式仅保持用户Debug值不变，并拒绝所有`ExistingProcessId`复用。统一消息框、授权失败、启动失败和关闭保存失败在后台验收时静默处理，启动初始化失败直接退出；运行中关闭先调用方案停止并等待排空。进程归属改为Windows父子关系，焦点与清理只覆盖本轮主进程及后代；焦点和内存检查覆盖启动、每轮运行、固定触发、结果排空与退出。
- 四窗口测试补强：四窗口压力和图像所有权测试改用`ShowWindow(..., 4)`不激活显示，禁止直接调用`Form.Show()`；组件级Mat转Bitmap增加P99记录及`<500ms`门槛，同时明确不包含完整节点执行链和生产CPU调度链。本轮只完成脚本语法与规则验证，用户办公期间未启动真实测试窗体。
- 四窗口统计边界：每窗每轮30个转换样本时，最近秩P99等同本轮最大值，只能作为严格卡顿哨兵，不能替代步骤9完整方案1000轮以上的稳定P99结论。
- 最终无窗口回归：`Debug|x64`单线程完整构建成功；步骤9运行时行为验收与完整方案进程I/O、真实落盘架构、启动动画、日志等级、UI与保存诊断、性能资源设置、CPU调度、四流程CPU保护共8组短测通过。CPU调度1000/1000；四流程CPU保护804/804，线程池计时探针A/B/C最大间隔18.151/18.147/18.153ms、私有/托管内存变化`+0.020/+0.001MB`、句柄0增长。计时探针不代表真实UI、相机或PLC链路。
- 结果可信度：正式JSON和检查点先标记等待退出验证、未完成且不可用于性能结论；测试主体、正常退出、自有后代清理和固定方案恢复全部成功后才发布完成。无窗口行为测试分别验证退出失败保持无效、退出成功才能发布完成状态。
- 最终门禁复核：使用65536MB动态阈值执行拒绝路径，多次采样可用内存1852～2983MB，均在视觉程序启动前停止，视觉进程保持0→0；额外隐藏父子进程探针确认Windows后代识别正确。当前仍低于默认4096MB办公保护线。
- 当前边界：不降低4096MB共享办公门禁；不在用户办公时运行四窗口真实显示、Debug关/开1000轮、10000轮或2小时长稳。步骤9第二小批待内存余量满足后，从Debug关闭1000轮开始继续，上述未执行项目不得标记为通过。

## 2026-08-21：客户电脑模板匹配闪退诊断

- 现场日志：客户发回的`20260821_模板匹配诊断.log`显示`Native`目录存在、`MatchTool.dll` API版本为`0x00010001`，CPU支持AVX2，运行配置为通用x64版本，排除缺DLL、版本过旧和AVX2拦截问题。
- 闪退位置：PID `7464`和`9196`均停在`FastMatchEngineHandle.Learn：准备调用LearnPatternMem`后没有返回，其中一次模板为`386x534`，另一次模板为`1024x683`，判断为native模板学习阶段进程级崩溃，C#异常捕获无法兜住。
- 正常对照：同一日志中`514x630`、`340x1076`、`348x1053`等模板可完整执行到`MatchMem返回`和`MatchSession.Match出口：成功`，说明不是所有模板匹配都会失败，而是特定模板/图像组合触发native学习崩溃。
- 现场兜底：新增`Native\MatchTool.runtime.txt`配置项`force_managed_cpu_fallback=false`，后续客户现场如再次遇到native学习闪退，可改为`true`，普通模板匹配会绕开`LearnPatternMem`并走OpenCvSharp CPU后备算法，优先保证软件不闪退。
- 诊断增强：模板匹配CPU后备日志增加`ForceManagedFallback`字段，便于确认现场是否已启用强制后备；NCC模板匹配仍保持demo一致native路径。
- DLL根修复：在`D:\MyCode\PublicWook\Fastest_Image_Pattern_Matching\Fastest_Image_Pattern_Matching\MatchTool\MatchInterface.cpp`中把`LearnPatternMem`和`MatchMem`拆为内部实现，并在MSVC导出入口外层增加SEH保护；native内部若出现访问冲突等进程级异常时返回`MATCH_ERROR_INTERNAL`，避免异常越过C ABI边界直接闪退。
- DLL编译与替换：使用`build-codex-ncc`通用x64配置重新生成`MatchTool.dll`，已替换`Native\MatchTool.dll`、`bin\x64\Release\Native\MatchTool.dll`和`bin\x64\Debug\Native\MatchTool.dll`；旧版已备份为`Native\MatchTool.dll.codex-bak-before-seh-20260821-162852`。
- 验证结果：独立P/Invoke探针调用新版`LearnPatternMem`，日志中卡住前的`386x534 stride=1160`和`1024x683 stride=3072`两组尺寸均返回`0`；故意传入无效图像地址时返回`-10`而不是打崩进程；`Release|x64`完整构建成功，`0`警告、`0`错误，四处DLL SHA256均为`5AB131E567397A60C49BDD84AB7235FEDFE27D6A199A8CCFFA133DAD5C8D3856`。
- 执行降级补强：客户机点击执行时若新版DLL返回`-10`，说明原闪退点已被native保护层拦截，但旧业务层仍会显示“模板学习失败”。已把普通模板匹配的Bitmap路径和Mat快路径改为识别`-10`后自动释放native引擎、清空学习状态并切换OpenCvSharp CPU后备算法；NCC强制demo native路径保持不降级，避免改变NCC算法口径。
- 释放保护补强：`ReleaseMatchEngine`同样增加SEH外层保护，避免`-10`后释放异常native引擎时再次触发进程级崩溃；新版`MatchTool.dll`已重新替换到`Native`、`bin\x64\Release\Native`和`bin\x64\Debug\Native`，旧版备份为`Native\MatchTool.dll.codex-bak-before-release-guard-20260821-165118`。
- 最终验证：故障探针传入无效图像地址返回`-10`且`ReleaseMatchEngine`正常返回；最终`Release|x64`构建成功，`0`警告、`0`错误。四处新版DLL SHA256均为`507336C2EB610B009CC98ED0F621F5E407EF510422B98ED5120592C745951303`。
- native阶段诊断：确认客户机已走`-10`自动后备后，未继续推进CPU后备速度优化，改为定位`-10`根因。`MatchTool.dll`新增线程本地native阶段记录和`GetLastMatchDiagnosticStage`导出，API版本升为`0x00010002`；`LearnPatternMem`覆盖图像校验、转灰度、进入`LearnPattern`、`meanStdDev`、`GetTopLayer`、`buildPyramid`、自适应层选择、边界均值、模板数据扩容和每层统计阶段。
- C#日志接入：`FastMatchEngineHandle.Learn`在`LearnPatternMem`返回后读取`nativeStage`并写入模板匹配诊断日志，异常文本也附带`native阶段`；如果客户机再次返回`-10`，日志会直接显示失败停在例如`ConvertToGray.cvtColor.BGR2GRAY`或`LearnPattern.before-buildPyramid`等具体阶段。
- 诊断包验证：独立探针确认新版DLL返回`api=0x00010002`，传入无效地址时返回`-10`且可读取`stage=ConvertToGray.cvtColor.BGR2GRAY`；新版DLL已替换`Native`、`bin\x64\Release\Native`和`bin\x64\Debug\Native`，四处SHA256均为`54468CE8C39E3A96EE3F455B8012E8E21EC923605B2C72B9CF579E4C09F21ECB`。最终`Release|x64`构建`0`错误，保留项目既有`171`个警告；旧DLL备份为`Native\MatchTool.dll.codex-bak-before-native-stage-20260821-172313`。

## 2026-08-20：步骤9多智能体生产安全与性能审查

- 状态：四路只读审查、主智能体交叉核对和无窗口低负载回归已完成；本批只形成问题清单和测试证据，不修改生产代码。
- 审查分工：资源生命周期组检查Mat、Bitmap、OutputImage、保存租约和停止竞态；性能组检查相机、UI、日志、保存、PLC/Modbus与CPU热路径；测试组检查假通过、采样口径、进程与焦点保护；WinForms组检查启动、后台验收、资源配置和热更新。
- P0确认：同一`Process`没有独立原子单飞门禁，方案会话计数允许多个流程并发，也允许同一流程重入。被动触发、脚本、参数预览和直接运行可绕过主窗体的完整方案准入器；重入后`BeginProcessRun`及节点`Result`互相覆盖，旧结果立即释放，而显示和部分算法节点未统一持有输入图像短租约，存在已释放Mat访问、异常、超时和原生崩溃风险。
- P1相机：回调模式在`BeginWaitAsync`后直接等待，没有使用图像源参数`TimeOut`；触发成功但回调丢失时流程可无限等待到方案取消。海康相机构造时序列号为空会启动无等待、无超时、无取消的后台轮询，每台异常相机可能长期占用线程和CPU。
- P1 CameraIO：异步脉冲每次创建不跟踪任务，内部又用线程池CPU自旋实现微秒延时；50ms高频触发且保持时间跨周期时任务会重叠增长，直接影响线程池、公平性和P95/P99。
- P1日志：普通日志文件队列无容量，Debug诊断虽有丢弃计数，但Info/Warn/Exception仍可无限排队；单消费者每条日志重新打开和关闭文件。慢盘、杀毒扫描或日志洪峰会形成内存增长与磁盘抖动。
- P1 Modbus：同一设备所有同步网络读写在一个锁内完成，传输超时固定5秒且无重试。一次通信停顿会让其他流程先等待该锁，随后可能再分别进入自己的超时，具备四流程连锁超时条件；该项是代码机制确认，现场归因仍需时间线验证。
- 其他生产风险：旧Bitmap每帧一个无界`Task.Run`等待释放；条件跳过或停用节点可能保留上一轮图像；保存快照在全局队列锁内遍历依赖；活动转换、编码和写盘无法中途取消；可用内存被随静态硬件快照缓存；内存水位仍未执行硬管控；线程池最低工作/IO线程固定64，没有按核心数生成。
- 测试P1：串行完整方案统计业务失败但不抛错，最终仍可能得到性能资格；固定50ms模式在准入0时仍满足流程和Modbus数量校验；串行新旧同比没有强制共同输入哈希；真实落盘同比绕过生产保存节点和共享池且正式样本只有1组，不能把组件吞吐提升直接推广到整套软件。
- 测试P2/P3：焦点轮询不能证明从未发生短暂激活；固定触发排空漏采部分CPU与托管堆峰值；落盘异常路径未可靠停止工作池；步骤9过多依赖源码文字；CPU分类只覆盖75种节点中的44种；中文无BOM脚本在Windows PowerShell 5.1存在解析兼容问题；P99算法不统一。
- 本机回归总览：21项唯一低负载测试中19项稳定通过、1项失败、1项波动；没有启动可见窗口、视觉主程序或键鼠焦点控制，测试子进程使用隐藏窗口和低于正常优先级。
- 通过证据：步骤9安全、进程I/O、落盘架构、启动动画、日志等级、性能诊断、性能配置、诊断惰性、流程追踪、保存架构与磁盘保护、相机快速路径、通信追踪和方案准入均通过；四流程CPU保护804/804，计时探针最大约18.13ms；相机追踪表50000次40ms；图像生命周期50000次、快照和依赖竞态各1000次共85ms；QR/ROI替换500次GDI增长0；保存工作池20次接收11、拒绝9、完成10、失败0、租约20/20释放、重复释放0、句柄0增长、私有内存`+0.129MB`、托管内存`-0.020MB`。
- 确定失败：资源释放门禁仍断言`RefreshRuntimeResourceProfile()`，但生产实现为`RefreshRuntimeResourceProfile(true)`；该测试过期并稳定失败，后续必须更新为新安全语义。
- CPU测试波动：独立进程执行4轮仅1轮通过8MB托管内存门槛，任务、许可和并发边界始终正确；直接Windows PowerShell诊断3轮全部通过，托管内存变化`+0.005/-0.866/-0.866MB`。判断为测试方法内1000个Task局部变量GC存活时机不稳定，需隔离压力作用域后复验，不能把失败直接归因于调度器泄漏。
- 资源门禁：测试期间可用物理内存约802～1207MB，远低于4096MB共享办公保护线，因此不执行完整编译、真实四窗口、Debug关/开1000轮、10000轮或2小时长稳；视觉进程测试前后均为0。
- 审查边界：本轮发现来自当前`HEAD 27b3a70`与工作树源码；`NodeTDAI.cs`过滤后内容哈希与HEAD一致，仅工作树行尾状态不同，本批不修改、不暂存、不提交该文件。
- 下一步：先建立同流程原子单飞与并发图像租约测试，修复P0；随后加固完整方案业务成功硬门槛、固定触发最低准入及样本闭合，再依次治理相机超时、CameraIO、日志、Modbus和Bitmap释放队列。上述修复完成前不开始步骤9正式1000轮。

## 2026-08-20：步骤9切换Release测试及首次冒烟

- 测试口径：步骤9完整方案统一使用`Release|x64`程序；Debug关闭和开启只切换完整耗时诊断日志，不再使用Debug编译产物参与性能数据。
- 构建与环境：Release完整构建0错误、0警告；启动前可用物理内存约`5446MB`，高于共享办公`4096MB`门槛，视觉程序残留0；当前Release与旧版Release的`device.license`哈希一致。
- 输入隔离：桌面`8_6方案.Sol`已与原始基准不同，因此创建`Tests\Results\Step9\步骤9-Release-固定方案.Sol`作为独立测试副本，避免测试自动保存覆盖用户桌面方案。
- 冒烟结果：Release Debug关闭3轮冒烟刚启动便触发“视觉测试主进程或工作进程取得Windows前台焦点”保护，工具立即停止并清理；未开始正式轮次、未生成有效JSON/CSV，测试后残留进程0。
- 初步定位：主窗体和启动页虽覆盖`ShowWithoutActivation`，独立启动页线程仍通过`Application.Run(startupForm)`建立顶层窗口；Release启动时序下出现短暂前台激活，现有轮询成功捕获，证明不能绕过保护直接运行1000轮。
- 下一步：在仅限后台验收的窗口路径增加`WS_EX_NOACTIVATE`系统扩展样式，并把步骤9运行时门禁切换为Release程序集；提交独立修复后重跑3轮冒烟，通过后再执行Release Debug关闭1000轮。
- 窗口修复：`FormStartup`和`FormMain`新增后台验收专用`WS_EX_NOACTIVATE`扩展样式，只有`TDJS_VISION_PERFORMANCE_ACCEPTANCE=1`与`TDJS_VISION_BACKGROUND_ACCEPTANCE=1`同时成立时生效；普通启动行为保持不变。
- 测试门禁：`Step9PerformanceAcceptance.Tests.ps1`改为加载最新`bin\x64\Release`程序集，增加启动页`CreateParams.ExStyle`运行时检查，并继续保留无键鼠模拟、焦点轮询、共享办公内存和结果资格门禁。
- 当前验证：Release完整构建0错误、171个既有警告；步骤9Release行为检查、启动动画回归、设备许可证启动同步均通过。测试方案副本哈希恢复正确、Release与旧版许可证哈希一致、视觉程序残留0；真实3轮复测待本批提交后执行。
- 修复后真实复测：提交`29cbd06`后以完全相同参数重跑Release Debug关闭3轮，仍在正式轮次前触发前台焦点保护；工具立即清理，残留视觉进程0，没有有效JSON/CSV，未开始1000轮。
- 独立审查：未发现P0/P1；两个P2分别是Release行为脚本没有动态启动真实主窗体、默认AppDomain加载程序集在长期PowerShell会话中不能单独卸载。当前脚本由独立`powershell.exe -File`进程执行，进程结束即释放程序集；真实主窗体和UI Automation仍必须由3轮冒烟验证。
- 下一步：焦点门禁错误增加前台窗口句柄、PID、标题、窗口类、可见和启用状态，避免清理阶段只留下泛化错误；Release行为门禁补普通启动样式反向断言。形成恢复点后只执行一次诊断冒烟，根据现场决定生产修复，不直接进入1000轮。
- 诊断工具完成：`FullSolutionComparison.Tests.ps1`使用单次前台窗口快照完成归属判断并输出完整现场；`Step9PerformanceAcceptance.Tests.ps1`新增普通Release启动页不带`WS_EX_NOACTIVATE`的反向断言。脚本语法、原有安全规则和Release运行时正反样式检查均通过。
- 首次焦点现场：Release诊断冒烟输出`句柄=0xD0F28;PID=27812;标题=WindowsFormsParkingWindow;窗口类=WindowsForms10.Window.8.app.0.21af1a5_r6_ad1;可见=False;启用=True`，随后工具清理完成、视觉进程残留0。
- 证据修正：上述窗口来自退出清理异常，现有`finally`会让清理异常覆盖测试主体原始异常，暂不能把隐藏停车窗口认定为启动根因。下一步给焦点检查增加阶段名称，并在测试主体失败时只附加清理警告、保留原始异常，再执行一次诊断冒烟。
- 异常优先级修复：等待主界面、流程运行、固定触发投递、固定触发排空和退出清理均输出阶段；测试主体异常由`catch`保存并原样抛出，清理异常不再覆盖它。步骤9Release行为脚本已验证相关规则及脚本语法通过。
- 原始异常结果：第三次诊断明确首次失败为`阶段=等待主界面`，PID `13920`的不可见`WindowsFormsParkingWindow`进入前台；退出清理观测到同一类隐藏窗口，但不再覆盖原始异常。测试后残留视觉进程0，未开始任何正式轮次。
- 修复范围：后台验收模式跳过透明最小化启动动画线程和2秒动画等待，避免创建无业务价值的停车窗；真实主窗体、方案加载、四流程和UI Automation链保持不变。普通用户启动不跳过动画。修改后先跑无窗口回归并提交，再执行Release 3轮冒烟。
- 修复与验证：`StartupApplicationContext`后台验收分支不再启动独立动画线程，也不等待2秒；普通启动分支仍执行原动画。Release完整构建0错误、171个既有警告；`StartupSplash.Tests.ps1`、`Step9PerformanceAcceptance.Tests.ps1`和`DeviceLicenseStartupSync.Tests.ps1`全部通过。
- 动画隔离后复测：真实Release冒烟仍在`等待主界面`捕获PID `21820`的不可见`WindowsFormsParkingWindow`，退出清理同类附加异常被正确保留，测试后残留0。停车窗由真实主窗体控件初始化产生，不能依赖单个Form的`CreateParams`完全覆盖。
- 下一步：新增后台验收主UI线程级`WH_CBT/HCBT_ACTIVATE`守卫，在Windows激活任何本线程窗口前拒绝；普通启动不安装，安装失败直接终止后台验收。补源码与Release门禁、编译、独立审查后再跑3轮冒烟。
- 焦点守卫实现：新增`Startup\BackgroundWindowActivationGuard.cs`并由`Program.Main`在创建任何主UI窗口前持有；后台验收拦截`HCBT_ACTIVATE`，普通启动不安装，AI隔离工作进程在此前已返回。钩子安装失败抛出Windows错误，退出时幂等解除。
- 当前验证：Release完整构建0错误、171个既有警告；启动动画、步骤9Release行为、设备许可证同步三组无窗口回归通过。真实主界面、UI Automation和焦点仍以提交后的3轮冒烟为最终门禁。
- 焦点守卫真实冒烟：启动、等待主界面和退出均未再出现焦点违规，证明线程级守卫有效；工具已找到UI Automation“单次运行”按钮，但在按钮仍禁用时立即进入预热并报错。清理阶段可用内存`3990MB`低于4096MB门槛，结果无效且残留0。
- 独立审查：无P0/P1；P2为Release产物时间门禁未包含`Program.cs`、守卫源码和项目文件，动态钩子/InvokePattern仍需完整冒烟证明；P3为解除钩子返回值未记录。下一步补按钮启用等待、Release新旧文件清单和解除失败日志，内存保护线保持4096MB。
- 门禁修复完成：`Find-RunOnceButton`只返回已经启用的按钮；Release新旧检查增加`Program.cs`、`BackgroundWindowActivationGuard.cs`和`TDJS-Vision.csproj`等关键输入；解除钩子失败写Warn且不在退出路径二次抛错。
- 验证与资源：Release完整构建0错误、171个既有警告，启动动画、步骤9Release行为和许可证同步回归全部通过；当前可用物理内存约`4403MB`，不足以覆盖程序加载后4096MB余量，故不降低门槛、不绕过共享办公保护，真实3轮和1000轮继续等待资源门禁满足。

## 2026-08-21：大模型与无监督训练自动识别分数校准

- 分支：按要求从当前`框架无监督集成`切出独立分支`codex/auto-score-calibration`，本次修改保留在该分支。
- 训练集拆分：大模型与无监督训练均要求 OK/NG 各至少 2 张有效图片；每类第 1 张保存到`Calibration\OK`和`Calibration\NG`作为预留校准图，不写入正式训练`OK`/`NG`目录。
- 自动分数：训练完成后使用预留 OK/NG 图像实际推理，按 OK 分数与 NG 分数中点生成识别阈值，并再次验证 OK 判 OK、NG 判 NG；验证失败时直接提示更换代表样本或调整面积参数。
- 模板写回：大模型模板`ImageThreshold`和无监督模板`Threshold`改为写入自动校准后的识别分数，使后续检测节点默认读取模板校准值。
- 无监督节点：默认异常阈值改为`0`表示跟随模板，保留`0.3`作为老模板无有效阈值时的兼容兜底；参数窗体阈值控件允许输入`0`，标签显示`异常阈值(0=模板)`。
- 界面说明：大模型与无监督训练页阈值标签改为`初始图像阈值`和`初始阈值`，避免把训练初始化阈值误认为最终识别分数。
- 回归脚本：更新大模型生产行为测试桩、无监督模板包检查、无监督节点检查和大模型集成检查，覆盖预留校准图、自动分数校准、模板阈值写回与无监督默认跟随模板。

## 2026-08-21：大产品方案拆分为相机入口与算法流程

- 方案输出：基于微信临时目录中的`大产品.Sol`生成`outputs\大产品_一个流程一个相机.Sol`，原始临时附件未覆盖。
- 相机入口：新增`上相机入口`和`下相机入口`两个主动流程，启用图像源相机分别只绑定`上相机`和`下相机`，均使用`LINE0`触发。
- 图像传递：入口流程采图后分别写入共享变量`上相机图像`、`下相机图像`；`流程1`、`流程2`、`流程3`改为读取`上相机图像`，`流程4`改为读取`下相机图像`。
- 流程触发：`流程1`至`流程4`改为被动流程；入口流程通过Modbus读取条件和`触发同组流程`节点触发对应算法流程，避免多个流程同时绑定同一相机。
- 旧分支处理：旧算法流程内原有Modbus读取和分支判断节点保留但禁用，并移除对应旧连线；算法与发信号链路保留。

## 2026-08-21：海康双相机官方回调Mat耗时Demo

- 开发前恢复点：提交`1680a36`保存当前已跟踪代码，未把既有测试结果和DLL备份加入提交。
- 官方基线：确认旧版`BasicDemo`采用`GetImageBuffer`轮询；本Demo改以官方`Grab_Callback`和`MultipleDemo`的`MV_CC_RegisterImageCallBackEx_NET`回调为基线。
- 双相机链路：两台相机分别注册SDK回调、配置连续采集或Line0硬触发，并使用独立预分配非托管缓冲执行`MV_CC_ConvertPixelType_NET`。
- Mat生命周期：SDK转换缓冲只建立Mat头，随后`Mat.Clone()`生成独立对象；显示使用容量1最新帧槽，覆盖帧立即释放，UI线程消费Mat并释放旧Bitmap。
- 统计口径：回调内分别记录SDK像素转换、Mat建立、Mat独立复制、最新帧发布和回调业务总耗时；UI另记Mat转Bitmap和换图耗时，停止采集后统一计算平均、P50/P95/P99/P99.9、最大值和超过20/30/50ms次数。
- 默认参数：`Release|x64`、Line0硬触发、预热10帧、每相机正式10000帧、实时显示开启、显示刷新上限20FPS；参数均已放到主窗体控件。
- 输出：程序目录`Results\时间戳`生成两份逐帧CSV和`测试汇总.txt`，汇总包含2026-08-21主程序关闭AI对照口径。
- 验证：Windows PowerShell静态规则检查通过；`Release|x64`重建0警告、0错误；隐藏启动4秒保持运行，工作集约60.8MB、线程15，证明SDK和OpenCvSharp依赖可以装载，随后只终止本轮PID且未打开相机。
- 现场边界：当前没有占用两台相机执行真实采集，Demo性能结论必须等待相机空闲后完成Line0显示开/关两轮正式测试。

## 2026-08-22：海康框架切换官方MVCamera类A/B测试

- 分支恢复点：已切换到`codex/hik-native-class-ab`，提交`cb0a95c`作为改造前恢复点。
- 第一阶段：引入与双相机Demo哈希一致的官方`MVCamera.cs`，新增框架中立的设备信息、枚举参数和数值参数模型；暂未改变现有相机运行行为。
- 验证：第一阶段`Release|x64`编译通过，0错误；现有172个历史警告保留并单独识别。
- 第二阶段：`CameraHik`已完整切换官方原生类，设备枚举、参数、触发、主动取图和回调Mat转换均不再经过托管海康DLL。
- 资源路径：每台相机持有一块按需扩容的非托管BGR8/Mono8转换缓冲；回调通过`Mat.FromPixelData`建立视图并`Clone()`为独立Mat，关闭相机时释放缓冲。
- 界面适配：相机添加页和实时调试页改用框架中立设备、枚举和数值模型，业务接口不再暴露`MvCameraControl`类型。
- 依赖收口：主工程已移除`MvCameraControl.Net.dll`引用和输出复制，静态门禁确认Release目录不再包含该托管DLL。
- 生命周期：退出时先完成方案排空和相机`Dispose()`，成功后才调用`FinalizeSDK()`；静态门禁会阻止顺序回退，资源未排空时不强行卸载SDK。
- 验证：原生类静态检查通过；`Release|x64`完整重建0错误、171个既有警告，较切换前减少1个`Finalize`命名冲突警告。
- 原生运行：SDK初始化602.984ms、枚举431.273ms、设备数0、反初始化成功；无原生DLL装载、入口点或调用异常。
- 隐藏启动：8秒时进程响应正常，18线程、工作集133.50MB、专用内存106.79MB、主窗口句柄0；两台配置相机均执行了原生枚举，本机无对应序列号所以未打开。
- 验收边界：当前并行修改的`LAB颜色识别`节点空值导致方案启动未完成，本轮整机启动不计为通过；本机无相机，真实双相机回调性能仍需测试机按Line0同条件采样。

## 2026-08-22 颜色识别整体判定订阅

- 修复多条件判定无法选择颜色识别结果的问题：颜色识别结果顶层新增可见的“整体判定OK”布尔订阅出口。
- `IsOk`保存LAB颜色、面积、占比和已启用线序规则形成的原始结果，`JudgeOk`初始值与其一致并支持多条件统一回写。
- 颜色识别结构化结果显式声明为`AlgorithmResult`类别，避免派生结果类型被订阅目录误分类。
- 新增`ColorDiscernMultiConditionSubscription.Tests.ps1`专项回归检查。
- 专项检查和`Release|x64`编译通过，0错误；程序集反射确认“整体判定OK”为多条件可见的单值布尔核心出口。

## 2026-08-22：YOLO隔离worker改造前100次耗时基线

- 测试口径：`Release|x64`，客户12类`DET`加密模型，CPU设备，输入尺寸640，置信度/NMS阈值`0.6/0.7`，固定使用同一张`1024x1280`端子图；先预热10次，再正式统计100次。
- 链路范围：完整覆盖当前`Mat.Clone -> Marshal.Copy -> Base64 -> JSON -> 标准输入worker -> Base64解码 -> 恢复Mat -> 原生推理 -> JSON结果`端到端路径，模型初始化单独计时。
- 基线结果：模型初始化`598.87ms`；正式100次平均`145.25ms`、P50 `144.52ms`、P95 `161.39ms`、P99 `167.01ms`、最小`123.06ms`、最大`186.70ms`；超过150ms共30次，超过200ms为0次。
- 客户端拆分：Clone平均`1.68ms`、Marshal.Copy平均`0.63ms`、Base64平均`6.06ms`、JSON序列化平均`11.26ms`、结果反序列化与映射平均`0.06ms`；worker往返平均`125.37ms`，其中仍包含worker端解码、恢复Mat和原生推理。
- 正确性与资源：100次均返回4个目标且无失败；客户端发生Gen0/Gen1/Gen2回收`91/89/89`次；worker私有内存起始/结束/峰值`268.52/232.92/310.71MB`，未观察到100次内单调增长。
- 对比规则：实验版必须继续使用完全相同模型、图片、参数、Release构建、预热10次和正式100次，并同时报告平均、P50、P95、P99、最大值、GC与内存，禁止只对比单次或纯原生推理耗时。

## 2026-08-23：YOLO共享内存worker实验

- 分支与恢复点：从隔离worker基线提交`494a64e`创建`codex/yolo-shared-memory-worker`；直接Mat性能实验保留在`codex/yolo-direct-mat-ab`，提交为`1b94765`，兼容性结论提交为`93f1caf`。
- 直接Mat对比：相同模型、图像和100次口径下，旧Base64 worker平均/P95/P99/最大为`142.93/159.51/172.76/181.32ms`，直接Mat为`49.36/53.89/54.23/55.95ms`，平均下降`65.47%`；旧链路Gen0/1/2为`70/70/70`，直接Mat为`2/0/0`。
- native阻断：无监督真实模板推理后再初始化YOLO，稳定在`NativeYoloInit`报`HRESULT 0x8007007F`；无监督目录使用`OpenVINO 2026.1 + onnxruntime 1.17`，YOLO根目录使用`OpenVINO 2025.4 + onnxruntime 1.25`。直接Mat不能作为同时包含无监督节点时的生产默认方案。
- 新传输路径：保留每个YOLO模型实例一个独立worker，图像由主进程按Mat真实`Step`逐行写入可复用命名共享内存；标准输入JSON只发送映射名称、宽高、类型、步长、字节数、阈值和偏移；worker直接在共享内存指针上建立Mat头，不再执行Clone、Marshal.Copy到byte数组、Base64编码/解码、图像JSON和worker端恢复Mat。
- 关键参数：共享映射按`4MB`粒度扩容，单模型worker最大`512MB`；容量足够时复用，扩容时生成新映射名称；多个模型实例不共用映射和请求锁，不增加全局AI并发限制。
- 生命周期：同一模型的写入、请求和响应继续串行；关闭与检测共用同一锁，关闭后拒绝新请求；客户端管道、进程、映射视图和映射对象逐项释放；worker保证先释放Mat头再归还映射指针，模型销毁异常也不能跳过映射释放。
- 无模型正确性：隐藏worker跨进程读取固定18字节BGR图像，零拷贝Mat全部通道像素和为`171`；非连续ROI生产复制方法循环`1000`次全部一致，最终累计`2.037ms`；扩容复用、两个客户端名称隔离、超过512MB拒绝和Dispose等待活动请求均通过。
- 最终传输压力：`Release|x64`、`1024x1280 BGR`、每帧`3932160`字节，稳定性修复后重新执行500次完整共享内存写入、控制消息往返、worker零拷贝Mat和全图像素扫描校验，平均`20.347ms`、P50 `19.782ms`、P95 `26.332ms`、P99 `28.900ms`、最大`33.428ms`，500次内容校验失败0。
- 性能解释：上述`20.347ms`包含PowerShell测试端`WriteArray`和额外的全图`Cv2.Sum`校验，不是生产AI总耗时；它证明共享传输稳定且上界明显低于旧worker端到端`142.93ms`，但不能与直接Mat的`49.36ms`相加后当作正式推理结论。
- 构建与审查：`Release|x64`完整编译0错误、171个既有警告；`Release|AnyCPU`编译通过并保留既有架构警告；独立审查提出的Detect/Dispose竞态、AnyCPU unsafe配置和异常清理问题均已修复。
- 当前限制：无监督真实推理兼容测试后，本机YOLO native初始化持续返回授权`-2`，真实加密模型尚未进入共享内存Detect，因此同模型100/500次AI总耗时、GC、CPU和worker内存最终同比必须在许可证恢复后补测，当前不虚构该数据。

## 2026-08-23：现场AI耗时分析口径纠正

- 当前日志：`C:\Users\34652\Desktop\20260823.log`中共有`5756`条AI检测节点结束样本，平均/P50/P95/P99为`89.37/89/97/102ms`；每个AI节点排除首轮后最大`111ms`，首轮两次为`155/154ms`。
- 不可同比原因：当前磁盘中的昨日`C:\Users\34652\Desktop\日志测试\20260822.log`只有模型加载记录，没有AI节点运行样本；此前提到的`193ms`只是历史观察值，不是可重新验证的昨日平均值。
- 结论纠正：不得使用单次`193ms`与今天平均值计算所谓`53.7%`降幅，也不得据此写“优化效果成立”；本次只能陈述今天日志自身的分布与稳定性。
- 强制规则：性能同比必须同时具备双方原始数据、相同统计字段、相同测试条件和明确样本量；缺少任一项时，只报告当前数据并标注证据缺口，不计算提升比例、不把单次值替代平均值、不下跨版本优化结论。

## 2026-08-23：测试机相机回调与AI节点现场验收基线

- 对比输入：旧组1为`C:\Users\34652\Desktop\日志测试\1\20260821.log`，旧组2为`C:\Users\34652\Desktop\日志测试\2\20260821.log`，当前组为`C:\Users\34652\Desktop\20260823.log`；`20260822.log`只有启动与模型加载记录，不参与耗时同比。
- 同口径核对：三组均使用相同两台相机、`1440x1080x3`输入、AI配置`1-2_encrypted.ai`、模型`best.onnx.encrypted`以及节点`2.AI检测`和`21.AI检测`；回调统一取“SDK回调结束/回调总耗时”，AI统一取“节点结束/实际经过”。
- 旧回调：旧组1共`21672`帧，平均/P50/P95/P99/最大为`11.038/10/21/26/72ms`；旧组2共`36657`帧，为`11.133/10/21/26/63ms`。
- 当前回调：共`5756`帧，其中`5755`帧记录为`0ms`、首帧为`1ms`，P50/P95/P99均为`0ms`、最大`1ms`；日志只有1ms分辨率，因此只能表述绝大多数回调低于1ms，不能表述真实耗时等于0。相对旧组平均回调占用至少下降`90.94%～91.02%`，最大值下降`98.41%～98.61%`。
- 旧AI节点：旧组1共`10232`次，平均/P50/P95/P99/最大为`217.426/217/245/260/308ms`；旧组2共`16964`次，为`220.288/220/248/264/294ms`。
- 当前AI节点：共`5756`次，平均/P50/P95/P99/P99.9/最大为`89.37/89/97/102/109/155ms`；仅两个节点各自首轮为`155/154ms`，排除首轮后最大`111ms`，超过`193ms`和`200ms`均为0次。
- AI同比：相对两组旧日志，平均减少`128.06～130.92ms`、下降`58.90%～59.43%`；P95下降`60.41%～60.89%`，P99下降`60.77%～61.36%`，最大值下降`47.28%～49.67%`。
- 证据边界：当前运行约16.5分钟，旧组分别约34分钟和53分钟，且SDK回调采样频率并不完全一致；本结果确认当前版本自身及相同字段下的明显改善，但相机总链路仍需固定触发节拍长稳复核，不能把回调不足1ms解释成全部图像转换不足1ms。

## 2026-08-23：生产队列不得把流程等同于AI

- 设计纠正：生产队列中的“流程处理”是用户可编辑的完整节点图，不是固定AI阶段；流程可组合逻辑判定、AI检测、无监督检测、大模型、传统视觉、测量、定位、结果处理、通信及其他合法节点。
- 上下文规则：`WorkpieceId`由触发准入生成并贯穿整轮流程、条件分支和组合模块，任何算法节点都不得自行生成或改写工件顺序。
- 信号规则：需要顺序保证的PLC、CameraIO、Modbus、TCP和串口节点按`OrderDomain + WorkpieceId + SignalSequence`校验，但继续保留用户在流程图中编辑的位置与执行语义，不强制改成“算法完成后统一写回”。
- 状态边界：当前仅完成设计原则记录，生产者队列、流程执行上下文和有序信号提交器尚未编码。

## 2026-08-23：流程编辑期识别有序外发节点

- 当前业务白名单：串口发送`ComSend`、相机IO`CameraIO`、PLC写入`PLCWrite`、Modbus写入`ModbusWrite`四类节点需要进入有序信号设计；分类依据为真实`NodeType`或统一能力接口，不依据可编辑节点名称。
- 明确排除：当前不把读取节点、软触发、`CameraIOManual`及其他通信类型自动视为有序外发节点，未来增加类型必须由业务明确确认。
- 编辑期规则：保存流程及生成运行快照时递归扫描顶层节点、条件分支和组合模块，生成包含流程版本、节点路径、节点ID、设备身份和顺序域的不可变外发清单；已经排队的工件继续使用入队时版本。
- 运行期规则：编辑期清单只描述可能路径，实际`SignalSequence`按本轮真正到达的外发节点生成；未命中的分支在流程终态明确封口，防止后续`WorkpieceId`永久等待。
- 状态边界：本轮只确认和记录设计规则，尚未修改四类节点及流程编辑器代码。

## 2026-08-23：相机生产者队列与有序信号完整计划

- 计划版本升级为`V2.32`，新增相机原始帧、工件流程、有序信号和UI最新帧四通道架构，以及触发准入、工件身份、多相机帧绑定、流程快照、异常终态和停止排空设计。
- 第一阶段单流程工件并发固定为1，不让现有节点实例重入；不同流程保持并行。有序信号协调器从第一版支持未来乱序完成，不把流程固化为AI链路。
- 生产满载策略定为触发前准入、高水位暂停、满载报警停机；SDK回调不等待队列空间，已经批准的生产工件不得静默丢弃，预览继续使用最新帧模式。
- 外发顺序按`ProductionLane + WorkpieceId + SignalSequence`校验，四类节点保持用户编辑位置；前序工件失败、缺口、发送未知或超时均停止后续发送，不盲重试和越序。
- 计划拆为10个Git小批，从核心契约、流程扫描、上下文传播、工件队列、海康票据、有序协调器、四类节点接入到Release真实1000轮和2小时长稳；每批专项测试、独立审查和Git提交。
- 状态：计划已写入，生产代码尚未开始修改；改造前恢复点以当前已测试分支为准。

## 2026-08-24：工件执行与有序信号核心契约

- 分支恢复点：已测试基线保留在`codex/8月23已测试待上机验证`的`2dee9b0`；本批在`codex/workpiece-queue-ordered-signals`开发，不改写基线分支。
- 工件身份：新增`WorkpieceIdentity`，以`SessionEpoch + Sequence`唯一标识工件；序号必须大于0，不同生产会话禁止直接比较先后。
- 执行上下文：新增`WorkpieceExecutionContext`，冻结流程版本、生产顺序域、触发来源、UTC创建时间和取消令牌，并为实际到达的外发节点分配工件内信号序号。
- 竞态保护：信号序号分配与唯一终态封口共用单工件实例锁，保证封口后不会在检查与递增的间隙继续产生信号序号；重复封口保持第一次终态。
- 外发契约：白名单严格限定`ComSend`、`CameraIO`、`PLCWrite`、`ModbusWrite`；新增不可变`OrderedSignalIntent`、明确的`Confirmed/Rejected/Unknown`确认结果及节点能力、顺序协调、设备串行和上下文传播接口。
- 行为边界：本批只建立纯内存契约，尚未接入相机回调、现有流程执行入口或四类真实发送节点，因此当前客户机生产行为不变。
- 验证：Debug与`Release|x64`完整构建均0错误、保留170个既有警告；专项测试并发分配2000个信号序号连续且无重复，跨会话保护、唯一终态、封口后拒绝、信号意图冻结和四类白名单全部通过。

## 2026-08-24：有序信号契约独立复审修复

- 复审发现：首版`OrderedSignalIntent`公开发送委托会绕过协调器，构造函数允许伪造或重复`SignalSequence`；序号分配与意图提交之间还能被工件封口插入，存在漏发或封口后迟发窗口。
- 原子协议：改为上下文在单工件实例锁内一次性预约`IntentId + SignalSequence + 在途名额`并立即交给协调器；封口请求立即拒绝新意图，但只有全部在途意图返回后才发布唯一终态。
- 执行边界：意图构造函数和真实发送入口降为框架内部可见，外部节点只能调用上下文的协调提交入口；同一`IntentId`的重复提交必须复用首次结果或明确拒绝，禁止再次执行设备动作。
- 设备互斥：端点串行器接口改为只返回独占租约，实际意图动作由框架协调器在取得工件顺序权和端点租约后执行，第三方不能通过串行器直接触发意图内部委托。
- 确认语义：发送结果拆分为`DeviceAcknowledged`、`LocalCallCompleted`、`Rejected`和`Unknown`；设备确认必须携带可记录凭据，没有远端协议确认的串口或IO只能标记本地调用完成。
- 键规范：生产顺序域和设备键统一去除首尾空白并按大小写不敏感规则生成稳定键，避免同一设备因字符串写法不同绕过互斥。
- 测试补强：并发预约2000个意图，序号与意图ID均无重复；请求封口后终态保持活动，逐个释放到最后一个意图才完成；外部无法构造意图、无法读取发送委托、封口后无法再预约，设备确认无凭据会被拒绝。

## 2026-08-24：发送结果与工件终态原子合并

- 第二轮复审P0：流程先请求`Succeeded`、最后一个信号随后返回`Rejected/Unknown`时，旧契约只释放在途名额，可能在节点收到失败前发布成功终态。
- 第三轮独立复审确认P0已经关闭；继续修复残留P1：任意`OperationCanceledException`不得伪装成发送前安全取消，现统一按`Unknown/Stopped`处理，只有协调器明确返回`CancelledBeforeStart`才允许进入取消终态。
- 专项测试新增公开`SubmitOrderedSignalAsync`路径，覆盖协调器明确拒绝、空结果、普通异常和取消异常，防止只通过内部反射验证而遗漏真实提交链路。
- 原子修复：`FinalizeSignalIntent`必须携带协调器最终发送状态；移除在途意图、累计最严重信号状态和发布工件终态均在同一把单工件实例锁内完成。
- 安全映射：`DeviceAcknowledged/LocalCallCompleted`不覆盖流程结果，`Rejected`升级为`Faulted`，`Unknown`或协调器异常升级为`Stopped`，只有确认副作用未开始且意图已永久移除时才允许`CancelledBeforeStart -> Cancelled`。
- 取消所有权：协调器任务返回取消前必须原子移除意图并保证后续绝不执行；外部副作用一旦开始，不允许用取消提前返回，必须等待明确结果或冻结端点后返回`Unknown`。
- 端点租约：新增`ISignalEndpointLease`，租约必须携带规范化`EndpointKey`、公开`IsReleased`并保证重复释放无害，防止`SemaphoreSlim`被异常路径重复增加许可。
- 验证：专项测试覆盖2000个在途意图、重复终结幂等、最后一个拒绝把成功升级为故障、未知结果升级停机、无信号正常成功以及封口后拒绝迟到意图；Debug构建0错误。

## 2026-08-24：不可变流程计划编译器

- 新增`ResourceManagement/FlowExecutionPlanCompiler.cs`，递归扫描顶层流程和组合模块，冻结节点、连线、流程版本及四类生产外发节点元数据。
- 新增流程静态诊断：重复节点ID、连线引用缺失、组合模块快照缺失、递归深度超限、分支路径分析超限，以及可共同执行但无明确先后关系的外发节点。
- 兼容旧顺序流程：无画布图时按节点列表合成稳定的`legacy-源ID-目标ID`连线，并纳入不可变计划。
- 新增`Tests/FlowExecutionPlanCompiler.Tests.ps1`，覆盖快照不可变、只读集合、组合模块递归、四类白名单、并行乱序拦截、True/False互斥分支、断线诊断和旧顺序连线，连续5轮通过。
- 本批只提供编译与校验能力，尚未接入生产运行；从活跃`Process`取样必须等统一编辑版本门实现后才能保证跨集合原子一致，计划书已明确该边界。
- 验证：Debug与`Release|x64`完整编译均为0错误，保留项目既有警告；测试全程后台运行，未启动软件界面。
- 独立审查P0：禁用条件节点在运行器中会放行全部分支，首版编译器却继续按True/False互斥，可能漏掉两个同时外发节点；现已改为禁用节点不产生分支约束，并增加回归测试。
- 同步修复审查P1中的静态语义差异：多组件起点按运行器连通分量规则推导；画布无连线且无起点时按旧顺序回退；空组合模块直接阻止生产。
- 修复后专项测试连续5轮通过。跨集合原子取样和完整可执行快照仍明确留待统一编辑门批次，当前编译器不得单独作为生产运行快照。

## 2026-08-24：工件上下文与严格FIFO入口基础设施

- 新增`ResourceManagement/WorkpieceRuntimeInfrastructure.cs`：包含异步上下文访问器、工件身份生成器、容量有界的单流程FIFO入口、满载异常、状态快照和幂等执行租约。
- 默认容量8包含活动工件，单流程执行并发固定1；排队取消永久移除，释放只推进FIFO队首，入口满载在任何流程副作用开始前明确拒绝。
- 独立审查发现首版身份未包含顺序域，导致不同生产线同序号身份相等P0；现已修改`WorkpieceIdentity`，把规范化`OrderDomain`纳入构造、比较、相等、哈希和日志文本，执行上下文同时校验域一致。
- 独立审查发现首版可变释放标记会跨`ExecutionContext`分支共享；现改为分支独立的不可变栈赋值，后进先出恢复，乱序释放明确抛错。
- `ResetSession`收窄为内部接口，真实接入时必须由排空控制器持有会话切换权，普通节点和插件不能直接更换纪元。
- 新增`Tests/WorkpieceRuntimeInfrastructure.Tests.ps1`，每轮验证100000个身份、嵌套上下文、乱序释放、满载计数、严格FIFO、重复释放和取消移除；连续3轮通过。
- 当前尚未改动真实运行入口，本批只作为可独立恢复的基础设施提交；下一提交接入`Solution`、`Process`和`ProcessTrigger`。

## 2026-08-24：工件上下文接入真实流程入口

- `Solution`现在按首活动会话重建工件纪元，并为每个流程组每轮发布共享工件上下文；四个`Process`运行入口均在节点和软触发前进入容量8、并发1的严格FIFO。
- 删除跨工件共享的流程触发委托；流程触发节点直接等待同组唯一被动流程，并使用`ProcessInvocationResult`传播本次失败、取消和满载，不再读取可能被后续运行覆盖的结果。
- 新增绑定工件身份的异步流程路径，触发环在排队前失败；旧会话或已封口延迟分支不复用旧路径和旧上下文。
- 新增在途流程分支租约，原子关闭“继承检查后父工件先封口”的窗口；父终态等待全部分支退出，失败、取消及未明确完成分支分别合并为对应故障终态。
- 四个入口最终封口只读取各自局部`invocationSucceeded`，共享`Process.Success`不再允许排队调用跨轮污染；调用方取消令牌已贯通FIFO、上下文和节点执行器。
- 保留旧公开`Run(bool)`、`RunInternal(...)`、`TriggerProcess(string)`签名；新增结果接口服务框架内部终态汇总。
- Debug与Release x64构建均为0错误；流程接入行为测试、工件契约、流程编译器、10万身份与FIFO压力四项均通过，全程未启动软件窗口。
- 独立审查最终结论为P0=0、P1=0，确认共享`Success`竞态、流程分支准入与工件终态合并已闭环；仅保留旧插件只按名称反射同名`TriggerProcess`重载可能歧义的P2，不影响当前直接调用。
- 当前仍未接入相机生产票据、图像字节预算、多相机绑定、实际有序信号协调器和四类发送节点，下一批从相机/工件准入或有序信号协调器继续。

## 2026-08-24：相机生产票据与图像字节预算基础设施

- 新增按相机稳定键分区的严格FIFO触发票据注册表；票据在真实触发前绑定`WorkpieceIdentity`并预留估算图像字节，同工件同相机重复登记和故障后继续登记均明确拒绝。
- 新增工件帧字节预算管理器，转换后按Mat实际`Rows × Step`校正记账；校正与释放串行化，释放幂等，全部票据结束后当前字节和活动租约必须归零。
- 新增拥有独立Mat与预算租约的`CameraFrameEnvelope`，图像和预算只允许成对转交；未转交、异常和取消路径负责完整释放。
- 内部FrameId或SDK帧号断层、票据超时、原始入口故障及实际字节超预算会锁定相机并清场全部后续票据，后续流程不会无限等待。
- `Tests/CameraWorkpiecePipeline.Tests.ps1`使用真实OpenCvSharp Mat验证FIFO工件绑定、300字节实际校正、断层后续票据清场、超时后续票据清场、故障门禁和预算归零；Debug x64编译0错误，专项测试通过。
- 当前只完成可独立恢复的基础设施，海康转换线程、图像源等待器、OutputImage预算所有权和方案停机尚未接入，下一提交继续完成真实链路。

## 2026-08-24：相机票据并发、迟到帧与关闭边界加固

- 软件触发提交点改为同一临界区内完成关闭复核、票据武装和活动SDK调用计数；真实SDK动作在锁外执行，避免SDK等待回调造成死锁，同时保证关闭与触发只能有一方先成功。
- 每台相机使用独立状态锁；取消、帧发布、入口故障、超时和重置按相机串行，不同相机可并行。超时扫描遇到忙相机本轮跳过，不能因一台相机拖住其余相机。
- 新增迟到帧恢复门禁：清场已武装票据或收到无法可信绑定的旧帧后，普通重置拒绝；只有物理停流、回调排空和重新取流完成后，内部确认入口才可恢复。
- 生产故障改为注册表自有后台线程FIFO派发；每相机待派发故障计数归零前禁止恢复，订阅者不在SDK回调和相机状态锁内执行，也不依赖通用线程池时效。
- Mat实际字节、预算校正和信封构造增加统一异常清场；Mat释放异常不再打断预算、票据及工件执行租约归还。
- 注册表实现显式关闭：阻止新操作，等待活动SDK触发最多2000ms，归零后失败全部排队票据并归还资源；超时明确抛错并允许解除SDK阻塞后重试，故障线程自行释放事件句柄。
- 专项测试覆盖同相机触发取消边界、不同相机并行、SDK动作未返回禁止重置、SDK返回后迟到帧风险仍禁止普通重置、未武装旧帧强制流重启、故障派发排空、关闭票据转Stopped、预算归零及释放后拒绝操作。
- Debug与Release x64完整构建均为0错误；相机管线、工件执行契约、工件运行基础设施和流程入口集成四组回归通过。独立审查最终结论：`P0=0、P1=0`。
- 本提交仍是可独立恢复的基础设施层，尚未接入`CameraHik`、`NodeImageSource`、`OutputImage`和方案停机；下一批完成真实链路后才能进行客户机相机压力验证。

## 2026-08-24：OutputImage接管相机实际字节预算生命周期

- `OutputImage`新增可插拔外部生命周期所有权，按引用去重并延迟到最后一个图像租约释放后统一回收；用于让相机实际字节预算与Mat保持完全相同的存活区间。
- 新增`FromOwnedSingleImageWithLifetime`工厂，所有权转移成功后调用方不再释放预算；构造异常会释放未接管生命周期，避免双释放和漏释放。
- 最终回收逐项隔离异常，Mat、父级图像依赖和外部生命周期任一释放失败都不会跳过其余资源。
- 图像生命周期专项测试通过：外部生命周期重复登记去重、租约期间不提前释放、最终Mat与外部资源各释放一次；原50000轮压力、1000轮快照释放竞态和1000轮依赖竞态保持通过。
- Debug与Release x64编译0错误；独立审查结论`P0=0、P1=0`。本批未修改真实相机和图像源生产行为。
## 2026-08-24：线路硬触发改为停止前持续等待

- 根据测试机`19:04:55.049～19:04:55.149`日志定位：两台相机均已成功进入`LINE0`取流，失败来自图像源把方案中的`TimeOut=100ms`错误用于等待外部线路信号，并非海康SDK设置Line0失败。
- `LINE0～LINE4`生产票据改用UTC最大截止时间，等待任务使用无限延时并只响应运行会话停止令牌；外部帧未到达时不再超时、停方案或把相机标记为生产故障。
- `Auto/SOFT`继续使用图像源`TimeOut`约束软件触发命令和返回帧，避免SDK命令卡住后无限占用相机。
- 相机生产管线专项新增三项边界：硬触发超过原20ms超时仍等待、随后物理帧可正常绑定、停止令牌可解除等待并归还全部图像预算。
- 验证结果：Debug x64完整编译与Release x64核心编译均为0错误；触发语义、图像源方案权威性、统一回调、相机等待器、回调快路径、工件流程集成全部通过；相机工件管线基础边界连续5轮通过，补强后每轮包含100次`registry.Cancel`与`TryPublishFrame`真实竞态并连续复跑3轮通过，且两种先后结果均有实际样本。
- 审查提出的三项P2测试缺口已经补齐：停止必须精确得到`OperationCanceledException`，软触发无帧必须精确得到`TimeoutException`，硬触发票据取消/到帧竞态结束后必须无Mat预算和工件租约残留；未被注册表接管的测试Mat由测试立即释放。
- 独立审查最终结论为`P0=0、P1=0、P2=0`，并由审查智能体在独立进程中再次连续复跑3轮相机工件管线测试，全部通过且未修改生产代码。
- `FixedIntervalTriggerArchitecture.Tests.ps1`由Windows PowerShell 5解析时因该文件既有中文编码损坏产生语法错误，未作为本轮通过项；该脚本失败发生在加载源码前，与本轮硬触发实现无关。

## 2026-08-24：硬触发第2帧跨流程FIFO修复

- 根因修复：原实现只在图像源等待期间拥有回调接管资格，完整算法执行期间到达的第2帧会在原始复制前被忽略，下一次收到SDK第3帧后触发断层安全停机。
- 当前行为：首个硬触发生产票据启用运行会话级持续接管；后续帧在没有下一票据时进入每相机容量8的FIFO，下一工件登记后按顺序立即执行。
- 触发隔离：仅`LINE0～LINE4`启用跨流程硬触发缓存；软触发继续要求帧在本次SDK命令武装之后到达，预览帧不进入生产FIFO。
- 停止边界：取消、生产故障、物理停流和注册表释放都会清场待执行Mat；停止后的迟到帧不得进入下一轮。
- 满载边界：第1～8张待执行帧正常积压；第9张明确报警停机并释放缓存，不静默丢弃、不覆盖旧帧、不阻塞SDK回调。
- 内存边界：全部相机共享动态字节预算，严格取流程图像预算25%，无资源档案时回退256MB；不设固定下限，预算不足一张Mat时明确报警，不能突破整机资源档案。
- 复审修复：无票据期间原始入口或Mat转换失败立即停机；硬触发持续接管期间拒绝生产软触发和预览；原始包冻结所属注册表，旧会话迟到帧不能进入新会话；关闭按单相机锁清场。
- 停止竞态：相机通过方案层一次性取得活动注册表，不再分别读取注册表和`IsRunning`；最后一个运行会话退出时先关闭旧注册表入口并排空缓存，停止前已复制的迟到包也不能重新写入旧FIFO。
- 预览兼容：回调同时冻结帧路由注册表和生产归属；生产门闩关闭后仍消费手动软件触发待返回计数，连续100次停止后预览无泄漏；旧注册表释放后迟到帧无异常返回未接管，不误报Mat转换失败，也不回退故障新会话。
- 入口失败归属：原始缓冲不足或回调复制异常不再只保留本批首条故障，改为无锁FIFO逐项冻结和处理；预览失败、随后生产失败不会相互覆盖，回调仍只入队和计数，不执行日志、票据锁或停机。
- 诊断：新增当前等待硬触发帧数、当前/峰值字节、全局预算、历史峰值和累计缓存帧数快照。
- 验证：Debug x64编译0错误；相机工件管线连续3轮通过，每轮含100次停止竞态、停止后旧包拒绝、64MB低内存档案严格分配16MB、第二帧FIFO、容量8满载、多相机共享字节预算、无票据转换失败、软硬触发隔离及Mat/预算归零；四项相关架构回归通过。
- 最终验证：`Release|x64`强制重编译0错误、155个项目既有警告；独立审查最终为`P0=0、P1=0、P2=0`。本机未连接真实相机，仍需客户机按连续硬触发节拍验证第2帧开始的实际流程执行和日志顺序。

## 2026-08-26：共享变量节点默认显示高级订阅结果

- 定位共享变量节点无法直接订阅图像源的原因：图像源“输出图像”属于复杂图像结果，在通用订阅控件中默认归入高级结果；共享变量节点使用任意类型契约时不会自动展开高级结果。
- `NodeSubscription`新增调用方可设置的默认高级结果展开入口，保持原有右键“显示高级结果”能力不变。
- 共享变量节点写入订阅初始化时默认展开高级结果，使其可以直接选择图像源“输出图像”等复杂结果，同时不影响其它普通节点默认订阅列表。
- 订阅消费者契约测试新增共享变量默认展开高级结果检查，避免后续误改导致图像源再次不可选。

## 2026-08-26：修复硬触发回调与下一流程票据的时间竞态

- 客户机日志中相机SDK第1959帧绑定工件1959后，第1960帧与工件1960仍完全连续，但因帧进入回调早于下一票据数毫秒，被软件触发式旧帧规则误判并主动停止方案。
- 相机生产管线把回调时间因果校验收窄到软件触发；`LINE0～LINE4`硬触发继续按每相机`FrameId + SDKFrameNum`连续性和FIFO队首工件绑定。
- 未改动海康SDK回调快速路径：回调仍只冻结身份、复制原始数据并投递单相机转换线程，不增加票据锁、等待或日志。
- 专项测试新增“回调先到、票据后建、Mat最后发布”的现场竞态复现，同时保留软件触发旧帧拒绝、停止清场和帧号异常保护。
- Debug与Release x64均0错误；相机生产管线首次通过并连续复跑3轮通过，回调快速路径、统一回调、流程入口、方案参数权威性和海康触发语义关联测试全部通过。
- 独立只读审查结果为`P0=0、P1=0、P2=0`，确认没有引入跨相机串帧、停止后旧帧串入、Mat泄漏或锁顺序风险。

## 2026-08-27：软件触发单次取帧超时改为可恢复故障

- 用户确认图像源`TimeOut=100ms`表示单次取帧等待边界，当前轮没有取得帧可以失败，但不得取消整套方案；循环运行必须继续下一轮。
- 本批将软触发普通取帧超时从全局停机故障降级为可恢复告警，只失败同一相机当前及已经排队的工件，不取消其他相机和方案运行令牌。
- 为防止超时后的迟到帧绑定下一`WorkpieceId`，超时相机在下一次采集前必须完成停流、SDK回调与转换线程排空、重新取流和流代数确认。
- 软件触发SDK命令自身卡死或返回失败、相机帧号错乱、Mat转换失败和内存预算破坏仍保留不可恢复故障语义，不与普通“命令成功但未在本次等待内收到帧”混淆。
- 修复原先用合成未来时间调用全局`FailExpired`可能连带超时其他相机票据的问题；新增按`CameraTriggerTicket`精确失败的`FailTimedOutTicket`，只清理本相机本工件。
- SDK软触发任务增加原子阶段仲裁：任务仍在线程池排队时，超时方可永久取消其SDK调用；SDK命令已经进入后卡死则使用专用异常走不可恢复停机路径，禁止与节点侧停流重启并发操作相机。
- 帧完成与超时同时到达时重新核对注册表提交结果，已成功绑定的帧优先；真正迟到帧仍由流代数和票据状态拒绝，不能串到下一`WorkpieceId`。
- `Debug|x64`和`Release|x64`均构建成功、0错误；相关7项回归脚本全部通过，`CameraWorkpiecePipeline.Tests.ps1`额外连续5轮通过，总耗时15.123秒。
- 验证覆盖：同相机迟到帧隔离、下一轮停流排空后恢复、多相机互不取消、排队软触发超时后不再调用SDK、SDK命令卡死不并发重启、Mat预算最终归零。
- 独立只读审查最终结论为`P0=0、P1=0、P2=0`；剩余风险仅为真实海康设备上停流解除卡死命令及原生回调完全排空能力，需要客户机上机验证。

## 2026-08-27：定位软件启动后第一次运行失败

- 现场日志证明第一次运行在相机重启取流后立即进入100ms生产帧等待并超时，第二次同节点以94ms成功；问题是首帧冷启动没有独立宽限，不是方案曝光、增益或触发源丢失。
- 计划增加相机连接会话级首个生产帧500ms启动宽限，仅允许一个软件触发票据原子领取；不额外拍摄预热帧，后续全部恢复图像源100ms稳态超时。
- 实现改为两阶段预留：同一相机持锁完成`registry.Register`，成功后`Commit`消费资格；登记失败由`finally Dispose`自动归还，保证取得宽限的一定是首个成功入队票据。
- 相机成功连接时原子发布新资格；关闭失败不重置，只有原生关闭、回调排空和转换资源释放全部成功才结束连接会话；普通停流重启不重复宽限。
- 首帧专项128路并发只有1个领取者；100ms仅第一次变600ms，后续100ms；失败归还、重连重置和整数溢出保护全部通过，另有7项相机关联回归通过。
- `Debug|x64`和独立目录`Release|x64`均0错误；正常Release目录最初因运行程序PID 26784锁定而未覆盖，未中断用户软件。进程退出后已完成正常`bin/x64/Release`构建，0警告、0错误；首次帧专项测试通过，独立审查最终为`P0=0、P1=0、P2=0`。

## 2026-08-31：相机采集转Mat解除跨相机共享许可

- 先以`dc5bafc`保存工作区已有改动作为恢复基线，不撤销或修改已有训练模块工作；本次功能只改海康采集转图。
- 生产回调与主动取图均取消共享CPU转换许可，每台相机沿用独立线程、队列和缓冲，不改变单相机FIFO和工件顺序。
- 转换前、取得本相机实例锁后、SDK转换返回后核对取消；SDK不可强制中断，停止仍以线程实际退出为资源释放前提。
- Debug日志保留现场统计字段并增加独立通道标识；原CPU许可等待字段为0，但回调到Mat总耗时继续真实测量。算法、保存、UI及AI策略均未改动。
- 新增生产方法原文加SDK替身测试，首次通过2/4/8路各100帧并行、单相机互斥及取消释放验证；真实多相机性能改善等待用户提供改造后日志，不用模拟耗时代替现场结果。
- 最终14项相机关联回归全部通过；独立转换专项额外连续5轮、工件管线额外连续3轮及Release首帧宽限测试均通过，复跑耗时21.846秒。
- 测试兼容修正：回调隔离脚本补UTF-8 BOM以便Windows PowerShell读取中文；帧诊断脚本更新早期`param.Camera`断言为当前固定`camera`对象，并补充生产票据先登记后触发的断言。其他两个无BOM旧脚本使用PowerShell 7通过，未修改它们。
- Debug构建0错误/154警告，Release构建0错误/155警告；独立审查无P0/P1/P2发现。真实SDK、像素正确性、停流重启及多相机性能仍需现场日志和设备验证。
- Release主程序生成时间`2026-08-31 14:16:17`，SHA256=`CCD04E839C41D44EB9100142E3BF1D6A8F67D236CEEF7242C7622D3A62A625AA`；全程未操作桌面或触发实体相机。

## 2026-09-07：修复运行参数页本地图像测试按钮持续禁用

- 原因：Init调用SetEnable(false)后，未恢复单图测试和多图测试按钮的可用状态。
- 修改：初始化完成时根据有效图像源节点是否存在恢复两个按钮，无需相机连接；无有效图像源时继续禁用。
- 验证：静态核对初始化重置、有效图像源赋值及按钮恢复路径，git diff --check检查；未启动界面或实际运行检测流程。

## 2026-09-09：添加基恩士 Nano OverTcp 通信

- 已从当前工作分支切换到 `codex/keyence-nano-overtcp`，保留工作区已有改动。
- 按现有 PLC 操作方式新增基恩士 Nano OverTcp 添加页，控件全部放入 `.Designer.cs`，提供 IP 地址、端口、设备名称及确认按钮。
- 通过 `IPlc` 封装 `KeyenceNanoSerialOverTcp`，接入品牌、设备创建、连接开关、参数展示及方案保存恢复；松下通信保持现状。
- 设计器新增独立页签及默认 IP 资源，默认端口填写为 `8501`，可以按 PLC 实际配置修改；添加时校验名称、IPv4、端口范围及重复 IP/名称。
- 新设备实现 `IPlc`，连续读写直接委托通信库；离散地址按顺序读取，避免照搬三菱地址跨度算法导致基恩士位地址或跨区域地址取值错误。
- 验证：`Debug|x64` 构建到独立目录 `bin/KeyenceNanoValidation` 成功，0 错误；专项测试通过真实添加按钮、设备列表、参数显示、现有设备转换器保存恢复、已连接设备恢复重连、有限超时、同步/异步/连续/离散读写及失败状态检查。
- 视觉验证：实际窗体渲染图片布局正常，四个协议页签均可见；`git diff --check` 通过。测试仅连接本机模拟服务，尚未连接实体基恩士 PLC。

## 2026-09-09：修复多条件判定无法订阅 PLC 读取节点

- 原因：PLC 读取仅公开复杂结果对象，多条件只接受布尔、数值和文本单值，因此订阅目录过滤掉 PLC 读取节点。
- 修复：增加配置阶段可见的单值动态输出，多地址逐项公开；运行结果保留地址和真实类型，兼容原有读取结果对象。
- 同步修正 Int16/Int64 及整数数组的结果类型标记，以及参数恢复时六种类型的下拉选项映射和字符串长度；每次读取前清除上轮结果，避免失败、取消和禁用后读到旧数据。
- 验证：`Debug|x64` 独立目录构建成功；专项测试验证六种标量、四种多地址输出类型、未运行时订阅、配置更改后旧地址失效、参数恢复、多条件实际订阅树、本机 PLC 读取及多条件实际比较、失败结果清理。
- 回归：订阅端口目录、类型兼容、动态输出即时刷新、失败上游判定和基恩士通信专项均通过；`git diff --check` 通过。未连接实体 PLC。

## 2026-09-10：添加基恩士 KV300/Older 通信

- 在当前基恩士开发分支增加独立的 KV300/Older 添加页，使用 HslCommunication 的 `KeyenceKvOld`，与 Nano OverTcp 分开保存协议类型。
- 保持现有添加、重复检查、设备列表、连接开关和方案恢复逻辑，控件继续放在 `.Designer.cs` 中；旧方案缺少协议字段时默认 Nano OverTcp。
- 抽取基恩士 TCP 设备公共适配层复用读写及生命周期实现，分别创建两种协议客户端。
- 核对 DLL 实现发现旧系列读接口忽略长度、写接口只使用前两个字节；新增连续 DM 地址逐字读写适配，避免 32/64 位数值、浮点和字符串静默截断。多字读写仅支持连续 DM 地址，其他格式明确返回失败；单点位读取适配现有 PLC 节点的长度为 1 的数组接口。
- 验证：`Debug|x64` 独立目录编译通过；KV300/Older 专项通过真实添加按钮、设备列表、参数保存恢复、重连、R 位读写、完整多字整数、浮点、字符串及无效写入拒绝检查；Nano 原协议和缺少协议字段的旧方案、PLC 读取订阅专项均回归通过。
- 实际窗体渲染确认五个协议页签和输入控件显示正常；本次变更范围的 `git diff --check` 通过。通信仅在本机模拟服务验证，实体旧系列 PLC 尚未联机验证。

## 2026-09-10：修复 KV300/Older 的 DM 字寄存器位读取

- 用户提供 `9_8.Sol`，保存的 PLC 为 KV300/Older，读取节点为 `DM100 / Int16`；原方案只读，测试中改为 `DM100.1 / Boolean` 复现用户描述。
- 当前 12.3.0 DLL 的单点布尔读返回地址格式错误，带长度的布尔读返回未实现；需要在适配层读取 DM 字并提取指定的位，保持原有 R 继电器读写。
- 已核对正在运行的 HSLdemo 位于 `D:/HslCommunicationDemo-v12.5.1`，它使用 12.5.1.0 DLL，项目使用 12.3.0.0；独立本机测试确认新版单点 ReadBool 支持 `DM100.1`，旧版不支持，两个版本带长度的原生布尔接口均返回未实现。
- 在旧系列客户端补齐 DM 字内位同步/异步及连续位读取，位索引限制为 0 到 15；读取 DM 字后按低位在前提取，跨字读取转到下一个 DM，普通继电器继续沿用原协议。
- 验证：Debug|x64 编译成功；按用户方案设备和节点参数在随机本机端口测试，原 DM100/Int16 读取成功，内存切换 DM100.1/Boolean 后，DM100 为 2/0/32768/3 时分别得到真/假/假/真，多条件实际判定一致。
- 专项覆盖 16 个位索引、同步/异步/单点数组、跨字数组、混合继电器和DM地址、非法地址、零长度、地址溢出、通信失败及节点旧结果清理；KV300/Older、Nano及PLC读取订阅回归均通过。
- 原方案前后 SHA256 相同；项目 DLL 和 HSLdemo DLL 均未替换，未操作现场 PLC；本次范围的差异格式检查通过。
## 2026-09-10：补齐 KV300/Older 的 DM 字内 bool 写入

- 用户要求完成 `DM100.1` 的 bool 写入；在旧系列适配层按读取原字、修改指定位、写回原字处理，保持未指定的位。
- 同步和异步写入共享同一客户端写锁，并与普通 DM 整字写入共用，避免本客户端并发读改写丢位；不改变原有继电器协议或用户方案。
- 验证：Debug|x64 独立目录编译成功；新增位写入专项通过16个位索引的同步/异步置位和清位、其他位保留、单元素及跨字数组、同步异步混合并发、非法地址/数量/溢出拒绝、设备拒绝写入后的锁释放以及断开通信失败检查。
- 现有PLC写入节点通过有序提交实际调用验证，Boolean类型的True/False/1/0均能写入DM100.1；原有R位、KV300/Older整字读写、Nano及PLC读取订阅回归通过。
- 重跑读取方案测试时，发现用户文件已经保存为DM100.1/Boolean；修正测试中仅假设旧方案DM100/Int16的初始期望，兼容这两种已知方案状态。
- 测试仅使用随机本机端口模拟PLC，实体PLC未联机写入。读改写锁只保护同一客户端，不能阻止PLC程序或其他连接同时修改同一DM字，跨字写入也不是设备原子事务。

## 2026-09-11：增加海康相机自动重连守护机制

- 按宏晟参考框架思路，在方案单例中启动后台相机重连守护线程，每秒检查需要保持连接的二维相机。
- 相机SDK判断掉线后，若当前方案正在运行，先提交停机取消，避免断线期间的硬触发票据和后续帧继续混用。
- 海康相机实现连接状态探测、断线后容错停止取流、排空转换线程、关闭并销毁旧SDK句柄，再按SN重新枚举打开。
- GigE相机打开后设置1000ms心跳超时，便于网线或相机掉线后更快进入自动重连判断。
- 重连成功后重新应用图像源节点保存的曝光、增益、触发源、触发沿、延迟和超时参数；若该相机被启用图像源使用，则恢复取流。
- 改前已先提交当前相机回调入口调试日志，提交号为 `a512055`；本次自动重连改动尚未提交。
- 验证：`Debug|Any CPU` 调用MSBuild完整编译通过，0错误；仍有项目既有warning，未连接实体海康相机做拔线重连实测。

## 2026-09-11：ROI结果绘制增加直角旋转

- 在旧版 `ROI结果绘制` 参数界面增加旋转下拉框，选项为0、90、180、270度；控件放在对应 `.Designer.cs` 中。
- 参数保存新增旋转角度字段，0度保持原有借用图像路径，不额外复制图像。
- 非0度时使用OpenCV直角旋转输出图像，并同步旋转ROI框、文本位置、线、圆、圆弧、椭圆、轮廓和裁剪矩形等图像坐标结果。
- 慢耗时诊断日志新增旋转角度和旋转耗时，便于现场判断旋转功能带来的额外耗时。
- 验证：本次范围 `git diff --check` 通过；`Debug|Any CPU` 完整 Rebuild 编译通过，0错误，保留项目既有153个warning。
- 用户反馈旋转时文字不应跟随旋转；已调整为只旋转图像、ROI框和几何结果，文本显示项保持原显示位置与方向。

## 2026-09-11：结果发送节点参数界面预览

- 用户确定新节点名称为“结果发送”，放入“结果处理”大类，要求支持订阅四则运算、目标检测、测量模块及多条件判定的结果。
- 本轮先制作参数界面交互预览，参考现有多条件判定、AI结果发送窗体及 FormBase，采用青绿色标题栏、浅灰背景、白色表格、右侧上游结果树和底部按钮。
- 预览包含 PLC/Modbus 设备选择、发送列表、单值及多值取值配置；数据和设备均为示例，保存仅作用于预览，不执行实际通信。本轮不接入正式节点或修改现有通信逻辑。
- 验证：使用本机无界面浏览器检查订阅选择、增删行、指定明细、汇总、多值容量校验、布尔映射和设备切换；736、360、320像素宽度无横向溢出，无脚本错误。已检查界面截图，任务记录差异格式检查通过。

## 2026-09-12：实现结果发送节点

- 按用户确认的预览实现“结果处理 / 结果发送”，支持四则运算动态变量、目标检测明细、测量标量及多目标结果、多条件判定订阅。
- 支持 PLC/Modbus 设备选择和 bool、int、uint、ushort、short、string、double、float 写入，加入独立取值、转换及设备适配接口；窗体控件定义在 Designer 文件中。
- 已接入现有工件有序发送机制、节点工厂和结果处理工具箱，发送前冻结本轮值并完成转换，多值支持指定项、连续发送、汇总、容量与数量地址；失败不复用旧结果，不静默截断。
- PLC通过可选类型能力接口补齐short、ushort、uint、double，复用底层协议字序；Modbus按报文容量分批写入，字符串支持ASCII、UTF-8、GB18030及固定字节长度补零。
- 增加已知地址范围重叠检查、失败行及成功条数输出；每批只扫描一次上游图，反射属性目录按类型缓存。参数窗体使用编辑副本，保存前不影响运行参数；截图检查发现的行选择与编辑器不同步问题已修复并添加专项验证。
- 验证：Debug|x64独立目录bin/ResultSendValidation编译通过，0错误，新增节点代码无编译警告；真实程序集专项覆盖八种类型的本机PLC/Modbus协议值、四类订阅、多目标、超容量及溢出拒绝、零目标补零、字符串编码、现有方案多态恢复、实际参数保存、有序发送、快照隔离、发送前取消和部分失败。
- 回归：基恩士KV300/Older通信、DM字内位写入、PLC读取订阅、订阅目录及类型兼容性通过。本次修改范围差异格式检查通过；项目其他既有改动未处理。仅使用随机本机端口模拟服务，未连接现场设备。
- 使用说明见docs/结果发送节点.md；实际参数窗体截图保存在bin/ResultSendValidation/ResultSend-parameters.png。

## 2026-09-12：简化结果发送参数及未检出默认值

- 按用户要求修正订阅树、发送列表与检测项输入框的中文显示，内部仍保存稳定检测项键。
- 移除值处理、倍率、偏移及真假映射配置，结果按所选类型直接发送。
- 未检出值统一按目标类型发送默认值：数值为0、bool为true、字符串为无效；保留本轮执行检查和真实通信错误反馈。
- 调整设计器布局，仅显示当前取值方式及类型适用的参数；补充专项验证和节点说明。
- 验证完成：Debug|x64独立目录编译通过；专项覆盖中文树/列表/编辑框、内部键保存、旧映射参数失效、八种类型四种取值方式的缺失默认值、实际AI缺失结果写入本机PLC、PLC/Modbus原有类型写入及窗体切换布局。最终截图已检查，本次差异格式检查通过；未连接现场设备。

## 2026-09-12：修复目标分类混合判定时全部ROI变红

- 用户描述一个检测项NG导致ROI结果绘制的所有矩形变红；所给路径不存在，核对同目录实际文件 `D:/项目/AST-Fakra/软件数据/9_8.Sol`，使用RL12解析器，绘制节点25订阅节点19的AI输出结果。
- 已定位自动ROI入口无条件设置OverrideColor，覆盖解析器按检测项写入的颜色；检查全部解析器的AddRect调用，公共解析工具只合并同一矩形关联的判定。
- 修复旧版自动ROI保留带检测明细的算法结果元素颜色，不依赖方案遗留UseJudgeColor字段；同步修复ROI结果绘制2旧颜色规则兼容分支，显式布尔颜色订阅仍按配置统一着色。
- 增加真实程序集回归，覆盖方案参数、混合OK/NG、同框多检测项、不同框隔离、旋转颜色保持及普通几何判定颜色。
- 验证：Debug|x64编译到独立目录bin/RoiColorValidation成功；同一测试在修复前bin/ResultSendValidation程序集明确复现OK框被整体NG覆盖，在修复后通过。实际RL12解析器分别使用单项NG配置和用户方案检测项/绘制参数，验证颜色、整体NG、文本及90/180/270度旋转；绘制2默认保色、旧规则保色、显式布尔统一覆盖均通过。
- 既有AI文本颜色、两版绘制参数简化、解析不提前返回及缺失值上下限回归通过；本次差异格式检查通过。方案仅提取参数离线测试，未加载模型、连接设备或改写方案，未替换正在运行的程序。
- 方案及截图中三个防水栓检测项的范围为60到70、当前值为0，因此它们自身也判NG；本次仅修复ROI颜色覆盖，不改检测阈值。

## 2026-09-12：修复结果发送在运行前的检测项目录

- 用户反馈未启用检测项被过滤，重启后必须先运行才能看到检测项。
- 改为从方案中AI节点关联的固定检测配置、通信默认模板和切换配置读取全部检测项，不依赖首轮运行，也不按启用状态过滤。
- 保留中文显示、检测值/明细判定/检测项判定三个订阅入口与未检出默认值；补充参数序列化恢复、未运行及未启用项的验证。
- 验证完成：Debug|x64独立目录编译通过，结果发送专项全通过；新增验证模拟方案配置反序列化和空运行缓存，覆盖固定模板全部未启用项、通信模板合并、中文旧键去重、重复打开刷新及未检出默认值。已检查运行前窗体截图，本次差异格式检查通过。

## 2026-09-14：图像裁剪改用常用显示控件与旋转ROI

- 按用户要求，将图像裁剪参数窗体的旧 ImageROIEditControl 替换为 ShowImageControl，控件及操作提示均定义在 Designer 文件中。
- 默认左键在图像空白处拖动绘制，松开后直接选中矩形，可拖动、缩放、旋转；增加清空ROI入口，支持Delete删除选中框。
- 新参数保存图像像素坐标和旋转角度，兼容旧方案按控件坐标保存的ROI；运行从已保存参数取区域，避免未保存编辑影响检测。
- 裁剪沿用项目AI旋转ROI的外接矩形取图规则，保持原图方向和下游矩形偏移协议；0度保持原矩形裁剪快速路径。
- 验证：Debug|x64完整编译到独立目录bin/ImageCropValidation成功，0错误，保留项目既有警告；真实控件专项覆盖快速绘制、拖动、旋转、旋转后缩放、Delete、清空、误点、混合按键、保存隔离、序列化、窗口缩放、旧参数迁移及裁剪像素/灰度缓存、越界拒绝。
- 交互验证补充：公共显示控件在左键松开时采用最终鼠标坐标，避免快速拖动少裁一段；右键松开不提前结束左键绘制。
- AI节点ROI、图像订阅契约、图像资源生命周期回归通过；已检查默认尺寸和放大后的实际窗体截图，操作说明未截断。本次差异格式检查通过；未替换正在运行的程序。

## 2026-09-14：旋转ROI裁剪时直接纠正为正矩形

- 根据用户三张示意图，将旋转ROI从外接矩形取图改为直接仿射采样到ROI宽高的正矩形，消除旋转大画布黑边；仅重采样一次并保留浮点中心与角度。
- 0度整数矩形保持独立Mat直接裁剪快速路径；彩色图和已有灰度缓存使用相同几何规则，越界ROI明确拒绝。
- 纠正后的图像使用新图坐标；多个ROI的独立裁图通过紧凑纵向预览图及对应偏移供下游统一绘制，避免把旋转后的检测坐标错误平移回原图。
- 同步修改参数页裁剪说明、预览取图入口及专项验证；保留旧方案兼容路径。
- 验证完成：Debug|x64独立目录bin/ImageCropValidation编译通过，0错误；0度整数ROI与直接裁剪逐像素一致，90/180/270度与OpenCV直角旋转逐像素一致，9组含负角度及浮点中心/宽高通过解析平面采样验证。
- 贴边ROI无新增黑边，临近整数宽高不会丢失一列；越界明确拒绝；单ROI以纠正图为坐标基准，多ROI预览位置与独立图逐像素一致；灰度同步、失败释放及上游租约释放通过。
- 已用用户第2张截图内区域实测得到竖直紧凑裁图，图内截图自带的ROI1文字属于输入像素，正式运行从干净源Mat取图。已检查默认窗体说明；AI ROI、订阅契约和资源生命周期回归通过，本次差异格式检查通过。未替换正在运行的程序。

## 2026-09-14：接入超声波焊接侧面三类模型

- 修改前已将现有源代码及相关文档提交到本地Git，提交号54bbbe1；临时二进制备份、历史压测产物保留原位。
- 在AI参数设计器中增加“超声波焊接侧面三类模型”，补齐模型枚举、参数保存恢复与DET运行解析分支；保留旧枚举值。
- 按新增解析文件的类别定义接入左右线芯长宽、焊接区域长宽和飞丝数量，清理旧端子残留逻辑，兼容飞丝内部键、空结果按0判上下限、尺寸比例换算及混合颜色输出。
- 补充可导入的检测项模板及使用说明，自动学习数据按节点参数隔离；随后进行独立目录编译和离线解析/窗体验证。
- 截图检查发现长模型名称在原下拉框中被截断，已在Designer中将模型下拉框改为横向填满当前单元格并加宽下拉列表，新增默认尺寸文字完整性验证。
- 验证完成：Debug|x64独立目录bin/UltrasoundValidation编译通过，0错误；真实程序集专项覆盖七个检测项、无序左右定位、飞丝计数、尺寸换算、空结果清零按限判定、单线芯、禁用项、多焊接区域、同框颜色合并、非法比例、学习节点隔离/重启/切换与缺失样本。
- 已验证实际参数保存、JSON恢复、模板随构建发布、旧枚举值保持及默认窗体模型名称完整显示，并检查最终截图；既有解析缺失值、不提前返回及ROI混合颜色回归通过。本次差异格式检查通过。
- 未使用现场模型或样图执行真实推理，未连接设备、改写现场方案或替换正在运行的程序；模板尺寸阈值需按产品要求设置。

## 2026-09-14：裁剪ROI越界时继续输出

- 按用户要求取消ROI超出图像范围的保存与运行报错；保留ROI宽高及摆正角度，超出部分按最近的图像边缘像素补齐，已在图内的区域仍按原位置采样。
- 新旋转ROI、0度矩形与旧方案ROI统一支持部分越界及完全在图外；彩图、灰度图和下游预览坐标同步处理。
- 无效尺寸、非有限坐标及空源图仍作为无效输入处理；补充四边/四角、整图外、灰度与保存运行回归。
- 验证完成：Debug|x64独立目录编译通过；四边、四角、完全越界等10个位置各验证0/45/-30/90度，共40组逐像素解析校验通过；保存越界ROI、旧方案越界、彩图/灰度同步、坐标基准及资源释放通过。
- 原有交互、旋转精度、0度直接裁剪、订阅契约与图像资源生命周期回归通过；默认窗体说明显示完整，本次差异格式检查通过。未替换正在运行的程序。

## 2026-09-15：提交当前分支并推送远程仓库

- 按用户要求，提交当前分支 codex/keyence-nano-overtcp 的现有代码、测试、检测项模板和使用说明，并推送到默认远程 origin 的同名分支。
- 提交内容包含超声波焊接侧面三类模型接入及裁剪ROI越界输出支持；保留此前任务记录中的编译和专项验证结果，本次不修改业务代码。
- 本地DLL备份、历史压测产物、临时检查目录及输出方案保留原位，不纳入本次提交。
- 提交前已执行 git diff --check，差异格式检查通过；本次仅执行版本提交和推送，未重新编译或运行测试。

## 2026-09-15：清理开发验证垃圾文件

- 按用户要求清理已确认的开发验证产物；删除前逐项校验绝对路径在当前工程内、无链接跳转且不包含Git已跟踪文件。
- 范围：12个专项验证目录、两份临时构建、编译与IDE缓存、示例工程bin/obj、视频抽帧及文档渲染中间产物、临时浏览器配置、历史DLL备份、未跟踪压测输出和历史日志。
- 保留正式运行目录内的AI依赖、模型及方案配置，保留Git工作区、项目依赖、示例源码、正式文档、全部Sol方案和已跟踪测试结果。
- 执行清理后核对剩余目标与Git状态；实际清理统计见后续结果记录。
- 清理完成：共删除4101个文件，按文件长度累计释放约7.98 GB；130个计划目标全部清理成功，无删除错误。未改动业务代码，未重新构建，避免立即生成刚清理的缓存。

## 2026-09-15：删除首帧修复历史测试版本

- 用户明确要求删除 bin/x64/Release-FirstFrameFix，即使该测试目录仍被使用也执行清理。
- 删除前核对绝对路径位于当前工程内，确认没有链接跳转及Git已跟踪文件；仅处理该目录及从该目录启动的进程。
- 保留正常bin/x64/Release、Debug运行目录以及项目源代码；清理结果见下方记录。
- 清理完成：删除整个历史测试目录，共108526个文件，按文件长度累计释放约19.95 GB；对应运行进程数量为0，删除后确认目录已不存在。

## 2026-09-15：整理相对上次远程版本的更新说明

- 用户再次明确要求推送到此前已说明的origin，即GitHub jiangjiahang792-ship-it/Frame仓库，并说明相对上次仓库版本的变化。
- 对比本地保存的origin/main提交fb520ad（2026-07-27）与当前功能提交a73bb64，新增docs/版本更新说明-2026-09-15.md，涵盖相机、通信、流程组、结果发送、裁剪、AI、性能及工程清理。
- 更新说明以最终直接发送行为为准，不把历史有序外发方案描述为当前行为；清理约27.93 GB为本地未跟踪或忽略产物清理。
- 获取远程最新状态时遇到GitHub连接重置及443端口连接失败，文档明确标注基线尚未在线复核；准备提交后尝试推送当前同名分支。

- 网络复核：GitHub域名解析正常，Git及系统均未配置代理；此前443连接超时，重新探测后恢复连通。git ls-remote在线确认远程main为fb520ad594a0fb1709db47cc045682f101bf7dcd，已更新版本说明中的基线确认状态，继续执行用户已授权的推送。

- 推送历史检查发现：本地旧提交仍含约347 MB的.codex/PrivateMyComputerCoder/Test/ProcessMap.zip，当前文件树已无此压缩包；已停止直接上传完整开发历史。
- 改用独立发布快照：以在线确认的远程main提交fb520ad为父提交，使用当前已跟踪文件树并排除.superpowers临时进程状态；本地开发分支及历史保持不变。发布到此前指定的远程codex/keyence-nano-overtcp分支，不覆盖远程main。

## 2026-09-16：集成轮廓模板匹配节点

- 来源：用户提供的桌面轮廓模板匹配Demo，使用其ShapeMatchNative原生算法，保留全部创建、运行参数及右键确认和涂抹交互。
- 新增独立轮廓模板匹配节点，接入工具箱、节点工厂、图像订阅、方案参数保存、匹配位姿与绘制结果。
- 运行直接读取上游Mat并复用已创建模型；编辑预览与已保存运行模型分离，避免未保存修改影响检测；新增控件均在Designer文件中。
- 原Demo目录只读，集成与验证均在当前工程完成，后续补充实际验证结果。

- 完成：工具箱“检测 → 轮廓模板匹配”、节点工厂、流程画布及向导、CPU算法调度、图像订阅与方案持久化均已接入；NodeType追加枚举值，不改变旧节点编号。
- 全量保留Demo创建与搜索参数、0～1得分、顺时针角度、右键确认ROI与涂抹操作；原生C++算法及C ABI等7份源文件与桌面Demo哈希一致。
- 方案保存模板源图、ROI、已确认建模参数、待创建参数、逐次删除遮罩及搜索参数；生产模型独立缓存，Mat采用资源租约，失败和取消清除旧结果；输出完整轮廓、位姿、动态目标中心及兼容下游裁剪的外接矩形。
- 使用SourceNodeId兼容框架的复制粘贴引用重映射，已用真实流程编辑器重映射方法验证。
- 原Demo预编译DLL缺少sm_get_contours导出，已从保留源码重编译Windows x64静态OpenCV库并验证；新增Native/ContourMatchSource/build.ps1和构建说明，无需运行或分发.NET 8 Demo。
- 验证：Debug/x64主工程构建完成，新节点无编译警告；真实原生CTest 1项通过；Tests/ContourMatch.Tests.ps1共49项断言通过，覆盖三模式、旋转、多目标、灰度/BGR/BGRA、非连续Mat、涂抹恢复、参数隔离、模型复用、取消、租约、复制粘贴、CPU分类及真实Designer交互。已检查1200和950宽度窗体布局截图。
- 现有回归：模板CPU兼容性、订阅类型兼容性、图像生命周期通过；后者包含50000轮资源压力与两类各1000轮并发竞争。未连接现场相机/PLC，实际产品图像参数需现场调试。
- 收尾构建的旧PostBuild二维码xcopy步骤卡住，停止该次复制子进程后构建正常结束；二维码模型文件已由前次成功构建复制。Git只读差异检查出现mmap failed（沙箱内外相同），因此未宣称git diff --check通过，也未改动Git配置或历史。
- 新增docs/轮廓模板匹配节点.md；验证日志与截图在artifacts/ContourMatchValidation，测试程序集在bin/ContourMatchValidation。未提交或推送本次代码。

## 2026-09-16：轮廓匹配拆分三页及多模板管理

- 按用户截图和明确要求重构为基础参数、特征模板、运行参数；模板创建与搜索设置分离，模板编辑使用独立窗口。
- 基础参数接入图像订阅、搜索矩形绘制和位置修正；特征页支持多个模板的创建、编辑、删除、启用、导入导出；运行页保留Demo搜索参数。
- 同步扩展生产会话、保存恢复及结果模板标识，并补充实际交互和算法验证。
- 完成三页主窗体：基础参数提供图像订阅、本地预览、全图/矩形区域搜索和位置修正订阅；特征模板提供列表启停、重命名、创建、编辑、删除、清空及模板包导入导出；运行页完整保留Demo搜索参数。
- 创建参数、ROI建模、轮廓涂抹单独放入ContourTemplateEditor窗口，创建窗口不再含搜索参数与匹配按钮。两套窗体控件均定义于各自Designer文件。
- 模板保存为独立定义，旧单模板方案兼容转换；所有启用模型独立缓存，按得分合并并以旋转框重叠去重，总结果数遵循运行页上限。输出补充模板名称和标识列表。
- 搜索矩形支持上游单项/多目标位置修正，按位姿变换四角并搜索有效范围，过滤区域外中心，结果转换回原图坐标。未确认绘制不能保存，位置修正失效会清除本次结果。
- 导入采用本节点.contour.json格式，整包原生验证通过后加入草稿并重新分配标识；不解析第三方专有模板格式。删除全部保留明确空列表，不会复活兼容字段中的旧模板。
- 验证完成：Debug/x64构建成功，本轮节点代码无新增编译警告；真实DLL与UI测试72项断言通过，包括多模板两目标、启停、全局限数、跨模板去重、区域坐标、位置修正订阅与失效、模板交换、空列表保存、预览按钮恢复、独立编辑器及缩放布局。
- 现有回归单独执行通过：订阅类型兼容性、位置修正订阅兼容性、模板CPU兼容性。人工查看三页界面和独立模板窗口截图，全部参数可见，结果可按模板标识区分。
- 已更新docs/轮廓模板匹配节点.md；截图位于artifacts/ContourMatchValidation/轮廓匹配-基础参数.png、轮廓匹配-特征模板.png、轮廓匹配-运行参数.png。验证使用合成图，未进行现场产品图像调参。本次修改未提交或推送。

## 2026-09-16：缩紧轮廓模板界面

- 按用户要求收紧窗口尺寸，模板编辑改为左侧参数与操作、右侧单一图像轮廓画布；模板管理页取消底部重复缩略图和结果区域，右侧显示选中模板及实际轮廓。
- 控件布局修改继续放在Designer文件，保持原创建、搜索和多模板参数不变。

## 2026-09-16：恢复相机节点触发模式选择

- 按用户要求，在图像源相机参数Designer中恢复触发模式开启/关闭选择，关闭时使用连续采集，保留触发源、触发沿及延迟设置。
- 同步处理参数保存恢复、硬件写入、流程校验和生产帧等待，移除旧Off参数强制迁移；验证结果随后补充。

### 轮廓模板紧凑布局验证结果

- 模板编辑窗口默认860×560、最小800×540；左侧为创建参数、画笔和保存操作，右侧为单一图像轮廓画布，取消上下堆叠的大图布局。
- 主参数窗体收紧至980×680；特征模板页删除底部缩略图，右侧显示选中模板ROI裁剪图及已确认轮廓，隐藏该页底部搜索结果表；基础参数和运行参数页保留搜索预览及结果。
- 模板预览与搜索图像分别持有资源，按模型引用复用轮廓预览，切换模板、清空及关闭时释放旧图像；全部布局修改位于Designer文件。
- 已人工检查默认与最小尺寸截图，修复紧凑布局中的按钮裁切和订阅输入框边界；完整构建成功，轮廓专项74项断言通过。
- 本轮编译曾遇到另一项相机任务尚未同步的控件声明，待该任务完成Designer修改后构建通过；本任务未修改相机相关文件。

- 完成：Designer新增中文触发模式下拉框（开启、关闭/连续采集），关闭时禁用触发源、触发沿和延迟但保留已选值；恢复方案不再强制把Off改为On，新节点默认开启。
- 运行：方案启动、节点逐次设置及参数保存恢复均使用所选模式；连续采集不发送软触发，使用独立生产票据等待新帧，跳过空闲帧且不启用硬触发缓存，保持图像预算、超时和停止释放。连续采集不受硬触发上游限制，同相机模式冲突可在启动前识别。
- 验证：Debug/x64独立目录bin/CameraTriggerModeValidation编译成功；真实窗体及接口替身26项检查通过，覆盖保存恢复、JSON往返、控件联动、硬件参数写入、同相机配置冲突及流程校验；相机生产管线和统一回调回归通过，补充连续帧新旧区分、跳帧、空闲不缓存、预算释放、超时与取消验证。
- 已查看开启及关闭两种界面截图，控件及保存按钮显示完整。验证日志和截图位于artifacts/CameraTriggerModeValidation及同名前缀日志；未连接实际相机，未覆盖正常运行目录。Git差异读取仍报mmap failed，未宣称git diff --check通过。本次代码未提交或推送。

## 2026-09-16：轮廓匹配参数即时生效

- 按用户要求移除主窗口保存参数，右下角仅保留执行、确定；确定仅关闭窗口。参数、订阅、模板管理操作即时更新节点内存参数，无需保存工程方案即可运行。
- 保留不可变运行快照与模板编辑确认，补充即时生效及未保存工程执行验证。

### 轮廓匹配即时生效验证结果

- 主窗体Designer删除保存参数按钮，将关闭改为确定，确定事件仅调用关闭；输入订阅、位置修正、全部运行控件、模板管理和确认后的搜索区域均即时发布独立节点参数快照。
- NodeSubscription新增去重的订阅变更事件，选择、程序恢复和清空都会通知；原有未订阅此事件的节点行为不变。运行线程只读参数快照，外部参数交换保持深复制隔离。
- 参数填写过程允许暂时不完整，执行时报告具体配置问题，不以保存工程为前置条件；清空输入、停用及删除模板不会沿用旧参数。独立模板编辑器仍需确认建模结果后返回管理页。
- Debug/x64完整构建成功，验证输出位于bin/ContourMatchValidation；真实原生算法与窗体85项断言通过，覆盖不保存方案直接运行、实时参数和订阅、模板启停/重命名/删除、ROI确认、确定仅关闭、参数快照与缓存。订阅类型和位置修正订阅兼容性回归通过。
- 已查看运行参数界面截图，确认右下角仅执行、确定；说明文档已同步。未提交或推送，本轮未修改相机任务文件。

## 2026-09-16：迁移用户桌面123旧方案

- 读取C:/Users/34652/Desktop/123.Sol，实际7个流程、154个节点，其中15个旧MatchTemplate；保留原文件，另存新版轮廓匹配方案。
- 迁移内嵌模板图像、涂抹遮罩、角度、阈值、匹配数量和搜索区域，保持节点编号、连线、名称及下游引用。使用当前程序集反序列化并逐模板执行离线验证，不连接相机或PLC。

- 用户追加要求：只改方案数据，不改项目代码。本轮未修改业务源码；一次性迁移工具移至系统临时目录，不保留在项目tools目录。
- 15个模板迁移并经真实节点离线自身匹配验证通过，原分数阈值、图片、9份涂抹、搜索区域及下游引用保留；两线圈改用单层自动特征配置，其余采用三层建模/二层搜索。
- 方案结构对比验证通过：7流程、154节点、176条连线；非模板节点数据完全不变。发现4个旧Modbus读取String配置在当前框架不支持，按用户限制保留原数据并在说明中明确，不能宣称整套方案运行通过。
- 输出outputs/123_新版轮廓模板匹配.Sol、123_迁移说明.md及123_迁移验证.json；未连接现场设备，未提交推送。

## 2026-09-16：同步客户端Release发布目录

- 按用户直接发送Release目录的方式，生成Release/x64到bin/x64/Release，未修改项目源码或项目配置。
- 主程序、配套config、工具箱、语言文件及ShapeMatchNative.dll由现有构建配置更新；手动完成构建后事件对应的二维码模型与程序自带模板资源复制。
- Release算法库与Native源DLL哈希一致；直接使用Release产物完成轮廓匹配85项验证，测试程序及测试配置随后清理。现场方案、模型、授权文件未替换。
- 此次验证覆盖轮廓匹配，不代表123方案中4个旧Modbus String配置问题已解决。


## 2026-09-16：模板细节涂抹支持缩放和平移

- 按用户要求增加鼠标锚点滚轮缩放、中键平移和适应画布；模板编辑器增加Designer缩放工具栏，保持紧凑尺寸。
- 画笔直径与遮罩始终使用原图像素；导航不能提交、清除或串联涂抹笔画，补充交互验证并同步Release发布目录。


- 完成ImageCanvas统一缩放和平移变换，滚轮以鼠标为锚点，中键拖动不写遮罩；视图变换中断当前笔画，避免跨视口连线，原图ROI及画笔尺寸不随缩放改变。
- 编辑器Designer新增34像素高的放大/缩小/适应工具栏和比例显示；保持默认860×560、最小800×540。进入涂抹与确认删除均保留视图，恢复适应不丢失未提交遮罩。
- 修复编辑器完成标记早于按钮恢复的问题。Release/x64编译成功，直接使用Release产物通过99项断言；新验证覆盖鼠标锚点、中键平移、缩放前后相同像素遮罩、笔画中缩放不串线、缩放极值、清空图像及涂抹期间按钮可用。
- 已查看放大涂抹及最小窗口截图，工具栏完整可见；bin/x64/Release已同步，测试程序已清理。本轮没有修改客户方案、模板数据或算法DLL。


## 2026-09-16：移植旧框架 Modbus 字符串读取

- 对照用户指定旧框架，移植保持寄存器字符串解码、编码和字节顺序配置、单值字符串订阅。保留当前设备接口及驱动实现；新增控件写入 Designer。
- 验证 ASCII、UTF-8、GB18030、UTF-16、两种字节顺序、参数还原、数值兼容性及 123 方案的四个 String 节点，并更新 Release/x64。

- 完成：新增 ModbusStringReader 扩展解码器，支持 ASCII、UTF-8、GB18030、UTF-16 及寄存器内高低字节顺序；一次读取1～125个保持寄存器并发布一个 string 订阅值。沿用旧方案字段与枚举顺序，保留当前设备驱动和接口。
- Designer 新增编码和字节顺序控件，数值模式禁用；校验失败不再执行旧参数。手动执行显示结果，读取开始清除上次结果，防止失败后沿用旧条码。已查看真实窗体截图，布局完整。
- Release/x64 编译通过（存在项目原有警告），字符串41项断言、原有数值订阅与直接写入回归通过。123新版方案154个节点参数均还原成功，4个字符串节点使用原地址900/16寄存器/ASCII/低字节在前完成模拟读取，8个存图条码引用通过验证。
- 更新项目内迁移说明和验证报告、使用文档；方案文件及用户指定旧工程未修改。未连接现场PLC，不能据此宣称整套生产流程实机通过。发布目录已更新，临时测试程序清理，未提交或推送。

## 2026-09-16：修复裁剪方案 ROI 空值读取误报

- 用户授权修复：ROIListConverter 读取 JSON null 时直接保留空值，避免新版 ImageRois 参数保存校验时误报 JObject 异常；补充空值、新旧格式兼容验证并更新 Release。

- 完成：仅在 ROIListConverter.ReadJson 入口处理 JsonToken.Null，保留 null 与旧版空对象的差异；不修改客户方案。新增首次异常事件监测，确保空值读取不再触发被内部捕获的 JSON 异常，并验证新版区域往返、旧空对象及缺失字段。
- 独立 Release 配置输出 bin/RoiNullValidation 构建通过；图像裁剪专项全部通过（包含原有旧 ROI 迁移、裁剪像素与交互回归），方案安全保存检查通过。
- 常用 bin/x64/Release 主程序被正在运行的软件及 Visual Studio 调试器锁定，覆盖构建失败；未强制结束用户进程，等待关闭后同步。

## 2026-09-17：参照 VM 改造轮廓模板 ROI 交互

- 修改前按用户要求提交当前源码基线：c4a6516；临时实验目录及生成的方案文件未纳入。
- 已实际操作用户打开的 VM 模板配置：矩形松开生成、中心移动、角点缩放、重复点击工具追加多个 ROI，右侧列表同步保留。
- 本轮实现顶部绘制工具、左侧图像与右侧区域/参数布局；矩形、椭圆、扇形、多边形、画笔区域可组合建模，支持选择、移动、缩放、旋转及撤销重做。
- ROI 使用原图坐标保存，通过原生已有掩膜接口构建组合区域，避免把区域间空白或裁切边界作为模板轮廓；控件均定义于 Designer 文件。
- 验证使用独立输出目录，避免覆盖当前正在调试的 Release 程序。

### 2026-09-18：组合 ROI 完成与验证

- 完成“模板配置”布局：顶部绘制工具、左侧画布、右侧区域列表及区域/创建参数页，默认1040×680、最小900×600；所有界面控件均在Designer的InitializeComponent中创建。
- 矩形、椭圆、扇形、画笔松开生成，多边形双击/右键/回车闭合；可反复追加并组合。支持画布/列表选择、移动、八个缩放柄、旋转柄、数值参数、区域启停、删除/清空及40步撤销重做。
- 新增可持久化区域定义与可选IMaskedShapeMatcher接口，原生已有掩膜入口只提取区域并集；源图不被遮黑，避免边界伪轮廓。区域和参数变更后必须重建，确定可自动创建；空区域及失败不能误保存旧模板。旧矩形方案仍能恢复和继续编辑。
- 修复原生算法对非完整矩形掩膜的闭环数量误判：取消这类区域与整图闭环数量的直接比较，保留掩膜内正向、反向和缺失轮廓评分；完整矩形模型原行为保持。已更新工程Native/ShapeMatchNative.dll。
- Release/x64独立目录bin/ContourRoiValidation构建成功，无本轮轮廓代码新增编译警告；组合ROI测试45项、原轮廓回归99项全部通过。原生测试通过，新增分离区域、忽略区改变、旋转平移及缺失区域检查。组合样本原图匹配分数0.9682。
- 已检查默认/最小尺寸、区域参数、创建参数和轮廓截图，位于artifacts/ContourRoiValidation；说明文档同步更新。
- 使用computer-use实际操作新版独立预览：绘制矩形、拖圆形柄旋转约64°、追加椭圆、创建组合轮廓，显示300个特征与27ms建模耗时（仅当前合成样本，不代表现场性能）。预览窗口留在桌面。
- 当前用户正在调试的bin/x64/Release程序未关闭或覆盖；可停止调试后从工程重新编译以使用新版。独立预览入口为bin/ContourRoiValidation/ContourRoi.Preview.exe，使用合成图且不连接设备。
- 修改前基线已提交c4a6516；本轮新增改动未提交或推送。

## 2026-09-18：排查用户测试方案原图无法匹配

- 用户提供桌面“测试模板匹配.Sol”，反馈在同一张图创建模板后无法识别；读取方案数据复现，保留用户原文件。
- 方案轮廓节点含三个分离矩形区域、一次擦除；运行快速模式、最低分数0.8、自动金字塔。已确认发布目录DLL与本轮新版算法DLL哈希一致。
- 先使用方案内嵌的模板原图及原参数建立真实算法回归，定位后修复并验证发布产物。
- 已复现：3072×2048 原图，三个区域外接框 2210×1876、擦除后 293 个特征；快速/平衡自动金字塔均返回零结果，单层同图得分 0.9986，固定 0° 搜索自动金字塔也通过。
- 根因：粗搜索及逐层精修仅按层数决定角度跨度，忽略大模板旋转产生的空间位移，正确角度在进入原图精修前已被淘汰。在已建模角度步长允许的范围内，按当前层模板半径约束相邻抽样角度的最远点位移为两像素，间隔保持二的幂以维持各层角度网格一致；保留模式预算与最低分数，不增加原图特判。
- 添加真实方案原图回归脚本（不提交用户图像）及原生大跨度三个矩形区域回归。原生新用例使用旧 DLL 时原图、+13°、−7° 均失败，修复 DLL 全部通过。
- 用户方案原阈值 0.8、快速自动金字塔：原图得分 0.9986，搜索约 200 毫秒；平移（300，400）后 +13° 得分 0.9895，−7° 得分 0.9933。增加生产会话及缓存验证，原文件不改写。
- 最终验证：原生产参数首次恢复和缓存复用均得分 0.9986；快速/平衡在自动、3、2、1 层均找到原图，平移及正负旋转通过，空白图无误检。99 项原有检查、45 项组合区域检查和原生 CTest 全部通过；小模板角度精度检查保持原断言，并补充失败时的模式/分数/角度诊断。
- Release/x64 构建成功，原生 DLL 已同步至 Native 与 bin/x64/Release，和验证目录哈希一致（6CD94CB6…ACC963）；可直接加载原方案，无需重建模板。编译测试脚本有既有 System.Memory 引用版本兼容警告，运行及全部断言通过。

## 2026-09-18：与 VM 全图像基准比较及优化
- 用户授权直接操作已搭建的 VM 流程，对桌面“工位3”下全部 101 张 JPG 逐张比较匹配速度、检出与定位精度，并修改本项目后完整测试。
- 保留 VM 原流程及用户原图，通过另存基准副本、机器可读逐图结果和已知几何变换验证保证可复现；不得以不同算法的分数直接充当精度比较。
- 修改前保留已完成的组合 ROI 交互和原图漏检修复代码基线；本次优化单独记录。
- 初测官方 SDK 与本项目全部 101 张原图：VM 检出 56 张，本项目快速模式检出 55 张；45 张为明显暗曝光帧，两套均漏检。开始验证大模板低分辨率候选搜索及原分辨率稀疏梯度精修，模板编辑、阈值、全图搜索不变。
- 大模板试验版原图检出 62/101，逐图中位数的平均值约 15.82 毫秒；正在加入候选复核、亚像素开关语义、多目标排序/抑制和暗图局部对比度试验，并以共同来源模板重建 VM 基准。
- 已加入全分辨率分区覆盖检查，剔除暗图错误位置；原生新增无目标噪声、缺失组合区域、多目标排序、整数输出及小角度回归通过。继续验证更小候选层和光照模板，不将调参试验数据作为最终报告。
- 决定仅对使用极性的自动金字塔大跨度稀疏组合模板启用新路径；完整矩形、忽略极性、重复孔洞和显式金字塔保留原算法。整图与暗光模板试验存在错误位置，未作为交付配置。共同模板下已知变换平均相对位置误差试测为本项目约 0.02 像素、VM 约 0.30 像素，最终重新串行计时。
- 最终正式串行基准：101 原图、1 建模原图、13 已知变换及 17 无目标共 132 输入，每图预热后运行 5 次。本项目与 VM 均检出 56/101、13/13 已知变换，无目标均 0/17 误检；本项目平均 13.09ms、VM 19.52ms，平均相对位置误差 0.0199px / 0.2992px。45 张暗光及大位移原图仍未检出，报告明确保留，不声称全场景全面超过。
- 原生 CTest、99 项轮廓、45 项 ROI 和用户方案生产会话/缓存/原图/平移旋转回归全部通过。Release/x64 编译通过，Native、Release、验证目录 DLL 哈希一致 DC1FEDF9…5774F6B。新增复现脚本、逐图 CSV/JSON、离线可视报告和复用真实 Designer 窗体的现场图预览入口。
- 已实际打开 Release 中复用生产窗体的工位3预览，原图显示 1 个目标和叠加轮廓；桌面“工位3模板匹配结果”提供运行界面及逐图报告快捷方式。浏览器检查已知旋转样本轮廓与页面交互通过，所有结果文件留在本机。

## 2026-09-19：接入 OpenCV 端子角度节点
- 用户要求：修改框架前先 Git 提交；沿用当前 OpenCV 旋转最小外接矩形算法，绘制两个 ROI 后只测角度。
- 修改前源码基线提交 d33ffef；已跟踪源码原本干净，使用空提交标记基线；未跟踪日志、测试数据、临时目录与方案文件保持原状。
- 已预读框架新增节点交接文档、节点契约、双工厂、图像资源租约、订阅输出及 ROI 交互代码。
- 按此前上下两框理解为端子 ROI＋基座 ROI；基座框只放置固定 180°/90° 基准，角度来自端子外接矩形长轴。不增加偏移、自身弯曲或批量报告到节点运行流程。
- 采用 .NET Framework 4.8/C# 7.3 与现有 OpenCvSharp，不新增 HALCON 依赖、不升级公共依赖。持久化两个原图坐标 ROI；算法接口化，参数窗体全部控件位于 Designer。
- 计划同步枚举、双工厂、工具箱及工程编译项；独立目录构建，验证合成角度、既有 56 张原图、两框交互、方案序列化、订阅、失败清理和资源释放。
- 首次独立 Release/x64 构建通过；补齐 CPU 算法调度分类和工具箱类型标识。发现图像控件使用左键空白拖动平移，修正操作说明；测试将实际走鼠标事件与参数保存，不仅检查源码注册。
- 测试编译发现框架 Result 具有受保护 setter，验证代码改走反射注入测试图像；并补齐测试编译的 System.Memory 引用，不修改生产接口。
- 实际方案多态往返测试发现 ExpandableObjectConverter 让 Newtonsoft.Json 将 ROI 序列化为字符串。给 ROI 添加显式 JsonObject 声明，保留属性面板展开能力同时强制结构化坐标；继续验证生产序列化链路。
- 租约测试发现通用 FromBorrowedSingleImage 在未传灰度缓存时会创建全图灰度。节点改为显式借用原图并复用尺寸匹配的上游灰度缓存，缺缓存时留空，避免为了只测 ROI 增加全图转换；不修改框架通用函数。
- 260 项节点专项断言通过，56 张/112 个端子与独立 OpenCV Demo 角度逐项相同；局部单端子算法平均 1.720 ms（含本轮样本首次调用，不含原图读取、解码、显示）。
- 订阅目录、消费者契约检查通过；原订阅分类脚本硬编码 251/48，基线实际已为 256/49。新增 6 个输出后更新为 262/49，保留所有分类与既有语义断言，同时将参数显示属性与输出属性规范区分。
- 补充 ROI 变更清除过期预览，以及实际订阅目录中的 6 个输出检查。
- 最终：261 项端子角度专项断言全部通过，56 张/112 个端子与独立 OpenCV Demo 角度无差异；最后一轮 ROI 算法平均 1.832 ms（单端子，不含读图/解码/显示）。
- 修正后的订阅输出分类、原订阅目录及消费者契约全部通过；默认与最小 Designer 界面截图已检查。
- 独立验证与常用 bin/x64/Release 均构建成功；工具箱 XML 已包含“端子角度”，未启动主程序或连接设备。原框架编译警告及 System.Memory 测试兼容警告保留，真实运行已通过。
- 已补充 docs/端子角度节点.md；结果日志、112 项数值及截图位于 artifacts/TerminalAngleValidation。新增改动保持未提交，修改前基线为 d33ffef。

## 2026-09-19：精简端子角度参数界面
- 用户要求：端子角度节点界面偏大，改成小巧、紧凑的风格。
- 本次仅调整该窗体 Designer 布局和外观：默认尺寸计划从 1160×780 缩至 860×600，减少顶部和状态区留白，统一按钮样式，保留图像、订阅、两个区域、测试和保存功能。
- 所有控件和布局继续保留在 .Designer.cs；沿用已有测量逻辑及接口。完成后独立编译并检查默认、最小窗口截图。
- 完成：默认窗口 860×600，最小窗口 780×540；去掉重复标题栏留白，订阅行 50、工具栏 38、右栏 276、状态行 76 像素；隐藏属性网格重复说明区，采用浅灰背景、白色细边框按钮及青色测试按钮。
- 已明确字体缩放基准，保留字体/DPI 缩放；绘制按钮使用中文区域名称，图像和参数区随窗口调整。全部布局修改位于 Node/4-Measurement/TerminalAngle/NodeParamFormTerminalAngle.Designer.cs。
- 验证：独立 Release/x64 和常用 bin/x64/Release 构建成功；现有专项 261 项断言通过（含两 ROI 鼠标交互、订阅、保存、资源释放和最小窗口工具栏）；56 张/112 个端子与原 Demo 角度差为零。原框架编译警告和 System.Memory 兼容警告保留。
- 已检查 artifacts/TerminalAngleValidation 下默认 860×600、最小 780×540 界面截图，按钮、保存区和说明显示完整；未更改测量算法，未启动主程序或连接设备。

## 2026-09-19：端子角度界面对齐卡尺工具风格
- 用户指定以卡尺找线截图为样式依据；将端子角度窗体改为左侧原生分组输入、右侧图像、底部原生按钮，移除属性表和自定义彩色按钮。
- 控件、分组及全部布局继续写入 Designer；补充参数输入与原图区域的双向同步，保留测量算法、订阅和保存语义。
- 已完成原生分组界面和输入双向同步；新增针对输入框保存、非法数值拦截、原始精度保留及坐标预览同步的回归验证，防止替换属性表后改变参数语义。
- 完成卡尺工具风格：默认 920×660、最小 880×640；左侧输入图像、两个区域、分割、质量、结果分组，右侧图像及下方六个原生按钮；全部控件有字段注释并位于 Designer，操作文档同步更新。
- 输入采用完整快照校验，非法值保留供修正且不污染已保存参数；区域坐标回填三位小数，未编辑值及未移动图形保留原始双精度。
- 现有及新增专项共 274 项断言通过，包括两框真实鼠标交互、数值/勾选项保存、非法高斯尺寸和越界拦截、精度保留与最小窗口确定按钮。56 张/112 个端子与原 Demo 角度差为零。
- 已检查默认和最小窗体截图，分组字段和操作按钮均完整；独立验证构建成功，常用 Release 构建及最终检查结果见后续记录。
- 最终：git diff --check 通过；独立 bin/TerminalAngleValidation 构建及 274 项运行验证成功。常用 bin/x64/Release 构建在复制主程序时因 Visual Studio 2022（PID 704）与运行中的主程序（PID 45992）锁定文件而失败（MSB3027/MSB3021）；未停止用户调试或终止主程序，需停止当前调试后重新生成。

## 2026-09-19：修复端子角度刷新并接入位置修正
- 用户反馈刷新图像无显示，并要求具备与卡尺工具一致的位置修正功能。
- 对照确认原刷新按钮先调用 RunForUpdateImages 重跑上游，而卡尺仅读取订阅图像；改为直接读取现有上游输出并保留图像租约，失败明确提示先运行上游。
- 增加位置修正开关、修正信息列表订阅和目标序号；两个区域保存基准坐标，跟随平移/旋转/缩放。保留单端子输出契约，角度仍相对当前图像固定垂线；局部采样测量后将几何变回原图。
- 进一步定位直接原因：用户当前 Designer 经设计器重写后，六个按钮 Click、输入框 TextChanged/Validated、勾选项和图像 MouseUp 绑定全部缺失；原测试反射调用处理方法，未覆盖真实事件绑定。已改用标准 new EventHandler(this.方法) 设计器语法恢复，并增加真实 PerformClick 回归。
- 完成：恢复全部标准 Designer 事件绑定；刷新直接读取上游已有输出，保留租约和中文失败提示。新增位置修正开关、列表订阅和目标序号，基准区域与当前图像双向变换，预览和生产共用修正测量适配层。
- 修正仅采样端子局部区域；恒等变换不重采样；检测角度重新由当前原图长轴计算，保持固定垂线语义；无效尺度、缺失目标、越界和取消明确失败并清除旧结果。
- 回归改用真实 PerformClick，验证事件绑定和刷新不重跑流程；独立窗体异步测试显式安装 UI 上下文，并将忙碌完成标记放在控件恢复之后。
- 最终 304 项专项断言全部通过，56 张/112 个原端子角度与 Demo 无差异；位置修正订阅兼容和订阅消费者契约检查通过。默认、最小及启用修正界面截图已检查，所有分组与按钮正常。
- 独立验证目录和常用 bin/x64/Release 均构建成功；docs/端子角度节点.md 已同步操作说明。原编译警告及测试 System.Memory 兼容警告保留；未启动主程序或连接设备。

## 2026-09-19：对齐卡尺修正语义并修复重画区域倾斜
- 用户指出：不应有手选目标、端子/基座坐标输入及左侧测量结果；结果直接显示在图像中。
- 确认 CaptureRoi 强制把新绘制矩形 Phi 赋为定位角度，导致新画水平/竖直框立即倾斜；改为保存绘制帧位姿，运行时只应用相对绘制帧的变换。切换图像取消旧绘制及延迟回调。
- 对齐卡尺：绘制时按 ROI 位置自动确定关联定位，运行遍历位置修正列表；删除手选目标。保留原有角度订阅兼容，并提供按顺序的结果集合。所有界面控件仍在 Designer。
- 完成界面精简：删除目标选择、两个 ROI 坐标分组及测量结果侧栏，默认 900×560、最小 860×520；图像直接显示操作提示和全部目标角度，确定保存保留已测叠加。
- 接入卡尺共用 MultiTargetTransformService 与 MultiTargetMeasurementRunner；绘制自动关联定位，运行完整修正列表，单个失败保留顺序；新增多目标结果订阅，第一目标角度兼容既有方案。
- 每个 ROI 保存独立绘制位姿，采用当前定位乘绘制位姿逆矩阵；同帧严格恒等，跨帧支持旋转/平移/非等比尺度。旧方案缺少快照沿用原坐标语义；重画后采用新语义。换图取消绘制并使排队 MouseUp 失效，重画时隐藏旧同角色轮廓。
- 最终 303 项专项断言通过：真实按钮与鼠标、弯曲轮廓切换直端子、非零定位下新 ROI 不倾斜、位姿保存重载、下一帧真实旋转 10°、非等比相对变换、自动归属与多目标失败隔离。56 张/112 个原端子与 Demo 角度差为零。
- 订阅输出分类、位置修正兼容和消费者契约检查通过；输出计数为 263/49。默认、最小与直端子重画界面截图已检查。独立构建通过，使用文档同步更新；未启动主程序或连接设备。
- 常用 bin/x64/Release 构建成功，与独立验证程序 SHA256 一致（3EA02FB4…378016）；git diff --check 通过，原框架编译及测试 System.Memory 兼容警告保留。

## 2026-09-19：修复端子窗口标题和确定关闭，精简结果叠加
- 用户要求修复顶部遮挡、删除加载图片按钮，确定保存后关闭窗口；测量结果仅保留端子中线与基座垂直中线，去除结果 ROI 框和辅助图形。
- 对照卡尺保存后 Hide 的复用方式；在端子 Designer 中启用标题栏按内容自适应，预留订阅控件高度，避免标题及下拉框裁切。执行测量时结束动态 ROI 编辑，避免动态框和 ROI 名称残留。
- 截图复核发现共用 RoiLine 无条件绘制中心十字；增加默认保持旧行为的线段标记开关，仅端子结果关闭，并在两版 ROI 结果绘制的复制/旋转适配中保留，防止下游重新出现辅助标记。
- 完成：端子 Designer 单独启用标题栏自适应、增大输入/修正订阅所在行，删除加载按钮及处理函数。确定校验并保存后结束绘制、Hide 窗口，可再次打开；校验失败留在窗口供修正。
- 结果阶段只保留端子拟合中线、基座固定垂直中线与角度文字；编辑轮廓独立构建，执行前清除动态区域。ColorLine/RoiLine 新增默认开启的中心十字开关，仅端子中线关闭；两版结果绘制的复制和旋转透传此设置。
- 355 项端子专项断言全部通过，包括保存关闭/重开、非法参数、标题和订阅布局、真实 ROI 编辑清理、两版 ROI 结果绘制及旋转无辅助标记。56 张/112 个原端子与 Demo 角度差为零；本轮算法平均 1.535 ms。已有 AI 结果绘制颜色专项通过。
- 已检查默认、最小、布局放大及直端子回归截图。独立目录与常用 Release 均编译成功；构建后既有 xcopy 等待未结束，终止本次构建的该复制子进程后以空 PostBuildEvent 完成构建，并通过 PowerShell 原生复制补齐相同模型/模板资源；没有修改工程后处理或停止用户程序。
- 最终 git diff --check 通过；Release 和独立验证主程序 SHA256 完全一致（A0093857…102933）。保留框架既有编译警告及测试 System.Memory 兼容提示。

## 2026-09-19：端子角度显示保留一位小数
- 按用户要求，将单目标和多目标图像叠加角度统一显示一位小数，保持正负号和角度单位；原始测量值及订阅输出精度不变。同步既有直端子显示断言和使用说明。
- 验证：独立构建成功；直接调用实际程序集的单目标/汇总显示构建器，5.018、-1.234、0 分别显示 +5.0°、-1.2°、0.0°，原始数值未改变。整套回归停在当前 Designer 已缺失标题自适应设置导致的 labelMinBox 裁切断言；本次未改动用户当前布局。常用 Release 因正在使用的主程序文件被锁定而复制失败（MSB3021），未停止用户程序，关闭调试后重新生成即可。

## 2026-09-19：修复订阅端子角度的文本精度
- 用户反馈订阅端子角度后仍显示多位小数。根因为两版结果绘制将数值/多目标值直接 Convert.ToString，未采用源节点显示格式。增加可扩展的订阅文本格式接口，由端子结果提供一位小数格式，接入单值及多目标文本绘制，保留数值型订阅及计算精度。
- 完成：新增 ISubscriptionTextFormatter，端子结果对中文端子角度/Angle 属性名返回固定一位小数；结果绘制单值及多目标数组调用源格式，结果绘制2对显式数值订阅优先使用源格式，避免回退为整套算法文字。数值类型及原始精度保持不变，空值不显示成零。
- 验证：新增针对实际订阅读取与两版完整绘制构建器的专项入口（-SubscriptionTextOnly），14 项断言通过，覆盖正负/零/空值、中文和属性名、单值及多目标、原始精度。已有 AI 绘制颜色专项通过。独立及常用 Release 构建均成功；未重跑此前已知受当前 Designer 标题裁切影响的全套窗体测试。使用说明同步更新。

## 2026-09-19：轮廓模板匹配移除位置修正
- 用户要求：该定位节点不需要位置修正，删除参数界面的启用位置修正。
- 计划：移除 Designer 控件、修正订阅及搜索区域变换，保留全图/固定区域搜索；同步文档和既有回归，验证旧方案修正字段被忽略。
- 完成：删除 Designer 内开关、订阅和说明控件，以及参数字段、事件、预览/生产修正订阅读取和区域变换。全图或固定 ROI 搜索保持原逻辑，下游位姿输出保持兼容；旧方案多余修正字段自动忽略，重新保存不再写出。使用文档同步更新。
- 验证：独立 Release/x64 构建成功；现有轮廓模板匹配专项 97 项断言全部通过，含旧方案无效修正订阅忽略、全图和固定区域、真实三页窗体、模板编辑和缓存；基础参数截图已复核。git diff --check 通过。常用 bin/x64/Release 输出被 Visual Studio（704）及运行中的主程序（37988）锁定，复制失败（MSB3027/MSB3021），未停止用户程序；关闭调试后重新生成即可。

## 2026-09-19：统一按最近上游自动订阅并修复轮廓节点初始化
- 用户要求：连线后图像、位置修正及其他订阅按所需类型从最近上游向前查找，图像源→裁切→卡尺必须默认选择裁切输出；新节点统一遵循规则。另反馈轮廓模板匹配没有自动订阅。
- 计划：公共订阅控件接入可替换的最近上游选择服务，跳过类型不匹配节点；保留手动选择和旧方案明确订阅，空参数恢复不覆盖自动选择；修复轮廓节点参数初始化并增加真实节点连线回归。

## 2026-09-19：确定按钮无条件关闭端子参数窗口
- 用户再次明确确定用于关闭窗体。此前 Hide 位于图像/ROI/定位校验之后，未满足条件就无法关闭。移除关闭所需的运行前置条件，将隐藏放入 finally，输入无效也关闭并保留原已保存参数；有效编辑仍保留。确定不触发控件验证，测量忙碌时也保持可点击并取消预览。
- 完成：确定的 Hide 移入 finally；移除缺图、缺 ROI、无定位及空订阅对关闭的拦截；Designer 设置 CausesValidation=false。忙碌时只禁用其他操作，确定仍可点击并请求取消预览。保留有效编辑，输入非法时保留原已保存参数但始终关闭。
- 15 项真实按钮专项验证通过（-ConfirmCloseOnly）：未配置、缺图、缺区域、无定位、非法输入、忙碌均关闭，可再次打开，有效编辑保留。已同步旧测试预期与使用文档。独立和常用 Release 构建成功；git diff --check 通过。未改动其他任务正在修改的轮廓匹配与订阅代码。
- 完成：新增可替换 ISubscriptionSourceSelector 与默认最近上游策略，反向邻接表按连线距离广度优先查找，同层按编号稳定排序，复用端口目录筛选类型和集合形态。标准控件连线后自动选最近兼容来源；手选、明确恢复路径、主动清空保留原意，自动来源随拓扑/输出定义变化更新。增加释放时解绑事件。
- 修复轮廓节点：创建时初始化快照，首次连线即可写入图像来源；恢复空参数使用公共自动选择，仅订阅实际变化才发布新快照，避免提前转换旧模板或改变数值精度。其他原有确定保存节点的输入控件即时填好，ROI/阈值等业务配置仍按原保存方式处理。
- 验证：真实裁切、卡尺、轮廓节点自动订阅 31 项断言通过，覆盖未打开窗口的参数与实际取值、跨无图像节点查找、修正列表、数值/布尔/文本、新输出接入、手选保护、改线/断线重连及环路。原轮廓匹配 97 项断言、订阅输入契约/目录/类型/输出分类/位置修正兼容共五项检查通过。独立验证与常用 bin/x64/Release 均构建成功，未连接现场设备。
- 新增 docs/节点自动订阅规则.md，并修正新增节点开发交接文档的输入类型与输出声明示例。契约检查改为扫描实际 NodeSubscription 字段声明，覆盖 imageSubscription 等命名；git diff --check 通过。

## 2026-09-20：修复轮廓模板与端子角度节点名称翻译
- 用户截图显示 ProcessNew.NodeType.ContourMatch/TerminalAngle 被当作标题，副标题直接显示枚举。工具箱改为通过统一名称入口使用中文键，补齐端子角度语言资源；流程图显示兼容旧错误名称及重复节点数字后缀，不修改方案中的实际节点名或订阅关系。
- 完成：新增 NodeDisplayText 统一类型/默认标题显示，工具箱采用轮廓模板匹配、端子角度中文键；流程图标题、副标题与预览条目使用翻译名称，切换语言刷新画布。兼容旧资源键、默认中英文名和自动数字后缀；自定义名称及实际节点模型不变。
- 验证：独立程序集加载实际中英文资源，两节点类型名、旧资源键加 _2 后缀及自定义名保留均通过；独立和常用 Release 构建成功，语言资源随构建更新。

## 2026-09-20：端子角度改为左右轮廓拟合
- 用户要求改前先提交当前代码；已创建检查点提交 850b700（添加端子角度节点），本轮只在该提交之后调整端子角度测量算法。
- 计划：移除仅取最大连通域的主体提取方式，改为在二值分割后的全部外轮廓中按左右边界点拟合端子两侧线，再由两侧线中线计算角度；保留原有阈值、高斯、宽度、高度覆盖和竖直方向质量检查。
- 完成：端子 ROI 分割后直接查找全部外轮廓，逐行提取最左/最右轮廓点分别拟合左右边界，取平均方向生成端子中线和角度；宽度采用中位数，仍保留触边、有效点数、高度覆盖、宽度范围、长宽比和竖直方向检查。
- 验证：独立 Release/x64 构建成功；新增几何专项入口 `-GeometryOnly`，23 项断言通过，覆盖完整端子、反光分段端子、空图、触边、短主体、缺失 ROI 与取消。使用说明已同步新算法流程；原 System.Memory 测试兼容警告保留。

## 2026-09-20：新增 AST-工位1模型
- 用户要求：目标分类新增 DET/640 模型，ParamFormTDAI 下拉显示 AST-工位1模型；严格沿用 RL12Parse 的检测项 switch、提取、判定、绘制和自动学习结构。
- 标签顺序：0 绝缘胶、1 屏蔽网、2 铜环、3 胶皮、4 飞丝。新增九项胶皮距离/位置及屏蔽网、铜环、绝缘胶宽高检测；不额外增加飞丝检测项。
- 计划：追加枚举保持旧方案值，接入 Designer 下拉及保存恢复、DET 分发、新解析器、中文键语言资源和可导入模板；创建 DET/640 配置指向用户现有加密模型。距离暂按水平相邻边缘间距，绝缘胶位置按距离，待用户补充口径。先添加真实程序集离线验证再实现。
- 完成接入：新增 ASTStation1Parse，九项中文 switch 按 RL12 流程完成提取、上下限判定、缺失清零、矩形去重与颜色合并、流程统计、物理转换和自动学习。学习样本按节点隔离，缺少依赖不提前中断后续尺寸项。
- Designer 下拉显示 AST-工位1模型，保存/恢复映射 AST_工位1模型；枚举追加到末尾，旧方案数值不变。DET 生产分发及项目编译项已接入。
- 新增 AST-工位1检测项.items、AST-工位1模型.ai 和可读配置 JSON；模型为 DET/640，五标签按原始顺序，指向原目录 work1.onnx.encrypted，默认 GPU/置信度0.5/NMS0.5。模板默认范围1～99999，实际公差由现场设置。新增语言资源使用中文键。
- 验证：先在旧程序集观察到解析器缺失的预期失败；实现后52项真实程序集离线断言通过，覆盖九项数值、缺失/禁用、换算、颜色、多目标、反向胶皮、重叠、学习隔离、真实窗体保存恢复、配置加解密和语言切换。四项既有AI解析/ROI回归通过，界面截图已复核。
- 独立验证目录及常用 bin/x64/Release 构建成功，两个主程序SHA256一致（8151237B…）；JSON与git diff --check通过。保留框架既有编译警告，未加载现场模型、连接设备或运行现场图片推理；未改动原有端子角度工作区改动。使用说明见 docs/AST-工位1模型.md。
- 只读复核发现学习完成事件仍按节点显示名广播，同名节点可能一并停止学习。补充真实双窗体同名回归，AST事件携带节点参数实例，公共接收端仅对该发送方式按实例匹配，保留旧解析器名称通知兼容。
- 最终复核：同名节点真实窗体测试先复现失败；AST完成事件改为携带参数实例后，55项断言全部通过（含旧事件兼容）。独立及常用Release重新构建成功，主程序SHA256一致：B9EF9C3AB1852A5FB9078FBA97DF374F25881E17BF4E953B208ACDF9399A05A2。只读审查的唯一P2问题已解决，距离口径保持文档中明确的暂定水平边缘间距。

## 2026-09-20：AST工位1距离改为Y轴
- 用户明确三个胶皮距离/位置项必须按Y轴计算。将水平左右边缘间距改为竖直上下边缘间距，胶皮在上方或下方均支持，Y投影重叠或接触仍为0。
- 先将真实解析回归改为X投影重叠、Y方向分离的检测框，覆盖三项距离、上下反向、多目标、接触/重叠和物理换算，再修改公共距离函数；同步使用说明并构建验证。
- 完成：三个检测项共用距离函数已改用Top/Bottom计算Y轴边缘间距，保留缺失零值、物理换算和原有框匹配。竖直回归先在旧X算法上失败，修改后59项离线断言全部通过，覆盖三个Y距离、上下反向、Y接触/重叠、X分离不影响Y值、多目标及学习/保存恢复。独立与常用Release构建成功且主程序哈希一致；使用说明已同步，未进行现场图片推理。

## 2026-09-20：提交当前代码并同步远程仓库
- 用户要求提交当前代码并推送远程；本地提交包含 AST 工位1接入、Y轴距离计算、端子角度左右轮廓拟合，以及相关模板、测试、语言资源和说明。
- 提交前重新执行 Release/x64 独立构建成功；AST工位1离线验证59项断言、端子角度几何专项23项断言全部通过。端子测试保留既有 System.Memory 程序集兼容警告。
- 沿用仓库既有独立快照发布方式：以 origin/codex/keyence-nano-overtcp 最新提交为父提交，发布当前已跟踪文件树并排除 .superpowers 临时状态，避免上传旧开发历史中的超大压缩包；本地开发历史保持原位。
- 运行日志、images_v2.db、_codex_temp_tie_inspection 临时工程、未跟踪的测试结果及 outputs 导出文件保留在本地，不纳入本次代码提交。
