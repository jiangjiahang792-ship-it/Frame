# TDJS-Vision 改造性能计划书

- 计划版本：V2.57
- 建立日期：2026-08-19
- 当前状态：步骤4图像资源生命周期第二轮前七小批、步骤5前三小批已验证；步骤6、步骤7、步骤8已完成；步骤9正式1000轮尚未开始；工件上下文、单流程FIFO、Solution、海康转换线程和软硬触发图像源节点已接入相机生产票据；停止后运行令牌、工件纪元、有序信号协调器和相机票据表统一换代；实际有序信号协调核心、四类外发节点及30秒顺序缺口主动报警均已接入；图像源方案参数权威性、相机离线恢复、对象换代隔离和共享相机冲突门禁已完成验证
- 改造前基线提交：`73462eb`
- 改造前备份分支：`codex/backup-before-resource-management-20260819`
- 当前开发电脑参考配置：Intel Core i7-14650HX，16个物理核心，24个逻辑线程，约16GB内存
- 核心约束：流程节点顺序由用户编辑决定，框架只管理资源，不改变业务节点顺序
- AI策略：AI节点按照各自流程图执行，本轮不增加强制全局AI并发限制
- 数据兼容策略：只面向最新框架数据模型，不为旧版方案、配置或序列化字段增加兼容分支；稳定性与运行效率同为验收条件

## 一、关键参数总表（每次实施前先核对本表）

以下参数采用稳定的配置键设计，后续可以绑定到`FrmSystemSetting`控件。硬件相关参数默认使用“自动”，用户手动设置后形成机器级覆盖值，不写入方案文件，防止方案复制到不同电脑后带入错误的线程和内存配置。

### 1. 总开关与诊断参数

| 配置键 | 中文名称 | 默认值或自动规则 | 允许范围 | 建议开放到界面 | 作用 |
| --- | --- | --- | --- | --- | --- |
| `AdaptiveResourceManagementEnabled` | 启用自适应资源管理 | `true` | 开/关 | 是 | 控制CPU、内存、保存和后台任务的自动调节 |
| `FullPerformanceDiagnosticsEnabled` | 完整性能诊断 | 跟随现有`调试(Debug)`开关 | 开/关 | 不新增控件 | Debug关闭时不采集完整链路现场 |
| `TraceEveryRunWhenDebug` | Debug记录每轮流程 | `true` | 开/关 | 是 | Debug开启后每轮都记录完整阶段耗时 |
| `RuntimeSampleIntervalMs` | 运行资源采样周期 | `1000ms` | 500～10000ms | 是 | 采集CPU、内存、GC、线程池、句柄等低频指标 |
| `DiagnosticQueueCapacity` | 诊断日志队列容量 | `4096`条 | 256～20000条 | 是 | 防止完整诊断直接阻塞检测线程 |
| `DiagnosticFlushIntervalMs` | 诊断日志刷新周期 | `250ms` | 50～5000ms | 是 | 后台批量写入诊断日志 |
| `DiagnosticBatchSize` | 单批诊断写入数量 | `200`条 | 10～1000条 | 是 | 限制每次写日志占用时间 |
| `CommonSlowThresholdMs` | 普通节点慢耗时阈值 | `20ms` | 1～10000ms | 是 | 标记普通节点性能毛刺 |
| `FrameConvertSlowThresholdMs` | 图像转换慢耗时阈值 | `100ms` | 10～10000ms | 是 | 标记相机像素转换异常 |
| `UiQueueSlowThresholdMs` | UI排队慢耗时阈值 | `20ms` | 1～5000ms | 是 | 标记UI线程消息积压 |
| `FlowSlowThresholdMs` | 整轮流程慢耗时阈值 | `1000ms` | 50～60000ms | 是 | 标记流程总耗时异常 |
| `CriticalStallThresholdMs` | 严重停顿阈值 | `3000ms` | 500～60000ms | 是 | 捕获类似现场6～10秒停顿 |

### 2. CPU与并发参数

| 配置键 | 中文名称 | 默认值或自动规则 | 允许范围 | 当前电脑预期值 | 建议开放到界面 |
| --- | --- | --- | --- | --- | --- |
| `CpuReserveCoreCount` | 系统保留核心数 | `max(2, ceil(物理核心数×20%))` | 1～物理核心数-1 | 4 | 是，支持自动/手动 |
| `CpuHeavyMaxConcurrency` | CPU重任务最大并发 | `min(启用流程数, 物理核心数-保留核心数, 8)` | 1～32 | 四流程时为4 | 是，支持自动/手动 |
| `ImageConvertMaxConcurrency` | 共享非相机图像转换最大并发 | `min(相机数, max(1, floor(物理核心数÷4)), 4)` | 1～8 | 四相机时为4；不约束海康采集转Mat | 是，支持自动/手动 |
| 每相机转换通道（当前固定行为，非配置键） | 海康采集独立转换 | 每台取流相机1个常驻线程、实例锁及私有缓冲；不同相机并行 | 单相机顺序执行，无跨相机许可上限 | 跟随实际取流相机数量 | 本轮不新增控件 |
| `ImageSaveWorkerCount` | 图片保存工作线程数 | `clamp(floor(物理核心数÷8), 1, 2)` | 1～8 | 2 | 是，支持自动/手动 |
| `CpuHighWatermarkPercent` | CPU高水位 | `85%` | 50%～100% | 85% | 是 |
| `CpuRecoveryWatermarkPercent` | CPU恢复水位 | `65%` | 20%～高水位-5% | 65% | 是 |
| `CpuHighDurationSeconds` | CPU高水位持续时间 | `5秒` | 1～60秒 | 5秒 | 是 |
| `CpuRecoveryDurationSeconds` | CPU恢复水位持续时间 | `10秒` | 1～300秒 | 10秒 | 是 |
| `ResourceAdjustmentIntervalMs` | 资源策略评估周期 | `5000ms` | 1000～60000ms | 5000ms | 是 |
| `ResourceAdjustmentCooldownSeconds` | 调整冷却时间 | `30秒` | 5～600秒 | 30秒 | 是 |
| `ConcurrencyAdjustmentStep` | 单次并发调整步长 | `1` | 1～4 | 1 | 是 |
| `AiConcurrencyPolicy` | AI并发策略 | `FollowEditableFlow` | 固定 | 按可编辑流程运行 | 否，本轮固定 |

说明：不固定绑定CPU核心，不直接按逻辑线程数创建同等数量的任务，也不默认修改全局线程池参数。节点内部并行必须占用同一份CPU预算，避免四个流程各自在内部再次使用全部核心。

### 3. 内存与图像生命周期参数

| 配置键 | 中文名称 | 默认值或自动规则 | 允许范围 | 当前电脑预期值 | 建议开放到界面 |
| --- | --- | --- | --- | --- | --- |
| `FlowImageMemoryBudgetPercent` | 流程图像内存预算比例 | 总内存`12.5%`，同时不超过启动可用内存`25%`和`2048MB` | 5%～30% | 最大约2048MB | 是 |
| `SaveQueueMemoryBudgetPercent` | 保存队列内存预算比例 | 总内存`4%`，同时不超过启动可用内存`10%`和`512MB` | 1%～15% | 最大约512MB | 是 |
| `MemoryLowWatermarkPercent` | 可用内存低水位 | `15%` | 5%～40% | 约2.4GB | 是 |
| `MemoryCriticalWatermarkPercent` | 可用内存严重水位 | `8%` | 2%～低水位-2% | 约1.3GB | 是 |
| `MemoryLowWatermarkMinimumMb` | 低水位最小值 | `1536MB` | 256～8192MB | 1536MB | 是 |
| `MemoryCriticalMinimumMb` | 严重水位最小值 | `768MB` | 128～4096MB | 768MB | 是 |
| `ImageSizeSampleCount` | 单帧内存采样数量 | 前`20`帧滚动平均 | 1～500帧 | 20帧 | 是 |
| `ImageLeaseLongHoldWarningMs` | 图像长期持有告警 | `30000ms` | 1000～600000ms | 30000ms | 是 |
| `ShutdownResourceDrainTimeoutMs` | 停止时资源排空等待 | `5000ms` | 0～60000ms | 5000ms | 是 |
| `CameraRawFrameMemoryBudgetRatio` | 相机原始帧总预算比例 | 流程图像内存预算的`25%` | 5%～50% | 25% | 是 |
| `CameraRawFrameBufferCountMinimum` | 每相机最少原始缓冲 | `2`块 | 2～8 | 2 | 是 |
| `CameraRawFrameBufferCountMaximum` | 每相机最多原始缓冲 | `8`块 | 2～32 | 8 | 是 |
| `CameraRawFrameFallbackMemoryBudgetMb` | 尚未生成资源档案时的原始帧总预算 | `256MB` | 64～2048MB | 256MB | 是 |
| `ImageOwnershipMode` | 图像所有权模式 | `ReferenceLease` | 固定 | 引用租约 | 否，框架安全规则 |
| `YoloImageTransportMode` | YOLO隔离进程图像传输模式 | `SharedMemory` | 固定为共享内存 | 共享内存 | 否，native隔离规则 |
| `YoloSharedImageCapacityGrowthMb` | YOLO共享图像扩容粒度 | `4MB` | 1～64MB | 4MB | 是 |
| `YoloSharedImageMaximumMbPerWorker` | 单模型worker共享图像上限 | `512MB` | 16～1024MB | 512MB | 是 |
| `YoloWorkerIsolationMode` | YOLO模型进程隔离方式 | 每个模型实例独立worker和独立映射 | 固定 | 独立 | 否，native依赖隔离规则 |

内存压力取“百分比阈值”和“最小MB阈值”中更严格的一项。Mat实际占用按`Rows × Step`或连续内存实际字节计算，不能只按宽、高、通道数估算。

### 4. UI显示参数

| 配置键 | 中文名称 | 默认值或自动规则 | 允许范围 | 建议开放到界面 | 作用 |
| --- | --- | --- | --- | --- | --- |
| `UiLatestFrameCapacity` | 每个显示窗口待显示容量 | `1` | 固定为1 | 否 | 新图替换未显示旧图，旧Bitmap立即释放 |
| `UiMaxRefreshFps` | UI最大刷新帧率 | `30` | 1～60 | 是 | 限制高频刷新占用UI线程 |
| `UiDropSupersededFrames` | 丢弃被新图替代的旧显示帧 | `true` | 固定开启 | 否 | 保证UI不积压历史图片 |
| `UiConversionOutsideUiThread` | UI外完成Mat转Bitmap | `true` | 固定开启 | 否 | UI线程只接管图片并刷新控件 |

四个流程对应四个显示窗口时，每个窗口分别拥有容量1，不是四个窗口共用一个显示位置。

### 5. 相机、流程与保存队列参数

| 配置键 | 中文名称 | 默认值或自动规则 | 允许范围 | 建议开放到界面 | 作用 |
| --- | --- | --- | --- | --- | --- |
| `CameraPendingFrameCapacity` | 每台相机流程待交付槽位容量 | `1` | 固定为1 | 否 | 仅指流程交付槽位，不等同于相机原始帧缓冲池；满载策略后续独立设计 |
| `CameraConversionWorkerCount` | 相机顺序消费线程 | 每台相机`1`条普通优先级线程 | 固定为1 | 否 | 保证单相机帧序；空闲时事件等待，不轮询 |
| `CameraConversionGlobalConcurrency` | 全局相机转换并发 | 复用`ImageConvertMaxConcurrency` | 1～8 | 复用现有控件 | 多相机只允许硬件档案批准的转换数量实际占用CPU |
| `CameraRawFrameFullPolicy` | 相机原始帧满载策略 | 生产模式`FaultAndStop`，预览模式`LatestOnly` | 固定两种模式 | 否 | SDK回调不得阻塞；生产帧无法交接时立即故障停机，禁止静默丢件 |
| `CameraTriggerTicketMaximumPending` | 单相机待匹配触发票据上限 | `64` | 2～1024 | 是 | 数量满载时暂停触发并报警，不能丢票据或无限增长 |
| `CameraTriggerDispatchShutdownTimeoutMs` | 关闭等待SDK触发返回 | `2000ms` | 100～60000ms | 是 | 超时明确失败关闭，先停相机解除SDK阻塞后重试，禁止静默留下触发动作 |
| `CameraFaultDispatcherShutdownTimeoutMs` | 关闭等待相机故障派发 | `2000ms` | 100～60000ms | 是 | 慢订阅者不能永久卡住UI或软件退出，后台线程返回后自行释放句柄 |
| `CameraProductionFaultDispatchMode` | 相机生产故障派发模式 | `DedicatedFifoThread` | 固定 | 否 | 独立单消费者按序派发，不占SDK回调线程和通用线程池 |
| `CameraLateFrameRecoveryPolicy` | 迟到帧恢复策略 | `StopDrainRestartConfirm` | 固定 | 否 | 清场已武装票据或收到无法绑定帧后，必须停流、等回调排空、重启取流后才能恢复 |
| `CameraProductionAcquisitionTimeoutScope` | 生产采图超时范围 | `TriggerCommandAndFrame` | 固定 | 否 | SDK软触发命令和对应生产帧必须在图像源同一超时内共同完成 |
| `CameraSoftwareFrameTimeoutPolicy` | 软件触发取帧超时策略 | `FailCurrentAndRecoverCamera` | 固定 | 否 | `Auto/SOFT`在单次等待超时后失败本相机当前排队工件，隔离迟到帧并只重启该相机，方案和其他相机继续运行 |
| `CameraSoftwareFrameRecoveryMode` | 软件触发迟到帧恢复方式 | `StopDrainRestartConfirm` | 固定 | 否 | 超时后停流、排空SDK回调和转换线程、重新取流并确认流代数，防止旧帧绑定下一工件 |
| `CameraSoftwareTimeoutStopScope` | 软件触发超时停机范围 | `NeverStopSolution` | 固定 | 否 | 普通取帧超时只记可恢复告警；SDK触发命令卡死或失败、帧号错乱和转换失败仍按不可恢复故障处理 |
| `CameraInitialProductionFrameGraceMs` | 相机连接后首个生产帧启动宽限 | `500ms` | 100～2000ms | 是 | 每次相机成功连接后仅首个成功登记的`Auto/SOFT`生产票据在方案超时外追加一次冷启动宽限；采用预留、提交、失败归还，后续票据严格使用图像源`TimeOut` |
| `CameraPreviewFrameIsolation` | 预览软触发帧隔离 | `TrackUntilConverted` | 固定 | 否 | 命令返回后继续保留待消费标记，该预览帧完成转换前禁止登记生产票据 |
| `CameraProductionFrameEstimateMode` | 生产帧触发前估算 | `max(Width×Height×4, 历史平均或8MB)` | 固定 | 后续只读显示 | 按每台相机当前宽高保守覆盖BGR源Mat与灰度Mat，避免只用全局平均低估 |
| `CameraHardwareTriggerProductionPolicy` | 硬触发生产策略 | `ArmTicketThenWaitPhysicalFrame` | 固定 | 否 | 在单相机临界区内原子登记并武装硬触发票据，只等待线路触发帧，绝不发送软件触发命令 |
| `CameraAutoTriggerSourcePolicy` | Auto触发兼容策略 | `NormalizeToSoftware` | 固定 | 否 | 旧配置Auto按SOFT处理，SDK收到明确软触发源，极性仅对LINE0～LINE4设置 |
| `CameraSessionRenewalPolicy` | 停止后相机会话换代 | `RenewAfterAllRunsStopped` | 固定 | 否 | 旧活动会话完全退出后才换运行令牌、工件纪元、协调器、票据表和图像预算，旧故障不得穿透新会话 |
| `ImageSourceParameterAuthority` | 图像源相机参数权威来源 | `SchemeWins` | 固定 | 只读显示 | 曝光、增益、触发、延迟和超时以方案为准，相机当前值不得反向覆盖 |
| `ImageSourceOfflineRestorePolicy` | 相机离线时方案恢复策略 | `RestoreWithoutHardwareRead` | 固定 | 只读显示 | 相机未枚举或离线时仍完整恢复图像源节点，不调用SDK参数读取 |
| `ImageSourceCameraRebindKey` | 图像源相机重新绑定键 | `UserDefinedName` | 固定 | 只读显示 | 方案恢复、回调启动和节点运行前按保存名称解析当前相机对象 |
| `ImageSourceParameterApplyTiming` | 方案参数写入相机时机 | `OnlineRestore,Reconnect,BeforeRun` | 三个固定时机 | 只读显示 | 在线恢复、重新连接和正式运行前均写入方案曝光与增益 |
| `ImageSourceExposureUiMaximumUs` | 图像源曝光输入上限 | `10000000us` | 1～10000000us | 是 | 只约束方案输入范围，硬件最终支持范围由SDK写入结果确认 |
| `ImageSourceGainUiMaximum` | 图像源增益输入上限 | `100000`，三位小数 | 0～100000 | 是 | 保留不同厂家相机的浮点增益方案值，不读取硬件当前增益修正 |
| `WorkpieceIdentityMode` | 工件身份模式 | `SessionEpoch + Int64Sequence` | 固定 | 否 | 每次生产会话唯一，顺序号只增不减，重启后不会与旧会话混淆 |
| `WorkpieceQueueInitialCapacity` | 工件队列初始容量 | `8` | 2～64 | 是 | 尚无完整流程耗时和图像持有样本时使用的保守容量 |
| `WorkpieceQueueMaximumCapacity` | 单生产顺序域最大工件容量 | `64` | 2～1024 | 是 | 实际容量同时受图像内存预算和工件突发需求限制 |
| `WorkpieceQueueMemoryBudgetRatio` | 工件队列内存预算比例 | 流程图像内存预算的`50%` | 10%～70% | 是 | 与相机原始帧预算、活动流程图像和保存预算分开核算 |
| `WorkpieceQueueHighWatermarkPercent` | 工件队列高水位 | `75%` | 50%～95% | 是 | 达到高水位后停止批准新触发并请求生产线暂停 |
| `WorkpieceQueueRecoveryWatermarkPercent` | 工件队列恢复水位 | `40%` | 10%～高水位-10% | 是 | 低于恢复水位且故障已确认后才允许恢复触发 |
| `WorkpieceQueueFullPolicy` | 生产工件队列满载策略 | `PauseTriggerAndAlarm` | 固定 | 否 | 不丢新、不丢旧、不无限等待；暂停触发并产生可见故障 |
| `PerProcessWorkpieceConcurrency` | 单流程工件并发 | 第一阶段固定`1` | 后续1～8 | 后续开放 | 第一阶段保护现有节点实例和结果对象不重入，不限制不同流程并行 |
| `FlowPlanSwapPolicy` | 运行中流程版本切换 | `DrainThenSwap` | 固定 | 否 | 已排队工件继续使用入队时不可变快照，新版本排空后生效 |
| `OrderedSignalEnabled` | 启用有序外发 | `true` | 固定开启 | 否 | 所有业务确认的外发节点必须通过顺序协调器 |
| `OrderedSignalNodeTypes` | 有序外发节点白名单 | `ComSend,CameraIO,PLCWrite,ModbusWrite` | 当前固定四类 | 只读显示 | 不按节点名称判断，不自动纳入读取、软触发和手动相机IO |
| `SignalOrderDomainMode` | 信号顺序域 | `ProductionLane` | 生产线/工位级 | 是 | 同一工件链路共享顺序域，不同独立生产线互不阻塞 |
| `SignalOrderGapTimeoutMs` | 前序工件缺口超时 | `30000ms` | 1000～600000ms | 是 | 超时后停机报警，不越过缺失工件继续发送 |
| `SignalCoordinatorRetryCount` | 协调器盲重试次数 | `0` | 固定为0 | 否 | 未知发送结果时禁止盲重试；重试由具备确认和幂等能力的设备适配器负责 |
| `SignalFailurePolicy` | 信号失败策略 | `StopAndAlarm` | 固定 | 否 | 发送失败、顺序缺口和结果未知均停止后续工件信号 |
| `SignalDuplicatePolicy` | 重复或过期信号策略 | `RejectAndAlarm` | 固定 | 否 | `WorkpieceId`小于已提交序号时拒绝，不允许重复外发 |
| `SignalIntentLifecycle` | 信号意图生命周期 | `ReserveSubmitDrainSeal` | 固定 | 否 | 上下文原子预约意图并立即提交，在途意图全部返回后才发布工件终态 |
| `SignalAcknowledgementLevels` | 发送确认等级 | `DeviceAcknowledged,LocalCallCompleted,Rejected,Unknown,CancelledBeforeStart` | 固定五级 | 只读显示 | 区分设备确认、本地返回、明确拒绝、结果未知和发送前安全取消 |
| `SignalKeyComparison` | 顺序域和设备键比较 | `Trim + OrdinalIgnoreCase` | 固定 | 否 | 防止大小写或首尾空白把同一生产线、同一设备拆成不同锁域 |
| `SignalDispatchConcurrencyPerDomain` | 单顺序域实际发送并发 | `1` | 固定为1 | 否 | 同一生产线任意时刻只提交一个外部副作用，防止后工件先发送 |
| `SignalEndpointConcurrency` | 单物理端点并发 | `1` | 固定为1 | 否 | 不同生产线共用同一连接时仍按端点键互斥，独立端点允许并行 |
| `SignalUnknownEndpointPolicy` | 结果未知端点策略 | `FreezeUntilCoordinatorShutdown` | 固定 | 否 | 外部副作用状态未知时保留端点租约，禁止其他顺序域继续写同一设备 |
| `SignalCompletedIntentRetention` | 完成意图保留周期 | `UntilWorkpieceSealed` | 固定 | 否 | 工件封口前重复提交返回首次结果，封口后旧工件意图明确拒绝且不永久持有委托 |
| `SignalShutdownPreCommitPolicy` | 关闭时未提交动作策略 | `CancelBeforeStart` | 固定 | 否 | 关闭与副作用提交点原子竞争，尚未开始的端点等待不得在关闭后迟到写设备 |
| `ComSendEndpointKeyMode` | 串口发送端点键 | `COM:PhysicalPortName` | 固定 | 否 | 同一物理COM端口跨节点、跨生产线共享唯一互斥端点，不使用可修改界面别名 |
| `ComSendAcknowledgementLevel` | 串口发送确认等级 | `LocalCallCompleted` | 固定 | 只读显示 | 只证明串口写入API正常返回，不伪造下位机已经处理的协议确认 |
| `ComSendWriteTimeoutMs` | 串口同步写超时 | `2000ms` | 100～60000ms | 是 | 防止驱动或设备阻塞导致工件封口和同端点后续信号永久等待 |
| `CameraIoEndpointKeyMode` | 相机IO端点键 | `CAMERA-IO:StableCameraIdentity` | 固定 | 否 | 同一物理相机的线路选择器是共享SDK状态，跨节点、跨生产线必须整体互斥 |
| `CameraIoPulseHoldMaximumUs` | 相机IO脉冲最大保持时间 | `1000000us` | 0～1000000us | 是 | 防止错误参数长期占住相机端点；较长等待使用异步等待加末段自旋 |
| `CameraIoAsyncMode` | 相机IO异步执行策略 | `ManagedIntentReturnEarly` | 固定 | 只读显示 | 节点可提前返回，但信号意图仍由工件上下文持有并在封口前排空，不创建游离后台脉冲 |
| `CameraIoAcknowledgementLevel` | 相机IO确认等级 | `LocalCallCompleted` | 固定 | 只读显示 | 只证明完整低高低脉冲的SDK调用正常返回，不伪造外部设备已经接收 |
| `CameraIoLineModePolicy` | 相机IO线路模式 | `OutputOnly` | 固定 | 否 | 生产信号节点只允许主动输出线路，输入极性不作为对外信号发送 |
| `PlcWriteEndpointKeyMode` | PLC写入端点键 | `Brand + Transport + PhysicalAddress` | 固定 | 否 | 同一品牌、IP端口或COM口的物理连接跨节点整体互斥，不按寄存器地址拆锁 |
| `PlcOperationTimeoutMs` | PLC连接与收发超时 | `5000ms` | 100～60000ms | 是 | 应用到HSL连接/接收及底层串口写超时，禁止超时后释放租约而让后台写入继续运行 |
| `PlcWriteAcknowledgementLevel` | PLC写入确认等级 | HSL成功响应为`DeviceAcknowledged` | 固定 | 只读显示 | 保存HSL响应、错误码、物理端点和写入地址作为确认凭据 |
| `PlcWriteFailureResponsePolicy` | PLC失败响应策略 | `UnknownAndFreeze` | 固定 | 否 | 写请求可能已生效但响应丢失，失败或空响应均按未知冻结端点，禁止盲重试 |
| `PlcWriteArraySeparator` | PLC多值分隔符 | 逗号 | 逗号、分号、中文逗号 | 是 | 避免连字符与负数、科学计数法负指数发生歧义；布尔值保留旧连字符解析 |
| `ModbusWriteEndpointKeyMode` | Modbus写入端点键 | TCP使用`IP:Port`，RTU使用`COM口` | 固定 | 否 | 锁覆盖整条物理连接而不是寄存器地址；相同端点跨节点、跨生产线互斥 |
| `ModbusOperationTimeoutMs` | Modbus连接与收发超时 | `5000ms` | 100～60000ms | 是 | TCP应用到连接和接收，RTU应用到管线接收及底层串口读写，避免无限占用端点 |
| `ModbusWriteAcknowledgementLevel` | Modbus写入确认等级 | HSL成功响应为`DeviceAcknowledged` | 固定 | 只读显示 | 现有设备实现检查协议响应，确认凭据保存物理端点、地址和数据类型 |
| `ModbusWriteFailurePolicy` | Modbus写入异常策略 | `UnknownAndFreeze` | 固定 | 否 | 写入开始后的异常无法证明设备未执行，按未知冻结端点且不盲重试 |
| `ModbusWriteArraySeparator` | Modbus多值分隔符 | 逗号 | 逗号、分号、中文逗号 | 是 | 布尔严格限制0、1、False、True，浮点拒绝NaN和无穷大 |
| `ParallelExternalSignalPolicy` | 并行外发节点策略 | `RejectUnlessOrderedOrExclusive` | 固定 | 否 | 可同轮执行的外发节点必须有图路径先后；True/False互斥分支允许并列 |
| `SaveQueueInitialCapacity` | 未完成图像采样时保存队列初始容量 | `8` | 1～50 | 是 | 启动阶段的保守容量 |
| `SaveQueueMaximumCapacity` | 保存队列最大容量 | `50` | 1～500 | 是 | 最终容量还要受内存预算限制 |
| `SaveQueueCalculatedCapacity` | 保存队列实际容量 | `clamp(floor(保存内存预算÷平均任务持有字节), 4, 最大容量)` | 自动 | 只读显示 | 随图片大小和电脑内存变化 |
| `SaveQueueEnqueueTimeoutMs` | 保存入队允许阻塞时间 | `0ms` | 0～5000ms | 是 | 默认不得阻塞检测主链路 |
| `SaveQueueFullPolicy` | 保存队列满策略 | `优先保留NG，淘汰最早普通任务` | 等待/丢新/丢旧/NG优先 | 是 | 满队列行为必须明确并写日志 |
| `RetiredSaveQueueCapacity` | 淘汰任务回收容量 | 默认等于保存主队列容量 | 1～500 | 是 | 防止异步释放通道形成第二个无界队列 |
| `RetiredSaveQueueMemoryBudgetBytes` | 淘汰任务回收字节预算 | 默认等于保存主队列字节预算 | 1MB～4096MB | 是 | 限制被NG淘汰但尚未释放的图像资源 |
| `IdleWorkerShutdownGraceMs` | 空闲工作线程退出保护时间 | `1000ms` | 100～5000ms | 是 | 资源重配时只用于唤醒并退出空闲固定线程，不延长活动任务配置超时 |
| `SaveDiskLowSpaceMb` | 保存磁盘空间告警 | `2048MB` | 256～102400MB | 是 | 磁盘不足时告警并停止非必要保存 |
| `SaveDiskCriticalSpaceMb` | 保存磁盘严重空间水位 | `512MB` | 64～低水位-1MB | 是 | 严重不足时停止包括NG图在内的全部保存 |
| `SaveSlowThresholdMs` | 单张保存慢耗时阈值 | `500ms` | 10～60000ms | 是 | 识别编码或磁盘性能下降 |
| `SaveDiskProbeIntervalMs` | 保存磁盘空间采样周期 | `1000ms` | 100～60000ms | 是 | 缓存同一磁盘空间结果，避免每张图重复访问磁盘状态 |

保存节点在可编辑流程中的位置不变。保存任务必须取得独立图像租约或独立副本，后台保存完成后释放自己的持有权。

### 6. 压力测试与验收参数

| 配置键或测试项 | 默认值 | 允许调整范围 | 用途 |
| --- | --- | --- | --- |
| `QuickStressCycles` | 1000轮 | 100～10000轮 | 每个阶段后的四流程快速压力测试 |
| `MilestoneStressMinutes` | 60分钟 | 10～480分钟 | 每个重要阶段的稳定性测试 |
| `FinalStressCycles` | 10000轮 | 1000～100000轮 | 最终并发回归 |
| `FinalSoakHours` | 2小时 | 1～24小时 | 最终内存和资源稳定性测试 |
| `DebugOffOverheadLimitPercent` | 2% | 0.5%～10% | Debug关闭时相对基准的性能上限 |
| `DebugOnOverheadTargetPercent` | 5% | 1%～20% | Debug完整记录时的性能目标 |
| `MemoryStableWindowMinutes` | 30分钟 | 10～240分钟 | 判断预热后内存是否进入稳定区间 |
| `P99FrameConvertTargetMs` | 500ms | 50～5000ms | 四相机接近同时转换的P99目标 |
| `UnexplainedStallAllowedCount` | 0次 | 固定为0 | 不允许再出现无法定位的长时间空白 |
| `FixedTriggerAttempts` | 500次 | 1～100000次 | 固定节拍压力发出的真实方案触发请求数 |
| `FixedTriggerIntervalMs` | 50ms | 1～60000ms | 相邻触发请求的绝对计划间隔，不是UI轮询间隔 |
| `SolutionRunAdmissionCapacity` | 1轮 | 固定为1 | 同一批可编辑流程对象禁止重入运行 |
| `SolutionRunBusyPolicy` | `RejectAndCount` | 固定 | 忙碌时立即拒绝并计数，不阻塞触发线程、不排无界队列 |
| `TriggerMessageTimeoutMs` | 5000ms | 10～30000ms | UI线程无响应时测试消息必须超时并判定本轮无效 |
| `FixedTriggerWarmupRounds` | 1轮 | 0～100轮 | 正式触发前完成AI和流程预热，随后清零准入计数 |
| `ProcessIoComparisonRounds` | 500轮 | 100～10000轮 | 新旧版本串行同负载进程I/O与资源同比 |
| `ProcessIoSampleIntervalRounds` | 1轮 | 1～100轮 | 每轮读取主程序和AI进程累计I/O计数，形成每轮增量分位 |
| `ImageSaveComparisonCount` | 500张 | 100～10000张 | 新旧保存实现同工作量真实JPEG落盘数量 |
| `ImageSaveComparisonSize` | `1024x768 CV_8UC3` | 64～8192像素 | 使用固定随机种子噪声图，避免纯色图压缩失真 |
| `ImageSaveComparisonJpegQuality` | 90 | 1～100 | 两版统一JPEG编码质量 |
| `ImageSaveComparisonWorkers` | 4 | 1～16 | 同工作量同比时与旧版节点默认保存线程一致 |
| `ImageSaveComparisonQueueCapacity` | 512 | 1～10000 | 当前版同比专用容量，保证500张全部接收；生产边界另用容量8测试 |
| `ImageSaveComparisonTimeoutSeconds` | 120秒 | 10～600秒 | 等待全部JPEG发布和工作线程停止的硬超时 |
| `ImageSaveResourceSampleMilliseconds` | 10毫秒 | 10～1000毫秒 | 采样私有/托管内存、GDI、句柄和按逻辑线程归一化CPU |
| `ImageSaveLongStressRounds` | 10轮 | 3～100轮 | 同一进程反复创建、排空和销毁保存工作池，观察强制回收后趋势 |
| `ImageSaveLongStressAttemptsPerRound` | 500次 | 60～10000次 | 长稳每轮四流程并发投递次数，默认正式累计5000次 |
| `Step9WindowMode` | `Minimized` | 固定 | 主程序最小化且只使用不激活显示，不占用用户键盘鼠标焦点 |
| `Step9ProcessPriority` | `BelowNormal` | 固定 | 办公期间让用户应用优先获得CPU调度 |
| `Step9DebugMode` | `Off`、`On`分别执行 | 固定 | 子进程环境覆盖，不修改用户Debug设置 |
| `Step9TestEnvironmentMode` | `SharedDesktopBackground` | `SharedDesktopBackground`、`Dedicated` | 共享办公环境只判稳定性，专用空闲环境才允许形成最终性能结论 |
| `Step9MinimumSharedDesktopAvailableMemoryMb` | `4096MB` | 512～65536MB | 共享办公环境低于保护线时拒绝启动完整方案压力，优先保障用户办公 |
| `WorkpieceQueueDeterministicCycles` | `100000`次 | 1000～1000000次 | 验证准入、高低水位、取消、停止和图像租约收敛 |
| `SignalReorderDeterministicCycles` | `10000`组 | 100～100000组 | 强制后工件先完成，验证四类外发始终按工件和信号序号提交 |
| `ProductionQueueReleaseRounds` | `1000`轮 | 100～100000轮 | `Release|x64`真实相机、完整流程、外发设备和UI旁路正式验收 |
| `ProductionQueueSoakHours` | `2小时` | 1～24小时 | 验证队列深度、内存、Mat、句柄、线程和信号顺序长期稳定 |

### 7. 每步骤新旧版本固定同比测试

每个性能改造步骤完成代码修改、专项验证和完整编译后，必须在进入下一步骤前执行一次固定口径的新旧版本同比测试，并把结果追加到本计划书和`任务记录.md`。未完成同比时，只能说明专项验证通过，不能宣称整套业务性能已经优化。

| 测试项目 | 固定口径 |
| --- | --- |
| 未改造基线程序 | `D:\MyCode\PublicWook\框架版本\框架8_18号版本\x64\x64\Release\机器视觉AI检测系统V1.0.exe` |
| 改造版本程序 | 主智能体完成当前步骤后生成的最新`Debug|x64`可执行程序 |
| 固定方案 | `C:\Users\34652\Desktop\8_6方案.Sol` |
| 图像来源 | 使用方案中图像源节点配置的本地目录图像，不连接真实相机 |
| Modbus环境 | 连接本机虚拟服务器`127.0.0.1:502`，测试前先确认端口可连接 |
| 预热规则 | 未改造版和改造版各预热1轮，预热数据不计入正式统计 |
| 正式轮数 | 未改造版和改造版各连续运行5000轮 |
| 环境一致性 | 同一电脑、同一方案、同一图像目录、同一Modbus数据、同一Debug开关、同一UI与保存开关；测试期间不得同时运行另一版本 |
| 允许写入 | 允许程序写入运行日志；不得修改未改造版源码、改造版源码或固定方案业务数据 |

每次同比至少记录以下数据：5000轮成功数、失败数、异常数、总耗时、吞吐量、整轮P50/P95/P99/最大耗时、CPU平均值与峰值、进程私有内存起始值/峰值/结束值、托管内存起始值/峰值/结束值、GDI对象起始值/峰值/结束值、总句柄起始值/峰值/结束值、Modbus通信成功/失败数量、保存队列峰值和丢弃数量。无法从旧版取得的指标必须标记“旧版无采集能力”，不得填入推测值。

结果表必须同时给出未改造版数值、改造版数值、绝对变化、变化百分比和结论。耗时、CPU、内存、GDI或句柄降低时使用“下降”描述；结果变差时必须如实标记回退并分析原因。构建耗时、隔离算法循环、单控件GDI测试、UI微基准和生命周期压力测试只能作为专项证据，不能替代本节规定的完整方案5000轮同比。

> 2026-08-20执行口径变更：用户明确停止完整方案5000轮长测，并指定步骤6以新旧版本各500轮完成阶段同比。原5000轮仍保留为步骤7～9完成后的全框架最终验收，不再作为步骤6进入步骤7的阻塞条件；步骤6另以同进程5000次保存工作池压力补足资源回收长稳证据。

## 二、参数保存与未来界面设计原则

1. 自动值、手动覆盖值、最终生效值必须分开保存和显示。
2. 硬件相关手动值保存为本机设置，不进入`.Sol`方案文件。
3. `FrmSystemSetting`未来可以增加“性能与资源”区域，控件必须放在`FrmSystemSetting.Designer.cs`。
4. 每个可调参数提供中文名称、自动开关、当前建议值、手动值、有效范围和恢复推荐值。
5. 不允许用户配置出零线程、负容量、恢复水位高于高水位等无效组合。
6. 参数变更写入Debug日志，并记录变更前值、变更后值和最终生效值。
7. 第一轮实现配置类和接口，不立即增加大量界面控件；经过压力测试确认参数确实有调整价值后再开放到界面。
8. 本轮允许直接升级方案和配置数据结构，不增加旧字段回填、双格式读取或运行时兼容判断；所有破坏性变化依靠Git恢复点和任务记录追溯。

## 三、Git版本保护执行规则

每次修改文件前执行：

1. 查看Git状态和当前分支。
2. 将已有正式源码、设计器、配置、测试和任务记录提交。
3. 临时工程、`bin`、`obj`和独立二进制备份不混入源码提交，也不擅自删除。
4. 记录改动前提交编号。
5. 修改、测试并更新本计划书和`任务记录.md`。
6. 本步骤完成后再次提交，提交信息使用简体中文并注明阶段。

禁止使用`git reset --hard`、强制覆盖和改写历史。需要恢复时优先从明确的提交或备份分支创建恢复分支。

## 四、分步执行计划

### 步骤0：建立改造前基线

- 状态：已完成
- 改动前提交：`73462eb`
- 备份分支：`codex/backup-before-resource-management-20260819`
- 关键决定：正式源码纳入基线；临时工程和两个`.codex-bak`文件保留在磁盘但不纳入源码版本。
- 验证：Git已经生成可恢复提交，尚未修改性能资源管理代码。

### 步骤1：建立改造前性能基准

- 状态：已完成本机可执行基准；真实四相机与PLC现场基准待现场环境复测
- 关键参数：`QuickStressCycles`、`MilestoneStressMinutes`、`RuntimeSampleIntervalMs`。
- 工作内容：记录当前四流程的P50、P95、P99、最大耗时、内存、GDI、句柄和线程池状态。
- 测试组合：Debug开/关、UI开/关、保存开/关、单流程和四流程接近同时触发。
- 关键决定：先取得基准再修改框架，后续所有优化必须与同一基准比较。
- 输出：在本文件“测试记录”章节写入机器配置、方案、图像尺寸和实测结果。
- 本机基准结果：`Debug|x64`完整编译通过，0个错误、175个既有警告，MSBuild报告耗时5.25秒。
- 回归基准结果：日志等级、Debug诊断等级、诊断惰性求值、相机回调快速链路、相机帧等待、统一相机回调、保存图像订阅、日志UI节流和方案资源释放共9项检查通过。
- 环境说明：日志UI测试依赖.NET Framework运行环境，使用Windows PowerShell执行通过；当前开发电脑未连接客户现场四相机和PLC，不把静态或模拟结果作为现场节拍数据。

### 步骤2：统一Debug完整链路诊断

- 状态：已完成；公共流程、相机帧、UI、保存后台、旧诊断开销和通信节点五轮均已验证
- 关键参数：`TraceEveryRunWhenDebug`、`DiagnosticQueueCapacity`、`DiagnosticFlushIntervalMs`、各阶段慢耗时阈值。
- 工作内容：复用`LogLevelSettings.IsEnabled(MsgLevel.Debug)`，贯通触发、相机回调、格式转换、流程排队、节点、UI、保存和通信阶段。
- 关键决定：Debug关闭时不得创建完整追踪对象或拼接日志文本；Debug开启时每轮完整记录。
- 关键决定：使用有界后台诊断队列，诊断本身不能制造新的流程卡顿。
- 验证：同一运行编号可以还原整轮时间线，能够明确定位6～10秒停顿发生在哪个阶段。
- 第一轮完成：公共日志增加4096条Debug诊断待写水位保护；流程按Debug开关创建`TraceId`，统一记录流程开始、节点开始、节点结果、节点异常和流程结束。
- 第一轮检查：覆盖静态流程入口、实例流程入口、相机回调后续流程入口和调参图像刷新入口；`Debug|x64`编译0错误，6项诊断与图运行专项检查通过。
- 第二轮完成：每台相机使用线程安全递增`FrameId`，通过弱引用关联表把SDK回调、`ConvertPixelType`、`CopyToMat`、颜色转换、多订阅者Mat分发、图像源等待器和当前流程`TraceId/RunId`串成同一时间线。
- 第二轮资源边界：帧关联表不拥有、不克隆也不释放Mat；每个订阅者仍独立接管自己的Mat包装对象，Mat不可达后关联元数据自动回收；Debug关闭时不创建帧元数据且不拼接完整耗时文本。
- 第二轮检查：`Debug|x64`编译0错误、175个既有警告，8项相机与诊断专项检查通过；弱引用关联表完成50000轮压力测试，用时26ms并通过强制GC回收检查。
- 第三轮完成：新增只读`PerformanceTraceContext`，将创建时的流程`TraceId/RunId`带入图像显示事件、UI线程投递和保存后台任务，记录显示节点到UI刷新、保存节点到后台取图及文件写完的完整耗时。
- 第三轮正式运行开销：显示控件、图像旋转、ROI结果绘制、保存节点和参数预览统一按Debug开关创建计时器；Debug关闭时保存链不再采集进程内存、GDI、句柄快照，也不构建Mat/Bitmap/OutputImage摘要。
- 第三轮检查：`Debug|x64`编译0错误、175个既有警告，12项UI、保存、ROI、资源释放、相机与诊断回归通过；其中2项OpenCv/System.Drawing运行程序集测试按项目环境使用Windows PowerShell执行。
- 第四轮完成：流程相机快速路径、跳过节点结果准备、`ImageDraw`、`ResultOverlayDraw2`和`FindPoint`的分段诊断统一按Debug开关创建；性能日志由Info直写改为Debug惰性生成，并补充原流程`TraceId/RunId`。
- 第四轮业务边界：并行图运行时用于计算`RunTime`的总计时和找点算法公开结果`AlgorithmMs`继续保留，不受Debug开关影响；只移除正式运行中的诊断对象、分段Stopwatch和诊断摘要。
- 第四轮检查：`Debug|x64`编译0错误、175个既有警告；13项流程、相机、绘制、找点、资源释放与诊断回归通过；Debug关闭快速入口100000次压力检查全部未创建Stopwatch，用时513ms。
- 第五轮完成：通用流程追踪按`NodeType`自动识别17类设备通信、协议通信和信号等待节点，统一记录“通信节点开始/结束/异常”、通信类型、实际墙钟耗时、节点报告耗时、异步前后线程和慢耗时运行时现场。
- 第五轮流程边界：通信识别挂在所有节点共有的运行与结果入口，不读取前后节点、不读取旧流程索引，也不假设AI、PLC或流程结束的固定顺序；用户编辑的任意合法流程顺序都会自动生效。
- 第五轮检查：编译产物逐项验证17类通信节点全部命中、4类非通信节点全部不误判；13项完整链路相关回归通过；通信分类完成200000次压力判断，用时772ms且结果稳定。
- 步骤2交付：完整链路已覆盖流程、相机、节点、UI、保存后台和通信阶段；后续步骤3硬件资源档案也已完成，详见下一节。

### 步骤3：读取硬件并计算资源档案

- 状态：已完成
- 关键参数：CPU保留核心数、CPU重任务并发、图像转换并发、保存线程、内存预算和资源采样周期。
- 工作内容：通过接口读取物理核心、逻辑线程、进程可用处理器、总内存、可用内存、GPU信息、磁盘空间、相机数量、流程数量和实际单帧字节数。
- 关键决定：硬件读取失败时采用保守降级值并写警告，不能阻止软件启动。
- 关键决定：GPU信息只用于诊断，本轮不据此限制AI节点并发。
- 验证：在不同核心数和内存组合的模拟输入下，所有自动参数都在允许范围内。
- 实现结果：新增`IHardwareResourceProbe`、`IResourceProfileCalculator`和`IRuntimeResourceProfileProvider`三层可替换接口；Windows探针读取WMI CPU/内存/GPU、进程亲和性和程序程序集所在磁盘，默认提供器缓存硬件快照，避免每轮方案重复执行WMI。
- 运行接入：`Solution.Run`在相机启动前按启用流程数和二维/三维相机数刷新`CurrentResourceProfile`；当前只生成建议和Debug日志，不修改线程池、CPU亲和性或AI并发。
- 公式边界：CPU重任务、图像转换和保存线程同时受物理核心及进程亲和性可用处理器限制；内存预算取总内存比例、启动可用内存比例和绝对上限中的最小值；未采样图像时保存队列容量使用8。
- 本机实测：Intel Core i7-14650HX，16个物理核心、24个逻辑线程、进程可用24线程、总内存16087MB，NVIDIA GeForce RTX 4060 Laptop GPU，程序磁盘`D:\`；连续复测可用内存在1168～1546MB间波动，曾低于自动严重水位1287MB。
- 本机四流程四相机建议：CPU保留核心4、CPU重任务并发4、图像转换并发4、保存线程2；流程图像和保存队列预算会随启动可用内存变化，末次为386MB与154MB；尚未采样实际图像，保存队列先使用容量8。
- 验证结果：16核、双核、零流程/相机、进程亲和性受限等公式用例与本机真实WMI探针通过；资源档案50000轮压力计算结果稳定，用时624ms。
- 下一轮范围：执行步骤4，先盘点`OutputImage`、节点`Result`、UI和保存后台之间现有Mat/Bitmap所有权，再引入可验证的租约与幂等释放边界。

### 步骤4：建立Mat、Bitmap和Result生命周期

- 状态：第一轮已完成，第二轮执行中
- 关键参数：`ImageOwnershipMode`、图像内存预算、长期持有告警、停止排空等待时间。
- 工作内容：建立图像租约、引用计数、统一所有权和幂等释放机制。
- 关键决定：处理`SrcImg`、`GrayImg`、`Bitmaps`和上下游节点引用同一个Mat的情况，不能重复Dispose。
- 关键决定：Result被下一轮替换时释放自身持有权，真正的Mat在最后一个使用者完成后释放。
- 关键决定：流程异常、取消、停止、节点删除和窗口关闭均有资源回收路径。
- 验证：专项测试覆盖共享Mat、保存延迟、UI延迟、异常和取消，不发生提前释放或重复释放。
- 第一轮范围：为`OutputImage`增加显式自产/借用所有权、引用租约和幂等释放；`NodeBase.Result`替换、节点删除及方案释放统一回收旧结果；先接入图像源、3D深度预览、裁切、分割、旋转、预处理和保存后台。
- 第一轮边界：保存队列本轮只补齐图像租约，不提前合并步骤6的容量、满队列策略和动态工作线程改造；UI最新帧槽仍留在步骤5。
- 第一轮实现：`OutputImage`按Mat对象引用去重自产资源，`SrcImg`、`GrayImg`和`Bitmaps`引用同一Mat时只释放一次；重复`Dispose`和重复释放租约不会重复回收。
- 借用链实现：借用输出持有上游父级租约，保存任务再持有当前输出租约；`图像源 -> 借用节点 -> 保存后台`任意一层替换Result时，真正拥有的Mat都会等最末使用者退出后再释放。
- Result回收：`NodeBase.Result`通过原子交换发布，替换后统一释放旧结果；节点删除、控件Dispose、删除流程和方案重置均释放最后一次结果。
- 生产者接入：本轮已覆盖二维图像源、共享变量图像、3D深度预览、裁切、分割、旋转、图像预处理、颜色识别借用输出和Blob二值图；创建中途异常会清理已经生成的Mat。
- 保存接入：保存任务入队前取得租约，正常、异常、入队失败和停止排空均幂等释放；本轮仍沿用原队列容量和工作线程，动态有界策略留在步骤6。
- 第一轮验证：生命周期专项测试覆盖自产/借用、同Mat去重、父级租约链、重复释放、结果反射回收和50000轮压力；最终压力用时36ms且每轮恰好释放一次测试Mat。
- 第一轮回归：相关19项回归全部通过；`Debug|x64`两次完整编译最终为0错误、175个既有警告，最终编译耗时3.00秒。
- 第二轮范围：迁移电池耳、二值化、找线、找圆、模板匹配、图像绘制、两套结果绘制和线合并等剩余直接写入`OutputImage`的节点，并继续核对临时灰度图和异常路径。
- 第二轮补充范围：纳入`TDAI`多ROI临时输出和Blob缺少上游灰度缓存时的临时转灰度，确保未发布到Result的中间Mat也有明确释放路径。
- 第二轮第一小批：电池耳和二值分析的自产结果图已登记所有权；找线和找圆改为借用上游Mat并持有父级租约，取消原有整图Clone；四个节点均先构建完整新Result再一次性发布，异常和取消释放未发布图像及算法临时Bitmap。
- 第二轮第一小批验证：首次完整编译发现电池耳新增局部`OutputImage`缺少命名空间引用，补齐后`Debug|x64`编译4.14秒、0错误、175个既有警告；生命周期50000轮压力用时50ms，方案释放、订阅契约和订阅类型隔离回归全部通过。
- 第二轮第二小批：电池耳释放ROI、模板匹配和治具测量临时Mat；找线/找圆释放ROI、模糊、Canny及绘制中间Mat，并在预览替换时回收旧GDI图像；二值分析预览释放返回Bitmap且等待异步分析完成。
- 第二轮临时链：`TDAI`多ROI输出拥有全部裁剪Mat和首张灰度图、持有上游父级租约并在单轮推理结束释放，OBB单张克隆使用作用域释放；Blob灰度获取显式返回释放责任，正式运行和参数预览只释放本轮新建灰度图，预览结果图通过统一资源管理器回收。
- 第二轮性能修正：全仓调用点确认找线/找圆返回Bitmap无人使用后，将其改为“值类型检测结果+成功标志”，删除每次检测额外的ROI Clone与Mat转Bitmap；不改变检测算法、结果绘制或上游输出。
- 第二轮第二小批验证：最终`Debug|x64`编译2.55秒、0错误、172个既有警告；5组隔离回归通过，生命周期50000轮压力用时47ms；中间编译发现OpenCv直线/圆为值类型，已用显式成功标志修正并复编通过。
- 第二轮第三小批：图像绘制读取上游Mat时持有短租约，生成的绘制Mat登记自产所有权；两套结果绘制不再仅保存裸上游Mat引用，改为向上游持有父级租约，并统一按新Result发布。
- 第二轮模板匹配：普通模板匹配的Bitmap转Mat登记自产所有权并在正式运行结束释放原Bitmap；NCC快路径在算法计算阶段持有短租约，结果发布后转成父级依赖租约，不复制整图且不会提前释放上游Mat。
- 第二轮线组合拟合：正式运行不再创建无意义空Mat；预览Mat在显示异常时也通过`finally`释放，存在输出时由结果容器接管；所有待发布结果在异常或发布失败时统一回收。
- 第二轮第三小批验证：`Debug|x64`完整编译4.66秒、0错误、172个既有警告；模板匹配5项、结果绘制4项、几何测量、性能诊断、方案资源释放和订阅契约均通过；生命周期50000轮压力用时54ms且每轮恰好释放一次。
- 第二轮输出审计：全仓直接构造或写入`OutputImage`的位置均已逐项确认，当前生产节点都已使用自产登记或父级租约；下一小批继续检查参数窗体、算法辅助类和显示控件中不进入Result的临时Mat/Bitmap。
- 第二轮第四小批：公共`ShowImageControl`在清空、替换显示结果和控件销毁时统一释放当前Bitmap、Mask叠加内部Bitmap、可释放ROI及交互定时器；重复Dispose通过原子状态保护。
- 第四小批GDI修正：棋盘背景、状态栏、ROI标签、旋转手柄和卡尺箭头重绘不再直接创建未释放的`SolidBrush/Pen`；共享画刷在单次绘制作用域内复用并确定释放，不改变显示效果。
- 第四小批验证：实际Windows STA环境连续完成250次控件重绘和100次Mask叠加创建/清空，GDI对象增长为0；生命周期50000轮用时44ms，UI诊断、诊断惰性求值、AI ROI和两套结果绘制等回归通过；`Debug|x64`编译4.33秒、0错误、172个既有警告。
- 第二轮第五小批：颜色识别节点修复同一轮两次发布同一个Result的问题，改为完整构建后只发布一次；从读取上游Mat到Modbus等待及颜色算法结束全程持有短租约，发布输出再持有父级依赖。
- 第五小批调参资源：颜色参数窗体和模板创建窗体通过标准Designer Dispose路径释放最后一张Mat；订阅刷新与文件读取先生成候选图再交换，异常时释放候选；模板对话框不再静态持有并在关闭后Dispose。
- 第五小批异步预览：颜色全量/单模板后台检测使用独立源图快照，刷新或关闭窗体不会释放后台正在使用的Mat；未发布预览Bitmap、源图快照、自动取色均值/标准差Mat以及文字绘制Font/Brush在正常与异常路径释放。
- 第五小批验证：`Debug|x64`完整编译2.52秒、0错误、172个既有警告；颜色资源、订阅契约、结果颜色和UI诊断回归通过；生命周期50000轮用时40ms，显示控件250次重绘与100次Mask循环的GDI增长仍为0。
- 第二轮第六小批：公共`ImageROIEditControl`明确接管传入Bitmap，在替换和Designer标准Dispose路径释放旧图；所有调用点完成所有权审计，两个训练窗体保留只读别名但不再重复释放已由控件回收的旧图。
- 第六小批二维码资源：二维码模型只创建一次可等待加载任务，窗体释放后回收模型并观察加载异常；每轮检测释放全部ROI Mat、模糊Mat、检测返回bbox Mat和CLAHE对象，边缘预览替换同步释放旧Bitmap。
- 第六小批异步边界：二维码预处理在进入后台前读取界面参数，后台不跨线程访问WinForms控件；删除未使用的均衡化Mat，模型未加载完成时由检测异步等待，不再以字段空值直接报错。
- 第六小批验证：`Debug|x64`完整编译2.59秒、0错误、172个既有警告；公共ROI控件连续替换500张图并确认旧图底层句柄失效，GDI增长0；公共显示控件250次重绘与100次Mask循环GDI增长0；生命周期50000轮用时47ms，AI ROI、训练行为和订阅契约回归通过。
- 第二轮第七小批：图像分割窗体分别管理可复用原图和分割线临时预览，刷新、重复预览及Designer Dispose均按引用关系释放且不误删仍要复用的原图；旋转窗体在标准Dispose路径释放最后一张预览Bitmap。
- 第七小批模板缓存：电池耳模板从“每个ROI重新读盘并更新PictureBox”改为每个节点内存缓存；每轮检测只各取一次极耳和Mark独立Mat快照，整轮匹配结束统一释放，模板编辑、重载和删除原子替换并释放旧Mat。
- 第七小批多节点同步：模板窗体不再使用进程级静态Form，每个电池耳节点独占并随节点释放；模板文件修改使用轻量全局版本号通知其他节点，仅版本变化后的下一轮重新读盘一次，正常检测没有文件I/O和隐藏模板窗体Bitmap转换。
- 第七小批异常边界：模板ROI无论数量是否正确都释放全部Mat；文件Bitmap复制后脱离已关闭流；Designer共享资源图只解除引用不Dispose；存图失败、模板匹配异常、窗体关闭和模板替换均有确定回收路径。
- 第七小批验证：真实生成6张BMP模板，Windows STA环境完成100轮六预览重载和250轮双模板组快照获取/释放，GDI增长0；ROI替换、公共显示、方案释放和订阅回归通过，生命周期50000轮用时50ms；`Debug|x64`最终编译2.64秒、0错误、171个既有警告。

### 步骤5：改造UI最新图像槽

- 状态：前三小批已完成并验证；真实相机分辨率长稳和现场业务耗时对照待后续小批
- 关键参数：`UiLatestFrameCapacity=1`、`UiMaxRefreshFps`、`UiDropSupersededFrames`。
- 工作内容：每个显示控件建立独立最新图像槽，Mat转Bitmap放在UI线程外完成。
- 关键决定：四个流程显示到四个窗口时互不覆盖；同一窗口只保留最新待显示图。
- 关键决定：窗口关闭、无订阅者、BeginInvoke失败和新图替换时立即释放对应Bitmap。
- 验证：高频显示时UI不卡死、内存和GDI不持续上涨、检测主链路耗时不随UI刷新明显增加。
- 第一小批窗口槽：每个`FrmSingleImage`实例独立持有一个`PendingViewerFrame`，将Bitmap、同帧叠加结果和Debug追踪上下文作为一组原子替换；新帧立即释放尚未显示的旧Bitmap，每个窗口最多保留一个待显示帧和一个已投递UI任务。
- 第一小批UI边界：UI线程每次只取当前最新帧交给`ShowImageControl`；`MatViewer`在调用线程完成Mat转Bitmap后才跨线程移交；公共`YTPictrueBox`统一接管新图并在替换、清空和Designer Dispose时释放旧图或最后一张图。
- 第一小批关闭边界：流程图像窗体销毁时解除图像、窗口名、运行参数、语言和权限静态事件，排空待显示帧；BeginInvoke失败、句柄未建立和控件已释放路径均回收Bitmap。
- 第一小批压力结果：真实Windows STA环境将UI线程保持忙碌并由后台连续提交500张`320x240`Bitmap；多轮后台提交61～176ms、UI消费2～8ms，最终构建产物复测为91ms和3ms；全部轮次始终只保留`1`个待帧和`1`个UI任务，GDI增长均为0。带500帧待槽的窗口销毁在0ms内排空，待帧Bitmap句柄确认失效。
- 第一小批公共控件结果：多轮各替换500张`320x240`Bitmap，用时83～250ms，最终构建产物复测为183ms；全部轮次GDI增长均为0。公共显示控件250次重绘和100次Mask替换复测GDI增长0。
- 第一小批最终代码复核：修正并发Debug日志快照后再连续三轮，公共控件500次替换为83ms、89ms、85ms，后台500帧提交为64ms、65ms、69ms，UI最终帧消费均为3ms；待帧/UI任务峰值仍为1/1，GDI增长仍为0/0。
- 第一小批证据边界：以上数据证明受控500帧突发下显示任务有界、旧帧及时释放且UI只消费最新帧，不等同于真实相机到PLC整轮耗时改善；当前也没有高分辨率实图、四窗口同时触发、长期运行的托管堆/私有工作集/Mat原生内存曲线。现场仍需用Debug完整追踪分别比较相机等待、节点、UI和通信P50/P95/P99，形成同工况改造前后基线。
- 第一小批编译回归：多次`Debug|x64`完整重建均为0错误，记录到4.80秒/171警告、4.93秒/171警告、7.37秒/172警告和9.16秒/171警告；构建时点和工作区状态不同，因此不把构建耗时与警告数差异作为运行性能改善证据，最新结果为9.16秒、171个既有警告，与上一批数量一致。UI与保存诊断、显示控件、图像生命周期、方案释放和订阅消费契约通过；生命周期50000轮实测56～107ms，提交前复核为90ms。
- 第二小批刷新门控：新增可替换`IUiFrameRateLimiter`，默认`UiMaxRefreshFps=30`并限制在1～60；每个标准化窗口名拥有独立时间状态和独立锁，四个窗口不共享总额度，也不建立跨窗口显示队列。
- 第二小批转换边界：二维与三维显示节点都在`Mat.ToBitmap()`之前取得本窗口许可；被限速帧只跳过本次显示转换与投递，节点仍按原逻辑成功并继续执行用户编辑的后续节点，不改变AI、PLC、通信或保存节点顺序。
- 第二小批发布所有权：显示事件参数采用“原子预认领+接管后确认”；订阅者预认领后抛异常但未确认转交时，发布器仍持有兜底引用并立即释放。无人订阅、窗口名不匹配、子显示控件句柄未建立和旧连续采图入口无人接管时同样由明确责任方释放；成功显示后由`ShowImageControl`接管，窗口关闭释放最后显示图并重置该窗口刷新状态。
- 第二小批四线程压力：四个工作线程同步起跑，每个线程固定访问一个窗口，连续四轮合计各调用100000次，耗时3ms、2ms、3ms、3ms；每轮各窗口通过数均为`1,1,1,1`，证明窗口额度未退化成全局锁或全局单帧。测试设置2秒上限；手动时钟另验证30FPS需要完整34个千分之一秒时间戳、单窗口Reset不影响其他窗口。
- 第二小批UI与资源结果：最终程序集多次复测公共图替换500帧72～147ms、单个流程窗口后台提交500帧60～90ms、UI最新帧清空2～4ms、窗口销毁排空0ms；待帧/UI任务峰值1/1，窗口与公共控件GDI增长0/0。公共显示控件250次绘制与100次Mask循环GDI增长0；结果图生命周期50000轮用时51～73ms。
- 第二小批硬件档案：本机实测16物理核心、24逻辑线程、16087MB总内存和RTX 4060 Laptop；四流程建议CPU重任务并发4、图像转换并发4、保存线程2、UI每窗口30FPS，AI策略保持`FollowEditableFlow`。
- 第二小批证据边界：以上数据证明帧率判断在四线程下开销低、四窗口额度独立、Bitmap拒收与订阅异常路径可回收，并证明单窗口UI突发任务继续有界；尚未测量四张高分辨率图同时执行`Mat.ToBitmap`和四个真实窗口同时绘制，也没有接入四台真实相机与PLC，因此不能据此声称解决客户现场8秒等待。步骤9仍需在同一工况采集相机等待、转换、算法、UI、通信和整轮P50/P95/P99。
- 第三小批真实窗口场景：启动四个具有真实句柄和子显示控件的`FrmSingleImage`，四个后台线程同步起跑；每个线程持有独立`1024x768 CV_8UC3 Mat`，按40ms节拍调用生产帧率门控、OpenCvSharp `BitmapConverter.ToBitmap`和生产发布器，主UI线程持续处理WinForms消息并约每16ms强制刷新四个窗体。
- 第三小批稳定数据：全部异常边界补强和`1024x768`完整链预热后，3个独立PowerShell进程各运行3轮，每轮四窗口各尝试、允许并接管30帧，合计9轮、1080次实际转换与投递，所有轮次均为`30,30,30,30`，没有窗口丢失已转换帧；每轮带40ms节拍的墙钟耗时1245～1259ms，最后UI排空0～5ms，每进程执行121～122次四窗强制刷新周期。
- 第三小批转换耗时：四线程同时运行时，各窗口单帧平均`Mat.ToBitmap`为0.74～1.50ms，30样本P95为0.95～2.69ms，单帧最大1.09～6.58ms；测试规定单次转换不得达到3000ms、每轮不得达到10000ms，实测未出现接近客户现场8秒的转换停顿。墙钟数据明确包含约1200ms主动节拍等待，不作为纯计算耗时。
- 第三小批资源结果：`1024x768 Mat→Bitmap→发布→窗口Refresh`完整链预热后再取基线，运行期间每25ms采样并在每轮GC前补采尾段；3个独立进程关闭四窗口并强制回收后的私有内存净增0.81～1.08MB、采样峰值增量28.07～28.32MB，托管内存净增0.65～0.66MB、采样峰值增量11.62～11.92MB；GDI最终增长均为0、采样峰值增长12～15。测试期间系统最低可用内存1158.02～1322.19MB。
- 第三小批安全边界：起跑前可用内存至少512MB、运行中低于256MB主动取消；Mat构造失败也释放就绪门闩，起跑等待10秒超时，单轮15秒超时后先取消并最多等待30秒Join全部线程，仍不能退出则终止隔离测试进程，绝不在工作线程仍发布时销毁窗口。Bitmap在反射调用生产发布器前完成责任转交，避免异常路径重复释放。
- 第三小批基线修正：最初在任何窗体创建前取GDI基线，结束固定多16个对象；最终改为预热同尺寸转换、生产发布、窗口接管和强制刷新完整链后再取基线，最终GDI增长稳定为0，运行采样峰值13～15且关闭窗口后完全回落。
- 第三小批证据边界：本测试已经覆盖四线程、真实OpenCV转换、四个真实WinForms窗口、生产发布与接管、消息泵和强制刷新，但窗体位于屏幕外，不能证明桌面合成显示耗时，也没有逐帧绘制计数；图像内容固定且不含复杂ROI叠加，未经过相机SDK、算法节点、PLC和现场磁盘，因此不能替代客户现场整轮Debug链路。

### 步骤6：改造图片保存有界队列

- 状态：已完成；共享有界工作池、磁盘保护、500次固定50ms触发、新旧完整方案各500轮、真实JPEG各500张、生产边界500次突发和同进程5000次长稳均已验证。
- 关键参数：保存工作线程数、保存内存预算、队列计算容量、满队列策略、保存慢阈值和磁盘空间告警。
- 工作内容：保存任务取得独立图像租约，队列容量根据实际单帧内存动态计算。
- 关键决定：默认入队不阻塞检测；队列满时优先保留NG并淘汰最早普通任务，所有淘汰都写日志和计数。
- 关键决定：保存工作线程不因为磁盘变慢而不断增加。
- 验证：模拟慢磁盘、空间不足、保存异常和软件退出，队列不超过容量且所有资源最终释放。
- 第一小批实现：所有保存节点接入`Solution`唯一共享工作池；固定消费者数量读取硬件自动档案，不按节点或流程重复创建线程，不修改AI并发或可编辑流程顺序。
- 第一小批双重边界：主队列同时限制任务数和完整`OutputImage`租约字节；淘汰回收通道使用相同的任务数和字节上限，回收通道满时明确告警并同步兜底释放，禁止出现隐蔽无界积压。
- 第一小批所有权：读取源图与估算字节前先取得候选租约，接收时原子转交，拒绝、构造异常和停止排空均释放；入队优先级、所有者和字节值冻结，外部后改属性不影响队列记账。
- 第一小批写盘：`Mat→Bitmap`、标注、JPEG编码和磁盘写入移至固定后台消费者；先写唯一`.saving`临时文件再原子发布，多目录中途失败回滚已发布文件并记录清理失败。
- 第一小批生命周期：方案运行、手动流程、脚本入口和参数预览统一登记会话；首会话在生命周期锁内完成资源档案刷新；方案重置先禁止新运行、等待流程退出、停止保存池，再释放节点、设备和图像；方案加载从读文件到新方案恢复结束持续关闭运行门禁，并与并发重置使用方案级互斥锁串行化。
- 第一小批自动采样：以20帧滚动平均记录完整图像租约字节，后续运行据此计算保存容量；本机四流程四相机实测建议为保存线程2、初始容量8、保存预算108MB。
- 第一小批压力结果：有界队列四生产者、两消费者共100000任务，队列峰值8/8、字节峰值8192/8192、释放100000、重复释放0；真实保存工作池三轮各由四流程并发尝试60张`1024x768`图，失败0、租约释放60/60、同步淘汰释放0，入队P99最差2.147ms、最大2.147ms，停止排空49～55ms。
- 第一小批资源结果：三轮真实保存预热后私有内存变化`+0.25MB`、托管内存变化`-0.04MB`、进程句柄变化`0`；图像生命周期50000轮48ms，QR/ROI替换500轮GDI增长0；`Debug|x64`完整编译0错误、171个既有警告。
- 第一小批证据边界：以上是共享队列、真实转图写盘、异常回滚、停止排空和资源生命周期专项证据；尚未覆盖低磁盘空间策略、可控慢磁盘、真实方案完整链和本章固定5000轮新旧同比，因此不能宣称步骤6或整套业务性能已经完成优化。
- 第二小批磁盘准入：保存消费者在`Mat→Bitmap`前检查全部目标磁盘；可用空间低于2048MB时跳过普通图片但继续保留NG图，低于512MB时停止全部图片保存；探测失败记录告警并放行，避免监控故障误停产。同一磁盘结果缓存1000ms，生产路径不增加主动等待。
- 第二小批慢写与诊断：单目标路径从准备目录到JPEG原子发布统一计时，达到500ms记录慢写；队列快照包含任务数、租约字节、活动消费者、拒绝、淘汰、低空间降级、探测失败、慢写次数和最慢写入。工作池停止只写一条文件级汇总，不强制刷新日志界面。
- 第二小批参数接入：磁盘低水位、严重水位、慢写阈值和探测周期进入硬件自动资源档案，`Solution`创建及空闲重配共享保存池时应用并比较全部参数；不修改线程池、CPU亲和性、AI并发或业务节点顺序。
- 第二小批故障压力：正常、缓存、低空间普通图、低空间NG、严重低空间和探测失败策略全部通过；固定100ms模拟慢盘、阈值50ms、队列容量4、消费者1、尝试20张时队列峰值不超过4、消费者不超过1且出现明确拒绝，完成慢写全部被捕获，失败0，20份Mat租约全部释放、重复释放0。
- 第二小批常规压力：三轮四流程各并发尝试60张真实图片，失败均0，队列峰值8/8、并发峰值2/2、入队P99最大0.125ms、停止排空32～39ms；私有内存变化`+0.07MB`、托管内存变化`-0.07MB`、句柄变化0；`Debug|x64`完整编译0错误、171个既有警告。
- 第二小批证据边界：本批证明低空间和慢盘下保存链路有明确降级、硬边界和资源回收，但未模拟真实磁盘硬件故障，也尚未完成500次固定50ms真实流程触发以及计划书规定的5000轮新旧同比，不能据此宣称步骤6整体完成。
- 第三小批准入规则：主界面单次运行和Debug固定节拍测试复用同一个单方案准入器；同一方案同时只允许1轮，忙碌请求立即拒绝并计数，不等待、不合并、不改变AI节点或PLC节点在可编辑流程中的顺序。
- 第三小批诊断：固定容量滚动记录完整方案墙钟耗时，输出请求、准入、忙碌拒绝、完成、异常、活动状态、P50/P95/P99和最大值；租约正常、异常和重复完成路径幂等归还运行权，正式压力前只能在空闲时清零预热数据。
- 第三小批测试入口：仅`Debug`构建且显式设置测试环境变量时处理窗口消息；测试器按绝对时钟每50ms发送，不激活窗口，逐次记录计划时刻、调度漂移、消息投递耗时和准入结果；任一窗口消息超时都会判定整轮无效。
- 第三小批硬预检：固定触发模式必须从旧版运行目录自动复制并逐文件校验`device.license`、`DockPanel.config`和`DockPanelImageWindows.config`；必须确认图像视图显示、结果与Logo隐藏、日志右侧自动折叠、恰好6个图像窗口以及主程序加两个AI共3个进程。
- 第三小批代码验证：触发架构、准入器动态测试、方案资源释放、保存队列架构、磁盘保护和测试脚本语法共6项检查通过；准入器覆盖忙碌拒绝、计数闭合、租约幂等、异常归还、耗时样本容量和空闲重置；`Debug|x64`最终编译0错误。
- 第三小批真实冒烟：20次固定50ms请求中准入4、忙碌拒绝16；四流程各成功4次、Modbus成功16/16、异常0。随后100次节拍校准中准入11、忙碌拒绝89，四流程各成功11次、Modbus成功44/44、异常0；确认高频同步资源采样会干扰触发节拍，正式测试改为首尾资源快照，软件Debug完整诊断保持开启。
- 第三小批正式压力：正常窗口、正常进程优先级、旧版有效许可证、旧版Dock布局、6个图像窗口、同一原始方案和本机虚拟Modbus条件下，按绝对计划时刻发出500次请求，间隔50ms；请求500、准入47、忙碌拒绝453、窗口消息超时0、完成47、运行异常0，准入率9.4%。忙碌请求被即时拒绝，没有进入等待队列，也没有发生方案重入。
- 第三小批节拍证据：首末实际投递跨度24957.699ms，499个相邻间隔平均50.015ms、P50为49.350ms、P95为61.278ms；500次投递和尾部资源快照总墙钟25059.411ms。调度漂移P50/P95/P99/最大值为8.648/20.723/79.572/200.448ms，窗口消息投递P95/最大值为1.557/13.701ms，消息超时0。
- 第三小批业务结果：47个准入轮次全部完成；正面、同心度、侧面左、侧面右分别47/47成功、失败0，Modbus成功188/188、失败0，异常日志0。完整方案耗时P50/P95/P99/最大值为519/627/640/640ms；四流程P50分别为510/114/341/308ms。
- 第三小批资源结果：24逻辑线程下CPU平均30.652%、采样峰值53.559%；三进程私有内存起始/采样峰值/结束为1183.941/1587.465/1269.219MB，GDI为811/820/820，句柄为2534/2778/2778。测试后主程序和两个AI进程正常退出，方案、许可证和两份布局文件SHA256均与测试前一致。
- 第三小批结论：有界准入在20次/秒过载输入下保持稳定，未出现超时、异常、重入或无界排队；但当前完整方案单轮P50为519ms，只能承接约1.9次/秒，不能把每50ms的500个工件触发全部处理。若现场确实要求每个50ms触发都检测，后续不能仅靠队列管理解决，必须改变工艺并行度、算法耗时或硬件能力；下一步先分析正面流程和两侧流程耗时，再决定步骤7的CPU分类调节及新旧版本各5000轮同比。
- 第四小批进程I/O同比口径：测试器从进程外调用Windows `GetProcessIoCounters`，对主程序和同路径AI工作进程统一累计读、写及其他I/O操作数与传输字节；每轮CSV记录增量，JSON记录总量、每轮P50/P95/P99/最大值、每轮平均量和MB/s。旧版与新版均不依赖业务代码埋点，正式数据要求全部进程计数器可读且进程ID全程稳定。
- 第四小批证据边界：Windows进程I/O包含文件、设备、命名管道等由目标进程产生的I/O，能够排除其他无关进程的物理盘流量，但不能等同于独占物理磁盘读写；保存队列内部等待、编码和原子发布耗时仅新版可提供，必须与跨版本进程I/O分栏呈现，不能伪造旧版指标。
- 第四小批工具验证：动态创建并完整读回4MB测试文件，当前PowerShell进程的读写操作数均增加，读写传输字节均不少于4MB；测试器和专项脚本语法通过。固定50ms架构、方案准入、保存队列架构、磁盘保护、方案释放及进程I/O共6组检查通过；`Debug|x64`完整编译1.65秒、0警告、0错误。
- 第四小批正式条件：旧版与当前版均使用正常窗口、正常优先级、5ms完成轮询、预热1轮、串行500轮、同一原始方案、有效许可证、旧版Dock布局、6个图像窗口和本机虚拟Modbus；两版均为主程序加两个AI工作进程，进程I/O计数全部可读且正式轮次内PID稳定。
- 第四小批业务校验：两版四个流程均各500/500成功，Modbus均2000/2000成功，异常日志均为0。旧版整轮P50/P95/P99/最大值551.622/637.389/674.139/765.688ms，当前版534.162/615.439/642.834/665.613ms，分别下降3.17%/3.44%/4.64%/13.07%；该耗时只作为同负载资源效率背景，不把方案自身约500ms判为框架问题。
- 第四小批进程I/O结果：旧版500轮累计读9802.834MB、写9827.866MB，每轮读20076.203KB、写20127.470KB；当前版累计读9802.701MB、写9827.840MB，每轮读20075.932KB、写20127.416KB。读写总量差异均小于0.002%，判定总体I/O负载持平；当前版每轮写入P95/P99/最大值由20299.367/20815.087/20935.597KB降至20133.814/20134.881/20134.984KB，分别下降0.82%/3.27%/3.82%，写入长尾更平稳。
- 第四小批CPU与原生资源：CPU平均34.699%降至33.189%，下降1.510个百分点；三进程私有内存峰值2471.508MB降至2051.371MB，下降17.00%，结束值1490.715MB降至1419.777MB，下降4.76%；主进程私有内存峰值下降28.87%，结束值下降11.78%；GDI峰值837降至823，句柄峰值2896降至2814。
- 第四小批托管堆边界：托管堆峰值1031.945MB升至1052.006MB，增加1.94%；结束快照541.391MB升至642.001MB，增加18.58%。单个结束快照受GC时点影响，不能直接判定泄漏，但属于后续长稳必须复核的回退项，不能因私有内存改善而忽略。
- 第四小批磁盘结论：本轮可以确认当前版没有增加跨进程总体I/O量，且每轮写入长尾略有收敛；两版日志都没有暴露保存队列活动，因此不能证明图片编码、文件写入或物理磁盘延迟更快。磁盘保护、慢盘有界队列和资源释放继续由故障注入专项提供证据，真实图片落盘同比必须使用明确包含保存节点和固定输出目录的独立方案。
- 第四小批清理：两版测试程序及AI进程均正常退出；桌面方案、当前版许可证和两份布局文件SHA256与测试前一致，用户原有HSL虚拟Modbus保持运行。
- 第五小批真实落盘口径：新增隔离工具直接调用两版各自生产`ImageQueueProcessor`；使用相同`1024x768 CV_8UC3`确定性噪声Mat、JPEG质量90、4个保存线程、预热20张和正式500张。旧版使用节点级无界队列，当前版同工作量同比使用容量512和足额租约字节预算，保证两版都完成相同500张；当前版生产安全边界另以容量8执行500次突发，不能把明确拒绝误算为保存失败。
- 第五小批文件验收：逐个JPEG真实解码并核对尺寸，全部文件计算SHA256并要求同源结果一致；同时检查文件数、空文件、总字节、`.saving`残留、进程I/O、入队分位、保存墙钟、文件吞吐、私有/托管内存、GDI、句柄、队列峰值、工作线程峰值和租约释放。保存墙钟包含Mat转Bitmap、JPEG编码及操作系统缓存写入，不冒充断电安全的物理介质刷盘时延。
- 第五小批工具验证：旧版和当前版各20张冒烟全部成功，文件字节与SHA256一致；当前版20个资源租约全部释放且无重复释放。`Debug|x64`整库编译0警告0错误，真实落盘架构、保存队列架构、磁盘保护、方案资源释放和保存工作池压力检查全部通过；10毫秒CPU采样也已在两版按24个逻辑线程口径成功出数，正式500张同比在该恢复点之后执行。
- 第五小批长稳工具验证：可调轮数和每轮尝试数已在同一宿主2轮、每轮20次通过；队列与线程边界保持8和2，40个租约全部释放，强制回收后私有内存变化`+0.059MB`、托管内存变化`-0.027MB`、句柄0增长。
- 第五小批耗时边界：文件吞吐必须使用“从首个入队到全部JPEG完成”的墙钟；全部文件完成后的`StopProcessing`单独记录，另保留两者相加的总墙钟。不得把旧版工作线程停止等待归到Mat转Bitmap、JPEG编码和系统缓存写入速度中。
- 第五小批同工作量结果：两版均完成500/500张有效`1024x768` JPEG，均为337,698,500字节，首图SHA256一致，空文件、无效图和`.saving`残留均为0。文件完成墙钟旧版6517ms、当前版1559ms，下降76.08%；吞吐49.418→206.578MB/s，提升318.02%；平均CPU 17.234%→15.736%，下降1.498个百分点；峰值CPU下降5.998个百分点；私有内存峰值117.809→108.555MB，下降7.86%。
- 第五小批资源回退项：当前版托管内存瞬时峰值21.891→25.912MB，增加4.021MB；句柄瞬时峰值898→926，增加28个。强制GC后托管内存只增加0.090MB，私有内存降低4.504MB，结束句柄减少9个，GDI峰值和结束值均为3；结合5000次长稳无增长，判定为新版工作池与诊断基础设施瞬时开销，不符合持续泄漏特征。
- 第五小批生产边界：2个保存线程、容量8、500次瞬时投递下，接收10、明确拒绝490、完成文件10、失败0；队列峰值8/8、租约字节峰值18MB、线程峰值2/2，500个租约全部释放、重复释放0。
- 第五小批同进程长稳：10轮×500次共5000次四流程并发投递，接收516、明确拒绝4484、淘汰普通237、完成279、失败0；5000个租约全部释放。队列峰值8、字节峰值18MB、线程峰值2，入队P99最差0.0693ms、绝对最大0.1732ms；平均CPU 16.057%，强制回收后私有内存`-0.031MB`、托管内存`-0.009MB`、句柄`-1`。
- 步骤6结论：当前版在相同真实JPEG工作量下更快、平均CPU更低、私有内存峰值更低；在过载、慢盘、低空间、写盘失败和停止超时场景下保持任务数、租约字节和线程硬边界，资源有明确最终去向。步骤6按用户确认的阶段口径完成。
- 步骤6最终验收：`Debug|x64`整库编译0警告0错误；真实落盘架构、保存队列架构、磁盘保护、方案资源释放和保存工作池压力5组回归通过；最终JSON交叉校验确认500张同工作量哈希一致、生产边界不超8任务/2线程、同进程5000次失败0且租约全部释放。

### 步骤7：CPU资源分类和动态调节

- 状态：已完成；第四小批真实四流程500轮、三轮VX只读审查、保存接纳后原子租约、明确预算拒绝、循环依赖保护及专项压力均已收口。
- 关键参数：CPU重任务并发、CPU高低水位、持续时间、调整周期、冷却时间和调整步长。
- 工作内容：给图像转换、CPU算法、图片编码等节点或操作标记资源类型，统一进入资源调度器。
- 关键决定：调度器不改变流程节点顺序，只控制节点取得CPU重任务许可的时间。
- 关键决定：AI节点按照可编辑流程执行，不进入本轮全局AI限流器。
- 关键决定：动态调整一次只变化一个额度，并设置冷却时间防止反复波动。
- 第一小批结构：`ICpuWorkScheduler`负责可替换接入；`CpuWorkScheduler`使用全局CPU额度和图像转换分类额度的统一等待队列，分类受限任务不能阻塞其他可运行CPU任务；取消、停止和重复释放必须幂等收敛。
- 第一小批动态规则：默认最大并发来自硬件档案，最小并发1；CPU达到85%持续5秒后减少1，CPU降到65%持续10秒后增加1，两次调整至少冷却30秒。调低额度不取消正在运行的节点，后续任务等待活动数自然回落。
- 第一小批诊断：记录当前/峰值活动数、图像转换数、等待数、许可/完成/取消、升降次数、最近CPU和许可等待P50/P95/P99/最大值；Debug关闭时只做固定容量计数，不写逐任务日志。
- 第一小批结果：本机16核24线程自动建议CPU重任务4、图像转换4；确定性压力1000/1000任务完成，全局活动峰值4/4、测试分类转换峰值2/2，高CPU模拟按4→3→2下降并在恢复条件满足后回到3。运行时预热后私有内存净变化-0.531MB、托管内存+0.005MB、句柄+8，许可和释放计数1000/1000，等待与活动最终均归零。
- 第一小批回归：`Debug|x64`完整编译5.29秒、0错误、171个既有警告；硬件档案、流程准入和保存队列架构回归通过。当前数据只证明调度基础设施边界，真实节点接入后的耗时与UI/相机/通信保护在后续小批验证。
- 第二小批生产接入：全流程六条节点运行路径统一调用资源策略；28类已确认的传统OpenCV、测量和图像绘制节点进入全方案CPU许可池。二维/三维显示先通过窗口独立30FPS门控，只在真正执行`Mat→Bitmap`时取得图像转换许可，转换结束即释放，不把UI事件投递时间算入CPU占用。
- 第二小批排除边界：TDAI、无监督、大模型、PLC/Modbus/TCP、相机采集与IO、图像显示节点外壳、保存节点入队和组合模块共16类明确保持直通；组合模块由内部流程的真实CPU节点分别取许可，避免外层持有许可后嵌套等待。
- 第二小批动态采样：使用Windows`GetSystemTimes`每5秒低频读取全机CPU，不使用WMI、性能计数器或UI定时器；相同硬件档案重复刷新不重置已经动态下降的额度。专项实测系统CPU样本17.78%，模拟下降后相同参数重配保持3→3。
- 第二小批验证：`Debug|x64`完整编译4.46秒、0错误、170个既有警告；传统节点分类28/28、明确直通16/16，CPU调度1000/1000任务完成；连接图顺序、相机回调快速路径和UI帧率/所有权回归通过，UI四线程同步突发100000次耗时8ms且每窗口只接收1帧。
- 第三小批保存分段：保存工作池的`Mat→Bitmap`和JPEG编码分别取得图像转换、图片编码许可；两个许可都在磁盘路径准备与文件写入前释放。每个固定保存线程独占并复用一个编码缓冲区，缓冲区数量受保存线程数硬限制；同一图片需要写入多个目录时只编码一次并复用相同字节。
- 第三小批慢盘证明：单张`128x128`图片写入两个目录、每个目录固定100ms路径准备时，只取得1次转换许可和1次编码许可，两个磁盘等待点采样到的CPU活动数均为0；两份JPEG均可按`128x128`解码且文件字节完全一致，证明慢盘不会占住CPU额度，也不会重复消耗JPEG编码CPU。
- 第三小批压力：3轮、每轮60次四流程并发投递共180次，接收54、明确拒绝126、淘汰普通13、完成41、失败0；队列/租约字节/保存线程峰值为8/18MB/2，180个Mat租约全部释放且重复释放0。入队P99最大1.531ms，压力墙钟345ms，平均CPU 6.963%；强制回收后私有内存`+0.121MB`、托管内存`-0.690MB`、句柄0增长。
- 第三小批回归：`Debug|x64`完整编译3.86秒、0错误、170个既有警告；CPU调度1000/1000、传统节点28/28、明确直通16/16、保存队列结构、磁盘保护和方案资源释放全部通过。当前结果证明保存CPU阶段与磁盘等待已隔离，完整四流程高CPU调节效果留到第四小批验证。
- 第四小批测试基础：生产调度器确定性执行四条流程，动态额度按`4→3→2→3→4`变化；四个已开始节点在降额后自然完成，四个阶段峰值分别为3/2/3/4，804次许可全部归还，流程内部节点顺序不变。UI发布、相机回调和PLC/Modbus通信三个非CPU控制通道最大响应间隔为18.156/18.156/18.158ms；墙钟354ms，私有/托管内存变化`+0.184/+0.002MB`，句柄`+2`。
- 第四小批显示复核：修复旧四窗口脚本的.NET Framework运行时入口后，3轮×4窗口×30帧共360帧全部接收；`1024x768`转换P95为1.45～1.99ms、最大2.32ms，私有内存最终`+0.56MB`、GDI最终0增长。
- VX只读审查：确认动态调节、显示所有权、编码复用、多目录回滚及AI/相机/UI/通信排除边界基本闭环；发现保存线程出队并持有Mat后使用不可取消令牌等待CPU许可，极端情况下可能使保存停止和方案重置超时。第四小批必须先补保存工作池停止令牌，并新增“CPU额度全部占满后停止保存池”的回归，修复验证前不得标记完成。
- VX问题修复：保存工作池增加独立停止令牌；正常停止先在预算内排空，只有进入最后最多500ms的取消保护期时才取消CPU许可等待并释放未开始任务。取消任务独立计入“停止取消”，不误记完成或失败，并进入保存队列诊断摘要。
- 饱和停止回归：调度器最大并发1并由外部任务占满后，保存任务确认已出队、持有Mat且等待CPU许可；`StopProcessing`在499ms完成，保存完成/失败/取消为0/0/1，CPU许可/归还/等待取消为1/1/1，Mat释放1次，文件0，最终队列、CPU活动、CPU等待和后台线程全部归零。
- 修复后回归：正常3轮×60次保存完成40、失败0，180个Mat租约全部释放，私有/托管内存变化`+0.113/-0.002MB`、句柄0；四流程动态保护804/804、CPU调度1000/1000、保存结构和方案资源释放全部通过。`Debug|x64`完整编译3.83秒、0错误、170个既有警告。
- 第四小批真实同比口径：当前版与旧版均使用最小化窗口、低于正常优先级、同一原始方案、有效许可证、旧版Dock布局、6个图像窗口和同一本机Modbus，各预热1轮后串行500轮；两版四流程均各500/500成功，Modbus均2000/2000成功，异常0，主程序加2个AI进程全程保持3个且正常退出。
- 第四小批真实耗时：旧版到当前版总耗时`268525.931→270321.533ms`，增加0.669%；吞吐`1.862018→1.849649轮/秒`，下降0.664%；P50/P95/P99/最大值由`531.954/622.652/650.477/758.661ms`变为`539.780/661.415/804.519/949.375ms`，分别回退1.471%/6.225%/23.681%/25.138%。本轮不得宣称完整方案更快。
- 第四小批长尾定位：当前版慢轮集中在第427～443轮，出现6轮不低于800ms；同期CPU采样仅约17%～26%，每轮I/O基本稳定，日志显示AI节点133/181推理阶段明显变慢。AI节点按用户规则不进入CPU调度器，因此该聚集更符合AI推理运行波动，而不是CPU许可等待；这是来源于日志和资源时间对齐的推断，仍保留为最终长稳复核项。
- 第四小批资源同比：CPU平均`35.305%→35.844%`，增加0.539个百分点；三进程私有内存峰值`2455.078→1992.906MB`，下降18.825%，结束值`1495.066→1386.180MB`，下降7.283%；托管堆峰值`1039.548→944.150MB`，下降9.177%，结束值`591.711→487.038MB`，下降17.691%；GDI峰值下降11，句柄峰值下降100。累计读增加0.579%，累计写基本持平，写入P99由20800.690KB降至20134.982KB。
- VX保存接纳审查：首次发现保存节点在队列确认接纳前取得候选Mat租约，满队列拒绝时可能在检测线程同步释放整条Mat依赖链；改为预接纳只读缓存字节，队列确认有空间后再原子取得源图和后台租约。终审又发现公开`Bitmaps`可原地变化使缓存失真，以及错误依赖环可能互持租约并形成反向锁。
- VX问题修复：`AcquireSaveSnapshot`在同一资源锁内取得源图与租约，再在租约保护下按真实Mat重算字节；快照值大于预接纳预算时拒绝发布并立即归还租约。`TakeDependency`串行检查直接和传递依赖环，父级缓存改为原子读取，不再持有子级锁继续取得父级锁。
- 保存接纳回归：容量1且消费者未启动时严格接收1、拒绝1，快照工厂调用次数等于接收数，拒绝任务租约0；预检后先释放会拒绝快照，`Bitmaps[0]`原地换成超预算大图则正常返回`RejectedItemExceedsBudget`、拒绝计数1、队列和租约归零，不再转成节点运行异常。生命周期50000轮、快照/Dispose争锁1000轮、双线程反向依赖1000轮、有界队列100000次、真实保存工作池180次均通过；最终工作池接收52、拒绝128、完成40、失败0，180份租约全部释放、入队P99最大0.1055ms、私有/托管内存变化`+0.277/-0.013MB`、句柄0增长。
- VX最终收口：第三轮复核确认无P0、P1、P2；随后补齐旧`Action`回调前冻结所有者/优先级的兼容语义，以及NG实际字节增大、减小、可淘汰资源不足三个正式回归。最终队列10万次压力全部释放、重复释放0，已知P3缺口一并关闭。
- 验证：不同核心数模拟测试、四流程并发测试和CPU高负载测试均不会饿死UI、相机回调或通信节点。

### 步骤8：系统设置参数界面

- 状态：已完成；机器级配置核心、系统设置界面、安全应用状态、事件生命周期和专项测试均已收口
- 关键参数：只开放总表中标记“是”的项目。
- 工作内容：在`FrmSystemSetting.Designer.cs`增加“性能与资源”控件，显示自动值、手动值、最终值和推荐范围。
- 关键决定：默认自动；手动覆盖必须提供“一键恢复自动推荐”。
- 关键决定：固定安全规则不开放修改，避免容量设为无限或关闭必要释放逻辑。
- 第一小批决定：机器覆盖值独立保存到`LocalApplicationData\TDJS-Vision\performance-resource-settings.json`，不进入`.Sol`方案；保存采用临时文件和替换发布，损坏配置降级为自动值并保留告警。
- 第一小批决定：自动档案、机器手动值和最终运行档案分离；手动保留核心数会重新计算未手动指定的CPU并发，最终并发仍受物理核心和成对水位关系约束。
- 界面状态语义：只把已有生产消费者的参数标为“运行中生效”；流程图像预算和内存水位当前仅参与档案计算与日志时，必须标为“规划值，尚未启用控制”，不能误导为已执行硬管控。
- 应用语义：CPU调度等可安全热更新的参数立即应用；正在处理任务的保存工作池不得被强制中断，界面必须区分已保存值、当前运行值和待下次空闲/重启生效值。
- 第二小批界面：`FrmSystemSetting.Designer.cs`提供“常规设置/性能与资源”双页、26项参数8列表格，以及本机硬件、当前方案和本次预览摘要；所有控件和中文Text均可在WinForms设计器中查看。
- 第二小批应用：草稿预览不落盘也不修改运行组件；“保存并应用”原子保存机器配置后分别报告CPU与显示、图像采样、保存工作池的状态。方案重置时全部等待下一轮；活动流程或保存池繁忙时不强制重建保存池。
- 第二小批诚实状态：流程图像预算和内存水位标记为“规划值，尚未启用内存硬管控”；停止排空超时同时影响运行停止和保存池，界面区分“运行停止已应用；保存池待下一轮”。
- 验证：控件可在WinForms设计器中查看，参数重启后保持，复制方案到其他电脑不会复制机器级覆盖值。

### 步骤9：完整回归和压力测试

- 状态：第一小批已完成；共享办公环境可用内存不足，1000轮以上压力按4096MB门禁暂缓
- 关键参数：快速轮数、阶段时长、最终轮数、最终持续时间和验收阈值。
- 工作内容：每个阶段至少1000轮，最终至少10000轮和2小时连续运行。
- 关键决定：Debug关闭和开启必须分别测试；完整诊断不能掩盖或制造原始问题。
- 办公保护：不得使用`SendKeys`、`AppActivate`、`SetForegroundWindow`、键盘或鼠标模拟；窗口默认最小化并以不激活方式建立控件树，测试进程保持`BelowNormal`。
- 进程保护：启动前拒绝同名视觉程序；测试只关闭自己创建的进程，正常退出失败时也只能按可执行路径和启动时间清理本轮进程树，禁止接管或关闭用户既有程序。
- 内存保护：共享办公环境启动完整方案前要求至少`4096MB`可用物理内存；不足时只运行无窗口低负载回归，不通过降低保护线换取测试进度。
- 证据分级：用户同时办公时只验证功能、资源边界和持续增长趋势，耗时数据标记为`SharedDesktopBackground`；最终性能同比必须在`Dedicated`专用空闲环境重跑，避免把办公负载噪声解释成框架回退。
- 输出：改造前后P50、P95、P99、最大耗时、内存曲线、GDI曲线、队列峰值和资源丢弃统计。
- 验收：无法解释的长停顿为0；所有超阈值事件都有明确阶段和系统现场。
- 第一小批结果：Debug关闭预热1轮加正式3轮四流程各3/3成功、Modbus 12/12、异常0；该轮发生在共享办公环境且工具安全修正前，只保留为功能冒烟。最终工具已通过无键鼠输入、启动页透明最小化、主窗体不激活、前台焦点检测、自有进程清理、默认结果目录和4096MB内存门禁检查。
- 第一小批低负载证据：CPU调度1000/1000、四流程CPU保护804/804；保存工作池1轮20次接收10、拒绝10、完成10、失败0，租约20/20释放、句柄0增长、私有内存`+0.164MB`、托管内存`-0.894MB`、平均CPU 6.889%。
- 第一小批最终安全补强：后台模式必须同时具备性能验收和后台请求两个进程开关；完整方案工具对全部自启测试子进程设置验收标记，`Current`只表示不覆盖用户Debug值，并彻底拒绝复用既有视觉进程。统一消息框、授权失败、启动失败和关闭保存失败在后台验收中均不创建模态窗口，启动初始化失败直接静默退出；运行中关闭先请求方案取消再排空。测试进程树按Windows父子进程关系识别，焦点和清理只覆盖本轮主进程及后代；焦点与内存检查贯穿启动、运行、结果排空和退出。
- 第一小批最终无窗口验证：`Debug|x64`单线程完整构建通过；步骤9行为验收和8组短测通过。CPU调度1000/1000，四流程CPU保护804/804，线程池计时探针A/B/C最大间隔18.151/18.147/18.153ms；该探针只反映线程池调度响应，不代表真实UI、相机或PLC链路。
- 第一小批结果发布规则：JSON和检查点先写为`AwaitingExitValidation=true`、`Completed=false`、`PerformanceConclusionEligible=false`；只有测试主体、正常退出、自有后代清理和固定方案恢复均成功后才发布完成状态。专项行为测试验证退出失败不能形成结论、退出成功才能发布完成。
- 四窗口P99边界：组件压力每窗每轮当前只有30个转换样本，因此最近秩P99等同本轮最大值，只作为严格卡顿哨兵；稳定尾延迟结论必须使用步骤9完整方案1000轮以上样本。
- 第一小批内存门禁复核：以65536MB动态门槛验证拒绝路径时，可用内存为1852～2983MB，视觉进程数量始终保持0→0；隐藏的低负载父子进程探针确认Windows后代识别正确。当前仍低于默认4096MB办公保护线，因此未执行Debug关/开1000轮、10000轮、2小时长稳及四窗口真实显示；这些项目不得标记为通过。

### 步骤9第二小批前置审查：生产安全与测试可信度

- 状态：审查与低负载测试已完成，生产修复尚未开始；本批不修改业务代码。
- 审查组织：四个只读智能体分别检查并发与图像生命周期、性能热路径、测试可信度、WinForms启动与资源配置；主智能体对流程重入、相机等待、CameraIO、日志队列、Modbus锁和测试误通过条件逐项交叉核对。
- P0流程重入：`Process`的多个公开运行入口只登记方案级会话，没有对同一个流程执行原子单飞准入；被动流程、脚本、参数预览或直接运行可与上一轮重叠。第二轮替换节点`Result`会立即释放上一轮图像，而显示、AI等节点读取订阅图像后未统一持有短租约，存在使用已释放Mat、OpenCV异常和原生崩溃风险。进入正式长测前必须先补流程级准入及并发回归。
- P1相机与IO：回调取图等待未应用图像源配置的`TimeOut`，丢帧后只能依赖整套方案取消；CameraIO异步脉冲使用不跟踪的`Task.Run`并以CPU自旋实现微秒等待，50ms重复触发且保持时间更长时会无界重叠；海康相机构造时序列号为空会在后台无等待、无超时、无取消地持续轮询。
- P1日志与通信：普通日志使用无容量`BlockingCollection<Action>`且单消费者每条日志重新打开文件；磁盘变慢时待写对象可持续增长。共享Modbus主站在同一互斥锁内执行最长5秒的同步网络调用，一次超时会阻塞同设备的其他流程并形成连锁等待；该机制与“四流程几乎同时超时”相符，但是否为客户现场根因仍需日志时间线证明。
- P1/P2资源缺口：显示控件每次换帧单独`Task.Run`释放旧Bitmap，释放任务本身无界；被跳过或停用节点可能长期保留上一轮图像；保存快照工厂在全局队列锁内遍历依赖链；活动保存任务的转换、编码和磁盘写入无法中途取消。内存预算和水位仍是规划值，且包含可用内存的硬件快照会长期缓存；固定线程池最低工作/IO线程64也尚未按机器配置计算。
- 测试P1：串行完整方案即使四流程失败、异常或Modbus数量不符也不会立即判失败，最终资格只受退出和环境模式控制；固定50ms模式允许准入0、完成0、四流程成功0和Modbus成功0仍满足相互一致；新旧串行同比没有强制共同输入清单和哈希门禁；真实落盘“提升76.08%”是直接调用保存工作池的一次组件批量结果，不等同于完整生产链收益。
- 测试P2/P3：步骤9大量检查仍是源码文字匹配，焦点轮询可能漏掉短暂激活，固定触发排空未覆盖完整CPU与托管堆峰值，CPU分类未覆盖全部可编辑节点，多个无BOM中文脚本在Windows PowerShell 5.1下无法可靠解析，P99算法也未统一。
- 本机无窗口结果：21项唯一低负载测试中19项稳定通过、1项确定失败、1项波动。通过项包括步骤9安全、进程I/O、落盘架构、性能设置、相机快速路径、通信追踪、方案准入、四流程CPU保护804/804、相机追踪表50000次、图像生命周期50000次、快照/依赖竞态各1000次、QR/ROI替换500次和保存工作池20次；保存租约20/20释放、重复释放0、句柄0增长。
- 确定失败：`SolutionResourceRelease.Tests.ps1`仍要求旧调用`RefreshRuntimeResourceProfile()`，生产代码已改为`RefreshRuntimeResourceProfile(true)`，属于测试门禁过期，不代表生产资源释放失败。
- 波动测试：CPU调度独立进程4轮仅1轮通过托管内存小于8MB断言，另3轮失败；直接Windows PowerShell诊断复跑3/3通过且托管内存变化`+0.005/-0.866/-0.866MB`。任务1000/1000、许可1000/1000、并发4/4和转换2/2始终正确，波动集中在测试方法内1000个Task对象的GC存活时机，需先隔离测量作用域再作为内存门禁。
- 办公边界：测试全程隐藏、低于正常优先级，不调用键盘、鼠标或焦点控制，不启动视觉主程序和真实窗体；可用内存约802～1207MB，低于4096MB门禁，因此未编译、未跑真实四窗口、完整方案1000轮、10000轮或2小时长稳。测试结束视觉程序残留0。
- 下一步顺序：先修复流程级单飞P0与图像租约竞态，再修复完整方案“失败也可发布结论”的测试P1；随后处理相机回调超时、CameraIO忙等、日志有界化、Modbus超时隔离和旧Bitmap释放队列，最后才恢复步骤9正式压力。

### 步骤9第三小批：Release后台验收启动安全

- 测试编译统一为`Release|x64`；步骤9所称Debug关闭和开启仅表示完整耗时诊断日志开关，两组不得混用Debug编译产物。
- 关键参数：`Configuration=Release`、`Platform=x64`、`WindowMode=Minimized`、`ProcessPriority=BelowNormal`、`TestEnvironmentMode=SharedDesktopBackground`、共享办公可用内存门槛`4096MB`、可见图像窗数量`6`、预期进程树数量`3`。
- 2026-08-20 23:57完成Release构建，结果0错误、0警告；启动前可用物理内存约`5446MB`，视觉程序残留0，当前与旧版`device.license`的SHA-256一致。
- 固定方案使用`Tests\Results\Step9\步骤9-Release-固定方案.Sol`独立副本，原始输入SHA-256为`CAC8C2A8F3E1CF1489AE0A8AB1A663CBD5291F11F3B01D3496A1270CAE369F4D`，不得覆盖桌面上已经变化的方案文件。
- 首次Release Debug关闭3轮冒烟在启动阶段触发前台焦点保护并立即中止，测试后视觉程序残留0；未形成JSON/CSV正式结果，不得标记为通过，也不得直接进入1000轮。
- 修复决定：后台验收时启动页和主窗体增加系统级`WS_EX_NOACTIVATE`扩展样式，正常用户启动路径保持不变；步骤9运行时行为门禁改用最新Release程序集验证。修复后必须重新执行3轮冒烟，确认焦点、四流程、Modbus、6窗口、3进程和正常退出全部通过，才允许开始Release Debug关闭1000轮。
- 实施结果：`FormStartup`和`FormMain`仅在性能验收与后台请求两个开关同时开启时向`CreateParams.ExStyle`加入`WS_EX_NOACTIVATE`；普通启动窗口样式不变。步骤9运行时行为检查已切换到`bin\x64\Release`，并动态验证Release启动页句柄参数确实包含该样式。
- 无窗口验证：Release完整构建0错误、171个既有警告；步骤9Release行为门禁、启动动画回归和设备许可证启动同步测试全部通过。独立方案副本与原始输入哈希一致，当前与旧版许可证哈希一致，视觉进程残留0；真实3轮复测仍待代码恢复点提交后执行。
- 第一次修复后复测：恢复点`29cbd06`上的同口径Release Debug关闭3轮冒烟仍在正式轮次前触发前台焦点保护，测试后残留进程0，未生成有效JSON/CSV。`WS_EX_NOACTIVATE`不是完整原因，禁止绕过焦点门禁进入1000轮。
- 审查结果：独立智能体未发现P0/P1，确认后台样式受双开关保护且UI Automation `InvokePattern`与该样式没有必然冲突；P2指出静态门禁只动态实例化启动页，不能替代真实主窗体冒烟。下一小批先让焦点异常携带前台窗口句柄、进程ID、标题、窗口类、可见和启用状态，并增加普通启动不含`WS_EX_NOACTIVATE`的反向断言，再执行一次诊断冒烟。
- 诊断门禁实施：前台焦点检测改为一次性快照，统一记录句柄、PID、标题、窗口类、可见和启用状态，避免诊断期间窗口切换造成字段错配；Release行为门禁同时验证普通启动不含、后台验收包含`WS_EX_NOACTIVATE`，无窗口检查通过。
- 首次现场结果：诊断冒烟捕获PID `27812`的不可见`WindowsFormsParkingWindow`，窗口类`WindowsForms10.Window.8.app.0.21af1a5_r6_ad1`、`Visible=False`、`Enabled=True`；该信息出现在退出清理异常中，当前工具会让清理异常覆盖测试主体的原始异常，因此不能据此判定启动阶段根因。
- 异常证据规则：后续为启动等待、流程运行、固定触发投递、固定触发排空和退出清理分别标记阶段；测试主体失败必须优先保留，清理异常只能作为附加警告，不得覆盖原始失败。该门禁修复后再执行一次诊断冒烟。
- 异常优先级实施：五个焦点检查阶段已显式标记；测试主体异常先保存并继续抛出，`finally`中的清理异常只在测试主体成功时独立判失败，已有主体异常时仅追加警告。步骤9Release无窗口行为门禁通过。
- 原始根因确认：异常优先级修复后的诊断冒烟证明首次失败发生在`等待主界面`，前台窗口仍为Release主进程的不可见`WindowsFormsParkingWindow`；退出清理再次观测到同一隐藏窗口，但已作为附加警告保留。说明后台验收在创建透明最小化启动动画及其控件句柄时仍会生成可进入前台链的WinForms停车窗。
- 生产修复决定：后台性能验收不创建本来不可见的独立启动动画线程，也不等待2秒最短动画时长；主窗体仍按真实生产路径完整初始化并使用系统级不激活样式。普通用户启动继续创建、显示和等待原启动动画，功能与视觉行为不变。修复后先执行启动动画和步骤9Release门禁，再重跑3轮冒烟。
- 根因修复实施：`StartupApplicationContext`仅在非后台验收时启动`StartupSplashController`并执行2秒最短动画等待；后台验收直接进入同一主窗体初始化链。Release构建0错误、171个既有警告，启动动画回归、步骤9Release门禁和设备许可证同步三组无窗口测试全部通过。
- 动画隔离复测：Release 3轮冒烟仍在`等待主界面`阶段捕获不可见`WindowsFormsParkingWindow`，PID `21820`，窗口类后缀变为`134c08f_r6_ad1`；证明停车窗来自真实主窗体控件句柄初始化，跳过启动动画虽减少无效测试开销，但不足以保证焦点隔离。
- 最终焦点策略：后台验收主UI线程在创建任何WinForms窗口前安装线程级`WH_CBT`守卫，对`HCBT_ACTIVATE`返回非零，阻止该线程的启动页、停车窗、主窗体或临时顶层窗激活；退出时解除钩子。双开关未同时开启时不安装，普通用户启动行为不变。钩子安装失败必须让后台验收直接失败，不允许降级运行。
- 焦点守卫实施：新增`BackgroundWindowActivationGuard`，在AI工作进程分流后、`Application.EnableVisualStyles`前安装当前STA线程钩子；回调委托由字段持有，除激活事件外全部传递，主入口`using`保证单实例拒绝、授权失败、正常退出和异常退出均解除钩子。Release构建0错误、171个既有警告，启动动画、步骤9Release和许可证同步三组门禁通过。
- 焦点守卫真实结果：提交`2c1ec73`后的Release冒烟从启动到退出未再捕获任何停车窗或主窗体前台激活，说明`WH_CBT`守卫真实生效。测试主体随后因工具过早返回尚未启用的“单次运行”按钮而停止；退出时可用内存为`3990MB`，低于4096MB门槛并作为附加清理异常保留，测试后残留0。
- 审查与下一修复：独立审查无P0/P1；P2指出Release新旧检查未覆盖`Program.cs`、焦点守卫和项目文件，P3指出解除钩子失败没有日志。下一小批要求按钮存在且已启用后才能返回，把三份守卫相关源码纳入Release新旧门禁，并记录`UnhookWindowsHookEx`失败；内存门槛不得降低。
- 门禁收口：主界面等待现在只返回已启用的“单次运行”按钮；Release产物时间检查覆盖显示策略、启动上下文、焦点守卫、`Program.cs`、项目文件、启动页、主窗体和统一消息框；解除钩子失败写Warn并允许Windows在线程退出时回收。Release构建0错误、171个既有警告，三组无窗口回归通过。
- 当前资源现场：修复后可用物理内存约`4403MB`，低于“程序加载后仍保留4096MB”的安全余量；本轮不降低保护线、不切换Dedicated绕过焦点与内存检查，因此暂不再次启动真实3轮，更不进入1000轮。

## 五、实施记录模板（后续每一步持续追加）

### 实施记录：步骤X

- 日期：
- 改动前Git提交：
- 改动后Git提交：
- 本次修改文件：
- 使用的关键参数：
- 自动计算输入：物理核心、逻辑线程、总内存、可用内存、相机数、流程数、平均单帧字节数。
- 自动建议值：
- 手动覆盖值：
- 最终生效值：
- 编译结果：
- 专项测试结果：
- 快速压力测试结果：
- P50/P95/P99/最大耗时：
- 内存、GDI、句柄变化：
- 发现的问题：
- 参数调整依据：
- 下一步决定：

## 六、测试记录

### 改造前基准

- 日期：2026-08-19。
- Git版本：`0c7464d`，其父提交`73462eb`为改造前源码基线。
- 开发电脑：Intel Core i7-14650HX，16个物理核心，24个逻辑线程，约16GB内存。
- 编译：`Debug|x64`通过，0个错误、175个既有警告，MSBuild报告耗时5.25秒。
- 专项测试：9项相关回归检查通过。
- 现场性能：真实四相机、PLC和生产节拍数据待现场环境复测；本轮后续使用可重复的模拟压力测试比较改造前后框架开销。

### 阶段压力测试

- 2026-08-19，步骤2第一轮：`Debug|x64`编译通过，0错误、175个既有警告；流程诊断、日志等级、惰性求值、相机快速路径和连接图等待共6项专项检查通过。
- 2026-08-19，步骤2第二轮：`Debug|x64`编译通过，0错误、175个既有警告；相机帧关联、统一回调、等待器、惰性诊断、日志等级、流程追踪和快速路径共8项检查通过；弱引用帧关联表50000轮压力测试用时26ms，强制GC后未保留Mat键。
- 2026-08-19，步骤2第三轮：`Debug|x64`编译通过，0错误、175个既有警告；UI、保存后台、ROI绘制、资源释放、相机与诊断共12项相关检查通过；Debug关闭路径已确认不再执行保存内存/GDI/句柄快照和图像摘要构建。
- 2026-08-19，步骤2第四轮：`Debug|x64`编译通过，MSBuild报告用时2.56秒，0错误、175个既有警告；流程快速路径、绘制2、AI结果绘制、找点、资源释放、相机和诊断共13项相关检查通过；Debug关闭快速入口100000次均返回空计时器，用时513ms，反射压力宿主强制GC后托管内存差值121904字节。
- 2026-08-19，步骤2第五轮：`Debug|x64`编译通过，MSBuild报告用时2.63秒，0错误、175个既有警告；通信分类、完整流程、UI与保存、资源释放、相机等待及诊断共13项相关检查通过；编译产物的17类通信节点和4类非通信节点分类全部正确；200000次分类压力判断用时772ms，反射宿主强制GC后托管内存差值170752字节。
- 2026-08-19，步骤3：`Debug|x64`编译通过，MSBuild报告用时4.73秒，0错误、175个既有警告；硬件档案与既有完整链路共14项相关检查通过；本机WMI实测为16核24线程、16087MB总内存、RTX 4060 Laptop GPU和`D:\`程序磁盘；资源档案50000轮计算用时624ms，强制GC后反射宿主托管内存差值190616字节。

- 2026-08-19，步骤4第一轮：建立自产/借用所有权、父级引用租约、Result替换回收和保存后台租约；生命周期50000轮压力用时36ms且释放次数准确；采集、保存、订阅、诊断、ROI绘制、方案释放和硬件档案等19项相关回归通过；`Debug|x64`最终编译3.00秒，0错误、175个既有警告。
- 2026-08-19，步骤4第二轮第一小批：电池耳、二值分析、找线和找圆完成新Result发布及所有权迁移；找线和找圆不再克隆整张结果底图；生命周期50000轮压力用时50ms，4项相关回归通过；`Debug|x64`最终编译4.14秒，0错误、175个既有警告。
- 2026-08-19，步骤4第二轮第二小批：补齐电池耳、二值分析、找线、找圆、AI多ROI/OBB和Blob临时图释放；删除找线/找圆无人使用的返回Bitmap转换；生命周期50000轮压力用时47ms，5组相关回归通过；`Debug|x64`最终编译2.55秒，0错误、172个既有警告。
- 2026-08-19，步骤4第二轮第三小批：图像绘制、两套结果绘制、普通/NCC模板匹配和线组合拟合完成所有权与异常回收迁移；生命周期50000轮压力用时54ms，14组相关专项回归通过；`Debug|x64`最终完整编译4.66秒，0错误、172个既有警告。
- 2026-08-19，步骤4第二轮第四小批：公共显示控件补齐当前图、Mask ROI、定时器和重绘GDI对象释放；Windows STA压力完成250次重绘与100次Mask替换，GDI增长0；生命周期50000轮用时44ms，7项相关回归通过；`Debug|x64`完整编译4.33秒，0错误、172个既有警告。
- 2026-08-19，步骤4第二轮第五小批：颜色识别节点改为单次完整Result发布，异步通信和检测全程持有输入短租约；两套颜色调参窗体补齐源Mat、预览Bitmap和GDI对象释放；生命周期50000轮用时40ms，显示GDI压力增长0，6项相关回归通过；`Debug|x64`完整编译2.52秒，0错误、172个既有警告。
- 2026-08-19，步骤4第二轮第六小批：公共ROI控件接管并释放替换Bitmap，二维码检测补齐模型、全部ROI、模糊图、bbox、CLAHE和PictureBox旧图释放；ROI控件500次替换与显示控件重绘压力的GDI增长均为0；生命周期50000轮用时47ms；`Debug|x64`完整编译2.59秒，0错误、172个既有警告。无监督窗体既有测试仍要求“选中设OK/NG”，而基线Designer为“设OK/NG”，与本批资源改造无关并保持现状。
- 2026-08-19，步骤4第二轮第七小批：分割和旋转参数窗体补齐最后预览图释放；电池耳模板改为节点缓存、每轮一次快照和全局版本同步，删除每ROI磁盘读取及隐藏模板窗体整图转换；6张真实模板完成100轮预览重载和250轮快照压力，GDI增长0；生命周期50000轮用时50ms；`Debug|x64`完整编译2.64秒，0错误、171个既有警告。
- 2026-08-19，步骤5第一小批：每个流程图像窗口建立独立容量1最新帧槽并限制为一个UI投递任务，Mat转Bitmap移出UI线程，公共图片控件补齐替换与销毁释放；多轮500帧突发测试的待帧/UI任务峰值均为1/1，后台提交61～176ms、UI消费2～8ms、GDI增长0，最终构建产物复测为91ms和3ms；生命周期50000轮实测56～107ms；`Debug|x64`最新完整重建9.16秒、0错误、171个既有警告。该结果只证明受控条件下资源与队列稳定，真实工艺节拍和长期内存曲线待现场Debug追踪对照。
- 2026-08-19，步骤5第二小批：二维/三维显示在转Bitmap前按窗口独立执行30FPS门控，发布事件使用“预认领+确认转交”并释放无人接管、订阅异常或子控件拒收的Bitmap；四线程同步起跑100000次调用多轮为2～3ms，各窗口通过数均为`1,1,1,1`，单窗口500帧后台提交60～90ms、UI清空2～4ms、排空0ms、峰值1/1、GDI增长0/0，生命周期50000轮51～73ms；硬件档案及相关回归通过，`Debug|x64`最终完整重建6.10秒、0错误、171个既有警告。四张高分辨率图同时转换与四窗口实际绘制仍待专门端到端压力测试。
- 2026-08-19，步骤5第三小批：四个真实窗口和四个后台线程同步执行`1024x768 Mat→Bitmap→生产发布器→UI消息泵与强制刷新`；完整链预热及异常边界补强后，3个独立进程共9轮、1080次转换投递全部接管，带40ms节拍的每轮墙钟1245～1259ms、UI排空0～5ms，单帧平均0.74～1.50ms、P95 0.95～2.69ms、最大1.09～6.58ms；25ms采样的私有内存峰值增量28.07～28.32MB，关闭后净增0.81～1.08MB，GDI最终增长0。`Debug|x64`完整重建6.05秒、0错误、171个既有警告；真实相机最大分辨率、复杂叠加和PLC整轮仍待现场Debug对照。
- 2026-08-19，步骤6第一小批：建立全方案共享、任务数与租约字节双重有界的固定保存工作池，NG优先淘汰最早普通任务，主队列和淘汰回收通道均有硬边界；候选租约覆盖源图读取与字节估算，写盘采用`.saving`临时文件原子发布和多目录失败回滚；运行会话、方案重置、工作池停止和方案加载形成统一生命周期门禁。10万任务队列压力释放100000、重复释放0、峰值8/8与8192/8192；三轮四流程真实保存压力失败0、P99最差2.147ms、停止49～55ms，私有内存`+0.25MB`、托管内存`-0.04MB`、句柄`+0`。本记录仅为专项证据，低磁盘、慢盘、完整方案与固定5000轮新旧同比未完成，步骤6仍在进行。
- 2026-08-19，用户指定旧跑法前500轮临时对比：未改造版前500轮整轮P50/P95/P99/最大值为548.726/664.994/704.586/762.740ms，改造版500轮表面值为348.302/388.534/406.256/416.158ms；但未改造版四流程均500/500成功，改造版左右侧面流程均0/500成功。改造版运行目录缺少`device.license`导致两个AI节点初始化失败，进程树也由旧版3个进程变为1个，因此本轮负载不等价，全部速度和内存差值判定无效，不写入最终优化收益。结果已留档，桌面方案恢复为原始SHA256，当前停止继续运行。
- 2026-08-19，旧界面与有效许可证500轮纠正重测：改造版已复制未改造版相同的`device.license`、主Dock布局和6图像窗口布局，三份文件SHA256逐项一致；两版均使用正常窗口、正常优先级、5ms轮询、同一原始方案和本机虚拟Modbus。改造版完整轮次500/500成功，四流程各500/500成功，异常0，Modbus 2000/2000成功，进程树与旧版同为3个进程。未改造版前500轮与改造版500轮的P50/P95/P99/最大值分别为548.726/664.994/704.586/762.740ms和513.016/609.870/655.885/702.962ms，改造版改善6.51%/8.29%/6.91%/7.84%，吞吐提升7.71%；私有内存采样峰值由2502.301MB降至2042.633MB。该结果作为本次有效500轮临时对比，仍不替代5000轮正式验收。
- 2026-08-20，步骤6第二小批：保存消费者接入磁盘低水位2048MB、严重水位512MB、慢写阈值500ms和空间采样1000ms；普通图、NG图、严重空间与探测故障策略专项检查全部通过。固定100ms模拟慢盘下队列峰值4/4、消费者1/1、明确拒绝且失败0，20份Mat租约全部释放、重复释放0；三轮常规保存压力失败0、P99最大0.125ms、停止32～39ms，私有内存`+0.07MB`、托管内存`-0.07MB`、句柄`+0`。本批仍不替代500次固定50ms真实触发及5000轮正式同比。
- 2026-08-20，步骤6第三小批：在旧版有效许可证、旧版Dock布局、6图像窗口、同一原始方案和虚拟Modbus下完成500次固定50ms正式触发；投递500、准入并完成47、忙碌即时拒绝453、消息超时0、异常0，四流程各47/47成功，Modbus 188/188成功。相邻投递间隔平均50.015ms、P50 49.350ms，完整方案P50/P95/P99/最大值519/627/640/640ms；CPU平均30.652%，三进程私有内存起始/采样峰值/结束1183.941/1587.465/1269.219MB，GDI 811/820/820。结果证明过载时准入和资源边界稳定，但当前方案不能逐件承接20次/秒触发；本批仍不替代5000轮正式同比。
- 2026-08-20，步骤6第四小批：旧版与当前版在同方案、同许可证、同6窗口和同Modbus条件下各串行500轮，四流程各500/500、Modbus 2000/2000、异常0，三个进程I/O计数完整且PID稳定。当前版相对旧版CPU平均下降1.510个百分点，三进程私有内存峰值下降17.00%、主进程峰值下降28.87%、句柄峰值下降2.83%、GDI峰值下降1.67%；进程读写总量差异小于0.002%，每轮写入P99下降3.27%。托管堆峰值增加1.94%、结束快照增加18.58%，留待长稳复核。两版均无保存队列活动证据，因此本轮不能宣称物理图片落盘更快。
- 2026-08-20，步骤7第四小批：同口径新旧版本各500轮功能全部成功，当前版私有内存峰值下降18.825%、托管峰值下降9.177%、句柄峰值下降100，但整轮P50/P95/P99/最大值回退1.471%/6.225%/23.681%/25.138%，长尾集中在AI推理波动区间，不能宣称完整方案提速。VX审查推动保存队列改为确认接纳后再原子取得Mat租约，并补齐实际字节复核、循环依赖拒绝和父子无嵌套锁；生命周期50000轮、快照/Dispose并发1000轮、有界队列100000次、保存工作池180次及完整编译均通过。
- 2026-08-20，步骤8第一小批：新增机器级性能配置存储、自动/手动/最终档案解析和CPU动态并发总开关；配置不进入方案，JSON采用临时文件原子发布，损坏时回退自动值。VX两轮审查先后发现写盘锁阻塞、关联值未收敛、纯自动忽略自定义上限、快照发布竞态和自动转换并发高于共享CPU额度5项P1，均已修复；最终复审无P0、P1、P2。专项测试覆盖手动覆盖、单参数关联边界、保留核心联动、1流程4相机共享额度、纯自动一致、关闭动态调节、重复覆盖、快照隔离和损坏降级并通过；`Debug|x64`完整编译通过，0错误、170个既有警告。界面和安全应用状态将在下一小批完成。
- 2026-08-20，步骤8第二小批：系统设置新增26项参数8列表格，显示自动/手动/最终值、范围和实际生效状态；窗体改为点击时创建并可靠释放，不增加主程序启动硬件探测。草稿哈希不变、真实保存按钮写入、首次合法保留核心、重置/运行中Pending、直接Dispose退订、WinForms资源绑定、布局边界和离屏渲染全部通过。VX审查发现的启动期过期预览、首次核心上限、保存池排空超时误报、重置竞态、Dispose泄漏、测试空白和数字区域解析等问题均关闭，最终无P0、P1、P2。保存工作池3轮×60次接收54、拒绝126、完成40、失败0，租约180/180释放、句柄0增长、私有内存`+0.336MB`、入队P99最大0.1132ms；完整编译0错误、170个既有警告。

### 最终对比结果

- 状态：待步骤9完成后填写。

## 2026-08-21：海康双相机官方回调Mat基准计划

### 关键参数

| 参数 | 默认值 | 允许调整范围 | 是否计划开放到控件 | 说明 |
| --- | ---: | ---: | --- | --- |
| `HikBenchmarkTriggerMode` | `Line0` | `Line0`/连续采集 | 是 | Line0用于与主程序现场条件同比 |
| `HikBenchmarkWarmupFrames` | 10帧 | 0～10000帧 | 是 | 预热帧保留CSV，但不进入正式统计 |
| `HikBenchmarkSampleFrames` | 10000帧/相机 | 100～1000000帧 | 是 | 两台相机均完成后自动停止 |
| `HikBenchmarkDisplayEnabled` | 开启 | 开启/关闭 | 是 | 用于显示开关A/B对照 |
| `HikBenchmarkDisplayFps` | 20FPS | 1～60FPS | 是 | UI只消费容量1最新帧 |
| `HikBenchmarkDestinationPixelType` | 彩色BGR8/单色Mono8 | 按输入自动选择 | 否 | 彩色输出直接对应OpenCvSharp BGR Mat |
| `HikBenchmarkBuild` | `Release|x64` | 固定 | 否 | 禁止使用Debug结果形成性能结论 |

### 执行步骤

1. 使用海康旧版官方`MV_CC_RegisterImageCallBackEx_NET`建立两台相机独立回调，不经过视觉主框架。
2. 每台相机持有独立可复用非托管转换缓冲，回调内调用`MV_CC_ConvertPixelType_NET`并单独计时。
3. 在转换缓冲区建立OpenCvSharp Mat头，使用`Mat.Clone()`形成回调返回后有效的独立Mat，分别记录建立和复制耗时。
4. 独立Mat发布到容量1最新帧槽；UI定时器只取得最新帧并记录Mat转Bitmap、换图耗时，旧Mat和旧Bitmap立即释放。
5. 回调热路径禁止逐帧写日志和磁盘；全部明细先进入预分配数组，停止相机后统一导出CSV和分位汇总。
6. 第一轮使用Line0硬触发、显示开启、预热10帧、正式10000帧；第二轮仅关闭显示，其余条件不变。
7. 与主程序比较`ConvertPixelType`、Mat独立复制和回调总耗时三个相同口径，统计平均、P50、P95、P99、P99.9、最大值及超过20/30/50ms次数。

### 当前验证

- 工具源码：`Tools\HikDualCameraMatBenchmark`，外部交付目录：`D:\MyCode\PublicWook\海康取图demo`。
- 静态规则检查通过：官方回调、官方转换、独立Mat、回调无磁盘/控制台写入、双PictureBox位于Designer、Release x64配置均已确认。
- `Release|x64`完整重建通过，0警告、0错误；隐藏启动4秒成功，海康SDK和OpenCvSharp运行库正常装载，验证过程没有打开相机。
- 真实双相机10000帧数据必须由相机空闲且触发条件一致的现场运行获得；在取得CSV前不填写SDK性能结论。

## 2026-08-22：海康原生MVCamera类框架A/B计划

### 关键参数

| 参数 | 固定值/默认值 | 是否计划开放到控件 | 说明 |
| --- | --- | --- | --- |
| `HikNativeCameraBranch` | `codex/hik-native-class-ab` | 否 | 与当前托管DLL版本隔离，便于快速恢复和同比 |
| `HikNativeCameraSdkClass` | 官方`MVCamera.cs` | 否 | 与海康双相机Demo使用同一份供应商源码 |
| `HikNativeDestinationPixelType` | 彩色BGR8、单色Mono8 | 否 | 避免额外RGB转BGR |
| `HikNativeConversionBufferCount` | 每台相机1块 | 是 | 按实际帧尺寸扩容并在关闭相机时释放 |
| `HikNativeImageNodeCount` | 1 | 是 | 底层SDK取流缓存节点数量 |
| `HikNativeMatOwnership` | `Mat.FromPixelData`后`Clone()` | 否 | 回调返回后Mat拥有独立生命周期 |
| `HikNativeTestTrigger` | Line0硬触发 | 是 | 与2026-08-21现场日志和官方Demo保持一致 |
| `HikNativeWarmupFrames` | 10帧 | 是 | 首次SDK/OpenCV初始化帧不进入正式统计 |
| `HikNativeFormalFrames` | 10000帧/相机 | 是 | 统计平均、P95、P99、P99.9、最大和长尾次数 |
| `HikNativeBuild` | `Release|x64` | 否 | 性能结论禁止使用Debug构建 |

### 执行步骤

1. 在独立分支建立恢复点，不提交并行存在的颜色识别等工作区改动。
2. 把官方Demo验证过的`MVCamera.cs`原样引入主工程，并建立不依赖具体SDK的设备、枚举和数值参数模型。
3. 使用原生类重写海康相机枚举、开关、参数、软硬触发、主动取图和官方图像回调。
4. 回调内使用每相机预分配转换缓冲生成BGR8/Mono8，并复制为独立Mat后再交给现有框架事件。
5. 更新相机添加和实时调试界面，移除主工程对`MvCameraControl.Net.dll`的编译与输出依赖。
6. 每个可构建阶段执行`Release|x64`编译并提交；最终执行静态检查、隐藏启动和现场双相机A/B测试。

### 当前状态

- 已创建分支`codex/hik-native-class-ab`和恢复点提交`cb0a95c`。
- 官方`MVCamera.cs`与双相机Demo源文件SHA256一致；框架中立相机参数模型已加入工程。
- 第一阶段`Release|x64`编译通过，0错误；现有工程仍有172个历史警告，本阶段没有引入编译错误。
- `CameraHik`已完整切换到官方原生类：设备枚举、句柄、参数、触发、主动取图、官方回调和像素转换不再经过`MvCameraControl.Net.dll`。
- 相机回调使用每实例预分配非托管缓冲，`Mat.FromPixelData`后执行`Clone()`，再按原有`OnMatReceived`合同交给流程等待器。
- 相机添加页和实时调试页已改用框架中立参数模型；主工程已移除`MvCameraControl.Net.dll`引用和输出复制。
- 原生类静态门禁通过；`Release|x64`完整重建通过，0错误、171个既有警告，较切换前减少1个`Finalize`命名冲突警告；Release输出确认不存在`MvCameraControl.Net.dll`。
- 退出生命周期已调整为先执行`SolReset()`停止任务并释放全部相机句柄，再调用`FinalizeSDK()`反初始化原生SDK；资源未排空时禁止强行反初始化。
- 原生运行验证：独立64位后台进程直接调用新框架，SDK初始化602.984ms、相机枚举431.273ms、设备数0、SDK反初始化成功；未发生原生DLL装载、入口点或调用异常。
- 整机隐藏启动：Release进程运行8秒后仍响应，18线程、工作集133.50MB、专用内存106.79MB、主窗口句柄0且未抢焦点；日志确认两台配置相机均完成原生枚举，只因本机没有对应序列号而未打开。
- 本轮整机启动未作为通过项：当前并行修改的`LAB颜色识别`节点恢复时发生空值异常，导致方案启动未完成；隐藏模式无活动主窗体，正常关窗消息无法投递，12秒后仅终止本次测试PID。该问题与海康原生DLL链路无关，但修复前不能宣称整机启动验收通过。
- 性能结论边界：本机无相机，尚未产生真实回调帧；必须在测试机连接两台相机后，使用相同Line0、分辨率、曝光、10帧预热和10000帧正式采样，才能与旧版和官方Demo比较平均、P95、P99、P99.9及最大耗时。

## 2026-08-22：海康SDK触发语义细节复查

### 关键参数

| 参数 | 固定值/默认值 | 是否计划开放到控件 | 说明 |
| --- | --- | --- | --- |
| `TriggerMode` | `Off=0`、`On=1` | 是 | 优先按SDK符号名读写，数值仅作官方定义兜底 |
| `TriggerSourceSoftware` | `Software=7` | 是 | 软件触发不读取、不显示、不写入触发极性 |
| `TriggerSourceLines` | `Line0=0`、`Line1=1`、`Line2=2`、`Line3=3` | 是 | 海康官方枚举的无歧义线路范围 |
| `TriggerSourceCounter0` | `Counter0=4` | 否 | 框架当前不支持，禁止错误解释为`Line4` |
| `TriggerSourceFrequencyConverter` | `FrequencyConverter=8` | 否 | 框架当前不支持，按未知触发源处理 |
| `TriggerActivationAccess` | 仅线路触发且SDK节点可读写 | 否 | 运行时使用`MV_XML_GetNodeAccessMode_NET`判断当前状态权限 |
| `TriggerSourceOptions` | 相机SDK动态枚举 | 否 | 参数界面不再固定提供未确认支持的`Line4` |

### 官方依据与发现

- 参考用户提供的《算子SDK Windows SDK开发指南V4.4.1（.NET）》和本机MVS《工业相机Windows SDK开发指南V4.4.1（.NET）》；官方软件触发示例只设置`TriggerMode=On`、`TriggerSource=Software`并执行`TriggerSoftware`，没有设置`TriggerActivation`。
- 官方`MV_CAM_TRIGGER_SOURCE`明确规定数值`4`为`Counter0`，旧代码把`0～4`连续转换为`Line0～Line4`，会把计数器触发错误显示和保存为线路4。
- 旧图像源参数保存、每轮参数应用、方案启动参数应用均无条件调用`SetTriggerEdge`；软触发界面虽然禁用了极性控件，底层仍然会写无效节点。
- 实时调试初始化和状态快照会在软件触发或连续采集时读取`TriggerActivation`；节点不可访问时可能中断整组相机参数加载。

### 修正与验证

- 触发源改为优先使用GenICam符号名；只有`Software`和`Line0～Line3`保留海康官方数值兜底，`Line4`仅在具体相机真正支持该符号时允许设置。
- 相机底层读取枚举前查询节点访问权限；软触发和连续采集直接返回不可用极性结果，设置极性时再次强制检查线路触发和节点可写性。
- 参数窗体、流程每轮应用、方案启动和实时调试四条调用链均只在线路触发时读取、保存或恢复极性；图像源触发源列表按当前相机SDK动态生成。
- 4组相机专项检查全部通过；编译产物反射实测`Counter0=Auto`、`Software=SOFT`；`Release|x64`完整编译通过，0错误、171个既有警告。
- 本机没有连接目标相机，因此节点访问权限在软触发、Line0和特殊线路型号上的真实SDK返回仍需测试机验证；本轮不填写真实采图耗时结论。

## 2026-08-22：海康SDK回调稳定性隔离

### 关键参数

| 参数 | 固定值/默认值 | 是否计划开放到控件 | 说明 |
| --- | --- | --- | --- |
| `CameraCallbackSubscriptionLifetime` | 单次图像源等待 | 否 | 成功、取消、超时和异常均在`finally`中解除订阅 |
| `CameraCallbackPersistentFormBinding` | 关闭 | 否 | 参数窗体不再长期持有运行时相机回调 |
| `CameraRawFrameBufferCount` | 每相机2～8块自动计算 | 是 | 按SDK `PayloadSize`、配置相机数及原始帧总预算在开始取流前分配 |
| `CameraRawFrameMemoryBudgetRatio` | 流程图像预算的25% | 是 | 总预算按配置相机数量均分，最少2块也放不下时明确拒绝启动 |
| `CameraConversionWorkerCount` | 每相机1条 | 否 | 只保证单相机顺序，普通优先级且空闲时事件等待 |
| `CameraConversionGlobalConcurrency` | 跟随`ImageConvertMaxConcurrency` | 已有控件 | 由核心数、相机数、CPU总额度和机器级覆盖共同收敛 |
| `CameraCallbackQueueCapacity` | 每台相机`8`张待执行硬触发Mat | 是 | 完整流程忙碌时按FIFO保存后续帧；第9张无法安全接收时明确报警停机，禁止静默丢帧 |
| `CameraCallbackTargetP99` | 不高于无AI基线加2ms | 是 | 使用同相机、同触发频率和同诊断等级验收 |
| `CameraCallbackTargetMaximum` | 小于15ms | 是 | 排除首次缓冲初始化帧后统计 |
| `CameraCallbackBuild` | `Release|x64` | 否 | 性能结论禁止使用Debug构建 |

### 第一阶段：清理失效订阅

- 图像源改为仅在本轮等待相机帧期间注册`OnMatReceived`，收到帧或退出等待后立即解除事件和拥有者注册。
- 参数窗体不再负责运行时回调绑定，方案启动只负责启动相机取流；避免上一轮流程结束后继续克隆图像并记录“丢弃迟到回调帧”。
- 海康原生回调在相机打开时完成注册，不依赖参数窗体预先创建长期业务订阅。
- `CameraFrameAwaiter.Tests.ps1`和`CameraImageSourceUnifiedCallback.Tests.ps1`通过；`Release|x64`完整编译通过，0错误，历史警告未在本阶段处理。
- 下一阶段只处理SDK回调入口：原始帧快速复制后立即返回，像素转换、Mat构建、业务分发和详细日志移到相机专用工作线程。

### 第二阶段：SDK回调热路径隔离

- SDK回调现在只执行拥有者快照检查、`RtlMoveMemory`原始帧复制、预分配缓冲入队和工作线程唤醒；不再执行Bayer转BGR、`Mat.Clone()`、多流程同步分发或逐帧日志写入。
- 每台相机使用一条独立的顺序转换线程，在回调外完成像素转换、独立Mat构建、诊断记录和业务分发；详细日志新增原始复制耗时与转换排队耗时。
- 原始帧缓冲在`StartGrabbing`时按SDK `PayloadSize`一次性分配，停止取流后等待在途SDK回调和转换线程退出，再释放全部非托管内存；线程未退出时禁止关闭设备句柄。
- 图像源事件绑定、拥有者注册和等待器武装使用同一生命周期锁；并发重入只终止当前新请求，不再取消上一轮有效等待。
- 只读审查发现并已修复：错误的Windows复制入口、注册失败未回滚、重入取消上一轮、转换线程异常边界不足、SDK停止失败状态错误和回调统计结束点偏早。
- `RtlMoveMemory`真实运行复制测试、3组回调/等待器专项检查、原生类检查和触发语义检查全部通过；`Release|x64`完整编译通过，0错误。
- 现场验收仍需连接两台相机，在相同AI、保存、显示和触发条件下确认SDK回调平均、P95、P99、最大值及入口失败计数。

### 第三阶段：多相机硬件自适应转换

- 每台相机仍使用一条FIFO顺序消费线程，但线程改为普通优先级且空闲时无限事件等待；相机数量增加只增加休眠等待者，不增加高优先级CPU竞争或20ms轮询唤醒。
- Bayer、Mono和Mat独立化转换统一取得全方案`ImageConversion`许可，许可范围只覆盖转换本身，释放许可后才执行Mat事件分发，避免并发为1时订阅者再次申请转换许可造成自锁。
- 全局转换额度复用现有`ImageConvertMaxConcurrency`机器级参数；16核/16相机自动收敛为4路，4核/16相机自动收敛为1路，AI仍按可编辑流程执行且不进入该许可。
- 相机停止会取消尚未取得的CPU许可；停止、主动取图、关闭和销毁由相机生命周期锁串行，等待转换线程时不持有设备锁，并拒绝订阅者在转换线程中同步关闭自身。
- 开始取流和主动取图同样禁止从当前相机转换回调线程反向调用；转换线程首次停止超时后，后续停止调用仍会继续重试清理，不会仅因SDK已停止而遗留线程与非托管缓冲。
- 主动取图也纳入全局图像转换许可，并保持SDK缓冲从取得、转换到`FreeImageBuffer`完成前禁止并发关闭设备。
- 主动取图等待全局转换许可的时间不超过该相机配置的取图超时，避免多相机竞争时无限占用生命周期；SDK原生转换一旦开始仍按SDK同步调用语义等待完成。
- 原始帧总预算默认取流程图像预算的25%，按配置相机数量均分；每相机按`PayloadSize`自动分配2～8块，最少2块仍超预算时明确拒绝启动，不允许相机数量增加后无界消耗非托管内存。
- 全方案预算管理器按实际字节原子申请和归还相机内存租约；即使相机并发启动或运行中新增相机，现有预留加新预留也不能突破当前活动预算，剩余空间不足时自动减少本次缓冲或明确拒绝启动。
- 相机对象成功销毁后永久禁止重新打开；海康关闭或销毁句柄失败会向方案重置上抛并保留托管引用，避免原生资源失败却被误报为释放成功。
- 取流启动按“清理残留线程、申请全局租约、分配缓冲、启动转换线程、启动SDK”的事务顺序执行；任一环节失败都会回滚CTS、线程引用、非托管缓冲与内存租约，不把首次分配重新推回SDK回调。
- 方案重置改为逐设备释放并汇总失败；单台设备失败不再跳过后续设备，也不会清空失败设备引用或错误报告重置成功。
- 多相机诊断新增`CPU许可等待`和`转换总等待`，可区分单相机本地排队与全局CPU额度等待；队列满载取舍仍按用户要求留到独立阶段，不在本轮改变业务策略。
- 多相机资源规则测试通过：16核/16相机时图像转换并发自动收敛为4，4核/16相机时自动收敛为1；本机16物理核心、24逻辑线程、四流程四相机建议值为4。
- CPU调度器1000任务压力测试通过：完成1000/1000，全局并发峰值4/4、图像转换峰值2/2，结束时活动、等待和图像转换许可均为0，未发现许可泄漏。
- 相机回调隔离、方案资源释放、帧等待器、统一回调、海康原生类和触发语义共7组专项检查全部通过；最终`Release|x64`编译0错误。为避免影响用户办公，编译输出到临时目录，未关闭或抢占正在运行的软件窗口。
- 全局相机原始帧预算实测通过：16条线程同时申请、每台2块1MB缓冲、总预算32MB时精确占满；第17台被拒绝，释放任意相机后可复用其额度，16条线程同时重复释放仍保持幂等，全部停止后活动租约、已预留字节和预算代次均归零。
- 全局预算接入后的8组专项测试、本机硬件资源档案检查和最终`Release|x64`编译再次通过；本机检查读取到16物理核心、24逻辑线程，四相机图像转换建议并发为4。
- 独立审查指出并已修复：转换线程启动失败时租约回滚不完整、残留旧线程可能清掉本轮刚分配缓冲、预算测试未真正并发三个问题；修正后8组专项测试与`Release|x64`再次通过。

### 后续生产队列设计边界：完整可编辑流程

- “流程处理”指用户编辑的完整节点图，不等同于AI推理，也不得固化为“采图→AI→写信号”。流程可以任意组合逻辑判定、AI检测、无监督检测、大模型、传统视觉、测量、定位、结果处理、通信及其他合法节点。
- `WorkpieceId`属于整轮流程执行上下文，必须随节点调度、条件分支、组合模块和结果对象完整传递；任何算法节点都只消费当前上下文，不负责生成或改写工件顺序。
- PLC、CameraIO、Modbus、TCP、串口等需要有序的外部副作用节点，从当前流程上下文取得`OrderDomain + WorkpieceId + SignalSequence`进行校验；节点在流程图中的位置和分支语义保持不变，不因引入顺序控制而被强制移动到流程末尾。
- 图像显示是容量1的最新帧旁路，不属于工件提交顺序；生产流程即使包含多个算法、判断和结果模块，也不能被UI刷新速度阻塞。
- 本节目前只记录架构硬边界，生产者队列、工件上下文和有序信号提交器尚未实施，后续编码前必须先确定队列容量、满载停机策略、顺序域划分、异常终态和设备确认机制。
- 当前有序外发节点白名单严格限定为`NodeType.ComSend`（串口发送）、`NodeType.CameraIO`（相机IO）、`NodeType.PLCWrite`（PLC写入）和`NodeType.ModbusWrite`（Modbus写入）；不按节点名称猜测，不把读取、软触发、手动相机IO或其他通信节点自动纳入。
- 流程编辑保存及运行快照生成时必须递归扫描顶层节点、条件分支和组合模块，形成不可变的外发节点清单；清单至少包含流程版本、节点路径、节点ID、节点类型、设备身份和顺序域。排队中的工件继续使用入队时的流程版本，禁止编辑结果中途改变其发送计划。
- 编辑期清单只表示某条路径可能执行外发；运行时由实际到达的外发节点按当前`WorkpieceId`生成`SignalSequence`。未经过的分支必须在整轮流程结束时明确封口，不能让后续工件因等待一个本轮不会执行的信号而永久阻塞。
- 后续扩展新的外发节点应实现统一可插拔能力接口并注册元数据，不继续扩大散落的`switch`判断；所有外发动作最终仍由运行期有序提交器校验，编辑期识别不能替代运行期顺序保护。

### 第四阶段：相机保存后自动重连状态修复

- 问题日志显示：方案保存时相机为连接状态，`IsOpen=true`被持久化；软件重启后原生`_device`句柄为空，但旧逻辑把持久化值误当成真实打开状态，导致`CreateDevice`报“相机打开时不能重新创建设备句柄”，随后两台相机及四个图像源均因“相机对象为空”加载失败。
- 运行时`IsOpen`改为只返回海康SDK真实状态；旧方案JSON里的`IsOpen`字段继续兼容，但反序列化后写入独立的`RestoreConnectionRequested`，只用于启动界面决定是否发起真实重连。
- `Open`、回调注册、句柄重建、关闭和全部SDK操作只依据原生真实状态；重启后恢复连接请求不会让`Open`提前返回，而会重建句柄并真实调用`MV_CC_OpenDevice_NET`，成功后`IsOpen`才返回true。
- 打开后配置失败时检查SDK回滚关闭结果：关闭成功才清除真实状态，关闭失败则保留真实打开状态并汇总异常；用户关闭或销毁会清除下次恢复请求。
- 句柄重建先检查旧句柄销毁结果，新句柄创建成功前不覆盖原字段；回调注册与关闭使用同一回调锁，避免关闭后被并发线程写回“已注册”。
- 原生状态进一步拆为“SDK句柄已打开”和“相机配置已完成”：只有两者都成立时`Open`才能直接返回；配置失败且SDK回滚关闭失败时保留真实打开、标记配置未完成，下次连接跳过重复SDK Open并重新执行网络、采集模式、触发源与回调配置。
- Open配置完成、异常回滚关闭、正常Close和回调注册统一遵守`设备锁 → 回调锁`顺序；配置完成前回调注册只保留拥有者，不调用SDK。
- Open内部使用专用Core方法读取触发模式与触发源，并完成原生回调注册后才原子开放“配置完成”门禁；其他参数线程在最后一步完成前不能通过`EnsureDeviceOpen`访问SDK。
- 自动重连、回调隔离、海康原生类、触发语义、方案释放、16线程全局缓冲预算、帧等待器和统一回调共8组专项测试全部通过；CPU调度压力独立复跑1000/1000通过，句柄变化7。
- 最终`Release|x64`核心程序集编译0错误。完整解决方案构建曾停在项目原有的BatteryEar模板`xcopy`构建后命令，终止本次编译会话并跳过构建后复制后通过，确认该等待与相机状态代码无关。
- 独立审查三轮指出的状态混用、SDK关闭失败回滚、旧句柄覆盖、回调注册竞争、配置未完成误判及配置门禁提前开放均已修复；最终复查未发现新增P0/P1。

## 2026-08-23：相机生产者队列、完整流程执行与有序信号改造计划

### 1. 目标、边界与固定原则

- 目标是把相机帧接管、完整可编辑流程执行、外部信号发送和图像显示拆成相互隔离但可由同一工件身份追踪的四条通道，防止SDK回调、算法耗时、设备通信和UI刷新互相阻塞。
- “流程”始终表示用户编辑的完整节点图，可包含逻辑判定、AI检测、无监督检测、大模型、传统视觉、测量、定位、结果处理、通信和组合模块；禁止固化成“采图→AI→写PLC”。
- 第一阶段单个`Process`只允许一个工件进入节点执行，保护现有节点参数、结果和中间状态不发生重入；不同流程继续并行。顺序协调器从第一版支持未来多工件乱序完成，但本轮不以开放同流程并发换取吞吐。
- 生产模式不允许静默丢弃已经批准的工件。SDK回调不能阻塞等待队列空间，因此必须在触发之前完成工件和原始帧容量准入；无法控制的外部硬触发在满载后只能进入故障停机，软件无法用有限内存保证持续过载时永不丢帧。
- UI继续采用每窗口容量1的最新帧槽；图片保存继续使用现有独立有界工作池。UI替换和保存策略均不参与工件处理与外发顺序。
- 当前数据模型只面向最新框架，不增加旧方案兼容字段。每批实现前后建立Git恢复点，测试结果必须写回本章节和`任务记录.md`。

### 2. 总体运行结构

```text
触发准入与WorkpieceId分配
        ↓
每相机原始帧缓冲队列
        ↓
像素转换与Mat所有权交接
        ↓
工件流程有界队列
        ↓
完整可编辑流程执行
        ├────────→ 图片保存有界队列
        ├────────→ UI最新帧槽（容量1）
        └────────→ 有序信号提交器
                         ↓
              串口 / 相机IO / PLC / Modbus
```

- 四条通道通过`WorkpieceIdentity`关联，但各自拥有独立容量、锁、取消令牌和资源所有权，不建立一把覆盖整条链路的全局锁。
- 原始帧按单相机`SDKFrameNum`顺序转换，工件按生产顺序域的`WorkpieceId`准入，外发按`WorkpieceId + SignalSequence`提交，显示只认最新可视帧。

### 3. 核心数据模型与接口

- `WorkpieceIdentity`：由`SessionEpoch`和64位递增`Sequence`组成；生产会话启动时生成新Epoch，Sequence从1递增，禁止复用、回退或由算法节点改写。
- `WorkpieceExecutionContext`：保存工件身份、流程快照版本、生产顺序域、触发来源、创建时间、取消令牌、实际信号序号计数器、终态和诊断上下文；通过统一节点执行入口传播到条件分支和组合模块。
- `CameraFrameEnvelope`：保存工件身份、相机身份、内部`FrameId`、SDK帧号、触发时间、回调时间、图像所有权租约和像素信息；释放必须幂等，任何失败、取消、队列拒绝和停止路径都必须归还缓冲或Mat。
- `CompiledFlowExecutionPlan`：流程保存或启动时生成的不可变运行快照，包含流程版本、节点拓扑、组合模块展开信息、可能外发节点清单和顺序域；排队工件始终引用入队时快照。
- `OrderedSignalIntent`：包含唯一意图ID、顺序域、工件身份、运行时信号序号、节点路径、节点类型、规范化设备键、内部发送动作和诊断信息，不持有可被后续流程修改的节点结果引用；外部调用方不能构造意图、伪造序号或直接执行发送动作。
- `IOrderedExternalSignalNode`：四类外发节点实现的可插拔能力接口，负责生成节点描述和实际发送动作；流程扫描器不通过中文名称或散落`switch`推断。
- `IWorkpieceAdmissionController`、`IWorkpieceContextAccessor`、`IOrderedSignalCoordinator`和`ISignalEndpointSerializer`分别负责准入、上下文传播、跨工件顺序和单设备串行化，接口之间不反向引用UI窗体。

### 4. 编辑期流程扫描与版本快照

- 流程编辑保存、方案加载和正式运行前都执行相同扫描器；递归遍历顶层节点、条件分支、组合模块和嵌套节点路径，禁止只读取编辑器第一层控件。
- 当前有序外发白名单固定为`ComSend`、`CameraIO`、`PLCWrite`和`ModbusWrite`。读取、软触发、`CameraIOManual`、TCP和其他通信节点当前明确排除，后续新增必须由业务确认并注册能力接口。
- 扫描结果记录流程版本、节点路径、节点ID、节点类型、配置设备身份、生产顺序域和可能执行条件；检测到外发节点缺少设备、顺序域或必要参数时，保存可以保留草稿，但启动生产必须明确拒绝。
- 编辑期清单只说明“可能经过”；实际`SignalSequence`由本轮真正执行到的外发节点按到达顺序生成，同一节点在循环或组合模块中多次执行时得到不同序号。
- 运行中编辑只修改草稿。应用新版本时先停止新工件准入，等待旧版本队列和活动工件排空，再原子切换快照；不允许已经排队的工件中途使用新节点图或新信号清单。

### 5. 触发准入与工件身份绑定

- 软件触发：先为生产顺序域申请工件槽位和`WorkpieceId`，再为目标相机登记待匹配触发票据，最后调用相机软触发；任何准备步骤失败都不得实际触发相机。
- PLC、CameraIO或其他可控硬触发：收到触发请求后先执行相同准入，再向设备返回“允许触发/忙碌”；达到高水位后停止确认新触发，低于恢复水位且故障确认后恢复。
- 无法握手的外部硬触发：为每台相机维护按触发顺序排列的待匹配票据；回调帧按相机和SDK帧号匹配最早票据。若没有票据、票据超时、SDK帧号倒退或原始缓冲耗尽，标记`FrameIngressFault`并停机，不把未知帧绑定给下一个工件。
- 多相机属于同一工件时共用一个`WorkpieceId`，上下文记录期望相机集合；所有必需图像到齐后才发布流程任务，缺失相机达到相机超时后整件故障，不使用其他工件帧补位。
- 自由取流和参数预览不创建生产`WorkpieceId`，只进入独立`LatestOnly`显示路径，禁止预览帧混入生产流程队列。

### 6. 相机原始帧队列修改思路

- 保留当前每相机2～8块预分配非托管缓冲和单相机FIFO转换语义，SDK回调继续只完成原始复制、元数据冻结、入队和事件唤醒。
- 现有`ConcurrentQueue`由可用缓冲数量形成物理上限，但“无可用缓冲”当前只累计入口失败并返回；改造后区分生产票据和预览帧，生产票据失败必须关联具体`WorkpieceId`并触发顺序域停机。
- SDK回调内不写文件、不等待CPU许可、不等待工件队列、不调用UI、不执行设备信号，也不在满载时`Sleep`或阻塞等待缓冲。
- 转换线程出队后校验相机帧号和触发票据，取得全局图像转换许可，构造独立Mat及`CameraFrameEnvelope`，随后归还原始缓冲；Mat所有权成功转交工件上下文前不得复用。
- 诊断增加当前/峰值原始队列深度、缓冲总量、入口故障、无票据帧、帧号断层、回调复制、转换排队、CPU许可等待和回调到Mat完成分位。

### 7. 工件流程有界队列与背压

- 每个生产顺序域拥有独立工件准入状态，每个`Process`拥有顺序消费入口；容量同时受任务数量和图像实际保留字节限制，不能只按队列项数量控制。
- 初始容量8；取得实际图像字节、完整流程P99和触发间隔样本后，仅在队列为空时重新计算。实际容量不超过64和内存预算允许值，不因短期空闲无限扩张。
- 高水位75%时立即停止批准新触发并产生“生产等待”状态；低于40%只代表资源恢复条件满足，必须同时确认不存在帧断层、信号故障和设备通信故障才恢复。
- 满队列策略固定为`PauseTriggerAndAlarm`。不丢新、不丢旧、不合并不同工件、不把旧图替换成新图，也不在相机SDK回调中等待。
- 若平均到达速度长期大于完整流程消费速度，任何有限队列都会最终满载；此时必须降低触发节拍、增加经过验证的流程并行度或提升硬件，不能把队列容量当作吞吐能力。
- 第一阶段`PerProcessWorkpieceConcurrency=1`，保证同一流程节点实例不重入。后续只有在节点结果、参数读取、订阅输出和图像所有权全部改为每轮隔离并通过压力测试后，才允许开放大于1。

### 8. 完整可编辑流程执行

- `WorkpieceExecutionContext`由流程统一入口创建并设置为当前运行上下文，所有`NodeBase`节点通过公共入口读取，不要求每种算法重复添加参数。
- 条件判断、Else、组合模块、流程触发和等待节点必须显式传播上下文；异步换线程使用显式上下文对象，不依赖容易串轮的静态字段或可被覆盖的全局变量。
- 节点在流程图中的顺序和分支语义保持原样。框架只在运行前后管理工件租约、终态和信号顺序，不把PLC、CameraIO或其他节点强制移动到流程末尾。
- 正常、算法失败、节点异常、取消、超时和停机都必须把工件置为唯一终态；终态发布幂等，重复完成不能二次释放图像或二次推进信号序号。

### 9. 有序信号提交器

- 工件准入成功时，先在其`ProductionLane`顺序域登记`WorkpieceId`，再允许相机触发和流程执行；禁止后工件先到发送节点时因前工件尚未登记而被误放行。
- 顺序域维护`NextExpectedWorkpieceId`。小于期望值视为重复或过期并拒绝；等于期望值才可取得发送权；大于期望值进入有界等待，不得越过前序工件。
- 同一工件每次实际到达四类外发节点时，由上下文在同一实例锁内原子预约`IntentId + SignalSequence + 在途名额`并立即交给唯一协调器。取得顺序域发送权后，该工件一直持有到完整流程封口，保证工件1的所有外发都在工件2任何外发之前。
- 同一流程图中可在同一轮共同执行的两个外发节点若彼此没有有向路径，生产启动检查直接拒绝；True/False等可静态证明互斥的分支不误报。禁止按并行线程谁先运行到节点来决定业务信号先后。
- 工件请求封口后立即禁止创建新意图，但存在已预约信号时保持活动状态；只有全部在途意图明确返回后才发布唯一终态并允许协调器推进下一工件，堵住“已分配序号、尚未提交、工件先封口”的竞态窗口。
- 工件流程走到没有外发节点的分支时，终态封口负责明确提交“本轮无更多信号”；不能根据编辑期可能节点数量等待一个本轮不会执行的信号。
- 算法或业务判断产生正常NG并走到配置的NG发送节点时，仍属于正常有序提交。未处理异常、顺序缺口、设备发送结果未知或超时默认`StopAndAlarm`，不自动跳过当前工件继续发送后续工件。
- 协调器不做盲重试。只有设备适配器能够确认前次未生效或协议支持工件号幂等确认时，才允许按节点配置重试；无法判断“已经发送但响应丢失”时立即停机等待人工或PLC握手恢复。
- 每个物理设备连接另有`ISignalEndpointSerializer`，防止两个独立顺序域同时并发调用同一串口、相机IO、PLC连接或Modbus连接；生产顺序和设备线程安全分别管理。
- 软件内部可以保证单进程会话内不越序、不重复调用已确认成功的意图；跨进程崩溃后的严格Exactly-Once必须依赖PLC或设备返回并保存工件号，计划中不伪造设备不支持的保证。

### 10. 四类外发节点接入

- `ComSend`、`CameraIO`、`PLCWrite`和`ModbusWrite`分别实现统一外发能力接口，节点到达时从当前流程上下文构造不可变意图并等待协调器执行。
- 节点原有参数解析、订阅取值、设备调用、结果对象、日志和异常语义保留；改造只包裹“真正产生外部副作用”的最小调用范围，不提前读取会被前序节点更新的动态值。
- 节点不在UI线程或SDK回调线程等待顺序；等待使用可取消异步路径。当前节点发送完成后，流程才按原连接继续，保持用户对节点位置的理解。
- `CameraIOManual`继续作为人工调试操作，不进入生产工件顺序域；运行中若人工操作与生产使用同一设备，设备串行器必须拒绝或要求先暂停生产，不能插入生产信号之间。
- 未来新增外发节点通过接口注册，不把四类判断复制到编辑器、流程执行器、诊断器和测试器多个位置。

### 11. 图像显示与保存旁路

- 每个显示窗口继续保持一个待显示帧和一个已显示图像；新显示帧替换未显示旧帧并立即释放旧Bitmap，显示帧丢弃不等于生产工件丢弃。
- UI显示使用工件身份和帧身份做诊断标签，但不参与`NextExpectedWorkpieceId`，也不阻塞流程终态和信号提交。
- 图片保存继续通过现有有界保存队列取得独立图像租约。保存失败按保存策略报告，不反向持有信号顺序域；若业务要求“保存成功后才能发信号”，用户必须在流程中使用明确等待/结果节点表达，框架不暗中改变顺序。

### 12. 停止、故障与资源释放

- 正常停止顺序：停止新工件准入和触发确认，停止相机取流，等待SDK在途回调结束，排空原始帧转换，排空或按超时终止工件流程，封口信号顺序域，停止设备发送器，最后释放Mat、非托管缓冲、事件、线程和设备句柄。
- 故障停机先原子关闭准入，保留首个根因并汇总后续释放异常；不得因清理异常覆盖帧断层、信号失败或流程异常的原始证据。
- 所有队列项、票据、上下文和信号意图均实现幂等终结；测试必须覆盖入队前失败、入队后取消、转换中停止、流程异常、等待信号时停止和设备发送结果未知。
- 重启创建新`SessionEpoch`。上次会话存在未确认信号时默认保持生产禁用并要求设备状态对账，不能简单从序号1恢复后继续发送。

### 13. 诊断与可观测性

- 每个工件统一记录：生产会话、`WorkpieceId`、流程版本、相机/SDK帧号、队列进入和离开时间、完整流程开始结束、实际外发节点、信号等待、设备发送和终态。
- 每个队列记录当前深度、字节数、峰值、高水位次数、暂停时长、拒绝或故障次数、最老任务年龄、P50/P95/P99和最大等待时间。
- 信号诊断记录`OrderDomain`、期望工件号、实际工件号、`SignalSequence`、节点路径、设备键、等待时间、发送耗时、确认状态和失败原因；日志不得包含无界增长的完整图像或大对象序列化。
- Debug关闭只保留计数器、故障和低频汇总，Debug开启才记录逐工件完整链路；诊断队列本身继续有界，不能反向阻塞生产线程。

### 14. 分批实施与Git恢复点

1. 第0批：提交本计划书并从`codex/8月23已测试待上机验证`创建独立开发分支，确认业务源码零变化。
2. 第1批：新增工件身份、执行上下文、终态、外发能力接口和纯内存确定性测试；不接入生产流程。
3. 第2批：实现流程递归扫描、不可变运行快照和四类外发节点识别；覆盖条件分支、组合模块、重复节点执行和运行中版本切换。
4. 第3批：把`WorkpieceExecutionContext`接入统一节点执行入口、条件分支和组合模块；保持单流程单工件，不改变现有节点业务结果。
5. 第4批：实现工件准入、触发票据、多相机帧绑定和数量/字节双有界队列；先用伪相机验证高低水位、满载停机和全部资源归还。
6. 第5批：加固现有海康原始帧队列的生产票据关联、帧号校验和入口故障停机；复跑真实相机回调基线，P99和最大值不得明显回退。
7. 第6批：实现有序信号协调器和设备串行器，使用伪发送器强制工件2先完成，验证工件1全部信号仍先发送。
8. 第7批：按`ComSend → CameraIO → PLCWrite → ModbusWrite`逐类接入，每接入一类完成异常、取消、分支跳过和重复提交测试并单独Git提交。
9. 第8批：完成停止排空、崩溃恢复门禁、诊断汇总、UI和保存旁路回归，执行独立代码审查并关闭P0/P1问题。
10. 第9批：`Release|x64`连接真实多相机和实际外发设备，执行1000轮固定条件验收及2小时长稳；形成旧版、8月23基线和队列改造版三方报告后再决定合并。

### 15. 测试矩阵与验收门槛

- 编辑器扫描：四类白名单全部识别，读取、软触发、手动相机IO和其他节点全部排除；条件分支和组合模块递归结果完整，流程版本切换不污染已排队工件。
- 工件身份：多线程生成100000个身份无重复、无倒退；多相机同工件绑定正确，缺帧、重复帧、SDK帧号断层和无票据帧全部转为明确故障。
- 队列边界：任务数和字节预算都不能越界；高水位停止准入、恢复水位滞回、满载停机、取消和停止后活动数、等待数、字节数全部归零。
- 顺序确定性：至少10000组强制乱序完成，四类外发实际调用始终按`OrderDomain → WorkpieceId → SignalSequence`排列；过期、重复、缺口、分支无信号和前工件失败均有确定结果。
- 节点语义：四类节点在流程任意位置和条件分支中执行结果与改造前一致，动态订阅值在节点到达时读取，发送完成后才继续后续连接。
- 性能：相机SDK回调继续以8月23基线为参照，绝大多数低于1ms记录精度且排除首次初始化后最大目标小于15ms；工件准入和信号提交元数据操作P99目标小于1ms，不包含真实设备通信等待。
- 业务：正式1000轮无静默丢件、无工件号错配、无信号越序、无重复发送、无流程重入、无UI阻塞生产；设备失败演练必须停机且后续工件不越过。
- 资源：预热后私有内存、托管堆、Mat、非托管缓冲、句柄和线程数量进入稳定窗口；停止后队列、租约、票据和信号意图全部归零，2小时长稳无单调增长。
- 构建与办公：每批执行专项测试和`Release|x64`完整构建；真实窗口压力继续采用不激活、最小化和低优先级方式，不占用用户键盘鼠标焦点。

### 16. 本轮计划状态

- 第0批已完成：基线提交`2dee9b0`保留在`codex/8月23已测试待上机验证`，后续开发在`codex/workpiece-queue-ordered-signals`独立进行。
- 第1批已完成：新增`WorkpieceIdentity`、`WorkpieceExecutionContext`、唯一终态、四类外发白名单、不可变信号意图/确认结果及三个可插拔接口；本批尚未接入现有相机、流程或真实设备发送路径，生产行为不变。
- 第1批并发规则：工件信号序号分配与终态封口使用单工件实例锁形成原子边界，禁止封口竞态下继续分配信号序号；锁不跨工件、不跨流程、不覆盖设备发送和图像处理。
- 第1批验证：Debug与`Release|x64`完整构建均为0错误、保留170个既有警告；专项测试并发分配2000个信号序号无重复且连续，跨会话比较、唯一终态、封口保护、意图冻结和四类白名单全部通过。
- 第1批独立复审发现首版意图公开发送委托、序号可伪造、意图预约与封口不原子、确认等级含糊等问题；现已改为上下文原子预约并强制通过协调器提交，意图构造和执行入口仅框架内部可见，2000个在途意图未释放前终态不会提前发布。
- 确认语义已拆分为设备协议确认、本地API完成、明确拒绝和未知四级；`DeviceAcknowledged`没有确认凭据时构造直接失败，已经开始的外部副作用不能因等待取消而提前释放设备租约。
- 第二轮复审发现“最后一个信号失败但流程已请求成功”仍可能提前发布成功终态；现已把意图终结改为携带最终发送状态并在同一实例锁内合并工件终态：`Rejected`升级为`Faulted`，`Unknown`或异常升级为`Stopped`，只有确认未开始且永久移除的取消才进入`Cancelled`。
- 取消异常不再自动视为安全取消；只有协调器明确返回`CancelledBeforeStart`，才能证明意图已在副作用开始前永久移除。协调器或设备适配器抛出的任何取消异常均保守合并为`Unknown/Stopped`。
- 契约专项测试必须同时覆盖公开提交入口，验证明确拒绝、空返回、普通异常和取消异常最终都会正确合并到工件终态；内部反射仅用于无法从公开入口制造的并发预约原子性压力检查。
- 端点互斥租约由普通`IDisposable`收紧为`ISignalEndpointLease`，必须携带规范化设备键、公开释放状态且重复释放无害；协调器返回取消前必须保证意图以后绝不会迟到发送，副作用开始后只能返回明确结果或冻结端点后返回`Unknown`。
- 当前已测试版本继续保留为独立分支和恢复点；后续任何代码回退不得覆盖该分支。

### 16.1 不可变流程计划编译器首批实现

- 新增`FlowExecutionPlanCompiler`，把顶层流程与组合模块递归复制为只读节点、连线、潜在外发节点和诊断集合；计划生成后不随源节点改名或连线修改而变化。
- 外发白名单仅包含串口发送、相机IO、PLC写入和Modbus写入；读取节点、手动相机IO及其他计算、判断、测量、定位、AI节点均不被误判为生产外发。
- 静态检查会阻止重复节点ID、断线、组合模块快照缺失、递归过深、路径分析超限，以及同轮可能共同执行但没有明确路径先后的两个外发节点。
- 同一条件节点的True与False互斥分支不会误报；旧版无画布连接流程按节点列表顺序生成带稳定ID的兼容连线。
- 重要边界：编译结果已经不可变，但从活跃`Process`取样必须由后续统一编辑门保护；当前单集合重试不能证明节点、连线和组合模块参数来自同一编辑版本，因此本批尚未接管生产运行。
- 验证结果：流程编译器专项测试连续5轮通过；Debug与`Release|x64`完整编译0错误，保留项目既有警告。
- 独立审查发现首版会把禁用条件节点的True/False分支继续视为互斥，而运行器跳过禁用节点时会放行全部分支，存在无序外发漏报P0；现已仅允许启用节点建立分支互斥约束，并新增禁用条件回归用例。
- 起点推导已与运行器对齐：显式起点先覆盖所属无向连通分量，再补充其他无入线组件；无有效连线且无显式起点时，无论`HasCanvasGraph`取值都按节点列表顺序合成兼容连线。
- 启用组合模块没有任何内部节点时直接产生`CompositeSnapshotMissing`错误，行为与组合模块运行时校验一致。
- 仍未完成：跨节点、连线、参数的统一编辑版本事务，以及真正冻结节点参数、订阅和执行对象的可执行快照。这两项是下一批生产接入的前置条件。

### 16.2 工件上下文与单流程FIFO入口基础设施

- `WorkpieceIdentity`正式由`OrderDomain + SessionEpoch + Int64Sequence`共同构成；顺序域参与构造、比较、相等、哈希和日志文本，禁止不同生产线同序号工件发生身份碰撞。
- 新增`WorkpieceContextAccessor`，使用`AsyncLocal`不可变栈传播上下文；嵌套作用域必须后进先出释放，乱序释放明确失败且不改变当前上下文，避免异步分支共享可变释放标记。
- 新增`WorkpieceIdentityGenerator`，同一规范化顺序域内序号严格连续；会话重置接口收窄为框架内部，后续只能由确认全部工件排空的生命周期控制器调用。
- 新增`ProcessWorkpieceGate`，默认容量8且活动工件计入容量，单流程并发固定1；等待者使用严格FIFO链表，取消会永久移除，租约重复释放不增加许可。
- 满载策略为`PauseTriggerAndAlarm`的底层实现：第9个工件在任何流程执行和相机软触发发生前抛出`ProcessWorkpieceQueueFullException`，不批准后再静默丢弃。
- 当前边界：本批是纯内存基础设施，尚未接入`Solution/Process/ProcessTrigger`真实运行入口，也尚未加入图像字节预算和多相机触发票据。
- 验证：100000个同域身份连续且无重复；上下文嵌套、乱序释放、容量4满载、严格FIFO、取消队首、幂等租约和入口停止全部通过；独立复审确认身份冲突P0关闭。

### 16.3 工件上下文接入真实流程运行入口

- `Solution`在首条活动运行会话建立新`SessionEpoch`，每个流程组每轮创建一个共享`WorkpieceExecutionContext`；同组主动流程、条件分支、组合模块和被动流程沿异步调用链继承同一工件身份。
- `Process`的按ID运行、普通运行、相机帧续跑和参数图像刷新四个入口全部在节点执行和相机软触发前进入容量8、并发1的严格FIFO；满载明确失败，不在SDK回调内等待。
- 流程触发不再使用可被其他工件覆盖的共享委托；`ProcessTrigger`直接等待同组唯一被动流程的本次不可变结果，目标缺失、重名、未设为被动、取消、满载或执行失败都会向父流程传播。
- 每条异步调用链维护绑定`WorkpieceIdentity`的流程路径，`A -> B -> A`在等待FIFO前明确拒绝；封口上下文和旧会话延迟分支不继承旧路径，也不复用旧工件身份。
- 工件上下文新增在途流程分支租约，分支准入与父工件封口在同一实例锁内竞争；父终态同时等待流程分支和信号意图，子分支失败、取消或未明确完成会按安全等级覆盖父成功。
- 四个入口使用局部`invocationSucceeded`提交本次终态；共享`Process.Success`只在取得FIFO后更新为界面兼容状态，排队中的下一调用不能污染上一调用的封口结果。
- 调用方取消令牌贯通FIFO等待、自有上下文和统一节点执行器；`CancellationToken.None`统一回退方案停止令牌。旧公开`Run(bool)`、`RunInternal(...)`和`TriggerProcess(string)`签名保留，新结果接口单独提供。
- 新增`Tests/WorkpieceProcessIntegration.Tests.ps1`，实际验证流程环拒绝、预取消、上下文同步激活与恢复、封口/旧纪元拒绝、分支租约阻止提前封口及失败分支覆盖父成功；Debug与Release x64构建均为0错误，四项专项测试通过。
- 独立审查连续发现并关闭共享触发闭包、异常吞掉、调用方取消失效、AsyncLocal反向传播、旧路径误判、继承封口竞态、分支失败未合并和共享`Success`跨轮污染问题；提交前最终复审结论为P0=0、P1=0，仅保留旧插件只按方法名反射同名`TriggerProcess`重载时可能歧义的P2，不影响框架直接CLR调用。
- 当前边界：尚未实现触发前相机票据、多相机帧绑定、数量/字节双有界工件准入、海康生产帧故障停机、有序信号协调器及四类真实发送节点接入，不得把本批描述为完整生产队列已经完成。

### 16.4 相机生产票据与图像实际字节预算基础设施

- 新增`CameraTriggerTicketRegistry`，按规范化相机稳定键维护互相独立的严格FIFO；同一工件和相机禁止重复登记，单相机待匹配票据硬上限64，故障相机必须确认并重置后才允许继续登记。
- 软件触发和硬触发统一先创建票据并预留估算图像字节；票据携带`WorkpieceIdentity`、触发类型、创建/截止/真实触发时间和异步帧结果，触发准备失败会归还预算且不得留下孤儿票据。
- 软件触发把“关闭状态复核、票据武装、活动SDK触发计数递增”放在同一个原子临界区；真实SDK调用在锁外执行，既防止取消/关闭越过提交点，又避免SDK同步等待回调时形成死锁。不同相机使用独立状态锁，不互相阻塞。
- 转换后按`Rows × Step`把估算记账校正为实际Mat字节；预算租约的校正与释放使用同一实例锁串行化，避免两者并发造成负记账或释放后重新加账。
- `CameraFrameEnvelope`绑定工件身份、相机键、票据ID、内部FrameId、SDK帧号、触发/接收时间、诊断信息、独立Mat和预算租约；Mat与预算只能成对转交，失败、取消和未转交释放均幂等。
- 内部FrameId和SDK帧号必须同步连续前进；断层、倒退、票据超时、原始入口故障或实际字节超预算会锁定该相机，并一次性失败同相机全部后续票据，禁止留下无限等待流程或把未知帧补给下一工件。
- 清场任何已武装票据或收到任何无法可信绑定的帧后都会设置`RequiresStreamRestart`；普通重置永久拒绝，只有相机生命周期层完成物理停流、等待回调排空和重新取流后，才能调用内部确认入口解除门禁。
- 生产故障由注册表自有后台线程按FIFO派发，既不占SDK回调线程，也不依赖通用线程池；相机保留待派发故障计数，该计数归零前禁止重置，避免旧故障跨越恢复边界影响新工件。
- 注册表关闭先封住新操作并等待全部真实SDK触发调用返回，默认上限2000ms；超时明确抛错并保留票据/租约，要求相机层解除SDK阻塞后重试。触发归零后统一失败排队票据、归还Mat预算和工件执行租约，故障线程最终自行释放事件句柄。
- Mat实际字节计算、预算校正和信封构造具有统一异常清场；Mat释放异常不会跳过预算、票据和工件执行租约终结。
- 新增`Tests/CameraWorkpiecePipeline.Tests.ps1`，使用真实OpenCvSharp Mat验证两工件FIFO、多相机并行、实际字节校正、所有权转移、断层清场、未武装旧帧门禁、触发/取消竞态、流重启确认、超时清场、关闭清场和预算全部归零；Debug与`Release|x64`构建0错误，四项工件/相机专项测试均通过。
- 独立审查连续检查触发取消、超时、回调死锁、迟到帧、Mat异常清理、故障通知顺序和关闭竞态；最终结论为`P0=0、P1=0`。
- 当前边界：本批尚未修改`CameraHik`转换线程、`NodeImageSource`和`OutputImage`，真实软触发仍未调用票据注册表，生产故障事件也尚未接入方案停止；必须完成下一接入提交后才能上机验证该链路。

### 16.5 相机字节预算接入OutputImage生命周期

- `OutputImage`新增外部生命周期所有权，按对象引用去重；相机帧实际字节预算租约可与Mat成对接管，并与自产Mat、父级图像依赖一起等待最后一个图像租约释放。
- 新增`FromOwnedSingleImageWithLifetime`原子工厂；构造失败时释放尚未接管的外部生命周期，成功后由`OutputImage`唯一负责，避免调用方在所有权转移边界重复释放。
- 最终回收顺序为自产Mat、父级图像租约、外部生命周期；任一资源释放异常不会阻断同批其余资源回收。
- 专项测试验证同一外部生命周期重复登记只保留一份；存在下游租约时Mat和预算均不提前释放，最后一个租约退出后各释放一次并归零。原图像生命周期50000轮、快照/释放竞态1000轮及依赖竞态1000轮全部通过。
- Debug与`Release|x64`完整构建0错误；独立审查结论`P0=0、P1=0`。
- 当前边界：本批只扩展`OutputImage`资源所有权，尚未让`CameraHik`和`NodeImageSource`实际创建或转交相机预算租约。

### 16.6 Solution接管相机生产管线生命周期

- `Solution`现在成对持有`WorkpieceFrameMemoryBudgetManager`和`CameraTriggerTicketRegistry`；注册表不是节点静态对象，也不由单台相机拥有，确保任意相机数量使用同一套全方案内存硬预算。
- 相机生产故障由注册表专用FIFO线程通知控制层；控制层只记录相机稳定键、工件身份和原因并取消方案，不在转换线程或SDK回调中同步停止、关闭相机。
- 旧方案重置必须先等待流程退出、停止保存工作池、释放节点资源，再逐台物理释放相机；只有全部设备释放成功后才关闭旧注册表并建立新预算纪元。任何设备释放失败都会保留旧注册表和设备引用，禁止带病加载新方案。
- 新增统一`CameraProductionIdentity`：相机对象第一次进入生产身份服务时即冻结键；当时已有SN则使用`设备品牌 + SN`，否则分配进程内不可变唯一身份。后续SDK补齐属性或界面改名都不会换键。海康转换线程与图像源节点后续必须调用同一入口，禁止各自拼接键。
- 旧注册表必须关闭成功后才能发布新注册表；关闭超时会恢复旧故障订阅并保留原引用，下一次重置仍可重试。故障处理委托在来源校验的同一临界区内抓取当代方案取消令牌，已经取出的旧故障也不能取消新方案。
- Debug和`Release|x64`完整构建均为0错误；相机票据专项测试通过。当前仍未改变真实帧分发路径，下一提交接入`CameraHik`。

### 16.7 海康转换线程接入生产票据

- 海康SDK回调仍只做状态快照、原始内存复制和有界队列发布；不转换Mat、不等待流程、不写日志、不执行票据锁。为防止触发前旧帧在转换排队后错配，回调进入后的第一步、取得任何锁之前固定采集一次`Stopwatch`单调时钟。
- 每台相机自己的顺序转换线程完成像素转换后，先按统一相机键尝试交给生产票据；成功接管后Mat不再进入旧事件，未登记生产票据时继续走原预览和参数页回调。
- 票据武装同时冻结单调时钟；生产帧除内部FrameId、SDK帧号连续外，还必须满足“SDK回调开始时刻不早于票据武装时刻”。触发前已经进入SDK或等待相机生命周期锁的旧帧一律故障清场，不能绑定当前工件。
- 原始缓冲耗尽和SDK入口异常只在回调里冻结首个`FrameId + 回调时刻 + 异常`并唤醒转换线程；转换线程按故障发生时刻归属，不能因报告延迟把前一帧故障嫁祸后一工件。没有活动生产票据时保留流重启门禁，下一次生产登记前必须完成物理恢复。
- 像素转换、Mat构造或生产发布异常会在转换线程失败对应票据并触发方案停机；SDK回调线程不执行停机和相机关闭。
- 流恢复采用两阶段故障代数：物理停流和回调排空后读取代数，成功重新取流后仅在代数未变化时确认；新流启动期间再次发生入口或转换故障会使确认失败，并立即回滚停流。
- `Tests/CameraWorkpiecePipeline.Tests.ps1`新增触发前已入队旧帧回归；海康回调隔离、触发语义和相机票据三组专项测试通过。Debug与`Release|x64`构建均为0错误。
- 当前边界：`NodeImageSource`尚未在真实软/硬触发前登记生产票据，因此海康生产分发分支只有下一提交完成后才会在真实流程中启用。

### 16.8 图像源软触发接入生产票据

- `NodeImageSource`在生产工件上下文中先按相机稳定键登记票据、取得流程分支租约并预留图像字节，再通过后台任务调用一次真实软件触发；SDK回调线程仍不等待节点、工件或触发任务。
- 单帧触发前估算改为每台相机当前`Width × Height × 4`，并与运行档案历史平均值或首轮8MB下限取最大值；转换后按彩色源Mat加标准灰度Mat实际字节校正，预算随`OutputImage`最后一个租约一起释放。
- 图像源节点的`TimeOut`同时约束SDK触发命令与对应生产帧；帧已到但SDK命令仍卡住也按超时失败，立即释放信封Mat和预算、锁定相机并要求物理停流排空重启，不能在超时点返回伪成功。
- 预览或手动软触发与生产票据双向互斥：生产票据一经登记就拒绝预览命令；预览命令成功后保留待返回帧计数，延迟预览帧完成转换前拒绝生产登记，该帧只交旧预览显示链路。
- 软件触发SDK返回未知会设置流重启门禁；同相机真实SDK命令禁止并发，只有FIFO队首票据能触发，前序票据取得帧或失败出队后才允许下一票据触发。不同相机继续使用各自状态锁并行。
- 节点删除和释放与生产取消源安装使用同一状态复核；删除先标记节点不可用，竞态中的运行线程不能重新登记票据或触发相机。
- 硬触发生产路径当前明确拒绝：仅有“开始等待后第一帧”无法证明物理触发沿，必须在后续接入PLC/IO外部触发就绪握手、流排空和工件身份绑定后才能开放。参数预览等无工件上下文路径保留原行为。
- 后台验证通过：Debug与Release x64编译均为0错误；相机票据、统一回调、快速回调、海康隔离/重连、工件上下文、流程编译和图像生命周期专项检查通过，其中图像生命周期压力为50000轮、快照/释放竞态1000轮、依赖竞态1000轮。独立复审最终结论`P0=0、P1=0`；未启动主界面，也未占用用户键盘焦点。

### 16.9 实际有序信号协调核心

- 新增`OrderedSignalCoordinator`，按`OrderDomain + SessionEpoch + WorkpieceSequence + SignalSequence`维护严格FIFO；后一工件即使先到达发送节点，也必须等前一工件的全部在途信号返回并发布正常终态后才能发送。
- 同一工件内按上下文原子预约的信号序号连续执行；信号2先提交时只登记不执行，信号1完成后才允许信号2取得设备发送权。
- 新增可插拔`SignalEndpointSerializer`，规范化设备键后每个物理端点只发放一个幂等租约；不同生产线共用同一PLC、串口或相机连接时不会并发写，使用不同端点时仍可并行。
- `Rejected`、`CancelledBeforeStart`和`Unknown`都会阻断当前生产顺序域；`Unknown`额外冻结物理端点租约直到协调器关闭，禁止无法判断是否已发送时盲重试或让其他生产线继续写同一设备。
- 协调器关闭拥有独立取消源，并与外部副作用提交点使用同一状态锁原子竞争；尚在等待端点的记录返回`CancelledBeforeStart`且永不调用设备委托，已经越过提交点的动作则继续取得明确结果。
- 活动工件内同一`IntentId`重复提交返回首次任务，不重复执行；工件正常封口后立即移除完整意图记录，旧意图再次提交因工件已退役而明确拒绝，避免长期生产永久持有委托、上下文和结果对象。
- 新增`Tests/OrderedSignalCoordinator.Tests.ps1`，覆盖跨工件倒序到达、工件内信号倒序、共享端点互斥、独立端点并行、发送前取消、未知冻结、关闭竞态、活动/退役重复意图和10000个信号倒序提交；10000次设备动作计数准确，无丢失、重复或卡死。
- Debug x64构建0错误；工件执行契约、100000次工件身份/FIFO、流程运行入口和流程编译器专项检查通过。独立审查发现的关闭迟到发送与完成意图长期持有问题均已修复并加入回归。
- 当前边界：`ComSend`、`CameraIO`、`PLCWrite`、`ModbusWrite`仍走原节点调用，尚未通过本协调器；`SignalOrderGapTimeoutMs=30000`的主动定时报警也尚未接入。因此本批证明协调核心，不宣称真实外设链路已经完成有序化。

### 16.10 有序协调器接入生产会话生命周期

- `Solution`持有当前生产会话唯一`IOrderedSignalCoordinator`；首条运行会话建立新`SessionEpoch`时同步替换协调器，旧协调器立即停止尚未开始的设备动作，防止上一轮阻断状态、冻结端点和工件序号污染新会话。
- 方案重置在全部运行会话排空后同样替换协调器，及时释放旧会话意图、端点租约和顺序域状态；节点不使用静态全局协调器。
- `CreateWorkpieceExecutionContext`在分配身份后立即登记协调器，登记失败不会让该工件进入流程；所有工件从创建时就占住自己的FIFO位置，后一工件不能因尚未到达外发节点而越过前一工件。
- 方案流程组根工件和单流程自有工件统一调用`CompleteWorkpieceExecutionAsync`；协调器请求上下文封口、等待继承分支及在途信号结束，再移除队首并调度下一工件。继承流程只释放自己的执行分支租约，不重复登记或重复封口父工件。
- 扩展`WorkpieceProcessIntegration.Tests.ps1`，验证身份纪元与协调器成对替换、创建即登记、两个根入口禁止直接`TryComplete`旁路、继承上下文不重复封口；Debug x64编译0错误，流程接入和10000信号倒序专项测试均通过。
- 当前边界：四类外发节点尚未调用`SubmitOrderedSignalAsync`，因此真实设备动作仍在下一批逐类接入。

### 16.11 串口发送节点接入有序协调器

- `NodeComSend`实现`IOrderedExternalSignalNode`；运行时必须取得当前`WorkpieceExecutionContext`和生产会话唯一协调器，缺少上下文时明确拒绝，不允许退回原直接发送旁路。
- 节点真正到达时复制`ComDevice`引用、命令、编码和端点键，协调器取得工件顺序权及物理端点租约后才调用一次`ComDevice.Send`；运行入口不再直接读取共享参数写串口。
- 端点键固定使用`COM:物理端口名`并忽略大小写，不使用可编辑的自定义名称；端口名为空时明确拒绝，避免多个错误配置坍缩为同一伪端点。
- 串口写入API正常返回记录为`LocalCallCompleted`，只证明本机调用完成；取得发送权后发现串口已关闭返回`Rejected`，真实写入调用抛异常则由协调器判为`Unknown`并冻结端点，禁止不确定状态盲重发。
- 修复`ComDevice.Close()`关闭后错误把`IsOpen`保持为`true`的问题，避免已关闭串口进入发送提交点后才失败。
- 串口配置新增默认`2000ms`有限写超时并夹紧到`100～60000ms`；写超时发生在副作用提交后，仍按`Unknown`处理，不假定没有部分字节写出。空命令在节点提交前和设备API内双重明确拒绝，不能无发送却返回成功。
- 参数页“测试发送”改为后台执行，并通过`Solution`运行生命周期门禁与生产会话启动原子互斥：手动动作先取得门禁则生产等待其有限超时结束，生产或重置先开始则手动动作明确拒绝，不能插入两个工件信号之间。
- 新增`ComSendOrderedSignalIntegration.Tests.ps1`，保护接口实现、当前工件与会话协调器、动态值冻结、物理端点键、唯一真实写入口、确认等级、关闭状态、空命令、有限写超时及手动门禁；Debug x64编译0错误，串口专项、流程接入及10000信号倒序压力均通过。
- 当前边界：本批只接入`ComSend`；`CameraIO`、`PLCWrite`和`ModbusWrite`仍未改变。

### 16.12 相机IO节点接入有序协调器

- `NodeCameraIO`实现`IOrderedExternalSignalNode`；运行入口必须取得当前工件上下文和当前生产会话协调器，缺少上下文时明确拒绝，真实相机IO写入不再允许绕过协调器。
- 节点到达时冻结相机对象、稳定相机身份、线路、模式、保持时间和异步开关；后续参数页编辑不能改变已经提交工件的脉冲行为。
- 端点键固定为`CAMERA-IO:StableCameraIdentity`，按整台物理相机互斥而不是按线路互斥。原因是`LineSelector`属于同一相机句柄的共享可变状态，两个线路节点并发设置会造成选线与电平写入串线。
- 单次信号在端点租约内完整执行“选线路、设输出模式、置低、拉高、保持、回低”；拉高后的任意异常都会在`finally`中再次尝试选回原线路并置低，协调器同时把异常按`Unknown`冻结该相机端点，禁止盲目重发。
- 相机IO信号节点只允许`输出/Output`模式，参数页设计器移除输入选项；线路能力枚举通过方案运行生命周期门禁执行，生产或重置期间不允许参数页写入相机的共享线路选择器。
- 保持时间限制为`0～1000000us`。小于2ms使用短时自旋，较长脉冲先异步等待绝大部分时间、末段再自旋，避免整个长脉冲持续占用CPU核心。
- 旧异步模式不再创建脱离工件生命周期的`Task.Run`。节点可以在信号提交后提前返回，但观察任务和信号意图由工件上下文管理，工件封口必须等待脉冲取得明确终态；同步模式则在节点内等待完整脉冲结果。
- SDK完整脉冲正常返回记为`LocalCallCompleted`，只证明本地相机API完成；发送前取消返回`CancelledBeforeStart`，取得发送权后的异常或取消均按未知状态停机处理。
- 新增`CameraIoOrderedSignalIntegration.Tests.ps1`，除源码与程序集契约外，使用可控假相机真实执行编译后的节点，验证同相机跨顺序域高电平最大并发为1、提交后替换参数不污染排队快照、在途脉冲阻止工件提前封口、拉高后SDK报错仍回低并进入`Stopped`终态，以及发送前取消不触碰相机线路。
- 当前边界：`ComSend`和`CameraIO`已经接入；`PLCWrite`、`ModbusWrite`及30秒顺序缺口报警继续按独立提交完成。

### 16.13 PLC写入节点接入有序协调器

- `NodePlcWrite`实现`IOrderedExternalSignalNode`；运行入口必须取得当前工件和生产会话唯一协调器，缺少上下文时明确拒绝，真实PLC写入只允许由协调器调用。
- 节点到达时只读取一次参数对象，把PLC对象、物理端点键、地址、数据类型和已经解析的布尔/整数/浮点/字符串值冻结为不可变快照；参数页后续替换不会改变已排队工件。
- 端点键按`PLC:品牌:通信方式:物理地址`生成：网口使用IP和端口，串口使用COM口。锁覆盖整条物理连接而不是单个寄存器地址，避免同一HSL连接上的请求与响应交叉；不同物理连接仍可并行。
- PLC取得发送权前后二次检查连接状态；取得发送权后已知断开返回`Rejected`且不调用写入。真实写入开始后的异常由协调器归类为`Unknown`，HSL空结果或失败结果也保守返回`Unknown`并冻结端点，因为无法证明PLC没有执行请求。
- HSL写入成功响应记为`DeviceAcknowledged`，确认凭据包含响应成功、错误码、物理端点和写入地址；不把本地任务完成单独当成设备确认。
- `IPlc`新增`OperationTimeoutMs`，默认5000ms并夹紧在100～60000ms，应用到三菱和松下网口连接/接收超时，以及松下串口接收和底层`SerialPort.WriteTimeout`；运行时调整也同步更新串口写超时。节点不使用`Task.WhenAny`提前放弃已经开始的写入，端点租约一直持有到HSL真实任务返回。
- 修复`PlcMelsec(PLCParms)`错误创建`PanasonicMcNet`的问题，构造和反序列化恢复现在统一使用`MelsecMcNet`。
- 参数页保存前严格验证PLC对象、地址、类型和值；整数和浮点数组统一使用逗号分隔，支持单个负数和负数数组，浮点拒绝`NaN`与无穷大，布尔值只接受0、1、False和True。
- 新增`PlcWriteOrderedSignalIntegration.Tests.ps1`，使用两个不同假PLC对象指向相同物理端点，真实执行编译节点并验证最大写并发为1、参数替换不污染快照、在途写入阻止提前封口、成功响应携带确认凭据、失败响应进入`Unknown/Stopped`、发送前取消零写入以及数值边界。
- Debug与`Release|x64`完整构建均为0错误；PLC写入、相机IO、协调器10000次倒序、工件契约、流程入口和流程编译回归通过；独立复审修复松下串口无限写超时后最终结论为`P0=0、P1=0`。
- 当前边界：`ComSend`、`CameraIO`和`PLCWrite`已经接入；`ModbusWrite`及30秒顺序缺口报警继续按独立提交完成。

### 16.14 Modbus写入节点接入有序协调器

- `NodeModbusWrite`实现`IOrderedExternalSignalNode`；生产运行入口必须取得当前工件上下文和会话唯一协调器，缺少上下文时明确拒绝，不再保留直接调用设备写入的生产旁路。
- 节点真正到达时只读取一次参数对象，并把Modbus设备、物理端点键、起始地址、寄存器类型及已经解析的独立值数组冻结为不可变快照；订阅值在排队前读取，不再写回共享`NodeParamModbusWrite.Data`，参数页后续修改不会污染已排队工件。
- TCP端点键使用`MODBUS:TCP:IP:端口`，RTU端点键使用`MODBUS:RTU:COM口`；锁覆盖整个物理连接而不是寄存器地址或站号，防止同一连接上的请求和响应交叉，不同物理连接仍允许并行。
- 取得发送权前后二次检查设备连接状态；取得发送权后已知断开返回`Rejected`且零写入。同步`Write`调用开始后的异常由协调器保守归类为`Unknown`并冻结端点，因为请求可能已到达设备且禁止盲目重发。
- 当前TCP和RTU主站的全部`Write`实现都会检查HSL协议结果，正常返回表示收到成功响应，因此记录为带凭据的`DeviceAcknowledged`；凭据包含物理端点、起始地址和数据类型。
- `IModbus`新增`OperationTimeoutMs`，默认5000ms并夹紧到100～60000ms。TCP同步应用连接和接收超时；RTU同步应用通信管线接收超时以及底层`SerialPort.ReadTimeout/WriteTimeout`，运行时调整也立即更新现有通信管线。
- 参数页保存时先验证设备仍存在、地址和自定义值格式；人工运行改为异步等待，并通过`Solution.TryExecuteManualExternalSignal`与生产会话及方案重载原子互斥，不能在两个工件信号之间插队。
- 数值按固定区域解析，支持逗号、分号和中文逗号数组；有符号整数保留负数，浮点支持科学计数法并拒绝`NaN`与无穷大，布尔和线圈只接受0、1、False和True，所有错误均在进入协调器前明确拒绝。
- 新增`ModbusWriteOrderedSignalIntegration.Tests.ps1`，用两个不同假设备对象指向相同TCP物理端点、两个RTU对象指向大小写不同的同一COM口，真实执行编译节点并验证最大写并发为1、参数替换不污染快照、在途写入阻止提前封口、成功响应携带凭据、写入异常进入`Unknown/Stopped`、发送前取消零写入和数值边界。
- 独立审查发现并修复TCP `ConnectServer()`失败返回仍发布连接成功的P1；连接结果现在于成功事件和`IsConnect=true`之前检查，专项通过反射实际执行编译后的检查方法，覆盖空结果和`IsSuccess=false`，复审最终`P0=0、P1=0`。
- Debug x64和跳过既有构建后复制命令的`Release|x64`核心项目编译均为0错误；Modbus专项连续复跑10次失败0，最终Modbus、PLC、相机IO、串口、协调器、工件契约、流程入口和流程编译共8组回归全部通过。完整解决方案Release编译器未报错，但再次等待于项目原有构建后复制命令，已主动终止且不把该等待计为代码测试通过。
- 当前边界：四类外发节点已经全部接入；下一批实现`SignalOrderGapTimeoutMs=30000`主动计时、停机和可观察报警，未实现前不宣称顺序缺口会自动超时。

### 16.15 30秒顺序缺口主动报警

- `OrderedSignalCoordinator`新增可构造注入的`SignalOrderGapTimeoutMs`，默认30000ms并夹紧到1000～600000ms；默认值用于生产，专项测试使用1000ms缩短验证时间，后续可以按计划书稳定键接系统设置控件。
- 缺口定义严格限定为：当前域没有设备动作正在执行，且当前队首工件的下一连续信号尚未提交，同时同工件更高信号号或后续工件已有一个以上意图排队。只有一个尚在算法处理中且后方没有信号压力的工件不计时；已取得顺序权的设备动作即使运行超过阈值也不按缺口处理，由设备自身有限超时负责。
- 每个顺序域最多一个异步`Task.Delay`计时器，不占用常驻线程。计时快照绑定期望工件号、期望信号号和代数；前序补齐、设备动作开始、工件推进、域阻断或协调器停止都会取消旧计时并增加代数，迟到回调必须重新在锁内确认同一缺口仍存在才可报警。
- 缺口到期后禁止跳过前序继续发送：协调器先阻断当前顺序域并把全部尚未开始意图置为`Rejected`，随后必须先把域内尚未发布终态的工件升级为`Stopped`，最后才完成等待任务。这个顺序保证自动`FinalizeSignalIntent`即使立即继续执行，也只能发布`Stopped`而不会竞态落成`Faulted`。
- 新增不可变`OrderedSignalGapEventArgs`，保存顺序域、会话纪元、期望工件/信号、首个等待工件/信号/节点、累计等待意图数、实际超时、UTC发现时间和完整中文消息；`IOrderedSignalCoordinator.OrderGapDetected`允许上层记录及执行停机，不通过解析日志反推状态。
- `Solution`创建每代协调器时绑定缺口处理器；处理器统一遵守`运行生命周期锁 → 协调器替换锁`顺序，避免与生产会话换代死锁。报警来源必须仍是当前协调器，旧会话迟到事件直接忽略；当前事件先在生命周期锁内提交不可恢复停机门闩并捕获旧令牌，活动会话退出前拒绝任何新入口，零活动会话则在下次启动时原子换代令牌和协调器，迟到的旧令牌取消不能误停新会话。
- 协调器专项新增跨工件持续缺口、同工件信号缺口、公开提交入口自动回收与终态竞态、阈值内恢复零报警，以及前一设备动作持续超过阈值且后一工件已经排队时零误报；同时保留10000意图倒序压力。流程集成专项验证旧协调器零提交、零活动会话安全换代、旧令牌迟到取消隔离，以及活动会话期间故障门闩拒绝第二入口。
- Debug x64和跳过既有构建后复制命令的Release x64核心工程编译均为0错误；有序协调器、Solution流程桥接、四类外发节点、工件契约和流程编译相关8组回归全部通过，协调器时序专项在完整回归后额外连续复跑3轮失败0。独立复审最终`P0=0、P1=0、P2=0`，并由审查智能体再次运行两个专项通过。
- 当前边界：计划中的工件上下文、有序信号协调器、四类外发节点和主动缺口报警已全部接入；下一阶段进入最终压力回归和步骤9正式测试，仍不改变用户可编辑流程节点顺序。

### 16.16 图像源软硬触发统一接入与停止会话隔离

- `NodeImageSource`按`TriggerSource`建立明确生产票据：`Auto/SOFT`为软件票据，登记成功后在线程池只调用一次`Camera.GrabOne`；`LINE0～LINE4`调用`RegisterHardwareWait`，在同一相机同步区内完成登记和武装后直接等待物理帧，不创建软件触发任务。
- 触发方式不改变用户编辑的流程顺序。软触发图像源允许有Modbus读取、PLC读取、逻辑判定或其他节点作为上游，运行到图像源时才发出软触发；硬触发图像源禁止活动上游节点，由方案流程先进入等待，再接收外部线路触发沿。
- `Auto`只作为旧参数默认值兼容并统一归一为`SOFT`；方案启动批量设置和节点`IsEveryTime`设置使用同一语义。`SetTriggerEdge`只对明确的`LINE0～LINE4`调用，软触发不设置无意义极性。
- `CameraTriggerTicketRegistry.RegisterHardwareWait`把票据登记和`MarkReadyForFrame`放在同一个单相机临界区；返回给节点时票据已经武装，SDK转换线程只能把帧发布给当前FIFO队首且已武装票据，避免登记后尚未就绪的时间窗口。
- 正常`Stop`和相机生产故障都先锁定当前会话停止状态；活动运行数未归零前所有新入口均拒绝。活动数归零后的第一次启动换代`CancellationTokenSource`、`WorkpieceIdentityGenerator`会话纪元、`OrderedSignalCoordinator`、`CameraTriggerTicketRegistry`和`WorkpieceFrameMemoryBudgetManager`。
- 相机故障提交遵守`运行生命周期锁 → 相机票据表锁`固定顺序，并同时校验故障来源仍是当前票据表；旧票据表迟到故障零提交，已捕获旧令牌的延迟取消也不会影响换代后的新令牌。
- 首会话初始化采用提交后计数：所有资源换代与硬件档案刷新成功后才增加`_activeRunSessionCount`。旧票据表关闭超过`CameraTriggerDispatchShutdownTimeoutMs=2000ms`时明确抛出超时，保持活动数0和`IsRunning=false`，资源解除阻塞后允许再次启动。
- 验证结果：Debug x64与跳过既有构建后复制命令的Release x64均为0错误；相机生产票据、统一回调、回调快路径、帧等待、原始帧预算、海康隔离/重连、触发语义、工件会话、流程编译、有序协调器和四类外发节点回归全部通过。专项真实阻塞SDK触发入口验证初始化失败回滚及恢复重试，确定性停机门闩测试验证取消前拒绝和旧令牌迟到取消隔离，反射执行编译后分类逻辑验证`Auto/SOFT/LINE0～LINE4`全部分流正确。独立复审最终`P0=0、P1=0、P2=0`，并由审查智能体复跑三组核心专项通过。
- 当前边界：本轮保证回调外的票据匹配、会话隔离和触发语义，不改变相机SDK回调内的现有轻量复制路径，也不在回调中启动流程；硬触发物理接线、极性和相机参数仍需在测试机按实际设备完成上机验证。

## 2026-08-22：颜色识别整体判定订阅修复

### 关键参数

| 参数 | 固定值/默认值 | 是否计划开放到控件 | 说明 |
| --- | --- | --- | --- |
| `ColorDiscern.IsOk` | 本轮算法原始整体结果 | 否 | LAB颜色、面积、占比及已启用线序规则共同决定，作为隐藏兼容输出 |
| `ColorDiscern.JudgeOk` | 初始等于`IsOk` | 否 | 多条件节点可见的“整体判定OK”布尔出口，并允许条件结果回写 |
| `ColorDiscern.ResultCategory` | `AlgorithmResult` | 否 | 明确结构化颜色结果的订阅类别，避免派生类型被误判为普通结构化对象 |

### 修正设计

- 多条件判定不递归展开复杂算法对象；颜色识别直接在顶层发布“整体判定OK”布尔值，避免条件节点依赖`Result.IsAllOk`内部路径。
- 开启线序校验时，整体结果同时包含颜色模板顺序、检测数量、单项面积及面积占比判定；关闭线序校验时，整体结果只汇总其余已启用规则。
- 原始算法结果与可回写最终结果分离：`IsOk`保存本轮算法事实，`JudgeOk`供后续多条件判定和显示汇总使用。
- 新增颜色识别到多条件判定的订阅契约回归检查；本轮不改变LAB计算、排序算法和图像资源生命周期。
- 专项回归检查通过；`Release|x64`编译通过，0错误、171个既有警告；对编译程序集执行反射验证后，“整体判定OK”实际分类为`Boolean / Single / Core`，属性路径为`JudgeOk`。

## 2026-08-24：图像源方案参数权威性与离线恢复

### 关键参数

| 参数 | 固定值/默认值 | 是否计划开放到控件 | 说明 |
| --- | --- | --- | --- |
| `ImageSourceParameterAuthority` | `SchemeWins` | 只读显示 | 方案曝光、增益和触发参数始终覆盖相机原有值 |
| `ImageSourceOfflineRestorePolicy` | `RestoreWithoutHardwareRead` | 只读显示 | 相机离线、枚举延迟或SDK不可用时仍恢复节点，不读取硬件当前参数 |
| `ImageSourceCameraRebindKey` | `UserDefinedName` | 只读显示 | 方案恢复、回调启动和节点运行前都按名称绑定当前相机对象 |
| `ImageSourceParameterApplyTiming` | `OnlineRestore,Reconnect,BeforeRun` | 只读显示 | 三个时机重复保证相机最终采用方案参数 |
| `ImageSourceExposureUiMaximumUs` | `10000000us` | 是 | 方案曝光输入上限，最终硬件范围由SDK写入结果确认 |
| `ImageSourceGainUiMaximum` | `100000`，三位小数 | 是 | 方案增益输入上限和精度 |

### 修改设计

- 参数窗体进入反序列化恢复状态后，所有下拉框事件不得读取相机当前曝光、增益和触发延迟；控件只接收`NodeParamImageSoucre`中的方案值。
- 相机不存在或尚未打开时，保留`CameraName`和全部方案参数并完成节点创建；SDK读取或写入异常不再升级为整个方案恢复失败。
- `Solution.ResolveImageSourceCamera`统一处理相机对象换代，旧对象被移除或设备重新添加后，图像源可以按方案名称取得当前对象。
- 在线方案恢复、相机手动重新连接和生产回调启动前统一写入方案参数；`Auto`仍归一为`SOFT`，线路极性只写入`LINE0～LINE4`。
- 运行调参窗口显示和保存方案值，不把相机当前状态当作配置来源；图像源节点正式运行前再次解析相机，防止启动期间设备对象变化。
- 相机已经取流时先停流再写方案并恢复取流；每轮采图固定相机对象，保证回调订阅、软件触发、生产票据和解绑使用同一实例。
- 全方案与单流程入口都扫描全部启用图像源；同一相机存在不同曝光、增益、触发、延迟或超时时，在任何硬件改写前明确拒绝，禁止两个流程分别运行时互相污染相机状态。
- 新增方案参数权威性专项，并与海康重连、触发语义、统一回调、相机工件管线、固定50ms架构和回调快路径共同验证；Debug完整构建0错误、154个既有警告，Release x64核心编译0错误、155个既有警告，9组相关回归通过。独立审查4项P1均完成修复和复审。
- 真实相机仍需在测试机确认“方案5000us、MVS原值2000us，连接后硬件变为5000us”，并覆盖启动离线后重连、已取流恢复、软触发、线路硬触发和多相机同时重连。
## 2026-08-24：线路硬触发等待策略修正

### 关键参数

| 参数 | 固定值/默认值 | 是否计划开放到控件 | 说明 |
| --- | --- | --- | --- |
| `HardwareTriggerWaitTimeout` | `Infinite` | 否 | `LINE0～LINE4`运行期间持续等待外部帧，不受图像源采图超时限制 |
| `HardwareTriggerStopBoundary` | 运行会话取消令牌 | 否 | 点击停止、方案重置或程序关闭时解除硬触发等待 |
| `SoftwareTriggerTimeout` | 图像源`TimeOut` | 是，已有控件 | 只约束`Auto/SOFT`的软件触发命令及返回帧 |

### 修改设计

- 线路硬触发的物理信号时刻由PLC、传感器或外部控制器决定，等待期间没有帧属于正常空闲状态，不能作为相机故障或方案故障。
- 硬触发票据仍在流程进入图像源时原子登记并武装，保持相机FIFO与`WorkpieceId`绑定规则不变；本轮只修正截止条件，不改变回调内转换和有序信号链路。
- 硬触发等待唯一正常出口为物理帧到达，唯一中止出口为运行会话取消；停止后票据、工件执行租约和图像预算必须全部归还。
- 软触发继续执行有界超时，防止真实SDK命令或返回帧异常时永久阻塞流程。
- 自动化验证把硬触发测试超时故意压缩为20ms并实际等待100ms，确认任务仍未结束；之后发布物理帧可成功完成，停止取消可结束另一轮等待。补强测试每轮执行100次`registry.Cancel`/`TryPublishFrame`并发竞态并连续复跑3轮，明确覆盖帧先完成与取消先完成两种结果，全部预算归零且未接管Mat即时释放；软触发20ms无帧仍精确返回超时。Debug/Release x64编译均为0错误。

## 2026-08-24：硬触发帧跨流程FIFO持续接管

### 本批关键参数

| 参数 | 当前值 | 后续可配置 | 说明 |
|---|---:|---|---|
| `MaximumBufferedHardwareFramesPerCamera` | 每相机`8`张 | 是 | 完整流程忙碌期间允许积压的待执行硬触发Mat数量 |
| `BufferedHardwareFrameBudgetBytes` | 严格取流程图像预算`25%`，无档案回退`256MB` | 是 | 全部相机共享，按每张Mat实际`Rows × Step`记账，和张数上限同时生效；预算不足一张时明确报警，不突破整机档案 |
| `MaximumPendingTicketsPerCamera` | 每相机`64`个 | 是 | 工件身份票据上限，不等同于图像队列容量 |
| `RawFrameBufferCountMinimum/Maximum` | 每相机`2～8`块 | 是 | SDK回调原始复制与转换隔离缓冲，不等同于待执行Mat队列 |
| 硬触发等待时间 | 直到停止 | 是 | `LINE0～LINE4`不使用图像源超时；软触发仍使用方案超时，缺省`2000ms` |

### 修改结果

- 首个生产硬触发票据建立后，该相机在当前方案运行期间持续接管SDK帧；图像源完成一轮并注销临时订阅后，不再丢弃算法忙碌期间到达的第2帧。
- 没有下一工件票据时，转换后的独立Mat进入每相机FIFO；下一轮图像源登记工件后立即取得最老帧，并按`FrameId + SDKFrameNum`连续性校验后绑定新的`WorkpieceId`。
- 停止或故障后的迟到帧禁止重新积压；相机物理停流后清空旧流待执行Mat并关闭持续接管，重新运行后由新硬触发票据重新武装。
- 最后一个运行会话退出时先原子关闭旧注册表入口，再排空全部相机待执行Mat；停止前已经复制但尚未转换的旧包发布时会被旧注册表拒绝，不能写入旧FIFO或串入新会话。
- 预览与生产共用帧路由快照但分开判定：生产门闩关闭后仍允许手动软件触发帧归还预览计数；已释放旧注册表对迟到帧无异常返回未接管，不把会话换代误记为Mat转换失败。
- 原始帧入口故障使用无锁FIFO逐项保留各帧的路由注册表和生产注册表；预览失败不能覆盖同批后续生产失败，转换线程按进入回调的先后逐条归还预览计数或触发对应生产停机。
- 满载策略固定为每相机8张：前8张不丢弃并等待执行；第9张触发明确生产故障、释放全部缓存Mat并建立物理停流重启门禁，SDK回调不等待队列空间。
- 全部相机待执行Mat另受运行资源档案计算出的全局字节预算约束，多台高分辨率相机不能分别积压8张后绕过内存边界。
- 诊断快照新增当前等待帧、当前/峰值字节、全局字节预算、历史峰值和累计缓存帧计数，用于判断触发节拍是否长期快于完整流程。

### 验证结果

- `Debug|x64`完整编译通过，0错误；保留项目既有154个警告。
- 相机工件管线专项连续3轮通过；每轮覆盖100次停止/到帧竞态、多相机并行、软硬触发隔离、停止后预览入口失败与转换失败计数归还、停止后100次预览计数归还、入口失败逐项排队、已释放旧注册表无异常拒绝迟到帧、无票据转换失败、跨会话注册表隔离、停止后旧包拒绝、64MB低内存档案严格分配16MB、全局字节预算、帧号断层、预算归零、第二帧跨流程FIFO和容量8满载清场。
- `CameraCallbackFastPath`、`CameraImageSourceUnifiedCallback`、`ImageSourceSchemeParameterAuthority`和`WorkpieceProcessIntegration`通过。
- 最终独立审查结论为`P0=0、P1=0、P2=0`；`Release|x64`强制重编译0错误、155个项目既有警告，未启动软件窗口和真实相机。
- `HikCameraCallbackIsolation.Tests.ps1`存在既有字符串断言`CPU许可等待=`与当前格式化源码`CPU许可等待={...}`不一致；`HikCameraReconnectPersistence`、`CameraRawFrameMemoryBudgetManager`和`WorkpieceRuntimeInfrastructure`脚本存在既有中文编码解析错误，本批未改动这些脚本。

## 2026-08-25：硬触发待执行FIFO无拒绝上限与非停机报警

### 本批关键参数

| 参数 | 当前值 | 后续可配置 | 说明 |
|---|---:|---|---|
| `BufferedHardwareFrameWarningIntervalPerCamera` | 每相机`8`张 | 是 | 待执行队列达到8、16、24张等阈值时记录一次积压报警 |
| 待执行帧张数上限 | 不设拒绝上限 | 是 | 超过报警阈值仍继续进入每相机FIFO，不因积压取消方案 |
| `BufferedHardwareFrameBudgetBytes` | 流程图像预算`25%`，无档案回退`256MB` | 是 | 保留为资源观测和日志参考值，不再拒绝超额Mat |
| 积压事件处置 | `Warn`日志，不停机 | 是 | 经独立故障派发线程写日志，不占用SDK回调和Mat转换快速路径 |

### 执行步骤与修改结果

1. 取消每相机固定8张和全局字节预算的帧接收拒绝分支，新帧始终按相机FIFO入队并继续实际字节计账。
2. 保留8张作为软报警间隔；队列增长到8、16、24张时生成非停机积压事件，队列回落到8张以下后重置报警起点。
3. 积压报警复用已有独立单消费者派发线程，方案层只记录`Warn`日志，不提交停机门闩，不取消当前流程。
4. 帧号错乱、Mat转换失败、内存所有权无法交接等数据已不可信的生产故障仍保留原停机策略，不把资源积压与数据完整性故障混为一类。
5. 专项测试改为一次积压17张，验证突破旧8张边界、8和16张各记录一次报警、不建立停流门禁、继续按SDK帧号FIFO消费、超过参考字节预算后仍正确计账，以及停止时全部Mat释放归零。
6. 独立审查指出无上限队列在停止时可能于全局状态锁内集中释放大量Mat；改为锁内快速摘除和字节归还、锁外逐张`Dispose`，避免阻塞其他相机状态操作。

### 风险记录

- 本策略按用户确认不设内存拒绝上限；若触发平均速度长期高于流程消费速度，待执行Mat和等待延迟会持续增长。
- 日志会明确记录当前张数、实际字节、报警间隔和参考内存预算，供客户机后续追查触发节拍与流程耗时差值。

### 验证结果

- `Debug|x64`和`Release|x64`完整强制重编译均通过，0错误；分别保留项目既有154和155个警告。
- `CameraWorkpiecePipeline.Tests.ps1`连续3轮通过；实测同一相机积压17张，报警计数为2，停流重启相机数为0，随后消费的SDK帧号为2，证明超过旧8张边界后仍保持FIFO；队列回落到7张后再进一张可第3次报警。
- 多相机待执行Mat实际字节从300增长到600，成功超过500字节参考预算且没有建立停流门禁；注册表释放后张数和字节均归零。
- `CameraCallbackFastPath`、`CameraImageSourceUnifiedCallback`、`ImageSourceSchemeParameterAuthority`和`WorkpieceProcessIntegration`关联回归全部通过。

## 2026-08-26：硬触发回调先于流程票据时保持FIFO运行

### 本批关键参数

| 参数 | 当前值 | 后续可配置 | 说明 |
|---|---:|---|---|
| 硬触发帧归属依据 | 单相机`FrameId + SDKFrameNum`连续FIFO | 否 | `LINE0～LINE4`由外部线路独立产生，不能仅因回调早于下一流程票据数毫秒就判为旧帧 |
| 软件触发旧帧校验 | 严格启用 | 否 | `Auto/SOFT`必须保持“票据武装并发送命令后才允许对应帧进入回调” |
| 硬触发提前帧处置 | 接管并交给FIFO队首工件 | 是 | 回调入口顺序由相机单线程原始包队列冻结，Mat转换完成后仍按原顺序发布 |
| 帧号异常处置 | 故障并要求停流重启 | 是 | SDK帧号重复、倒退或断层仍视为身份不可信，不放宽数据完整性保护 |
| 显式停止处置 | 停止接管并清空待执行Mat | 否 | 新一轮运行不继承上一轮相机帧 |

### 执行步骤

1. 保留SDK回调入口的高精度时间戳、`FrameId`和SDK帧号冻结，不在回调线程增加票据锁或日志。
2. 旧帧时间因果校验仅约束软件触发票据；硬触发票据按相机连续帧号和FIFO顺序绑定工件。
3. 保留每相机独立转换线程和票据锁，禁止同一相机后一帧越过前一帧，多相机继续互不阻塞。
4. 新增现场竞态复现：第2轮硬触发帧先进入回调，下一工件票据后建立，Mat最后发布时必须成功绑定且不得停止方案。
5. 回归软件触发旧帧拒绝、硬触发积压FIFO、停止清场、帧号断层、多相机隔离和Mat预算归零。

### 验证结果

- `Debug|x64`完整编译通过，0错误、154个项目既有警告；`Release|x64`强制重编译通过，0错误、155个项目既有警告。
- `CameraWorkpiecePipeline.Tests.ps1`首次通过后连续复跑3轮全部通过；每轮均覆盖硬触发回调早于下一票据、SDK帧号连续绑定、软件触发旧帧拒绝、100次停止竞态、多相机并行、积压17张继续FIFO和全部Mat预算归零。
- `CameraCallbackFastPath`、`CameraImageSourceUnifiedCallback`、`WorkpieceProcessIntegration`、`ImageSourceSchemeParameterAuthority`和`HikTriggerSemanticSafety`全部通过。
- `HikCameraCallbackIsolation.Tests.ps1`仍因项目既有日志字符串断言过时而失败：测试查找字面量`CPU许可等待=`，当前源码使用格式化占位符；该问题在前批计划中已经记录，本批未修改回调诊断格式和该脚本。
- 独立只读审查结论为`P0=0、P1=0、P2=0`；确认硬触发仍受单相机转换线程、双帧号连续性和FIFO约束，多相机隔离、显式停止换代、Mat所有权及锁顺序均未发生回归。

## 2026-08-27：软件触发100ms无帧不停止方案

### 执行策略

1. `Auto/SOFT`命令已经成功、但在图像源`TimeOut`内没有对应帧时，只失败当前相机当前及已排队工件并记录可恢复告警；方案运行令牌和其他相机保持运行。
2. 当前相机执行`StopGrabbing -> 排空SDK回调与转换线程 -> StartGrabbing`，确认新流代数后才允许下一轮，迟到帧不得绑定下一`WorkpieceId`。
3. 超时失败必须按具体`CameraTriggerTicket`定位，禁止使用未来时间扫描全局票据，避免一台相机的超时误伤其他相机。
4. 线程池尚未执行的软触发通过原子阶段取消，超时后不得继续调用SDK；已经进入SDK且命令本身卡死时使用专用严重异常，停止方案且不并发重启相机。
5. 帧完成与超时边界竞争时，以生产注册表是否已经成功提交该票据为最终依据，已经绑定成功的帧不能被误判为超时。

### 验证结果

- `Debug|x64`和`Release|x64`均构建成功、0错误；Release输出文件为`bin/x64/Release/机器视觉AI检测系统V1.0.exe`。
- `CameraWorkpiecePipeline`、`CameraImageSourceUnifiedCallback`、`WorkpieceProcessIntegration`、`CameraCallbackFastPath`、`HikTriggerSemanticSafety`、`HikNativeCameraClass`和`ImageSourceSchemeParameterAuthority`全部通过。
- 相机生产管线额外连续复跑5轮全部通过，总耗时15.123秒，覆盖多相机隔离、FIFO、超时边界、迟到帧、触发取消和资源释放。
- 独立只读审查最终结论为`P0=0、P1=0、P2=0`，未发现新增托管死锁、Mat、内存预算或执行租约泄漏。
- 真实海康相机仍需上机验证SDK执行`StopGrabbing`后对原生回调的实际排空行为；该项不影响静态构建和自动化竞争测试结论。

## 2026-08-27：修复软件启动后第一次运行取帧失败

### 现场依据

- 新日志中首次运行于`13:00:01.403`完成相机重启取流，`13:00:01.430`开始流程，随后首个生产帧超过100ms失败；超时恢复后第二次运行同一图像源耗时94ms成功。
- 第二次已经接近100ms边界，说明方案参数、连接和软触发命令本身可用，首次失败来自相机流、托管回调、转换线程及像素转换首次执行的冷启动长尾。

### 修改原则

1. 每次相机成功连接后，仅首个软件触发生产票据追加500ms启动宽限；图像源保存的100ms仍是稳态每轮超时。
2. 宽限票据仍只发送一次真实软触发，不允许通过额外拍摄并丢弃所谓预热帧，避免工件与`WorkpieceId`错位。
3. 宽限资格以相机连接会话为边界原子领取，多流程或并发首轮只能有一个票据取得；相机重新连接后重新获得一次。
4. 首轮仍未到帧时按普通可恢复超时处理，后续轮次立即恢复严格100ms，不允许无限延长。
5. 新增首次宽限、仅一次领取、重连重置、并发领取和后续严格超时回归；完成Debug/Release x64构建和独立审查。

### 验证结果

- `Debug|x64`构建成功、0错误；正常`Release`目录最初被运行中的PID 26784锁定，因此先在`bin/x64/Release-FirstFrameFix`完成独立`Release|x64`验证。旧进程退出后，已将同一相机修复重新生成到正常`bin/x64/Release`目录，最终构建0警告、0错误。
- 8项相机关联回归全部通过；首帧专项以128个并发领取者验证只有1个成功，100ms仅首个成功登记票据变为600ms，后续保持100ms，未提交预留可由下一个票据继续取得。
- 宽限采用两阶段租约：持有相机专用锁完成票据登记，成功后提交，登记异常由`finally`归还；解决并发流程领取顺序与实际FIFO入队顺序可能倒置的问题。
- `Open`在同一锁内发布配置完成状态和新连接资格；`Close`仅在原生关闭、回调排空及转换资源释放全部成功后重置，普通停流重启不会重复获得宽限。
- 独立只读审查最终结论为`P0=0、P1=0、P2=0`，确认多相机隔离、硬触发语义、FIFO帧号绑定和整数饱和均未回归。

## 2026-08-31：每相机独立转换通道

### 关键参数与范围

- 改造前恢复提交：`dc5bafc`，包含此前尚未提交的现有训练模块改动及其新增源码；本批不修改AI、训练、通信或队列满载策略。
- 每台海康相机保留1个常驻顺序转换线程，优先级仍为Normal；不建立跨相机的转换许可上限，不修改线程池或CPU亲和性。
- `ImageConvertMaxConcurrency`继续控制共享调度器内的显示等转换，海康采集转Mat不再参与该许可池；算法与保存原有CPU限制不变。
- 保留每相机2～8块原始帧缓冲、全局原始帧内存预算、原有工件队列及报警策略；停止仍等待转换线程最多5000ms，失败时禁止提前释放缓冲和句柄。
- 完整诊断仍只在Debug打开时采集；保留`SDKFrameNum`、`回调总耗时`、`排队耗时`、`CPU许可等待`、`回调到转换`、像素转换和Mat复制字段。共享CPU许可等待固定为0，另记录`转换通道=每相机独立；共享CPU许可=不参与`，总耗时仍使用真实时间戳计算。

### 实施与验证

1. 同时移除`ProcessRawFrame`及`GetOneFrameImage`中的共享转换许可，主动取图仍保留SDK取帧超时、相机生命周期锁及SDK帧归还。
2. 保留转换缓冲扩容、SDK转换和Mat克隆的实例锁；生产转换在开始前、取得实例锁后及返回后检查停止令牌，未交出的Mat取消后释放，不误报为采集故障。
3. 不改变SDK回调快速入队、单相机FIFO、`FrameId/SDKFrameNum/WorkpieceId`绑定和停止换代边界。
4. 新增使用两个生产方法原文的SDK替身测试：2/4/8路同时进入转换、每路100帧、单相机16个并发调用仍互斥、转换前/转换中/等待实例锁时取消及Mat归零。测试不等同于真实相机性能测试。
5. 完成独立审查、相机关联回归、Debug和Release构建后记录结果；现场由用户提供新日志，对比两台各6229帧的8月31日基线，重点比较总耗时平均/P99/最大、超过10ms与50ms数量以及资源占用，不能仅凭许可等待为0判定性能改善。

### 本轮验证结果

- `Debug|x64`构建0错误、154警告；`Release|x64`构建0错误、155警告。本批不清理项目其他警告。
- 14项关联回归最终全部通过：回调快速路径、回调隔离、相机帧诊断、图像源统一回调、触发语义、原生SDK类、方案参数权威、自动重连、原始帧内存预算、工件管线、流程集成、首帧宽限、CPU调度器和CPU节点接入。
- 独立转换专项初次通过后再连续复跑5轮，每轮为2/4/8路各100帧及实例互斥、三种取消检查；工件管线额外复跑3轮，Release程序集首帧宽限测试通过。复跑批次共21.846秒，这是测试执行时间，不是真实相机性能指标。
- 独立审查无P0/P1/P2发现。SDK替身测试不覆盖真实像素、驱动、停流与主动取图的完整硬件行为；相应静态边界和既有管线测试通过，但仍必须进行现场验证。
- 正常输出：`bin/x64/Release/机器视觉AI检测系统V1.0.exe`，生成时间`2026-08-31 14:16:17`，SHA256=`CCD04E839C41D44EB9100142E3BF1D6A8F67D236CEEF7242C7622D3A62A625AA`。测试未启动界面、连接相机或占用键鼠。
